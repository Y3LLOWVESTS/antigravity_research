# Research Journal — 2026-09-07

## 032H17A–032H17A5 — HOOK17 Rescue-Family Atlas, Exact Healthy-Mode Projectors, Tree-Level Action Scaffolds, Symmetry Protection, and Scoped K3 Ward Falsification

## Objective

The primary scientific objective of this research slice was:

> **Determine whether the HOOK17 low-capacity mechanism can be promoted from a collection of separately established ingredients into a physically credible, symmetry-consistent same-action theory, without weakening the project's Ward, health, universal-metric, field-redefinition, naturalness, and claim-discipline gates.**

This slice followed the earlier V26 sequence:

```text
V24
INTRINSIC DIRAC HYPERMOMENTUM SOURCE

+

V26B1
NONLINEAR QUADRATIC HOOK-TO-METRIC NUMERATOR

+

V26B1
ACTIVE / OFFSTATE LINEAR SEPARATION

+

V26B1R1
EXCEPTIONALLY LOW CANONICAL FIELD-CAPACITY REFERENCE

+

V26C
DIRECT MASSLESS HOOK-SHIFT SAME-ACTION WARD FAILURE

+

V26D
PROTECTED cT=1 DHOST/KMM FALLBACK
```

The active task was not to reduce the HOOK17 capacity number further.

The active task was:

```text
PHYSICALIZE THE MECHANISM

WITHOUT

LOSING THE MICROSCOPIC SOURCE

LOSING HEALTHY-MODE OVERLAP

VIOLATING A SOURCE WARD IDENTITY

USING AN UNPROTECTED ACCIDENTAL TUNING

REMOVING THE METRIC RESPONSE BY FIELD REDEFINITION

OR

MISTAKING A LOW FIELD CAPACITY FOR LOW COMPLETE ENERGY
```

The complete scientific slice consisted of:

```text
032H17A
RESCUE-FAMILY THEOREM AND ACTION ATLAS

032H17A2
EXACT V24 -> HEALTHY HOOK 2+ / 1+ SOURCE PROJECTORS

032H17A3
TREE-LEVEL SAME-ACTION SCAFFOLD
+
FIELD-REDEFINITION TEST
+
PROTECTION / NATURALNESS STOP RULE

032H17A4
SYMMETRY-FIRST PROTECTED TORSION-LIKE / VECTOR ATLAS
+
GAUGE-INVARIANT STUECKELBERG METRIC WITNESS

032H17A5
EXACT PROTECTED K3 1+ SOURCE-WARD FALSIFICATION
```

The final full regression state at the end of the slice was:

```text
759 PASSED
```

The historical protected baseline remains:

```text
94 PASSED
```

The current project does not have a certified physical antigravity model.

The current project does not have a certified sub-10-MJ complete operating-energy model.

The current project does not have a practical antigravity device.

---

# Starting State

Before 032H17A began, HOOK17 already contained several unusually strong but incomplete ingredients.

## 1. V24 microscopic source

V24 established an intrinsic Dirac hypermomentum source with nonzero hook content.

The source was not an arbitrarily prescribed classical current.

It arose from a microscopic Dirac construction.

Therefore:

```text
HOOK17_MICROSCOPIC_INTRINSIC_SOURCE=
PRESENT_AT_SOURCE_LEVEL
```

This was an important improvement over earlier architectures whose source charge was merely prescribed.

However source existence alone did not prove overlap with a healthy propagating mode.

---

## 2. V26B1 nonlinear physical-metric numerator

V26B1 established a quadratic hook descendant capable of contributing to a universal symmetric rank-two physical metric.

Schematically,

```math
g^{\rm phys}_{\mu\nu}
=
g_{\mu\nu}
+
\lambda B_{\mu\nu}(H,H)
```

with a representative descendant of the form

```math
B_{\mu\nu}
=
H_{a\mu b}H^{a}{}_{\nu}{}^{b}
```

The clean V24 rest-pair hook produced a nonzero active numerator.

The same quadratic structure gave:

```text
OFFSTATE FIRST VARIATION=
ZERO

ACTIVE-BACKGROUND FIRST VARIATION=
NONZERO
```

This active/offstate separation was one of the central features that made HOOK17 worth rescuing.

---

## 3. V26B1R1 field-capacity result

V26B1R1 reconstructed a canonical spherical field-capacity estimate

```math
E_{\rm ext}
=
2\pi R H^2
\left(
1+\frac{R}{\ell}
\right)
```

for the declared reference geometry.

At the optimistic projector normalization,

```text
R_P=1
```

the reference field capacity was approximately

```text
1.706764421959e-5 J
```

The conservative project reference adopted

```text
R_P=1e12
```

which gave

```text
HOOK17_REFERENCE_CAPACITY_RP1E12_J=
17.0676442196
```

The scaling was

```math
E_{\rm capacity}
\propto
R_P^{1/2}
```

The projector penalty required for the field-capacity term alone to reach the strict 10-MJ project threshold was extraordinarily large.

However this result was never interpreted as complete operating energy.

The permanent distinction is:

```text
17.0676442196 J
=
FIELD-CAPACITY REFERENCE

NOT

COMPLETE MODEL ENERGY
```

Complete HOOK17 energy remains unknown.

---

## 4. V26C obstruction

V26C tested a direct massless hook-shift realization.

The direct source coupling failed the corresponding hook-shift Ward compatibility test.

The quadratic metric also failed full hook-shift compatibility despite preserving offstate first-order silence.

The permanent lesson was:

```text
OFFSTATE LINEAR SILENCE

IS NOT

FULL GAUGE INVARIANCE
```

V26C closed only the tested direct realization.

It did not close:

```text
INTRINSIC DIRAC HYPERMOMENTUM

ALL METRIC-AFFINE GRAVITY

COMPENSATOR RESCUES

STUECKELBERG RESCUES

HIGGSED HOOKS

MASSIVE HOOK / NONMETRICITY

OTHER PROTECTED MAG PROJECTORS

NONLINEAR VECTOR-GRAVITON COMPLETIONS

GAUGE-INVARIANT COMPOSITE PORTALS

ALTERNATIVE DIRAC SOURCE STATES
```

---

## 5. Preserved fallback

V26D had independently established a protected `cT=1` DHOST/KMM scaffold.

V26D remained:

```text
OPEN
VALUABLE
PAUSED
```

The associated V26E continuation remained:

```text
PAUSED
NOT CLOSED
```

The decision entering H17A was to test HOOK17 rescue families first because HOOK17 combined:

```text
MICROSCOPIC SOURCE

+

LARGE NONLINEAR METRIC NUMERATOR

+

ACTIVE/OFFSTATE SEPARATION

+

LOW FIELD-CAPACITY REFERENCE

+

A PRECISELY IDENTIFIED IMPLEMENTATION FAILURE
```

---

# Scientific Question

The central scientific question was:

> **Can a symmetry-protected or technically natural action family preserve the V24 intrinsic Dirac source, give that source nonzero overlap with a healthy propagating carrier, retain one universal physical metric with a nonremovable active response, and avoid the Ward obstruction that killed the tested V26C realization?**

The sequence was deliberately theorem-first.

No generic energy optimization was authorized.

No large parameter scan was authorized.

No AGMINER candidate-database mutation was authorized.

No attempt was made to reduce the already sub-100-J field-capacity reference.

---

# 032H17A — Rescue-Family Theorem and Action Atlas

## Objective

The first H17A gate asked which physically distinct action families could plausibly repair V26C without discarding the properties that made HOOK17 attractive.

The declared rescue order was:

```text
1.
COMPENSATOR / STUECKELBERG / HIGGSED HOOK

2.
ALTERNATIVE PROTECTED MAG PROJECTORS

3.
MASSIVE HOOK / SPIN-3 / NONMETRICITY

4.
GENUINELY NONLINEAR VECTOR-GRAVITON COMPLETIONS

5.
GAUGE-INVARIANT COMPOSITE / CURVATURE PORTALS

6.
DIRAC SOURCE-STATE ENGINEERING
```

The promotion requirements included:

```text
EXPLICIT ACTION

SYMMETRY

SAME-ACTION SOURCE PROVENANCE

SOURCE WARD CONSISTENCY

HEALTHY MODE

NONZERO SOURCE PROJECTOR

ONE UNIVERSAL PHYSICAL METRIC

METRIC WARD CONSISTENCY

FIELD-REDEFINITION SURVIVAL

PRINCIPAL HEALTH

NATURALNESS / EMPIRICAL PREFLIGHT
```

---

## Hook-source decomposition

For the clean V24 rest-pair source, define the spatial matrices

```math
A_{ij}
=
H_{0ij}
```

and

```math
B_{ij}
=
H_{i0j}
```

The hook identity gave

```math
A+B+B^T
=
0
```

to numerical precision.

Every nonzero clean-source hook component contained exactly one temporal or longitudinal index.

This immediately separated the source from the fully transverse hook sectors.

The source decomposition gave:

```math
\left\|
A_{\rm traceless}
\right\|^2
=
\frac{128}{9}
```

which established nonzero clean-source support in the hook `2+` sector.

The antisymmetric component gave

```math
\left\|
\frac{B-B^T}{2}
\right\|^2
=
32
```

which established nonzero clean-source support in the hook `1+` sector.

The scalar trace gave

```math
{\rm Tr}(A)
=
0
```

Therefore the clean rest-pair source prefilter became:

```text
HOOK_2_PLUS=
NONZERO SUPPORT

HOOK_1_PLUS=
NONZERO SUPPORT

HOOK_2_MINUS=
ZERO SUPPORT

HOOK_1_MINUS=
ZERO SUPPORT

HOOK_0_PLUS=
ZERO SUPPORT
```

This reduced the strongest healthy-hook search from five candidate pole sectors to two.

---

## Curtright / compensated-hook preflight

A compensator/Stueckelberg completion could evade the exact massless direct-shift obstruction of V26C at the kinematic level.

However the four-dimensional massive Curtright carrier is closely related to a massive spin-two dual description.

Because the project already contains substantial massive-spin-two failure memory, this family was not promoted merely because a Stueckelberg completion can be written.

The H17A Curtright conclusions were:

```text
CURTRIGHT_KINEMATIC_WARD_RESCUE=
TRUE

CURTRIGHT_REST_FRAME_POLE_OVERLAP_ZERO=
TRUE

CURTRIGHT_STATIC_OFFSHELL_RESPONSE=
NOT CLOSED

CURTRIGHT_SAME_ACTION_HOOK17=
NOT ESTABLISHED
```

This family remained open but was demoted below the exact healthy-MAG projector calculation.

---

## H17A decision

The first H17A run returned:

```text
032H17A_DECISION=
YELLOW_H17A_WARD_RESCUE_AND_HEALTHY_ACTION_FAMILIES_EXIST_
BUT_NO_SAME_ACTION_COMPLETE_HOOK17_SURVIVOR
```

The protected source prefilter was:

```text
HOOK_MAG_SUPPORT_SURVIVORS=
HOOK_2_PLUS,HOOK_1_PLUS
```

The exact source zeros were:

```text
HOOK_MAG_EXACT_SUPPORT_ZEROS=
HOOK_2_MINUS,HOOK_1_MINUS,HOOK_0_PLUS
```

No same-action complete survivor existed.

Regression:

```text
TARGETED=
16 PASSED

FULL=
689 PASSED

SIM_RC=
0
```

The next gate became exact source-to-pole projection.

---

# 032H17A2 — Exact V24 to Healthy Hook 2+ and 1+ Projectors

## Objective

H17A established only representation support.

H17A2 asked the stronger question:

> **Does the actual microscopic V24 source survive the canonical source maps seen by the healthy propagating hook `2+` and `1+` poles?**

This distinction was essential.

A raw tensor component in the correct representation does not guarantee nonzero coupling to the physical pole.

---

## Healthy 2+ source map

For the healthy hook `2+` branch, the canonical two-index source numerator was implemented in momentum space as

```math
N^{(2+)}_{\mu\nu}
=
q_\lambda
\tau^\lambda{}_{\mu\nu}
-
q_\lambda
\tau_{(\mu}{}^\lambda{}_{\nu)}
```

with the common Fourier factor omitted for norm calculations.

At a timelike carrier-rest witness,

```math
q_\mu
=
(1,0,0,0)
```

the spatial projected tensor was symmetric and traceless.

The exact clean-source norm was:

```text
HOOK_2PLUS_RAW_REST_SOURCE_NORM2=
32.000000000000
```

The explicit spatial spin-two projection retained the full source norm.

Therefore:

```text
HOOK_2PLUS_EXACT_POLE_SOURCE_NONZERO=
TRUE
```

---

## Healthy 1+ source map

For the healthy hook `1+` branch, the canonical source numerator was implemented as

```math
N^{(1+)}_{\mu\nu}
=
q_\lambda
\tau_\mu{}^\lambda{}_\nu
-
q_\lambda
\tau_\nu{}^\lambda{}_\mu
```

At the same timelike witness, the spatial source was antisymmetric.

The exact norm was:

```text
HOOK_1PLUS_RAW_REST_SOURCE_NORM2=
128.000000000000
```

Therefore:

```text
HOOK_1PLUS_EXACT_POLE_SOURCE_NONZERO=
TRUE
```

This was a substantial promotion over H17A.

The V24 microscopic source was no longer merely in a compatible representation.

It demonstrably fed the canonical source channels of healthy propagating hook modes.

---

## Canonical branch identities

Representative healthy branch conditions were tested over multiple separated parameter points.

For the `2+` reference branch:

```text
m1 < 0

b6 < 0
```

The pole mass satisfied:

```math
m_{2+}^2
=
\frac{3m_1}{2b_6}
```

The canonical source norm reconstructed the branch identity

```math
\left\|
J_{2+}
\right\|^2
=
-\frac{32}{b_6}
```

For the `1+` reference branch:

```text
m1 < 0

b7 < 0
```

with

```math
m_{1+}^2
=
\frac{2m_1}{b_7}
```

and canonical source identity

```math
\left\|
J_{1+}
\right\|^2
=
-\frac{128}{b_7}
```

The tested identities passed across separated healthy points.

---

## Static Fourier support

H17A2 also evaluated the derivative source maps at

```math
q_0
=
0
```

For the clean source orientation, the exact quadratic forms were

```math
\left\|
N_{2+}
\right\|^2
=
128 k_y^2
+
32 k_z^2
```

and

```math
\left\|
N_{1+}
\right\|^2
=
128 k_z^2
```

Therefore both channels had generic nonzero static-gradient support.

The unit-sphere angular averages were:

```text
HOOK_2PLUS_STATIC_ANGULAR_AVG_NORM2=
53.333333333333

HOOK_1PLUS_STATIC_ANGULAR_AVG_NORM2=
42.666666666667
```

This did not yet constitute a static Green-function calculation or finite-payload force calculation.

---

## H17A2 decision

The live result was:

```text
032H17A2_DECISION=
GREEN_H17A2_V24_SOURCE_COUPLES_NONTRIVIALLY_TO_HEALTHY_
HOOK_2PLUS_AND_1PLUS_POLE_SOURCES__
SAME_ACTION_STILL_REQUIRED
```

The important outputs were:

```text
HOOK_2PLUS_EXACT_POLE_SOURCE_NONZERO=
True

HOOK_1PLUS_EXACT_POLE_SOURCE_NONZERO=
True

HOOK_2PLUS_CANONICAL_IDENTITY_PASS=
True

HOOK_1PLUS_CANONICAL_IDENTITY_PASS=
True

HOOK_2PLUS_STATIC_GENERIC_SUPPORT_NONZERO=
True

HOOK_1PLUS_STATIC_GENERIC_SUPPORT_NONZERO=
True
```

But:

```text
SAME_ACTION_COMPLETE=
False

H17B_AUTHORIZED=
False

ENERGY_OPTIMIZATION_AUTHORIZED=
False
```

Regression:

```text
TARGETED=
16 PASSED

FULL=
705 PASSED

SIM_RC=
0
```

This was the first point at which the project could state:

> **The V24 intrinsic microscopic source has nonzero exact source overlap with known healthy propagating hook channels.**

It could not state that those source and kinetic sectors had yet been derived from one complete interacting action.

---

# 032H17A3 — Tree-Level Same-Action Scaffold, Field Redefinition, and Protection Gate

## Objective

H17A3 asked whether the H17A2 source overlap could be placed inside a tree-level action scaffold containing:

```text
GRAVITY

+

HEALTHY HOOK 2+ OR 1+ QUADRATIC SECTOR

+

DIRAC HYPERMOMENTUM SOURCE

+

ONE UNIVERSAL QUADRATIC PHYSICAL METRIC
```

without immediately losing the V26B1 response.

It then asked whether the healthy single-state branches were protected or merely finely tuned surfaces.

---

## Tree-level action scaffold

The declared schematic action was

```math
S
=
\int d^4x
\sqrt{-g}
\left[
L_{\rm EH}
+
L_Q
+
L_{\rm Dirac}
\right]
+
S_{\rm matter}
\left[
g^{\rm phys}
\right]
```

with a physical metric of the form

```math
g^{\rm phys}_{\mu\nu}
=
g_{\mu\nu}
+
\lambda_B
B_{\mu\nu}(Q,Q)
```

For both `2+` and `1+` representative healthy branches:

```text
TREE_LEVEL_ACTION_SCAFFOLD=
TRUE

H17A2_SOURCE_PROJECTOR_NONZERO=
TRUE

FREE_HEALTHY_MODE=
TRUE
```

This was explicitly classified as a scaffold.

It was not classified as complete same-action provenance.

---

## Active-state metric source feedback

Because ordinary matter couples to the universal physical metric, variation with respect to the geometric carrier does not contain only the intrinsic affine current on an active background.

Schematically,

```math
\frac{\delta S_m}{\delta Q}
=
J_{\rm intrinsic}
+
\frac{1}{2}
T^{\mu\nu}
\frac{\delta g^{\rm phys}_{\mu\nu}}
{\delta Q}
```

For a quadratic physical metric,

```math
g^{\rm phys}
=
g+\lambda Q^2
```

the second contribution vanishes at

```math
Q=0
```

but is generically nonzero on an active background.

H17A3 therefore permanently recorded:

```text
OFFSTATE METRIC-FEEDBACK SOURCE=
ZERO

ACTIVE METRIC-FEEDBACK SOURCE=
NONZERO
```

This prevents future work from incorrectly reusing the off-state V24 current unchanged as the complete active-device source.

---

## Field-redefinition invariance

A local linearized active-background model was used to test whether the apparent metric portal could be removed by an invertible field redefinition.

Write the local tangent variables as

```math
y
=
\begin{pmatrix}
h\\
x
\end{pmatrix}
```

with kinetic matrix

```math
K
```

source vector

```math
J
```

and physical-metric projection

```math
M
```

The physical linear response is proportional to

```math
{\cal R}
=
J^T K^{-1}M
```

Under an invertible field transformation

```math
y
=
R z
```

the transformed objects satisfy

```math
K'
=
R^T K R
```

```math
J'
=
R^T J
```

and

```math
M'
=
R^T M
```

Therefore

```math
J'^T K'^{-1}M'
=
J^T K^{-1}M
```

The test deliberately chose a transformation that removed the explicit carrier component from the metric projection.

The response survived numerically.

Therefore:

```text
LINEAR_RESPONSE_FIELD_REDEFINITION_INVARIANT=
True
```

The principal matrices also remained positive in the declared local witness.

Thus the surviving local response was not merely an artifact of where the interaction was written.

This was not a full nonlinear field-redefinition theorem.

---

## Protection / naturalness gate

The healthy `2+` and `1+` single-state branches require multiple exact relations among the coefficients of the quadratic MAG action.

The H17A3 implementation represented the branch constraints in a ten-dimensional parameter space:

```text
m1

m3

b1

b3

b6

b7

b10

b14

bRQ4

bRQ6
```

The Jacobian rank of each declared healthy branch was:

```text
HOOK_2PLUS_HEALTHY_SURFACE_CODIMENSION=
7

HOOK_1PLUS_HEALTHY_SURFACE_CODIMENSION=
7
```

The local tangent nullity was therefore three.

A generic beta-vector witness had a nonzero projection normal to both constraint surfaces.

This was not a loop calculation.

It established only:

```text
GENERIC RG FLOW IS NOT AUTOMATICALLY TANGENT
```

No independent symmetry protecting all seven relations had been established.

Therefore:

```text
HOOK_2PLUS_PROTECTION_GATE_PASS=
False

HOOK_1PLUS_PROTECTION_GATE_PASS=
False
```

The unprotected single-state branches were not promoted.

---

## H17A3 decision

The live result was:

```text
032H17A3_DECISION=
YELLOW_H17A3_TREE_LEVEL_DIRAC_HOOK_MAG_2PLUS_1PLUS_SCAFFOLDS_
AND_NONREMOVABLE_ACTIVE_RESPONSE_SURVIVE__
UNPROTECTED_SINGLE_STATE_REALIZATIONS_BLOCKED_PENDING_SYMMETRY_PROTECTION
```

Key outputs:

```text
HOOK_2PLUS_TREE_LEVEL_ACTION_SCAFFOLD=
True

HOOK_1PLUS_TREE_LEVEL_ACTION_SCAFFOLD=
True

ACTIVE_OFFSTATE_METRIC_STRUCTURE_PRESERVED=
True

ACTIVE_METRIC_SOURCE_FEEDBACK_NONZERO=
True

LINEAR_RESPONSE_FIELD_REDEFINITION_INVARIANT=
True

SAME_ACTION_PROVENANCE_COMPLETE=
False

FULL_NOETHER_COMPLETION=
False
```

Regression:

```text
TARGETED=
18 PASSED

FULL=
723 PASSED

SIM_RC=
0
```

This run changed the frontier from:

```text
CAN A HEALTHY HOOK MODE RECEIVE V24?
```

to:

```text
CAN A SYMMETRY-PROTECTED HEALTHY ACTION RECEIVE V24?
```

---

# 032H17A4 — Symmetry-First Protected Torsion-Like / Vector Atlas

## Objective

A3 showed that unprotected single-state hook models were not sufficient.

A4 therefore moved to genuinely symmetry-first protected action families.

The strongest immediate literature class was the exhaustive linear parity-conserving pair-antisymmetric rank-three classification of Barker, Marzo, and Santoni.

The relevant field representation is schematically

```math
T_{abc}
=
-T_{bac}
```

The previously established V24C algebraic map was

```math
T_{abc}
=
H_{abc}
-
H_{bac}
```

which is invertible on the declared hook representation.

---

## Exact V24 hook to torsion-like map

For the clean V24 source, the A4 run found:

```text
HOOK_NORM=
9.237604307034013

TORSIONLIKE_NORM=
16.0

FIRST_PAIR_ANTISYMMETRY_ERROR=
0.0

HOOK_ROUNDTRIP_RELATIVE_ERROR=
1.3597399555105182e-16
```

Therefore:

```text
V24_HOOK_TO_TORSIONLIKE_MAP_GREEN=
True
```

The map used no Levi-Civita tensor.

Therefore it was a parity-even algebraic representation map.

---

## Irreducible trace information

For the mapped clean V24 source, both simple Lorentz traces vanished:

```math
\eta^{bc}T_{abc}
=
0
```

and

```math
\eta^{ac}T_{abc}
=
0
```

The axial pseudotrace also vanished.

The cyclic sum vanished exactly.

Numerically:

```text
MAPPED_SOURCE_SIMPLE_VECTOR_TRACE_ZERO=
True

MAPPED_SOURCE_AXIAL_PSEUDOTRACE_ZERO=
True

MAPPED_SOURCE_CYCLIC_IDENTITY_ZERO=
True
```

This could have led to a dangerous false conclusion that the source had no vector-mode support.

A4 explicitly prohibited that inference.

---

## 1+ spatial dual vector

The exact H17A2 `1+` spatial source matrix was

```text
[
    [ 0,  0,  0],
    [ 0,  0, -8],
    [ 0,  8,  0]
]
```

Its three-dimensional dual vector was

```text
[-8, 0, 0]
```

with

```text
H17A2_1PLUS_SPATIAL_DUAL_VECTOR_NORM2=
64.0
```

Therefore:

```text
TRACE_ZERO_CLOSES_ALL_VECTOR_MODES=
False
```

This distinction became permanent failure memory.

A Lorentz trace current and an O(3) physical vector projector are not interchangeable tests.

---

## Symmetry-first torsion-like catalogue

The scoped literature classification contained:

```text
GENERAL QUADRATIC PARAMETER COUNT=
12

GAUGE-SYMMETRIC SPECIALIZATIONS=
206

GHOST / TACHYON FREE SPECIALIZATIONS=
22
```

Within the declared torsion interpretation of that catalogue, the unitary propagating torsion modes were vector modes.

Therefore:

```text
TORSIONLIKE_CATALOGUE_VECTOR_ONLY_UNITARY_MODES=
True
```

The catalogue did not directly provide a protected `2+` carrier.

Therefore:

```text
CATALOGUE_DIRECTLY_RESCUES_2PLUS=
False
```

It did keep the vector / `1+` target open:

```text
CATALOGUE_KEEPS_1PLUS_TARGET_OPEN=
True
```

The catalogue's assumptions did not justify globally closing all protected `2+` possibilities.

Therefore:

```text
CATALOGUE_CLOSES_ALL_PROTECTED_2PLUS=
False
```

---

## Gauge-invariant Stueckelberg metric witness

A4 also constructed a kinematic protected-vector metric witness.

For

```math
\delta V_\mu
=
\partial_\mu\alpha
```

and

```math
\delta\pi
=
m\alpha
```

define

```math
W_\mu
=
V_\mu
-
\frac{\partial_\mu\pi}{m}
```

Then

```math
\delta W_\mu
=
0
```

A gauge-invariant quadratic metric witness is

```math
g^{\rm phys}_{\mu\nu}
=
g_{\mu\nu}
+
\lambda
W_\mu W_\nu
```

The numerical gauge-invariance errors were at floating-point roundoff:

```text
GAUGE-INVARIANT VECTOR ERROR=
6.938893903907228e-17

METRIC GAUGE-INVARIANCE ERROR=
9.904983868343586e-17
```

The off-state first variation was exactly zero.

The active first variation was nonzero:

```text
ACTIVE FIRST-VARIATION NORM=
0.3087476012626564
```

Therefore:

```text
STUECKELBERG_METRIC_GAUGE_INVARIANT=
True

STUECKELBERG_METRIC_OFFSTATE_SILENT=
True

STUECKELBERG_METRIC_ACTIVE_RESPONSE_NONZERO=
True
```

This was only a kinematic metric witness.

It did not yet identify the V24 source with the protected vector inside one action.

It also did not prove that the 17-J capacity survives the carrier change.

---

## Protected family rerank

A4 ranked the protected rescue families:

```text
1.
BMS TORSION-LIKE UNITARY VECTOR CATALOGUE

2.
MARZO ABELIAN NONMETRICITY VECTOR / STUECKELBERG

3.
BARKER-ZELL EXTENDED-PROJECTIVE / ALTERNATIVE DOUBLE-VECTOR

4.
NONLINEAR VECTOR-GRAVITON NOETHER COMPLETION

5.
MASSIVE CURTRIGHT / STUECKELBERG HOOK
```

At the end of A4:

```text
PROTECTED_EXACT_SAME_ACTION_SURVIVORS=
0
```

Therefore A4 was a family-level narrowing result, not a candidate-model certification.

---

## H17A4 decision

The live decision was:

```text
032H17A4_DECISION=
YELLOW_H17A4_SYMMETRY_FIRST_TORSIONLIKE_VECTOR_FAMILIES_SURVIVE_
AND_GAUGE_INVARIANT_STUECKELBERG_METRIC_WITNESS_EXISTS__
PROTECTED_EXACT_V24_1PLUS_ACTION_PROJECTOR_STILL_REQUIRED
```

Regression:

```text
TARGETED=
18 PASSED

FULL=
741 PASSED

SIM_RC=
0
```

This made a specific protected `1+` model the highest-information next target.

---

# 032H17A5 — Exact Protected K3 1+ Source-Ward Falsification

## Objective

A5 tested the direct microscopic V24 current against an actual healthy gauge-symmetric protected `1+` action rather than another representation-level proxy.

The selected target was the Barker-Marzo-Santoni K3 model.

The K3 classification used in this gate was:

```text
PAIR-ANTISYMMETRIC RANK-3=
True

PARITY CONSERVING=
True

GAUGE SYMMETRIC=
True

GHOST / TACHYON FREE=
True

PROPAGATING SECTOR=
AXIAL_VECTOR_EVEN_PARITY_1PLUS

MASSLESS=
True

PHYSICAL POLARIZATIONS=
2

FREE KINETIC PARAMETERS=
1
```

The published K3 source surface contains 21 source constraints.

A direct current must satisfy those Ward constraints because the gauge null directions of the kinetic operator cannot be sourced inconsistently.

---

## Index-convention map

The project torsion-like tensor is antisymmetric in its first pair.

The K3 current convention is antisymmetric in its final pair.

The algebraic permutation used was

```math
J^{\rm BMS}_{abc}
=
T_{bca}
```

The actual mapped clean V24 current had four nonzero entries:

```text
J[0,2,3] = +8

J[0,3,2] = -8

J[3,0,2] = -8

J[3,2,0] = +8
```

Its norm was

```text
V24_BMS_CURRENT_NORM=
16.0
```

and the required final-pair antisymmetry passed exactly.

---

## Helicity prefilter

The published helicity combinations were evaluated directly on the mapped current.

The spin-two seeds vanished:

```text
V24_BMS_SPIN2_HELICITY_SEED_ZERO=
True
```

The zero-helicity seeds vanished:

```text
V24_BMS_ZERO_HELICITY_SEED_ZERO=
True
```

The spin-one seeds were nonzero:

```text
V24_BMS_SPIN1_HELICITY_SEED_NONZERO=
True
```

with norm squared

```text
V24_BMS_SPIN1_HELICITY_SEED_NORM2=
128.0
```

Therefore the A5 failure could not be attributed to absence of the desired `1+` representation content.

This is a critical distinction.

---

## Necessary K3 Ward condition

One necessary direct-source K3 Ward condition was implemented in momentum space as

```math
q_\chi
q_\beta
J^{\beta\alpha\chi}
=
0
```

The lightlike witness

```math
q_\mu
=
(1,0,0,1)
```

satisfied

```math
q^2
=
0
```

and therefore directly probed the massless K3 pole kinematics.

For the actual mapped V24 source, the residual was

```text
[0, 0, -16, 0]
```

with norm

```text
16.0
```

Therefore:

```text
K3_DIRECT_V24_SOURCE_WARD_COMPATIBLE=
False
```

This single nonzero necessary-Ward counterexample was sufficient to reject the direct source coupling.

---

## Additional Ward witnesses

A second lightlike witness,

```math
q_\mu
=
(1,0,1,0)
```

also had

```math
q^2
=
0
```

and produced residual norm

```text
8.0
```

A lightlike x-directed witness,

```math
q_\mu
=
(1,1,0,0)
```

produced an accidental zero residual.

This did not rescue the theory because a direct gauge-compatible current must satisfy the Ward condition throughout its physical Fourier support, not along one accidental direction.

A static diagonal witness,

```math
q_\mu
=
(0,1,1,1)
```

produced residual norm

```text
8.0
```

The direct current therefore failed generically.

---

## Fixed-tensor scalar-envelope theorem

Consider a localized clean-source ansatz

```math
J_{abc}(q)
=
F(q)
J^{(0)}_{abc}
```

where the tensor orientation is fixed and all spatial localization is supplied by a scalar Fourier envelope `F`.

The Ward residual becomes

```math
q_\chi q_\beta
J^{\beta\alpha\chi}(q)
=
F(q)
P^\alpha(q)
```

with

```math
P^\alpha(q)
=
q_\chi q_\beta
J^{(0)\beta\alpha\chi}
```

A5 exhibited explicit momenta for which

```math
P^\alpha(q)
\neq
0
```

Therefore the Ward polynomial is not identically zero.

A nontrivial localized scalar envelope cannot repair the direct K3 Ward identity over generic Fourier support.

Thus:

```text
CLEAN_SCALAR_ENVELOPE_K3_CLOSED=
True
```

This closure is intentionally scoped.

It applies to:

```text
FIXED CLEAN V24 TENSOR

TIMES

COMMON SCALAR ENVELOPE

DIRECTLY COUPLED TO K3
```

It does not apply to a current whose microscopic spinor texture varies in space or momentum in a way that changes the tensor structure.

It does not apply to a compensator or current-improvement construction.

---

## H17A5 decision

The live result was:

```text
032H17A5_DECISION=
RED_SCOPED_H17A5_K3_DIRECT_PROTECTED_1PLUS_V24_SOURCE_WARD_FAIL__
REPRESENTATION_OVERLAP_SURVIVES_BUT_DIRECT_K3_SAME_ACTION_CLOSED
```

The exact outputs were:

```text
K3_DIRECT_V24_SOURCE_CLOSED=
True

K3_REPRESENTATION_SPIN1_OVERLAP_PRESERVED=
True

K3_DIRECT_V24_SOURCE_WARD_COMPATIBLE=
False

K3_LIGHTLIKE_Z_WARD_RESIDUAL_NORM=
16.0
```

The following routes remained open:

```text
Q_DEPENDENT_SOURCE_ENGINEERING_OPEN=
True

COMPENSATED_CURRENT_OPEN=
True

J11_OPEN=
True

HOOK17_ALL_PROTECTED_1PLUS_CLOSED=
False
```

The K3 carrier was not silently identified with the separate A4 Stueckelberg vector witness or a nonlinear vector-graviton carrier.

Therefore:

```text
K3_VECTOR_GRAVITON_SAME_ACTION_IDENTIFICATION=
False
```

No same-action complete HOOK17 theory existed at A5 closeout.

Regression:

```text
TARGETED=
18 PASSED

FULL=
759 PASSED

SIM_RC=
0
```

---

# Consolidated Regression Progression

The HOOK17 rescue sequence advanced the full regression monotonically:

| Gate | Targeted tests | Full regression | Simulation |
| --- | ---: | ---: | --- |
| `032H17A` | 16 passed | 689 passed | RC 0 |
| `032H17A2` | 16 passed | 705 passed | RC 0 |
| `032H17A3` | 18 passed | 723 passed | RC 0 |
| `032H17A4` | 18 passed | 741 passed | RC 0 |
| `032H17A5` | 18 passed | 759 passed | RC 0 |

The progression did not overwrite or weaken the historical 94-test protected baseline.

The new tests are additional scientific regression gates.

---

# Durable Result Artifacts

The H17A family atlas produced:

```text
results/data/032h17a_hook17_rescue_family_summary.json

results/data/032h17a_hook17_family_atlas.csv

results/data/032h17a_hook17_projector_prefilter.csv
```

H17A2 produced:

```text
results/data/032h17a2_hook17_exact_healthy_mag_projector_summary.json

results/data/032h17a2_hook17_exact_projector_atlas.csv

results/data/032h17a2_hook17_static_fourier_source_atlas.csv
```

H17A3 produced:

```text
results/data/032h17a3_hook17_same_action_naturalness_summary.json

results/data/032h17a3_hook17_same_action_scaffold_atlas.csv

results/data/032h17a3_hook17_protection_rg_atlas.csv
```

H17A4 produced:

```text
results/data/032h17a4_hook17_protected_torsionlike_summary.json

results/data/032h17a4_hook17_protected_family_atlas.csv
```

H17A5 produced:

```text
results/data/032h17a5_hook17_protected_k3_source_ward_summary.json

results/data/032h17a5_hook17_k3_ward_witnesses.csv

results/data/032h17a5_hook17_protected_rescue_rerank.csv
```

The corresponding implementation modules are:

```text
src/antigravity_research/agminer/hook17_rescue_family_atlas.py

src/antigravity_research/agminer/hook17_healthy_mag_projector.py

src/antigravity_research/agminer/hook17_same_action_naturalness.py

src/antigravity_research/agminer/hook17_protected_torsionlike_atlas.py

src/antigravity_research/agminer/hook17_protected_k3_ward.py
```

The corresponding simulations are:

```text
simulations/032h17a_hook17_rescue_family_theorem_and_action_atlas.py

simulations/032h17a2_hook17_exact_healthy_mag_projector_gate.py

simulations/032h17a3_hook17_same_action_noether_naturalness_gate.py

simulations/032h17a4_hook17_protected_torsionlike_symmetry_atlas.py

simulations/032h17a5_hook17_protected_k3_source_ward_gate.py
```

The corresponding scientific regression tests are:

```text
tests/agminer/test_032h17a_hook17_rescue_family_atlas.py

tests/agminer/test_032h17a2_hook17_healthy_mag_projector.py

tests/agminer/test_032h17a3_hook17_same_action_naturalness.py

tests/agminer/test_032h17a4_hook17_protected_torsionlike_atlas.py

tests/agminer/test_032h17a5_hook17_protected_k3_ward.py
```

---

# Validation

## 1. Independent layers of validation

The H17A sequence deliberately used multiple logically distinct checks.

### Representation validation

The source decomposition was performed directly from the actual V24 rest-pair hook tensor.

This established exact zeros and nonzero irreducible support before introducing a propagator.

### Pole-source validation

H17A2 applied the published canonical source maps rather than accepting representation compatibility alone.

### Canonical normalization validation

The parameter-dependent healthy branch identities were checked over multiple separated healthy points.

### Static-support validation

The `q0=0` Fourier source maps were tested independently from the timelike pole witnesses.

### Field-redefinition validation

H17A3 moved the explicit metric portal into the kinetic/source matrix and verified invariance of the physical response.

### Naturalness validation

The healthy single-state branches were treated as constraint surfaces and their local codimensions were computed.

### Symmetry-first validation

H17A4 reranked the search around actual gauge-protected action classes.

### Ward validation

H17A5 tested the microscopic current against a necessary source-Ward identity of an actual protected `1+` theory.

This progression was designed to prevent promotion based on any one isolated favorable feature.

---

# Falsification Attempts and Negative Results

## 1. Representation-only promotion was rejected

H17A did not promote `2+` or `1+` merely because their raw source components were nonzero.

Exact canonical source projectors were required.

---

## 2. Healthy-mode overlap was not treated as same-action completion

H17A2 demonstrated exact nonzero healthy source overlap.

Nevertheless:

```text
SAME_ACTION_COMPLETE=
False
```

was preserved.

This prevented literature stitching from becoming a physical claim.

---

## 3. Tree-level action scaffolds were not treated as technically natural

H17A3 constructed both `2+` and `1+` tree-level scaffolds.

Both were blocked because their healthy branch surfaces had codimension seven and no protecting symmetry had been identified.

Thus:

```text
UNPROTECTED_2PLUS_1PLUS_PROMOTED=
False
```

---

## 4. Field-redefinition artifact was explicitly tested

The active response survived an invertible local linearized field redefinition.

Therefore the tree-level response was not rejected as a mere variable-choice artifact.

This result does not establish nonlinear field-redefinition invariance.

---

## 5. Trace-vector shortcut was rejected

The mapped V24 source had zero simple Lorentz traces and zero axial pseudotrace.

However the exact `1+` spatial source remained nonzero.

Therefore:

```text
TRACE_ZERO_CLOSES_ALL_VECTOR_MODES=
False
```

was preserved as permanent failure memory.

---

## 6. Protected K3 direct coupling was falsified

K3 provided precisely the kind of symmetry protection H17A sought.

The V24 source possessed the correct spin-one representation content.

Nevertheless the actual direct current failed the protected action's necessary Ward constraint.

This is a scientifically stronger negative result than a representation mismatch.

The route failed because:

```text
SOURCE EXISTS

AND

DESIRED REPRESENTATION EXISTS

AND

HEALTHY PROTECTED CARRIER EXISTS

BUT

THE DIRECT MICROSCOPIC CURRENT DOES NOT LIE ON THE CARRIER'S SOURCE-WARD SURFACE
```

This is the exact distinction AGMINER was designed to expose.

---

# What This Slice Established

The following statements are now supported by completed project calculations.

```text
H17A_RESCUE_FAMILY_ATLAS_COMPLETED=
True

V24_CLEAN_SOURCE_HOOK_2PLUS_SUPPORT=
NONZERO

V24_CLEAN_SOURCE_HOOK_1PLUS_SUPPORT=
NONZERO

V24_CLEAN_SOURCE_HOOK_2MINUS_SUPPORT=
ZERO

V24_CLEAN_SOURCE_HOOK_1MINUS_SUPPORT=
ZERO

V24_CLEAN_SOURCE_HOOK_0PLUS_SUPPORT=
ZERO
```

The stronger exact healthy-mode result is:

```text
V24_TO_HEALTHY_HOOK_2PLUS_EXACT_SOURCE_PROJECTOR=
NONZERO

V24_TO_HEALTHY_HOOK_1PLUS_EXACT_SOURCE_PROJECTOR=
NONZERO
```

The exact rest-source norms are:

```text
HOOK_2PLUS_SOURCE_NORM2=
32

HOOK_1PLUS_SOURCE_NORM2=
128
```

Both have generic static Fourier support.

Tree-level scaffolds exist for both healthy branches.

The V26B1 active/offstate physical-metric architecture survives at the scaffold level.

The local active response survives the declared invertible field-redefinition test.

The tested unprotected single-state `2+` and `1+` models require high-codimension relations and fail the project's protection gate.

The V24 hook maps invertibly into the tested pair-antisymmetric representation.

The clean mapped source has:

```text
SIMPLE LORENTZ TRACES=
ZERO

AXIAL PSEUDOTRACE=
ZERO

CYCLIC SUM=
ZERO
```

while retaining a nonzero `1+` physical-vector seed.

The exact H17A2 spatial dual vector has norm squared:

```text
64
```

A gauge-invariant Stueckelberg quadratic metric witness exists with:

```text
OFFSTATE LINEAR SILENCE=
True

ACTIVE RESPONSE=
Nonzero
```

The protected K3 direct clean-V24 coupling is closed by source-Ward inconsistency.

---

# What This Slice Did Not Establish

The following remain unestablished.

```text
HOOK17_SAME_ACTION_PROVENANCE_COMPLETE=
False

HOOK17_FULL_NOETHER_COMPLETION=
False

HOOK17_PROTECTED_EXACT_SOURCE_TO_METRIC_ACTION=
False

HOOK17_FINITE_PAYLOAD_OUTWARD_FIELD=
Not established

HOOK17_TRUE_STANDOFF=
Not established

HOOK17_ACTIVE_SOURCE_REACTION=
Not established

HOOK17_COMPLETE_STABILITY=
Not established

HOOK17_NONLINEAR_COMPLETION=
Not established

HOOK17_QUANTUM_RG_UV_COMPLETION=
Not established

HOOK17_COMPLETE_ENERGY=
Unknown

HOOK17_CERTIFIED_SUB10MJ_MODEL=
No

HOOK17_PHYSICAL_ANTIGRAVITY_MODEL=
No

PRACTICAL_DEVICE=
No
```

The project must not call the current HOOK17 state:

```text
A 17-J ANTIGRAVITY MODEL
```

The scientifically correct statement is:

> **HOOK17 retains a 17.0676442196-J conservative field-capacity reference while increasingly strong source, healthy-mode, action-scaffold, and symmetry tests are being performed. No complete theory or complete energy ledger has yet preserved that number.**

---

# Energy Interpretation

The low HOOK17 field-capacity reference remains important because it shows that catastrophic GR-like field capacity is not automatically present in the algebraic/canonical corridor already studied.

However the project now has an explicit policy:

```text
IF A RELEVANT MODELED ENERGY IS ALREADY BELOW 100 J

DO NOT SPEND RESEARCH EFFORT ON FURTHER EFFICIENCY TUNING
SOLELY TO REDUCE THAT NUMBER
```

For the current HOOK17 reference:

```text
HOOK17_REFERENCE_CAPACITY_RP1E12_J=
17.0676442196

SUB100J_EFFICIENCY_TUNING_AUTHORIZED=
False
```

This does not alter the practicality gate.

The complete conservative operating-energy requirement remains:

```text
E_COMPLETE
<
10 MJ
```

Exactly:

```text
10 MJ
```

fails.

The complete ledger must eventually include, where applicable:

```text
MICROSCOPIC SOURCE ENERGY

MEDIATOR FIELD ENERGY

COMPENSATOR / SYMMETRY-BREAKING ENERGY

SUPPORT / CONFINEMENT ENERGY

ACTIVATION ENERGY

CONTROL ENERGY

RESET ENERGY

SOURCE REACTION

RADIATION

BACKREACTION

OFFSTATE COSTS

QUANTUM / LOOP CONTRIBUTIONS

RG / UV COSTS

LEAKAGE

THERMAL COSTS

MATERIAL / STRUCTURAL SUPPORT
```

Therefore:

```text
LOW FIELD CAPACITY

DOES NOT IMPLY

LOW COMPLETE OPERATING ENERGY
```

---

# Claim Classification

The strongest new positive result of this research slice is:

> **The actual V24 intrinsic Dirac hook source has nonzero exact canonical source overlap with known healthy hook `2+` and `1+` propagating channels, and those channels admit tree-level HOOK17 action scaffolds whose local active physical-metric response survives an invertible field-redefinition test.**

The strongest new negative result is:

> **The direct clean-rest-pair V24 current, including any realization obtained solely by multiplying its fixed tensor structure by a common scalar localization envelope, fails a necessary Ward condition of the tested protected K3 massless axial `1+` theory and is therefore not a valid direct K3 source.**

The combined claim classification is:

```text
CLAIM_CLASSIFICATION=
HOOK17_HEALTHY_SOURCE_PROJECTOR_AND_TREE_SCAFFOLD_PROGRESS_
WITH_SCOPED_PROTECTED_K3_DIRECT_SOURCE_WARD_FALSIFICATION
```

The project must preserve the following distinction:

```text
HEALTHY SOURCE OVERLAP=
YES FOR TESTED 2+ AND 1+ CHANNELS

PROTECTED DIRECT K3 SOURCE=
NO

ALL PROTECTED 1+ ROUTES CLOSED=
NO

SAME-ACTION PHYSICAL MODEL=
NO
```

---

# Scoped K3 Closure

The exact closed realization is:

```text
DIRECT CLEAN V24 REST-PAIR CURRENT

+

FIXED TENSOR STRUCTURE

+

COMMON SCALAR LOCALIZATION ENVELOPE

+

BMS K3 PROTECTED MASSLESS AXIAL 1+ ACTION
```

This route should not be reopened by coefficient scanning.

Its failure is structural at the source-Ward level.

The closure does not include:

```text
BMS J11

Q-DEPENDENT DIRAC SOURCE-STATE ENGINEERING

POSITION-DEPENDENT SPINOR TEXTURE

CURRENT IMPROVEMENTS

COMPENSATOR CURRENTS

STUECKELBERG SOURCE COMPLETIONS

MARZO ABELIAN NONMETRICITY VECTOR

BARKER-ZELL ALTERNATIVE DOUBLE-VECTOR FAMILY

NONLINEAR VECTOR-GRAVITON COMPLETION

MASSIVE CURTRIGHT / HOOK COMPLETIONS

OTHER PROTECTED MAG PROJECTORS

PROTECTED 2+ FAMILIES OUTSIDE THE TESTED CATALOGUE SCOPE
```

---

# Current Protected Rescue Ranking

After A5, the preserved rescue order is:

```text
1.
BMS J11 LESS-CONSTRAINING AXIAL-VECTOR ACTION
EXACT V24 SOURCE-WARD TEST

2.
COMPENSATED OR CURRENT-IMPROVED V24 SOURCE
IN A K3-LIKE PROTECTED 1+ ACTION

3.
MARZO ABELIAN NONMETRICITY / STUECKELBERG VECTOR
WITH EXPLICIT V24 SOURCE MATCH

4.
BARKER-ZELL ALTERNATIVE DOUBLE-VECTOR / PROJECTIVE FAMILY
WITH EXPLICIT DIRAC SOURCE MATCH

5.
NONLINEAR VECTOR-GRAVITON COMPLETION
WITH EXPLICIT SOURCE IDENTIFICATION

6.
LOWER-RANKED MASSIVE CURTRIGHT / HOOK COMPLETIONS
WITH DUALITY AND FIELD-REDEFINITION AUDIT
```

The project should continue theorem-first.

Do not begin an energy scan at this stage.

Do not optimize the 17-J capacity reference.

---

# Immediate Next Action

The next gate is:

```text
032H17A6
J11
+
COMPENSATED SOURCE WARD GATE
```

The primary question is:

> **Does the actual V24 current satisfy the less-constraining protected J11 source-Ward surface, or can a local compensator/current-improvement term derived from the same action repair the K3-type Ward obstruction while preserving the nonzero `1+` physical projector?**

The gate should be ordered:

```text
1.
WRITE THE EXACT J11 ACTION / GAUGE SYMMETRY

2.
EXTRACT ALL NECESSARY SOURCE-WARD CONDITIONS

3.
MAP THE ACTUAL V24 CURRENT INTO THE ACTION CONVENTION

4.
TEST THE DIRECT CURRENT

5.
IF DIRECT FAILS, DERIVE THE MOST GENERAL LOCAL COMPENSATED / IMPROVED CURRENT
ALLOWED BY THE SAME SYMMETRY

6.
RETEST THE WARD IDENTITIES

7.
CHECK WHETHER THE PHYSICAL 1+ PROJECTOR REMAINS NONZERO

8.
CHECK WHETHER THE COMPENSATOR INTRODUCES NEW GHOST / STRONG-COUPLING MODES

9.
ONLY AFTER SOURCE-WARD SURVIVAL,
CONSTRUCT THE GAUGE-INVARIANT UNIVERSAL METRIC

10.
ONLY AFTER SAME-ACTION METRIC SURVIVAL,
RECONSTRUCT THE ACTUAL CANONICAL CAPACITY
```

A source repair that cancels the physical `1+` projector is not a rescue.

A source repair that introduces a pathological longitudinal mode is not a rescue.

A source repair that exists only by attaching a current from a separate paper is not a rescue.

A source repair that relies on an unprotected tuning is not a rescue.

---

# H17B Promotion Rule

H17B remains unauthorized.

A route should advance beyond the H17A rescue program only after one explicit theory establishes all of:

```text
EXPLICIT SAME ACTION

MICROSCOPIC SOURCE DERIVED FROM THAT ACTION

SOURCE WARD CONSISTENCY

HEALTHY PROPAGATING CARRIER

NONZERO SOURCE PROJECTOR

CANONICAL NORMALIZATION

ONE UNIVERSAL PHYSICAL METRIC

METRIC WARD CONSISTENCY

NONZERO ACTIVE SOURCE-TO-METRIC RESPONSE

FIELD-REDEFINITION SURVIVAL

PRINCIPAL HEALTH

NO IMMEDIATE NATURALNESS / EMPIRICAL KILLER
```

Only then should the project proceed to:

```text
STATIC GREEN FUNCTION

FINITE PAYLOAD

OUTWARD SIGN

SIDEDNESS

SOURCE REACTION

RANGE

SUPPORT / CONTROL

NONLINEAR STABILITY

QUANTUM / RG / UV

COMPLETE OPERATING ENERGY
```

---

# Fallback Rule

V26D remains the immediate independent fallback.

Preserve:

```text
V26D=
OPEN_AND_VALUABLE_FALLBACK

032V26E=
PAUSED_NOT_CLOSED
```

If the protected HOOK17 rescue families fail at:

```text
SAME-ACTION PROVENANCE

WARD CONSISTENCY

HEALTHY PROJECTOR

PHYSICAL METRIC RESPONSE

EMPIRICAL CONSISTENCY

OR

UNAVOIDABLE COMPLETE-ENERGY FLOOR
```

then resume:

```text
032V26E
STATIC SPACELIKE PRINCIPAL SYMBOL
+
CROSS PROPAGATOR
+
NUMERATOR BOUND
```

Do not force metric-affine gravity indefinitely.

---

# Relationship to 006D and Earlier Anchors

Nothing in H17A–A5 supersedes the project's established GR anchor.

Preserve:

```text
006D=
STRONGEST ESTABLISHED CONSERVATIVE TRUE-STANDOFF RESULT
```

with

```text
C=
23.591586299249
```

006D remains the strongest conservative true-stand-off architecture because it already contains an explicit finite source with positive energy, local conservation at linearized order, DEC, outward near gravity, and positive far-field active mass.

HOOK17 has not yet demonstrated finite-payload outward acceleration or true stand-off.

Likewise preserve:

```text
018B=
MICROSCOPIC FIELD EXISTENCE

018C=
FATAL m=2 INSTABILITY

PROTECTED VECTOR IMPLEMENTATIONS THROUGH 022A=
CLOSED
```

Preserve the false-core B7 Skyrmion as the strongest older microscopic GR field.

Its force convergence remains unresolved.

The required `026C N=89` check remains outstanding.

Nothing in H17A–A5 changes those historical conclusions.

---

# Relationship to Introspective Mechanism Knowledge

The completed Introspective program remains mechanism knowledge rather than a physical field.

Its durable lessons remain directly relevant to HOOK17:

```text
HIGH KERNEL LEVERAGE

COMPACT PAYLOAD-ADJACENT PRODUCTIVE STRUCTURE

HIGH RESPONSE PER JOULE

LOW CANCELLATION

HIGH PRODUCTIVE PARTICIPATION

LOW SCAFFOLDING TAX
```

Preserve:

```text
RAW_GE1000X_SIGNAL=
PRESENT

CERTIFIED_GE1000X_CONTINUUM_SOURCE=
NO
```

The HOOK17 rescue program should preserve the same discipline.

Do not accept a formal source whose productive physical projector is nearly zero.

Do not accept gain produced by cancellation or principal-margin collapse.

Do not accept a low mediator capacity if source or scaffolding costs dominate the complete ledger.

---

# Literature Context

The principal external theory references used for this slice include:

## Propagating nonmetricity

Yusuke Mikura and Roberto Percacci,

"Some simple theories of gravity with propagating nonmetricity"

arXiv:2401.10097.

This work supplied explicit healthy single-state symmetric metric-affine models and the source maps used for the hook `2+` and `1+` projector tests.

Its free-theory analysis did not by itself supply the complete interacting Dirac source required by HOOK17.

---

## Dirac hypermomentum in metric-affine gravity

James T. Wheeler,

"Dirac Sources for Nonmetricity and Torsion in Metric-affine Gravity"

arXiv:2601.09013.

This work supplied an explicit microscopic Dirac/affine source context relevant to intrinsic torsion and nonmetricity hypermomentum.

It does not by itself provide the healthy single-state action or HOOK17 universal metric.

---

## Symmetry-first torsion-like classification

Will Barker, Carlo Marzo, and Alessandro Santoni,

"Infrared foundations for quantum geometry II: Catalogue of all torsion-like theories including new ghost-tachyon-free cases"

arXiv:2507.05349.

This classification treats linear parity-conserving pair-antisymmetric rank-three fields on Minkowski space using symmetry-first criteria.

It supplied the protected vector-family catalogue and the concrete K3 source-Ward target used in A5.

The catalogue's scope must not be generalized beyond its stated assumptions.

---

## Protected Abelian metric-affine precedent

Carlo Marzo,

"Radiatively stable ghost and tachyon freedom in metric affine gravity"

arXiv:2110.14788.

This work provided an important precedent that healthy metric-affine relations can be enforced by a true Abelian gauge symmetry and extended with a Stueckelberg mass mechanism.

It motivated the symmetry-protection requirement used after H17A3.

---

# Reproducibility Summary

A researcher reconstructing this slice should perform the following checks in order.

## H17A

Verify:

```text
HOOK_MAG_SUPPORT_SURVIVORS=
HOOK_2_PLUS,HOOK_1_PLUS
```

and:

```text
HOOK_MAG_EXACT_SUPPORT_ZEROS=
HOOK_2_MINUS,HOOK_1_MINUS,HOOK_0_PLUS
```

---

## H17A2

Verify:

```text
HOOK_2PLUS_EXACT_POLE_SOURCE_NONZERO=
True

HOOK_1PLUS_EXACT_POLE_SOURCE_NONZERO=
True

HOOK_2PLUS_RAW_REST_SOURCE_NORM2=
32

HOOK_1PLUS_RAW_REST_SOURCE_NORM2=
128
```

---

## H17A3

Verify:

```text
HOOK_2PLUS_TREE_LEVEL_ACTION_SCAFFOLD=
True

HOOK_1PLUS_TREE_LEVEL_ACTION_SCAFFOLD=
True

HOOK_2PLUS_HEALTHY_SURFACE_CODIMENSION=
7

HOOK_1PLUS_HEALTHY_SURFACE_CODIMENSION=
7

LINEAR_RESPONSE_FIELD_REDEFINITION_INVARIANT=
True

HOOK_2PLUS_PROTECTION_GATE_PASS=
False

HOOK_1PLUS_PROTECTION_GATE_PASS=
False
```

---

## H17A4

Verify:

```text
V24_HOOK_TO_TORSIONLIKE_MAP_GREEN=
True

H17A2_1PLUS_SPATIAL_DUAL_VECTOR_NORM2=
64

STUECKELBERG_METRIC_GAUGE_INVARIANT=
True

STUECKELBERG_METRIC_OFFSTATE_SILENT=
True

STUECKELBERG_METRIC_ACTIVE_RESPONSE_NONZERO=
True

PROTECTED_EXACT_SAME_ACTION_SURVIVORS=
0
```

---

## H17A5

Verify:

```text
V24_BMS_SPIN1_HELICITY_SEED_NORM2=
128

K3_LIGHTLIKE_Z_Q2=
0

K3_LIGHTLIKE_Z_WARD_RESIDUAL=
[0,0,-16,0]

K3_LIGHTLIKE_Z_WARD_RESIDUAL_NORM=
16

K3_DIRECT_V24_SOURCE_WARD_COMPATIBLE=
False

K3_DIRECT_V24_SOURCE_CLOSED=
True

HOOK17_ALL_PROTECTED_1PLUS_CLOSED=
False
```

Finally verify:

```text
FULL REGRESSION=
759 PASSED
```

---

# Current Scientific State After H17A5

The correct concise state is:

```text
HOOK17_MICROSCOPIC_INTRINSIC_SOURCE=
PRESENT

HOOK17_HEALTHY_2PLUS_SOURCE_OVERLAP=
PRESENT

HOOK17_HEALTHY_1PLUS_SOURCE_OVERLAP=
PRESENT

HOOK17_STATIC_SOURCE_SUPPORT=
PRESENT

HOOK17_TREE_LEVEL_2PLUS_SCAFFOLD=
PRESENT

HOOK17_TREE_LEVEL_1PLUS_SCAFFOLD=
PRESENT

HOOK17_ACTIVE_OFFSTATE_METRIC_STRUCTURE=
PRESERVED_AT_SCAFFOLD_LEVEL

HOOK17_LINEAR_RESPONSE_FIELD_REDEFINITION_SURVIVAL=
PRESENT

UNPROTECTED_SINGLE_STATE_2PLUS=
BLOCKED_ON_PROTECTION

UNPROTECTED_SINGLE_STATE_1PLUS=
BLOCKED_ON_PROTECTION

PROTECTED_K3_DIRECT_V24_1PLUS=
CLOSED_ON_SOURCE_WARD

J11=
OPEN

COMPENSATED_V24_CURRENT=
OPEN

OTHER_PROTECTED_VECTOR_FAMILIES=
OPEN

HOOK17_SAME_ACTION_PHYSICAL_MODEL=
NO

HOOK17_COMPLETE_ENERGY=
UNKNOWN
```

The most accurate qualitative summary is:

> **HOOK17 has progressed beyond a low-energy algebraic witness into a source-to-healthy-mode and tree-level action-scaffold program, but it has not yet crossed the protected same-action Ward gate required for a physical candidate model.**

---

# Strategic Interpretation

This slice is a major milestone even though its final gate is red.

The scientific progression was:

```text
LOW CAPACITY EXISTS

->

MICROSCOPIC SOURCE EXISTS

->

HEALTHY REPRESENTATION SUPPORT EXISTS

->

EXACT HEALTHY SOURCE PROJECTORS ARE NONZERO

->

TREE-LEVEL ACTION SCAFFOLDS EXIST

->

ACTIVE RESPONSE SURVIVES FIELD REDEFINITION

->

UNPROTECTED REALIZATIONS FAIL NATURALNESS / PROTECTION

->

PROTECTED VECTOR FAMILIES EXIST

->

GAUGE-INVARIANT QUADRATIC VECTOR METRIC WITNESS EXISTS

->

DIRECT K3 MICROSCOPIC SOURCE FAILS THE PROTECTED SOURCE-WARD SURFACE
```

The final failure is not a return to blind search.

It identifies the next exact obstruction.

The central issue is now:

```text
CAN A PROTECTED VECTOR ACTION
ACCEPT A MICROSCOPIC V24-DERIVED CURRENT
THAT SATISFIES ITS WARD IDENTITIES
WITHOUT PROJECTING AWAY THE PRODUCTIVE 1+ MODE?
```

This is substantially narrower than the original HOOK17 physicalization problem.

---

# Permanent Lessons

Preserve the following lessons.

```text
REPRESENTATION OVERLAP
IS NOT
SOURCE-WARD COMPATIBILITY
```

```text
A HEALTHY FREE MODE
IS NOT
A HEALTHY INTERACTING SOURCE-MODE SYSTEM
```

```text
TREE-LEVEL SAME-PAGE TERMS
ARE NOT
SAME-ACTION PROVENANCE
```

```text
OFFSTATE LINEAR SILENCE
IS NOT
FULL GAUGE INVARIANCE
```

```text
TRACE-VECTOR ZERO
DOES NOT
IMPLY ALL VECTOR PROJECTORS ZERO
```

```text
FIELD-REDEFINITION SURVIVAL
IS NECESSARY
BUT NOT SUFFICIENT
```

```text
SYMMETRY PROTECTION
MATTERS MORE THAN
HIGH-CODIMENSION ACCIDENTAL HEALTH
```

```text
17 J FIELD CAPACITY
IS NOT
17 J COMPLETE MODEL ENERGY
```

```text
A SUB-100-J FIELD TERM
DOES NOT NEED FURTHER EFFICIENCY TUNING
BEFORE PHYSICAL CONSISTENCY IS ESTABLISHED
```

```text
A SCOPED RED RESULT
SHOULD CLOSE THE EXACT FAILED REALIZATION
WITHOUT CLOSING UNTESTED FAMILIES
```

---

# AI Assistance

AI assistant used:

```text
ChatGPT by OpenAI
```

AI assistance during this research slice included substantial:

```text
PROJECT-STATE RECONSTRUCTION

HOOK17 FAMILY RERANKING

MIXED-SYMMETRY ACTION LITERATURE COMPARISON

HEALTHY HOOK SOURCE-PROJECTOR DERIVATION

O(3) IRREP DECOMPOSITION

STATIC FOURIER SOURCE ANALYSIS

CANONICAL SOURCE-NORM IDENTITY DERIVATION

TREE-LEVEL SAME-ACTION SCAFFOLD DESIGN

FIELD-REDEFINITION INVARIANCE TEST DESIGN

HEALTHY-SURFACE CODIMENSION ANALYSIS

NATURALNESS / PROTECTION STOP-RULE DESIGN

SYMMETRY-FIRST TORSION-LIKE FAMILY RERANKING

HOOK-TO-TORSIONLIKE REPRESENTATION AUDIT

LORENTZ TRACE AND AXIAL-PSEUDOTRACE ANALYSIS

STUECKELBERG GAUGE-INVARIANT METRIC CONSTRUCTION

PROTECTED K3 ACTION SELECTION

K3 SOURCE-WARD COUNTEREXAMPLE DESIGN

FIXED-TENSOR SCALAR-ENVELOPE WARD THEOREM

PYTHON IMPLEMENTATION

SCIENTIFIC TEST GENERATION

SIMULATION GENERATION

RESULT INTERPRETATION

FAILURE-SCOPE ENFORCEMENT

CLAIM-BOUNDARY ENFORCEMENT

JOURNAL RECONSTRUCTION
```

The AI-assisted work does not constitute independent experimental confirmation or independent peer review.

All project claims remain subject to external verification.

---

# Final Decision

The durable decision for this research slice is:

```text
032H17A_TO_A5=
MAJOR_MECHANISM_PROGRESS_WITH_SCOPED_PROTECTED_K3_DIRECT_WARD_CLOSURE
```

Preserve:

```text
V24_INTRINSIC_SOURCE=
OPEN_AND_HIGH_VALUE

HEALTHY_2PLUS_OVERLAP=
ESTABLISHED

HEALTHY_1PLUS_OVERLAP=
ESTABLISHED

UNPROTECTED_2PLUS_1PLUS=
NOT_PROMOTED

DIRECT_PROTECTED_K3_V24_1PLUS=
CLOSED

J11=
OPEN

COMPENSATED_CURRENT=
OPEN

OTHER_PROTECTED_VECTOR_FAMILIES=
OPEN

V26D=
PRESERVED_FALLBACK
```

Do not pursue further energy reduction of the current approximately 17-J field-capacity reference.

Do not promote HOOK17 to a physical model.

Do not call the K3 result a closure of all protected `1+` routes.

The next authorized scientific gate is:

```text
032H17A6
J11
+
COMPENSATED SOURCE-WARD GATE
```

The core instruction remains:

> **Do not chase 17 J. Chase a physically consistent same-action theory that preserves why 17 J was possible.**
