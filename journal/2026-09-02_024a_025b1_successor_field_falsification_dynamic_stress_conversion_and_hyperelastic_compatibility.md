# Research Journal — 2026-09-02

## 024A–025B1 Successor-Field Falsification, True-Stand-Off Repair, Dynamic Stress Conversion, Portal Participation Obstruction, and Hyperelastic Compatibility Closeout

## Objective

The primary scientific question of this research slice was:

> **After Introspective demonstrated substantial source-level efficiency headroom relative to the strongest current microscopic antigravity field, can that headroom be converted into a physically consistent, conserved, finite-payload matter or field realization that improves on the strongest conservative true-stand-off source, 006D, without relying on misleading source/payload geometry, omitted support sectors, transient bookkeeping, or nonphysical stress assignment?**

This journal intentionally spans two consecutive working sessions because the first session after the Introspective closeout was not separately journaled.

The covered sequence is:

```text
024A
GENERALIZED L2+L4+L6+V CONSTITUTIVE PREFILTER

024A1
FIXED-FIELD SEXTIC B0^2 KERNEL-PLACEMENT GATE

024A1R
ORIENTATION-CRITERION REPAIR

024A2
EXPLICIT OMEGA/PROCA TOPOLOGICAL MEDIATOR PREFILTER

024A3
RIGID ISOROTATION / INERTIA TOMOGRAPHY
+
TEACHER GEOMETRY AUDIT

024A4
TRUE-STAND-OFF TEACHER RECONSTRUCTION

024A4R
STAND-OFF FEASIBILITY / DENSITY-CAP DECOMPOSITION

024B–024B3
DYNAMIC VIRIAL / DOMAIN-WALL PULSE PROGRAM

024C–024C1
STATIC TEACHER-GUIDED STRESS-LENS PROGRAM

024D–024D1R
COUNTERROTATION, STRESS-STATE TRANSPORT,
AND KERNEL-ADAPTIVE CLOSED-ORBIT PROGRAM

024E–024E1
NONCOLLIDING CLOSE-PASS SCALAR PORTAL PROGRAM

025A
SATURATING RELATIVISTIC HENCKY SOLID

025B0
GLOBAL RADIAL COMPATIBILITY
+
FULL RELATIVISTIC ACOUSTIC CONE

025B1
GEOMETRIC COMPATIBILITY
+
NON-EUCLIDEAN PRESTRAIN CLOSEOUT
```

The slice was deliberately fail-closed.

Several apparently promising intermediate effects were rejected or demoted after stronger tests.

The most important outcomes are:

1. the raw Introspective teacher was reinterpreted and retired as a true one-sided stand-off blueprint;
2. a strict stand-off type-I DEC lower bound

```math
C\ge\frac12
```

was derived for the normalized source class;
3. sixth-order/topological, omega/Proca, rigid-isorotation, wall-pulse, static stress-lens, close-pass portal, and ordinary radial hyperelastic realization routes were subjected to increasingly strong falsification;
4. source-level scalar stress-state transport retained significant effective headroom, but no microscopic conserved field realization was obtained;
5. an explicit positive-energy relativistic hyperelastic constitutive law was constructed that locally realizes nearly the required 006B/006D stress states and survives the tested full local acoustic cone;
6. ordinary global radial material compatibility erased that efficiency gain and returned the source to approximately the old 005B supported-tension scale;
7. the approximately 3000:1 material grading requirement and approximately 3000-fold intrinsic geometric incompatibility were shown to satisfy an exact mathematical duality;
8. the associated axisymmetric stress-free non-Euclidean reference metric was proven not to admit a Euclidean surface-of-revolution isometric embedding;
9. no new global pure-GR realization superseded 006D;
10. no route found in this slice removed the catastrophic pure-GR $1/G$ absolute scaling.

The strongest conservative global result therefore remains:

```text
006D
```

with:

```math
C_{006D}
=
23.591586299249.
```

The strongest actual microscopic field stack remains:

```text
B=7
ETA=0.4
M=8
FALSE-CORE SKYRMION

023BR
+
023CR4R STRICT N65
```

The internal knowledge/accomplishment heuristic remains:

```text
APPROXIMATELY_70_TO_71_PERCENT
```

and is explicitly **not a probability that practical antigravity exists**.

This journal does **not** establish:

* a practical antigravity device;
* a new law of physics;
* a new experimentally observed interaction;
* a nonlinear Einstein-matter antigravity solution;
* a stable global material realization of 006D;
* a continuum-certified thousand-fold true-stand-off source;
* an escape from pure-GR $1/G$ scaling;
* reactionless propulsion.

---

# Starting State

The preceding formal journal closed the Introspective program.

The durable state entering this slice was:

```text
006D_STRONGEST_CONSERVATIVE_TRUE_STANDOFF_SOURCE=
YES

C_006D=
23.591586299249

023BR_PROMOTION_GRADE_B7=
PASS

023CR4R_STRICT_UNRESTRICTED_N65_STATIONARITY=
PASS

STRICT_N73_STATIONARITY=
UNRESOLVED

FULL_HESSIAN_FISSION_STABILITY=
UNRESOLVED

NONLINEAR_EINSTEIN_MATTER=
UNRESOLVED
```

The exact-map $B=7$ field reference used in the final Introspective comparison had approximately:

```text
E=
1822.472700555285

A=
28.81787638114564

C=
422.2220709083088
```

The robust independently constructed conserved-DEC source-level headroom relative to that field was approximately:

```text
12.8X_TO_17.9X.
```

The relaxed finite-density teacher produced the much larger raw coefficient:

```math
C_{\rm teacher}
\approx
0.02450446248
```

and therefore raw numerical headroom of approximately:

```text
17230X.
```

However:

```text
RAW_GE1000X_SIGNAL=
PRESENT

CERTIFIED_GE1000X_CONTINUUM_SOURCE=
NO

TEACHER_MICROSCOPIC_FIELD=
NO
```

The teacher remained a diagnostic optimization source rather than a physical field.

Its productive anatomy had suggested:

```text
COMPACT
PAYLOAD_ADJACENT
LOW_CANCELLATION
STRONGLY_STRESS_DRIVEN
NEAR_DEC_SATURATION
HIGH_S_OVER_RHO
```

with force-weighted median approximately:

```math
\frac{S}{\rho}
\approx
2.649629.
```

The ordinary positive-energy $e_2+e_4+V$ Skyrme model obeys the local static ceiling:

```math
\frac{S}{\rho}
\le
2.
```

This motivated a teacher-guided successor containing a sixth-order stress sector.

---

# Work Performed

# 1. 024A — generalized $L_2+L_4+L_6+V$ constitutive prefilter

The first candidate successor was a minimally generalized topological theory containing derivative orders:

```text
L2
L4
L6
V
```

The goal was not to assume that a sixth-order term solved the problem.

The goal was to test analytically whether such a sector could even access the teacher stress regime without immediately violating energy-condition, scaling, or hyperbolicity requirements.

---

## 1.1 Derivative-order active-source identity

For a static contribution homogeneous in spatial derivatives of order $N$, the project uses the scaling identity:

```math
S_N
=
(N-2)e_N.
```

Therefore:

```math
S_2=0,
```

```math
S_4=2e_4,
```

```math
S_6=4e_6,
```

while a zero-derivative potential contributes:

```math
S_0=-2V.
```

The generalized static source therefore has schematic form:

```math
\rho
=
e_2+e_4+e_6+V,
```

```math
S
=
2e_4+4e_6-2V.
```

This gives a local positive-active-source ceiling of 4 for a sextic-dominated positive-energy sector, compared with 2 for the ordinary $e_2+e_4+V$ model.

---

## 1.2 Teacher sextic-fraction requirement

Let the target force-weighted active ratio be:

```math
q_T
=
2.649629.
```

In the most favorable simplified $L_4/L_6$ mixture,

```math
\frac{S}{\rho}
=
2(1-f_6)+4f_6
=
2+2f_6.
```

Therefore:

```math
f_6
=
\frac{q_T-2}{2}
```

and numerically:

```math
\boxed{
f_6
\gtrsim
0.3248145.
}
```

Thus at least approximately 32.5% sextic energy would be required in the most favorable simplified static mixture.

Including quadratic and potential fractions gives the more general normalized relation:

```math
f_6
=
0.3248145
+
f_2
+
2f_0.
```

The required sextic fraction therefore increases when energy is spent in the $L_2$ or potential sectors.

---

## 1.3 Higher-derivative DEC design bound

For a pure positive homogeneous derivative-order sector:

```math
S_N=(N-2)e_N.
```

If that sector independently satisfies type-I DEC,

```math
|p_i|\le e_N,
```

then:

```math
p_1+p_2+p_3
\le
3e_N.
```

Since:

```math
S_N
=
e_N+p_1+p_2+p_3,
```

DEC requires:

```math
S_N\le4e_N.
```

Therefore:

```math
N-2\le4,
```

or:

```math
\boxed{
N\le6.
}
```

A pure positive homogeneous sector with $N>6$ cannot dominate while independently retaining the type-I DEC bound.

At $N=6$, saturation:

```math
\frac{S}{\rho}=4
```

requires:

```math
p_1=p_2=p_3=\rho.
```

This became an important design warning:

> Higher and higher derivative order is not automatically a healthy route to arbitrarily large useful stress.

---

## 1.4 Generalized Derrick identity

Under:

```math
\phi_\lambda(\mathbf x)
=
\phi(\mathbf x/\lambda),
```

the generalized energy scales as:

```math
E(\lambda)
=
\lambda E_2
+
\lambda^{-1}E_4
+
\lambda^{-3}E_6
+
\lambda^3E_0.
```

Stationarity requires:

```math
\frac{dE}{d\lambda}
\bigg|_{\lambda=1}
=
0,
```

hence:

```math
\boxed{
E_2-E_4-3E_6+3E_0=0.
}
```

The integrated active mass is:

```math
M_A
=
2E_4+4E_6-2E_0.
```

The total energy is:

```math
E
=
E_2+E_4+E_6+E_0.
```

Subtracting gives:

```math
M_A-E
=
-\left(
E_2-E_4-3E_6+3E_0
\right).
```

Therefore at exact dilation equilibrium:

```math
\boxed{
M_A=E>0.
}
```

A static isolated generalized Skyrmion can therefore possess local active-source structure without acquiring negative total active mass.

---

## 1.5 Pure BPS route demoted

For the pure BPS-type:

```text
L6 + V
```

static balance satisfies:

```math
e_6=V.
```

The BPS solution has zero pressure.

Therefore:

```math
S=\rho,
```

or:

```math
\boxed{
\frac{S}{\rho}=1.
}
```

Thus the pure BPS limit is not the desired teacher-like high-stress static successor.

Decision:

```text
PURE_BPS_L6_PLUS_V_STATIC_TEACHER_MATCH=
DEMOTED
```

---

## 1.6 Preliminary hyperbolicity result

The quadratic kinetic sector:

```text
c2 > 0
```

provides a conventional hyperbolic anchor on finite static backgrounds.

The preliminary characteristic analysis found the relevant static-background modes within:

```math
0<c_{\rm char}^2\le1.
```

The pure BPS limit:

```text
c2=0
```

can become kinetically degenerate.

This was a prefilter, not a complete nonlinear hyperbolicity proof.

### 024A result

```text
MIXED_L2_L4_L6_V_CONSTITUTIVE_PREFLIGHT=
PROVISIONAL_GREEN

PURE_BPS_STATIC_TEACHER_SUCCESSOR=
DEMOTED

FIELD_REALIZATION=
NO
```

---

# 2. 024A1 — natural $B_0^2$ sextic placement

024A1 asked whether the natural static sextic density:

```math
e_6
\propto
B_0^2
```

already occupied useful gravitational-kernel regions on the two strongest $B=7$ geometries:

```text
PROMOTION_GRADE_EXACT_RATIONAL_MAP
STRICT_UNRESTRICTED_N65
```

For a payload direction $\hat n$, define:

```math
A_0(\hat n)
=
\int S_0K_P\,dV,
```

```math
\eta_0(\hat n)
=
\frac{A_0}{E_0}.
```

Normalize the added sextic energy to:

```math
E_6
=
\lambda E_0.
```

Then:

```math
\rho_\lambda
=
\rho_0+\alpha e_6,
```

```math
S_\lambda
=
S_0+4\alpha e_6.
```

Define sextic kernel leverage:

```math
L_6(\hat n)
=
\frac{
\int e_6K_P\,dV
}{
\int e_6\,dV
}.
```

The exact frozen-field infinitesimal efficiency-improvement criterion is:

```math
4L_6
>
\frac{A_0}{E_0}.
```

Define:

```math
G_6
=
\frac{
4L_6
}{
A_0/E_0
}.
```

Then:

```text
G6 > 1:
INFINITESIMAL SEXTIC ADDITION IMPROVES THAT ORIENTATION

G6 < 1:
IT WORSENS THAT ORIENTATION
```

---

## 2.1 Exact-map result

Representative exact-map quantities were:

```text
E=
1822.472725062167

E6_RAW=
4340.807360364925

BASELINE_MIN_A=
18.37348697935440

DENSE_320_WORST_C=
662.2337731600285
```

Orientation-dependent sextic gain:

```text
MIN_G6=
-34.8184

MEDIAN_G6=
0.07376196

MAX_G6=
1.12822275

FRACTION_G6_GT_1=
0.059375
```

The teacher F90 sextic-energy overlap was approximately:

```text
9.3e-8
```

and therefore essentially zero.

---

## 2.2 Strict N65 result

Representative values:

```text
E=
1780.230521772526

E6_RAW=
10.39991966699472

BASELINE_MIN_A=
5.601853299485973

DENSE_320_WORST_C=
2121.711768426323
```

Orientation gains included:

```text
MEDIAN_G6=
-0.33000244

MAX_G6=
2.84318001767

FRACTION_G6_GT_1=
0.06875
```

At:

```math
\lambda=0.01
```

the all-outward dense-orientation property was lost.

Teacher F90 overlap was effectively zero.

### Original 024A1 conclusion

```text
RED_NAIVE_B0_SQUARED_SEXTIC_PLACEMENT_ON_CURRENT_GEOMETRIES
```

This remained correct for robust arbitrary-orientation performance and teacher spatial overlap.

The worst-orientation promotion criterion itself was then audited.

---

# 3. 024A1R — orientation criterion repair

024A1 had implicitly required a positive added active-source sector to improve every payload orientation.

024A1R showed that this is structurally impossible in the relevant geometry.

---

## 3.1 Point-payload orientation-average theorem

Let the payload center be:

```math
\mathbf c
=
h\hat{\mathbf n}.
```

For source point $\mathbf x$, define:

```math
K(\mathbf x;\hat n)
=
\frac{
\mathbf x\cdot\hat{\mathbf n}-h
}{
|\mathbf x-h\hat{\mathbf n}|^3
}.
```

The shell theorem gives:

```math
\left\langle
\frac{
1
}{
|\mathbf x-h\hat{\mathbf n}|
}
\right\rangle_{\hat n}
=
\frac{1}{\max(r,h)}.
```

Differentiate with respect to $h$.

For:

```math
r<h,
```

one obtains:

```math
\boxed{
\langle K\rangle_{\hat n}
=
-\frac1{h^2}.
}
```

For:

```math
r>h,
```

one obtains:

```math
\boxed{
\langle K\rangle_{\hat n}
=
0.
}
```

Therefore the spherical orientation average of the kernel is never positive.

Hence:

```math
\boxed{
\text{A nonnegative added active-source sector cannot increase the outward response in every orientation.}
}
```

This is a structural geometric result, not a property unique to $B_0^2$.

The finite-spherical-payload kernel was separately checked numerically and also had nonpositive orientation average in the tested geometry.

---

## 3.2 Correct orientation-locked result

Once arbitrary-orientation robustness was separated from an orientation-locked device axis, the fixed-field sextic sector showed a genuine first-order signal.

Exact-map baseline-max axis:

```math
G_6
=
1.12293896508755.
```

Exploratory exact-map sextic maximum:

```math
G_{6,\max}
=
1.128222753402486.
```

N65 baseline-max axis:

```math
G_6
=
2.142954955629833.
```

Exploratory N65 maximum:

```math
G_{6,\max}
=
2.843180017666509.
```

Therefore:

```text
024A1R_ORIENTATION_CRITERION_REPAIR=
GREEN_ORIENTATION_LOCKED_SIGNAL_IN_BOTH_REFERENCES
```

But:

```text
LARGE_GENERALIZED_3D_PDE_SCAN_AUTHORIZED=
NO
```

because:

* the majority of orientations were not improved;
* teacher spatial overlap remained negligible;
* a real orientation-lock/control sector would require its own energy and reaction ledger;
* there was still no re-equilibrated generalized field.

Permanent lesson:

> Constitutive leverage and spatial kernel leverage are independent requirements.

---

# 4. 024A2 — explicit omega/Proca topological mediator

024A2 attempted to solve the spatial-placement problem by replacing the local $B_0^2$ sextic response with a finite-range vector mediator.

The tested class was schematically:

```math
\mathcal L
=
\mathcal L_{\rm Skyrme}
-\frac14W_{\mu\nu}W^{\mu\nu}
-\frac12m_\omega^2\omega_\mu\omega^\mu
+
g_\omega\omega_\mu B^\mu.
```

For:

```math
\omega_\mu=(\omega_0,0,0,0),
```

the static field obeys:

```math
(-\nabla^2+m_\omega^2)\omega_0
=
g_\omega B_0.
```

---

## 4.1 Proca stress anatomy

Define:

```math
e_E
=
\frac12|\nabla\omega_0|^2,
```

```math
e_m
=
\frac12m_\omega^2\omega_0^2.
```

Then:

```math
\rho_\omega
=
e_E+e_m.
```

With the local gradient chosen as one principal direction:

```math
p_{\perp1}
=
\rho_\omega,
```

```math
p_{\perp2}
=
\rho_\omega,
```

```math
p_\parallel
=
-e_E+e_m.
```

Therefore:

```math
S_\omega
=
2e_E+4e_m.
```

Define mass-energy fraction:

```math
f_m
=
\frac{e_m}{e_E+e_m}.
```

Then:

```math
\boxed{
\frac{S_\omega}{\rho_\omega}
=
2+2f_m.
}
```

Matching:

```math
S/\rho
=
2.649629
```

requires:

```math
f_m
=
0.3248145,
```

and gives approximately:

```math
\frac{p_\parallel}{\rho}
=
-0.350371.
```

Thus the Proca field was an excellent constitutive match.

---

## 4.2 Heavy-mass limit

For large $m_\omega$,

```math
\omega_0
\approx
\frac{
g_\omega B_0
}{
m_\omega^2
}.
```

The mass energy then approaches a local:

```math
B_0^2
```

shape.

This provided an internal falsifier:

> The finite-range vector model must approach the `024A1` local-sextic placement in the heavy-mass limit.

---

## 4.3 Numerical trap and rejection

The cheap midpoint force approximation looked promising.

For example:

```text
MU_H=
1.875

MIDPOINT_GAIN_VS_BASELINE=
APPROXIMATELY_2.04X
```

But the repaired promotion-grade finite-payload calculation reversed the sign.

Best resolved selected candidate:

```text
MU_H=
1.875

MU_DX=
0.4853897

OMEGA_FORCE=
-0.1032465247

ETA_OMEGA=
-0.6722593705

GAIN_VS_BASELINE=
-5.578554833

GAIN_VS_LOCAL_B0_SQUARED=
-11.06897040
```

The explicit omega field therefore produced inward rather than improved outward response in the repaired observable.

Decision:

```text
024A2_EXPLICIT_OMEGA_PROCA_TEACHER_MATCH_PREFILTER=
RED

LARGE_COUPLED_OMEGA_SKYRME_SCAN_AUTHORIZED=
NO
```

Permanent numerical lesson:

> Midpoint payload force is a scout, not promotion evidence when the source structure varies appreciably across the payload/kernel scale.

---

# 5. 024A3 — rigid SU(2) isorotation and inertia tomography

024A3 asked whether ordinary time-dependent dynamics of the existing Skyrme field could produce the required high active stress without introducing a new field.

Let:

```math
\phi
=
(\sigma,\boldsymbol\pi)
```

and choose unit isospin axis:

```math
\mathbf a.
```

At unit angular frequency:

```math
u
=
(0,\mathbf a\times\boldsymbol\pi).
```

Define spatial derivatives:

```math
q_i=\partial_i\phi,
```

and:

```math
t=u\cdot u,
```

```math
b_i=u\cdot q_i,
```

```math
s=\sum_iq_i\cdot q_i.
```

The rotational energy coefficient is:

```math
k
=
t(1+s)
-
\sum_i b_i^2.
```

The active-source coefficient is:

```math
\Delta S
=
2t(2+s)
-
2\sum_i b_i^2.
```

Therefore:

```math
\boxed{
\Delta S
=
2(k+t).
}
```

Hence:

```math
\frac{\Delta S}{k}
=
2+2\frac{t}{k}.
```

Using Cauchy-Schwarz gives:

```math
k\ge t,
```

and therefore:

```math
\boxed{
2
\le
\frac{\Delta S}{k}
\le
4.
}
```

Thus ordinary time-dependent Skyrme dynamics can in principle cross the static $S/\rho\le2$ ceiling.

---

## 5.1 Exact optimal isospin orientation

Define:

```math
T
=
|\boldsymbol\pi|^2I
-
\boldsymbol\pi\boldsymbol\pi^T,
```

and:

```math
C
=
\sum_i
(
\boldsymbol\pi\times\partial_i\boldsymbol\pi
)
(
\boldsymbol\pi\times\partial_i\boldsymbol\pi
)^T.
```

Then:

```math
M_E
=
(1+s)T-C,
```

```math
M_S
=
2[(2+s)T-C].
```

For isospin axis $\mathbf a$:

```math
k
=
\mathbf a^TM_E\mathbf a,
```

```math
\Delta S
=
\mathbf a^TM_S\mathbf a.
```

Integrated:

```math
J_E
=
\int M_E\,d^3x,
```

```math
J_A
=
\int K_PM_S\,d^3x.
```

The exact globally optimal rigid isorotation axis solves:

```math
\boxed{
J_A\mathbf a
=
\eta_{\rm iso}
J_E\mathbf a.
}
```

Therefore failure cannot be attributed to an arbitrary choice of internal axis.

---

## 5.2 Numerical result

Representative continuous reconstructions:

```text
CUBIC:
ETA0=
0.1308436427

ETA_ISO_OPT=
0.09479063347

GAIN=
0.7244573103
```

and:

```text
QUINTIC:
GAIN=
0.7256605046
```

Cross-representation axis agreement was approximately:

```text
ABS_DOT=
0.9999999984.
```

A conservative gain floor was:

```math
G_{\rm floor}
=
0.718441339.
```

Thus even the exactly optimized rigid-isorotation sector was approximately 28% less efficient than the static baseline.

Decision:

```text
024A3_ISOROTATION_INERTIA_TOMOGRAPHY=
RED_RIGID_ISOROTATION_NO_CERTIFIED_EFFICIENCY_ADVANTAGE

LARGE_COUPLED_ISOSPINNING_SOLVE_AUTHORIZED=
NO
```

---

# 6. 024A3 teacher mechanism audit — major reinterpretation

The same run audited the complete signed teacher source/kernel ledger.

Teacher:

```math
E
=
0.02450446248126442,
```

```math
A
=
1.000000000001593,
```

```math
C
=
0.02450446248122538.
```

Gross outward influence:

```math
A_+
=
1.029610949544636.
```

Gross opposing influence:

```math
A_-
=
0.0296109495430435.
```

Cancellation factor:

```math
\mathcal C
=
1.05922189909.
```

The signed channels were:

```text
S+ K+:
+1.005419284259049

S+ K-:
-0.007497667407335

S- K+:
-0.022113282135709

S- K-:
+0.024191665285587
```

The fraction of gross outward influence supplied by:

```text
S_POSITIVE
AND
K_POSITIVE
```

was:

```math
0.976504071468659
```

or:

```text
97.6504%.
```

The conventional one-sided repulsive channel:

```text
S_NEGATIVE
AND
K_NEGATIVE
```

supplied only:

```math
0.023495928531341
```

or:

```text
2.3496%
```

of gross outward influence.

For the dominant $S_+K_+$ channel:

```text
FRACTION_BEYOND_PAYLOAD_CENTER=
100_PERCENT

FRACTION_COMPLETELY_BEYOND_PAYLOAD_SPHERE=
90.71862055_PERCENT
```

with force-weighted centroid approximately:

```math
r/h
\approx
0.0471631,
```

```math
z/h
\approx
1.0803867.
```

The payload center is:

```math
z/h=1.
```

Therefore the dominant teacher mechanism was:

> **Positive active stress-energy located beyond the payload, gravitationally attracting the payload toward it in the nominal outward direction.**

This forced a permanent reinterpretation.

The statement:

```text
RAW_TEACHER_HEADROOM=
APPROXIMATELY_17230X
```

remains numerically true for the original relaxed operational-force objective.

But:

```text
TRUE_ONE_SIDED_REPULSIVE_TEACHER_HEADROOM=
NOT_ESTABLISHED
```

and:

```text
OLD_RAW_17230X_TRUE_STANDOFF_TARGET=
RETIRED.
```

Future work must distinguish:

```text
CLASS_A:
TRUE_ONE_SIDED_STANDOFF_REPULSION

CLASS_B:
EMBEDDED_OR_TWO_SIDED_OPERATIONAL_OUTWARD_GRAVITY
```

006D is Class A.

The raw teacher is overwhelmingly Class B.

---

# 7. 024A4 — corrected true-stand-off source reconstruction

024A4 rebuilt the optimization problem with the source forced onto the source side of the payload.

Normalize payload center to:

```math
z/h=1.
```

The strict source satisfies:

```math
z\le0.
```

Then the axial payload kernel has fixed sign:

```math
K<0.
```

---

## 7.1 Strict stand-off type-I DEC bound

For the normalized geometry:

```math
|K|\le1.
```

Type-I DEC gives:

```math
-\rho
\le
p_i
\le
\rho.
```

Therefore:

```math
S
=
\rho+p_1+p_2+p_3
\ge
-2\rho.
```

Since $K<0$:

```math
SK
\le
2\rho|K|
\le
2\rho.
```

Integrating:

```math
A
\le
2E.
```

Thus:

```math
\boxed{
C
=
\frac{E}{A}
\ge
\frac12.
}
```

This is a rigorous class bound for the normalized strict type-I DEC stand-off source.

It does **not** prove that $C=1/2$ is physically attainable.

Using:

```math
C_{B7}
=
422.2220709083,
```

the maximum possible B7-relative headroom under this broad class bound is:

```math
\frac{422.2220709083}{0.5}
=
844.4441418166.
```

Therefore:

```text
RAW_17230X_COMPATIBLE_WITH_STRICT_Z_LE_0_STATIC_DEC=
NO.
```

This proved mathematically that the original raw teacher signal could not represent a strict one-sided static DEC source.

---

## 7.2 Geometry ladder

The source reconstructions used the previous teacher diagnostics:

```text
R99/H=
3.3536817710

FROZEN_RHO_CAP=
1.6698553194
```

Three geometries were tested.

### Kernel-negative tangent scout

At N16:

```math
C
=
0.902275268073.
```

This would correspond to large source-level headroom, but its force effective width was only:

```text
1.696 CELLS.
```

It was under-resolved and never promoted.

### Half-gap scout

With source front:

```math
z_{\max}
=
0.5h,
```

the N16 result was:

```math
C
=
11.22468087816.
```

Participation widths were approximately:

```text
ENERGY=
9.57 CELLS

FORCE=
4.70 CELLS.
```

This was scientifically interesting but not a strict stand-off source and not a certified record.

### Strict source

At:

```math
z\le0
```

the frozen-cap optimization was infeasible at:

```text
N16
N24
N32
N40
```

for both freecore and matched-core variants.

An independent SCS solve agreed with Clarabel at N16.

The initial result was therefore:

```text
RED_TRUE_STANDOFF_SOURCE_PRIMARY_NOT_GREEN
```

but this was not promoted to a physical no-go because the inherited density cap was not a fundamental law.

---

# 8. 024A4R — stand-off feasibility decomposition

024A4R identified what had actually caused the strict-source infeasibility.

It separated:

```text
RADIAL_SUPPORT

DENSITY_CAPACITY

SOURCE_THICKNESS
```

rather than treating the failed capped solve as a theorem.

---

## 8.1 Positive control

The known thin 006B architecture was reconstructed as a positive control.

This established that the optimization machinery could reproduce a known true-stand-off conserved source.

Therefore:

```text
006B_POSITIVE_CONTROL=
PASS.
```

---

## 8.2 Radial support was not the primary obstruction

The uncapped solver showed that the known thin spatial stress-transfer architecture remained feasible over appropriate radial support.

Thus:

```text
RADIAL_SUPPORT_OBSTRUCTION=
NO
```

for the tested known architecture.

---

## 8.3 Density cap was the primary obstruction

At fixed strict geometry, multiplying the inherited Introspective density cap eventually restored feasibility.

The critical cap multiplier was bracketed approximately by:

```text
3.625
TO
3.75.
```

Therefore:

```text
024A4R_DENSITY_CAP_IS_A_FEASIBILITY_OBSTRUCTION=
YES.
```

The original cap was a numerical regularizer inherited from Introspective, not a fundamental material bound.

Thus the original strict-source infeasibility was **not** a theorem against static stand-off DEC sources.

---

## 8.4 Thickness rescue

Increasing the available source volume/thickness could also restore feasibility under the frozen cap.

But the resulting source was not more efficient than 006D.

Decision:

```text
024A4R_STANDOFF_FEASIBILITY_DECOMPOSITION=
YELLOW_CAUSE_IDENTIFIED_NO_NEW_CERTIFIED_STANDOFF_RECORD

NEW_CERTIFIED_STANDOFF_RECORD=
NO

BEST_CERTIFIED_TRUE_STANDOFF_SOURCE=
006D
```

This ended abstract strict-source coefficient polishing.

---

# 9. 024B — dynamic virial and pulse rerank

The project next asked whether a transient source could exploit stress timing and spatial kernel differences while avoiding the static support penalty.

The correct operational observable was defined as:

```math
\Delta v
=
\int a(t)\,dt.
```

Peak acceleration alone was not considered sufficient.

---

## 9.1 Closed-cycle virial identity

For an isolated conserved symmetric stress tensor:

```math
\partial_\mu T^{\mu\nu}=0.
```

Define:

```math
I_{ij}
=
\int
x_i x_j T^{00}\,d^3x.
```

Differentiate:

```math
\frac{dI_{ij}}{dt}
=
\int
(
x_iT^{0j}
+
x_jT^{0i}
)
\,d^3x.
```

Differentiate again and use conservation:

```math
\boxed{
\frac{d^2I_{ij}}{dt^2}
=
2
\int T^{ij}\,d^3x.
}
```

For a complete cycle returning to the same macroscopic state and first moment:

```math
\dot I_{ij}(T)
=
\dot I_{ij}(0),
```

so:

```math
\boxed{
\int_0^Tdt
\int T^{ij}\,d^3x
=
0.
}
```

Taking the trace:

```math
\boxed{
\int_0^Tdt
\int
(T^{xx}+T^{yy}+T^{zz})
\,d^3x
=
0.
}
```

A negative-stress phase must therefore be compensated by positive stress elsewhere in the cycle.

The only possible gain is **spatiotemporal kernel rectification**:

```text
NEGATIVE STRESS:
HIGH PAYLOAD KERNEL

POSITIVE RESET STRESS:
LOW PAYLOAD KERNEL
```

A static source multiplied by a time envelope is not a conserved pulse solution.

---

# 10. Canonical scalar active-source identity

For canonical real scalar matter:

```math
\rho
=
\frac12\dot\phi^2
+
\frac12|\nabla\phi|^2
+
V.
```

The pressure trace is:

```math
p_x+p_y+p_z
=
\frac32\dot\phi^2
-
\frac12|\nabla\phi|^2
-
3V.
```

Thus:

```math
S
=
\rho+p_x+p_y+p_z
```

gives:

```math
\boxed{
S
=
2\dot\phi^2
-
2V.
}
```

The gradient term cancels exactly.

Therefore:

```text
POTENTIAL_DOMINATED:
S < 0

KINETIC_DOMINATED:
S > 0
```

This became the central dynamic stress-conversion identity.

---

# 11. 024B source-level pulse results

The direct Q-wall route was unattractive in the tested exact/reference configuration.

A thin collapsing wall source-level calculation gave:

```math
C_{\rm wall,opt}
\approx
2.11803398875.
```

A relaxed idealized rectifier reached:

```math
C
=
2.
```

These were source-level upper bounds, not microscopic conserved pulse solutions.

The $1/G$ absolute scaling remained.

---

# 12. 024B1–024B3 — wall pulse falsification

## 024B1

Natural outer-edge collapse produced the wrong full-cycle sign.

A held-rim center-out hole initially yielded a relaxed source-level result around:

```math
C
\approx
1.92734,
```

but the support holding the rim had not yet been gravitationally included.

A free two-boundary annulus also failed to produce useful closed-cycle outward response.

## 024B2

The minimum stationary DEC rim support was then included.

Its gravitational effect was inward and larger than the apparent wall gain.

The corrected reference became inward.

An optimistic supported-annulus limit reproduced approximately:

```math
C
\approx
79.753234,
```

essentially the old 005B result.

This closed the tested stationary-rim loophole.

It was **not** a generic no-go theorem for all vortons or all transient walls.

## 024B3

A large free annular Nambu-Goto-type pulse scan tested:

```text
16384 REQUESTED CASES.
```

Results:

```text
CLOSED_POSITIVE_ETA_CASES=
0

CLOSED_POSITIVE_INSTANTANEOUS_CASES=
0

ENERGY_CONSERVATION_RELATIVE_ERROR<
7.45e-15
```

Decision:

```text
FREE_OUTWARD_ANNULAR_WALL_PULSE=
RED_IN_TESTED_NAMBU_GOTO_CLASS

PURE_GR_DOMAIN_WALL_PULSE_FAMILY=
PAUSED

PULSE_MICROSCOPIC_PDE_AUTHORIZED=
NO
```

Permanent pulse requirement:

```text
FORMATION
T0I
TIJ
COLLAPSE_OR_RETURN
REACTION_MOMENTUM
RADIATION
RESET
FULL_CYCLE_DELTA_V
TOTAL_CYCLE_ENERGY
REPETITION_RATE
LOSSES
```

must all be included.

---

# 13. 024C — teacher-guided strict static stress lens

024C returned to strict one-sided static sources but used the Introspective design principles rather than the old teacher morphology.

The source was entirely below the payload plane.

Refined coefficients were:

```text
MEDIUM:
C=
38.3223910871

HIGH:
C=
33.8332520842
```

The conservative comparison was therefore worse than:

```math
C_{006D}
=
23.591586299249.
```

An independent vector-force reconstruction agreed at approximately:

```text
5.9e-11
```

relative error.

---

## 13.1 Working-region quality

At aperture radius:

```math
0.5h
```

all sampled axial forces remained outward, but:

```text
FLATNESS=
12.08_PERCENT

MAX_TRANSVERSE_FRACTION=
57.4_PERCENT.
```

Thus the planar working-region gate failed.

---

## 13.2 Stress anatomy

The selected source decomposed approximately as:

```text
A_RHO=
-3.17778

A_STRESS=
+4.17778

NET=
+1.
```

Therefore the ordinary energy density attracted the payload.

Spatial stress supplied more than the entire net outward response and overcame that attraction.

Durable result:

```math
\boxed{
\text{Useful conservative outward gravity can be stress-dominated while all local energy density remains positive.}
}
```

Cancellation was approximately:

```text
3.43.
```

Approximately:

```text
21.6_PERCENT
```

of source energy accounted for 90% of selected useful-force concentration.

Vertical burial/routing improved the relevant contribution only modestly.

Decision:

```text
024C=
RED_VS_006D.
```

---

# 14. 024C1 — intuitive guarded stress sandwich

An intuitive architecture containing:

```text
TOP WORKING PLATE
SIDE STRESS-RETURN GUARD
BURIED BACKPLANE
```

was tested.

Some point-payload cases occurred in approximately:

```text
C=
75_TO_82
```

but no selected physically admissible planar configuration passed the full working-region gate.

Decision:

```text
RED_INTUITIVE_SANDWICH_NO_PLANAR_FEASIBLE_SCOUT
```

Hand-designed static source geometry scans were therefore stopped absent a genuinely new physical stress mechanism.

---

# 15. 024D — counterrotating toroidal stress conversion

The project then tested the user's two-counterrotating-toroid idea.

The important result was not that spin itself created repulsion.

It was that a circulating field might carry different **stress states** through regions with different gravitational kernels.

---

## 15.1 Counterrotation cancels momentum, not spatial stress

For two equal opposite streams:

```math
T^{0i}_{(+)}
+
T^{0i}_{(-)}
=
0.
```

Net momentum density cancels.

However spatial kinetic stress scales quadratically:

```math
T^{ij}
\propto
v_i v_j.
```

Under:

```math
v_i\rightarrow-v_i,
```

one has:

```math
(-v_i)(-v_j)
=
v_iv_j.
```

Therefore:

```math
\boxed{
T^{ij}_{(+)}
+
T^{ij}_{(-)}
\neq0.
}
```

Counterrotation is not free stress generation.

---

# 16. Pure Maxwell circulation sign theorem

The Maxwell stress tensor is traceless:

```math
T^\mu_{\ \mu}=0.
```

For signature $(-,+,+,+)$:

```math
-\rho+p_1+p_2+p_3=0.
```

Therefore:

```math
p_1+p_2+p_3=\rho.
```

Thus:

```math
\boxed{
S
=
\rho+p_1+p_2+p_3
=
2\rho>0.
}
```

Pure linear electromagnetic circulation cannot provide the negative active source required for strict one-sided stand-off repulsion.

Decision:

```text
PURE_LINEAR_EM_COUNTERROTATION=
RED_ANALYTIC.
```

---

# 17. 024D numerical campaign

A large low-discrepancy campaign tested:

```text
IDEAL_DEC_SPINNING_CEILING
CANONICAL_SCALAR_TRANSPORT
MASSIVE_HEADON_COLLIDER
MASSLESS_HYBRID_COLLIDER
ELASTIC_VORTON_CONVERTER
```

The strongest relaxed canonical-scalar result was:

```math
\boxed{
C_{\rm 024D,scalar}
=
6.610457607426174.
}
```

The selected transport speed was low:

```text
BETA≈0.116
```

and the interaction phase was broad.

Hypothetical loss tests still retained source-level headroom for moderate losses.

However:

```text
MICROSCOPIC_FIELD_REALIZATION=
NO

FULL_DYNAMIC_LOCAL_TMUNU_CONSERVATION=
NOT_ESTABLISHED

FULL_STABILITY=
NO

NONLINEAR_GR=
NO
```

The spin-speed audit showed that once the conversion was required to be physically limited by available kinetic/spin energy, the spin advantage disappeared.

Decision:

```text
STRESS_CONVERSION_IS_PROMISING

SPIN_IS_NOT_THE_DRIVER

024D=
YELLOW_EFFECTIVE_SOURCE_CYCLE_ONLY
```

---

# 18. 024D1R — kernel-adaptive orbit repair

The first orbit model had assigned the useful phase to the geometric top of the torus and reset to the geometric bottom.

For an on-axis payload the actual kernel is:

```math
K
=
\frac{
1-z
}{
[
\rho^2+(1-z)^2
]^{3/2}
}.
```

Its extrema need not coincide with geometric top/bottom.

024D1R repaired the placement.

---

## 18.1 Pure toroidal constant-kernel theorem

For pure toroidal motion:

```math
\rho=\mathrm{constant},
```

```math
z=\mathrm{constant}.
```

Therefore:

```math
K=\mathrm{constant}
```

around the full orbit.

The negative conversion phase and required positive reset then occur at identical kernel.

The closed-cycle stress compensation cancels there.

Therefore:

```math
\boxed{
\text{Pure toroidal transport cannot rectify the on-axis gravitational kernel in this mechanism.}
}
```

Poloidal displacement is required.

---

## 18.2 Fenchel-theorem claim repair

For any closed space curve:

```math
\oint\kappa\,ds
\ge
2\pi.
```

This is a geometric theorem.

It does **not** imply a universal support-energy law:

```math
E_{\rm support}
\propto
\oint\kappa\,ds.
```

The later run therefore separated:

```text
EXACT_GEOMETRIC_CURVATURE_BOUND
```

from:

```text
HEURISTIC_ENGINEERING_CURVATURE_PENALTY.
```

This prevented a geometric theorem from being overpromoted into an energetic theorem.

---

## 18.3 024D1R result

The strongest finite-window scalar DEC-floor no-spin result was:

```math
C
\approx
19.046178.
```

An independent phase reconstruction agreed at approximately $10^{-8}$ relative scale.

The selected winding was effectively pure poloidal.

The interaction/reset duties were approximately:

```text
INTERACTION=
0.5412

RESET=
0.4538.
```

Thus almost the entire cycle participated.

The mechanism was not a sparse collision.

It was better described as:

> **A closed stress-state conveyor moving a field between different gravitational-kernel regions.**

Kinetic-limited variants did not retain the improvement.

Decision:

```text
024D1R=
YELLOW_KERNEL_ADAPTIVE_SCALAR_ORBIT_HEADROOM_KINETIC_DRIVER_NOT_PROMOTED
```

Durable statement:

> **The orbit matters because it transports stress states through different gravitational kernels, not because energy spins fast.**

`024D2`, an actual canonical-scalar poloidal field preflight, remains deferred.

---

# 19. 024E — noncolliding dual-toroid scalar portal

024E tested whether two distinct scalar packets could pass near one another and produce a local potential-dominated interaction zone without collision.

Use:

```math
V_{\rm int}
=
g|\Phi|^2|\Chi|^2.
```

For a positive potential contribution:

```math
\rho_{\rm int}
=
V_{\rm int},
```

and:

```math
p_1=p_2=p_3=-V_{\rm int}.
```

Therefore:

```math
S_{\rm int}
=
V_{\rm int}
-
3V_{\rm int}
=
-2V_{\rm int}.
```

Hence:

```math
\boxed{
-\frac{S_{\rm int}}{\rho_{\rm int}}
=
2.
}
```

The positive scalar portal saturates the type-I DEC negative-active limit.

Therefore a failure cannot be fixed simply by demanding a more negative local type-I DEC potential sector.

The remaining levers are:

```text
PRODUCTIVE_PARTICIPATION
KERNEL_PLACEMENT
SUPPORT_OVERHEAD
```

---

# 20. 024E result

More than two million strict directional cases were evaluated.

No strict positive survivor appeared at the declared perturbative coupling ceiling.

The best candidate required approximately:

```text
G_REQUIRED_TO_BEAT_006D
≈
1.79e4
```

versus:

```math
4\pi
\approx
12.57.
```

The asymptotic source geometry could in principle have useful kernel leverage, but too little total energy occupied the interaction.

Decision:

```text
024E=
RED
```

and one final teacher-guided morphology rescue was authorized.

---

# 21. 024E1 — teacher-guided anisotropic close-pass rescue

024E1 broadened the packet morphology substantially.

Anisotropic compact packets were allowed to become:

```text
WIDE_HORIZONTALLY
THIN_VERTICALLY
```

while keeping strict half-amplitude cores nonoverlapping.

Campaign size:

```text
524288 MORPHOLOGIES
X
2 THROAT DIRECTION CLASSES
=
1048576 KINEMATIC CASES.
```

The predecessor spherical result was independently reconstructed before interpreting the new search.

---

## 21.1 Productive participation result

Maximum strict peak portal participation:

```math
f_{\rm peak}
=
1.18388\times10^{-3}
```

or:

```text
0.1184_PERCENT.
```

Maximum strict cycle participation:

```math
f_{\rm cycle}
=
5.3583\times10^{-5}
```

or:

```text
0.00536_PERCENT.
```

Compare with the approximate Introspective productive-energy reference:

```math
f_{\rm teacher}
\approx
0.197.
```

Then:

```math
\frac{
f_{\rm peak}
}{
0.197
}
\approx
0.00601.
```

The best peak portal interaction achieved only approximately 0.6% of the teacher-like participation scale.

It was also worse than the previous spherical portal participation by a factor of approximately:

```text
5.3.
```

---

## 21.2 Coupling obstruction

The selected strict actual-routing candidate required approximately:

```math
g_{\rm 006D}
\approx
3.81\times10^5
```

to beat 006D.

Even the deliberately over-favorable teacher-routing ceiling required approximately:

```math
g_{\rm teacher}
\approx
1.67\times10^5.
```

Cancellation was excellent:

```text
≈1.00035
```

and kernel placement was favorable.

Therefore the failure was not primarily caused by either.

It was isolated to:

```math
\boxed{
\text{INSUFFICIENT PRODUCTIVE INTERACTION PARTICIPATION PER TOTAL FIELD ENERGY.}
}
```

Decision:

```text
024E_NONCOLLIDING_CLOSE_PASS_PORTAL=
CLOSED

024E1_ANISOTROPIC_RESCUE=
CLOSED

DO_NOT_RUN_ANOTHER_CLOSE_PASS_PROFILE_SCAN=
YES
```

---

# 22. Necessary-conditions map

After the repeated source-level failures, the project stopped inventing geometries and explicitly listed the independent requirements for physically useful antigravity.

The resulting ladder is:

```text
1. CORRECT LOCAL STRESS SIGN / MAGNITUDE

2. SOURCE-PAYLOAD SIDEDNESS

3. LARGE PRODUCTIVE PARTICIPATION

4. HIGH GRAVITATIONAL-KERNEL LEVERAGE

5. LOW-KERNEL COMPENSATION / SUPPORT

6. LOW CANCELLATION

7. LOCAL TMUNU CONSERVATION

8. ACTUAL MICROSCOPIC OR MATERIAL LAW

9. GLOBAL FIELD / DEFORMATION COMPATIBILITY

10. STATIONARITY OR COMPLETE DYNAMIC CYCLE

11. CAUSALITY / HYPERBOLICITY

12. STABILITY

13. FINITE INTERFACES / SUPPORT COMPLETION

14. NONLINEAR EINSTEIN-MATTER CONTINUATION

15. ABSOLUTE ENERGY SCALING

16. EMPIRICAL / TECHNOLOGICAL REALIZABILITY
```

This conditions map directly motivated `025A`.

---

# 23. 025A — Saturating Relativistic Hencky Solid

The project next abandoned the previous field families and asked a more direct question:

> Can an explicit positive-energy relativistic matter law locally generate almost exactly the stress architecture required by 006B/006D?

The proposed continuum uses principal logarithmic material-density strains:

```math
y_i
=
\ln
\left(
\frac{n_i}{n_{i0}}
\right).
```

The stored energy is:

```math
\boxed{
\rho
=
\rho_\star(\Phi)
\exp
\left[
\sum_i y_i
+
\frac1k
\sum_i
\ln\cosh(ky_i)
\right].
}
```

This is a project-derived candidate relativistic continuum EFT.

It is not a known microscopic material.

---

# 24. Exact Hencky pressure law

For relativistic hyperelastic matter:

```math
\rho+p_i
=
\frac{\partial\rho}{\partial y_i}.
```

Take logarithm:

```math
\ln\rho
=
\ln\rho_\star
+
\sum_jy_j
+
\frac1k
\sum_j\ln\cosh(ky_j).
```

Differentiate:

```math
\frac{\partial\ln\rho}{\partial y_i}
=
1+\tanh(ky_i).
```

Therefore:

```math
\frac1\rho
\frac{\partial\rho}{\partial y_i}
=
1+\tanh(ky_i).
```

Using the hyperelastic identity:

```math
1+\frac{p_i}{\rho}
=
1+\tanh(ky_i).
```

Thus:

```math
\boxed{
\frac{p_i}{\rho}
=
\tanh(ky_i).
}
```

This is the central constitutive result of 025A.

---

# 25. Positive energy and finite-strain DEC

The exponential stored energy gives:

```math
\rho>0.
```

At every finite strain:

```math
|\tanh(ky_i)|<1.
```

Therefore:

```math
|p_i|<\rho.
```

Thus the local type-I dominant energy condition follows automatically for finite strains in the declared model.

WEC and NEC follow locally.

This is stronger than assigning a desired stress tensor and checking DEC afterward.

---

# 26. Log-strain convexity

Differentiate again:

```math
\frac{
\partial^2\ln\rho
}{
\partial y_i\partial y_j
}
=
k\operatorname{sech}^2(ky_i)\delta_{ij}.
```

For:

```math
k>0,
```

the right-hand side is nonnegative.

Thus the logarithmic stored energy is convex in the declared principal strain variables.

This is a local constitutive-health result, not a complete global stability proof.

---

# 27. Principal longitudinal characteristic speed

Let:

```math
w
=
\tanh(ky_i)
=
\frac{p_i}{\rho}.
```

Then:

```math
\rho_{,i}
=
\rho(1+w).
```

Also:

```math
w_{,i}
=
k(1-w^2).
```

Thus:

```math
\rho_{,ii}
=
\rho(1+w)^2
+
\rho k(1-w^2).
```

The principal longitudinal characteristic relation is:

```math
c_{L,i}^2
=
\frac{
\rho_{,ii}-\rho_{,i}
}{
\rho_{,i}
}.
```

Substitution and factorization give:

```math
\boxed{
c_{L,i}^2
=
k+(1-k)w
=
k+(1-k)\frac{p_i}{\rho}.
}
```

For:

```math
1/2\le k<1
```

and:

```math
-1<w<1,
```

the principal longitudinal mode remains nonnegative and subluminal.

At the unstrained state:

```math
c_L^2=k,
```

and the reference transverse value is:

```math
c_T^2=\frac{k}{2}.
```

This principal-mode result was later replaced by a stronger full acoustic-cone reconstruction in 025B0.

---

# 28. Exact state-energy factors

For one activated principal direction:

```math
w
=
\tanh(ky),
```

so:

```math
y
=
\frac{\operatorname{atanh}w}{k}.
```

The stored-energy factor simplifies to:

```math
\boxed{
\frac{\rho}{\rho_\star}
=
(1-w)^{-1/k}.
}
```

Therefore:

### Core

For:

```math
p_r=p_\phi=-t\rho,
```

```math
\boxed{
\frac{
\rho_{\rm core}
}{
\rho_{\star,\rm core}
}
=
(1+t)^{-2/k}.
}
```

### Transfer annulus

For:

```math
p_r=-q\rho,
```

```math
p_\phi=+q\rho,
```

```math
\boxed{
\frac{
\rho_{\rm ann}
}{
\rho_{\star,\rm ann}
}
=
(1-q^2)^{-1/k}.
}
```

### Support

For support stress ratio $s$:

```math
\boxed{
\frac{
\rho_{\rm support}
}{
\rho_{\star,\rm support}
}
=
(1-s)^{-1/k}.
}
```

These formulas later produced the grading–geometry identity.

---

# 29. Generalized 006B material architecture

The finite-ratio hyperelastic analogue of the 006B source uses:

### Core

```math
p_r=p_\phi=-tU_0.
```

### Transfer annulus

```math
\rho
=
\frac{t}{q}
U_0
\frac{a^2}{r^2},
```

```math
p_r
=
-tU_0\frac{a^2}{r^2},
```

```math
p_\phi
=
+tU_0\frac{a^2}{r^2}.
```

### Outer support

```math
\lambda
=
\frac{
tU_0a^2
}{
sR
}.
```

At:

```math
t=q=s=1,
```

this recovers the known thin 006B architecture.

The 006B coefficient was independently reconstructed to approximately:

```text
3.49e-15
```

relative error.

---

# 30. 025A numerical campaign

The primary scan evaluated:

```text
2^20
=
1048576
```

joint constitutive/geometry cases.

Results included:

```text
OUTWARD_CAUSAL_CASES=
516453

CASES_BEATING_006D_FINITE_COEFFICIENT=
284

CASES_BEATING_006B_THIN_LIMIT=
0
```

The strain frontier was:

```text
MAX_ABS_Y <= 1.0:
C≈95.76

MAX_ABS_Y <= 1.5:
C≈36.71

MAX_ABS_Y <= 2.0:
C≈27.60

MAX_ABS_Y <= 2.5:
C≈24.93

MAX_ABS_Y <= 3.0:
C≈23.99

MAX_ABS_Y <= 3.5:
C≈23.64

MAX_ABS_Y <= 4.0:
C≈23.5104

MAX_ABS_Y <= 5.0:
C≈23.44
```

The selected cap-4 candidate was:

```math
\boxed{
C_{025A}
=
23.51038455027865.
}
```

Its stress ratios were approximately:

```text
K=
0.9499420066

T=
0.9989983885

Q=
0.9989988645

SUPPORT=
0.9989836091
```

and:

```math
Y_{\rm core}
\approx
3.999608,
```

```math
Y_{\rm ann}
\approx
3.999858.
```

An independent finite spherical-payload calculation gave approximately:

```math
C_{\rm payload}
\approx
23.51058
```

with all tested payload samples outward.

But:

```text
SUPERSEDES_006D_CLAIM_CLASS=
NO
```

because this remained a thin, locally assigned material state rather than one globally compatible finite material body.

The required intrinsic material-scale grading was approximately:

```math
\frac{
\rho_{\star,\rm ann}
}{
\rho_{\star,\rm core}
}
\approx
3.36\times10^{-4},
```

approximately a 3000:1 contrast.

Decision:

```text
025A=
YELLOW_STRONG_LOCAL_CONSTITUTIVE_REALIZATION_PREFLIGHT
```

---

# 31. 025B0 — exact ordinary radial material compatibility

025B0 asked whether all of the locally allowed 025A stress states could arise from **one globally continuous deformation**.

For an ordinary Euclidean-reference axisymmetric radial material map:

```math
X=X(r),
```

the principal material densities are:

```math
n_r
=
\frac{dX}{dr},
```

```math
n_\phi
=
\frac{X}{r}.
```

Hence:

```math
y_r
=
\ln\frac{dX}{dr},
```

```math
y_\phi
=
\ln\frac{X}{r}.
```

Define:

```math
g(r)
=
\ln\frac{X}{r}.
```

Then:

```math
X=re^g.
```

Therefore:

```math
\frac{dX}{dr}
=
e^g(1+rg').
```

Thus the exact compatibility relations are:

```math
\boxed{
y_\phi=g,
}
```

```math
\boxed{
y_r
=
g+\ln(1+rg').
}
```

Orientation preservation requires:

```math
1+rg'>0.
```

---

# 32. Exact incompatibility of the piecewise 025A target

The selected core requires approximately:

```math
y_\phi=-4.
```

Therefore:

```math
\frac{X}{r}
\approx
e^{-4}
=
0.01832.
```

The annulus requires approximately:

```math
y_\phi=+4.
```

Therefore:

```math
\frac{X}{r}
\approx
e^4
=
54.59.
```

The required ratio is:

```math
\boxed{
\frac{
(X/r)_{\rm ann}
}{
(X/r)_{\rm core}
}
=
2.9793675\times10^3.
}
```

A continuous deformation has continuous $X/r$ for $r>0$.

Therefore the exact piecewise target cannot be realized by one ordinary radial deformation.

There is an additional sign obstruction.

Suppose $g$ passes upward through zero.

At the crossing:

```math
g=0,
```

and for the required rising transition:

```math
g'>0.
```

Then:

```math
y_r
=
\ln(1+rg')
>
0.
```

Therefore the transition develops radial compression, while the ideal transfer annulus wants radial tension.

Thus:

```math
\boxed{
\text{The direct diagonal 006B/025A stress transformer is incompatible with an ordinary Euclidean-reference radial solid.}
}
```

This is not a theorem against every possible 2-D elastic deformation.

---

# 33. Compatible radial equilibrium identity

Static radial force balance is:

```math
\frac{dp_r}{dr}
+
\frac{p_r-p_\phi}{r}
=
0.
```

Define:

```math
w_r
=
\frac{p_r}{\rho},
```

```math
w_\phi
=
\frac{p_\phi}{\rho}.
```

For sign-definite radial tension:

```math
p_r=-P,
```

with $P>0$.

Then:

```math
\boxed{
\frac{d\ln P}{d\ln r}
=
-1+\frac{w_\phi}{w_r}.
}
```

025B0 used this relation to reconstruct the stress magnitude from the compatible deformation rather than assigning the desired source by hand.

---

# 34. 025B0 full relativistic acoustic cone

The final local stability gap was tested using the relativistic Hadamard elasticity tensor and generalized characteristic equation:

```math
[
v^2(\rho h^{ac}+p^{ac})
-
Q^{ac}
]
\iota_c
=
0,
```

where:

```math
Q^{ac}
=
A^{abcd}\nu_b\nu_d.
```

The numerical implementation was first validated at zero strain and against the exact principal-longitudinal formula.

Validation:

```text
ZERO_STRAIN_MAX_ERROR=
1.388957641124122e-7

ZERO_STRAIN=
PASS

PRINCIPAL_LONGITUDINAL_MAX_ERROR=
1.769641040018310e-3

PRINCIPAL_LONGITUDINAL=
PASS
```

Full selected-state directional scan:

### Core

```text
MIN_V2=
0.3349168112

MAX_V2=
0.9993336964

NEGATIVE_DIRECTIONS=
0

SUPERLUMINAL_DIRECTIONS=
0
```

### Annulus

```text
MIN_V2=
3.4096367e-4

MAX_V2=
0.9999593115

NEGATIVE_DIRECTIONS=
0

SUPERLUMINAL_DIRECTIONS=
0
```

### Support

```text
MIN_V2=
3.6804990e-4

MAX_V2=
0.9999495667

NEGATIVE_DIRECTIONS=
0

SUPERLUMINAL_DIRECTIONS=
0
```

Thus:

```text
025B0_FULL_SELECTED_STATE_ACOUSTIC_CONE=
PASS
```

within the tested continuum model.

These margins are near the boundaries and should not be described as comfortably stable.

---

# 35. Compatible radial source result

Once compatibility was enforced from the beginning, the optimized material ceased to reproduce the efficient 006B stress-transfer annulus.

At strain cap 4:

```math
C
=
80.1054496251.
```

Independent finite-payload value:

```math
C_{\rm payload}
=
80.1052662886.
```

All 128 tested payload points remained outward.

At strain cap 6:

```math
C
=
79.7614963466.
```

Compare:

```math
C_{005B}
=
79.7531481160.
```

The compatible radial material had effectively rediscovered the old supported-tension disk.

Therefore:

```text
025A_LOCAL_CONSTITUTIVE_LAW=
RETAIN

DIRECT_006B_RADIAL_HYPERELASTIC_REALIZATION=
DEMOTED

COMPATIBILITY_ERASES_006B_STRESS_TRANSFER_ADVANTAGE_IN_TESTED_RADIAL_CLASS=
YES
```

Permanent lesson:

```math
\boxed{
\text{Local constitutive accessibility is not enough; the stress field must arise from one globally integrable field or deformation.}
}
```

---

# 36. 025B1 — grading–geometry duality

025B1 asked whether the extreme material grading and the geometric incompatibility were independent problems.

They are not.

Define:

```math
G
=
\frac{
\rho_{\star,\rm ann}
}{
\rho_{\star,\rm core}
}.
```

From the state-energy formulas:

```math
G
=
\frac{t}{q}
(1+t)^{-2/k}
(1-q^2)^{1/k}.
```

Define the annular intrinsic geometric factor:

```math
\chi
=
e^{2Y_{\rm ann}}.
```

Since:

```math
Y_{\rm ann}
=
\frac{\operatorname{atanh}q}{k},
```

one has:

```math
\chi
=
\left(
\frac{1+q}{1-q}
\right)^{1/k}.
```

Multiply:

```math
G\chi
=
\frac{t}{q}
(1+t)^{-2/k}
(1-q^2)^{1/k}
\left(
\frac{1+q}{1-q}
\right)^{1/k}.
```

Using:

```math
1-q^2
=
(1-q)(1+q),
```

gives:

```math
\boxed{
G\chi
=
\frac{t}{q}
\left(
\frac{1+q}{1+t}
\right)^{2/k}.
}
```

When:

```math
t=q,
```

this reduces exactly to:

```math
\boxed{
G\chi=1.
}
```

For the selected 025A candidate the run found:

```text
G_TIMES_CHI=
1.000000024856745
```

with independently derived right-hand side agreeing to approximately:

```text
5.1e-15
```

relative error.

Thus the approximately:

```text
3000:1
```

material grading and:

```text
3000X
```

intrinsic geometric excess are the **same leading realization burden written in two languages**.

---

# 37. Axisymmetric non-Euclidean reference metric

Consider the axisymmetric reference metric:

```math
d\bar\ell^2
=
A(r)^2dr^2
+
B(r)^2d\phi^2.
```

Use:

```math
A=e^{y_r},
```

```math
B=re^{y_\phi}.
```

Define proper radial coordinate:

```math
du=A\,dr.
```

In the target annulus:

```math
y_r=-Y_{\rm ann},
```

```math
y_\phi=+Y_{\rm ann}.
```

Therefore:

```math
\frac{dB}{du}
=
\frac1A
\frac{dB}{dr}
=
e^{2Y_{\rm ann}}
=
\chi.
```

For the selected source:

```math
\boxed{
\chi
\approx
2980.1134.
}
```

---

## 37.1 Exact surface-of-revolution embedding obstruction

A stress-free axisymmetric Euclidean surface of revolution can be written:

```math
\mathbf X(u,\phi)
=
(
B(u)\cos\phi,
B(u)\sin\phi,
Z(u)
).
```

Because $u$ is proper meridional distance:

```math
\left(
\frac{dB}{du}
\right)^2
+
\left(
\frac{dZ}{du}
\right)^2
=
1.
```

Therefore necessarily:

```math
\boxed{
\left|
\frac{dB}{du}
\right|
\le1.
}
```

But the target requires approximately:

```math
\left|
\frac{dB}{du}
\right|
=
2980.
```

Therefore:

```math
\boxed{
\text{The selected target reference metric has no stress-free axisymmetric Euclidean surface-of-revolution isometric embedding.}
}
```

Decision:

```text
NON_EUCLIDEAN_AXISYMMETRIC_STRESS_FREE_ROUTE=
RED_EXACT_EMBEDDING_CONDITION
```

This does not rule out non-axisymmetric wrinkled embeddings, active prestrain, or genuinely 3-D geometries.

---

# 38. Gaussian-curvature transition invariant

For:

```math
d\bar\ell^2
=
A(r)^2dr^2
+
B(r)^2d\phi^2,
```

define:

```math
f(r)
=
\frac1A\frac{dB}{dr}.
```

The Gaussian curvature is:

```math
\boxed{
K
=
-\frac1{AB}
\frac{df}{dr}.
}
```

The area element is:

```math
dA
=
AB\,dr\,d\phi.
```

Therefore:

```math
\int K\,dA
=
-2\pi
\int
\frac{df}{dr}\,dr.
```

Hence:

```math
\boxed{
\int K\,dA
=
-2\pi
[
f_{\rm out}-f_{\rm in}
].
}
```

For the target transition:

```math
f_{\rm in}=1,
```

```math
f_{\rm out}=\chi.
```

Therefore:

```math
\boxed{
\int K\,dA
=
-2\pi(\chi-1).
}
```

For the selected candidate:

```math
\boxed{
\int K\,dA
\approx
-1.8718321573\times10^4.
}
```

Equivalently:

```math
\frac1{4\pi}
\int K\,dA
\approx
-1489.5567.
```

This is independent of transition width or smoothing profile.

---

# 39. Numerical curvature-validator repair

The first 025B1 smoke run evaluated $df/dr$ with a finite-difference gradient on extremely narrow high-curvature transitions.

It missed the exact invariant by more than the frozen numerical tolerance.

This was a numerical-validator failure, not a physical failure.

The implementation was repaired to use:

```text
ANALYTIC_DF_DR
+
SIMPSON_QUADRATURE
```

and the scientific thresholds were **not relaxed**.

Final full-resolution validation:

```text
DETERMINISTIC_CURVATURE_MAX_RELERR=
2.64e-14

RANDOMIZED_MONOTONE_TRANSITIONS=
512

RANDOMIZED_CURVATURE_MAX_RELERR=
2.34e-5

PASS=
YES
```

The failed original smoke log remains useful provenance but is superseded scientifically by the repaired result.

---

# 40. General 2-D same-$z$ mean-value lower bound

A more general axisymmetric material map can depend on both $r$ and $z$:

```math
X=X(r,z),
```

```math
Z=Z(r,z).
```

The hoop density nevertheless contains:

```math
n_\phi
=
\frac{X}{r}.
```

The selected target demands approximately:

```math
X_{\rm core}
=
ae^{-Y_c},
```

and:

```math
X_{\rm ann}
=
Re^{Y_a}.
```

Along a direct same-$z$ radial path, the mean-value theorem guarantees that somewhere:

```math
\left|
\frac{\partial X}{\partial r}
\right|
\ge
\frac{
Re^{Y_a}
-
ae^{-Y_c}
}{
R-a
}.
```

For the selected candidate:

```math
\boxed{
\max
\left|
\frac{\partial X}{\partial r}
\right|
\ge
78.75433.
}
```

The largest singular value of the deformation gradient satisfies:

```math
\sigma_{\max}
\ge
78.75433.
```

Thus the corresponding logarithmic strain obeys:

```math
\boxed{
y_{\max}
\ge
\ln(78.75433)
=
4.36633.
}
```

Therefore a direct same-$z$ path cannot realize the selected target under:

```math
|y|\le4.
```

A 2-D material path can detour through $z$, so this is not a general no-go theorem.

At strain cap 4, the minimum path-length diagnostic gave approximately:

```text
PATH_LENGTH_MULTIPLIER=
1.44244
```

and the simple straight-detour interpretation corresponds to vertical excursion approximately:

```text
3.38h.
```

At cap 5, the direct kinematic lower bound no longer forbids the target.

Thus:

```text
GENERAL_2D_EUCLIDEAN_SHEAR=
UNRESOLVED_DEFERRED
```

rather than RED.

---

# Result

The complete 024A–025B1 program substantially narrowed the problem but did not produce a new global pure-GR antigravity realization.

The strongest final interpretation is:

```text
006D=
STRONGEST_CONSERVATIVE_GLOBAL_TRUE_STANDOFF_SOURCE

C_006D=
23.591586299249

023BR_PLUS_023CR4R_N65=
STRONGEST_ACTUAL_MICROSCOPIC_FIELD_STACK

INTROSPECTIVE_RAW_TEACHER=
RETAIN_AS_MECHANISM_DIAGNOSTIC

INTROSPECTIVE_RAW_17230X=
NOT_TRUE_STANDOFF_HEADROOM

024A_MIXED_L2_L4_L6_V=
CONSTITUTIVELY_INTERESTING

024A1_NATURAL_B0SQ_PLACEMENT=
POOR_FOR_ROBUST_GEOMETRY

024A1R_ORIENTATION_LOCKED_SEXTIC_SIGNAL=
PRESENT

024A2_OMEGA_PROCA_FIXED_FIELD=
RED

024A3_RIGID_ISOROTATION=
RED

024A4_STRICT_STANDOFF_FROZEN_CAP=
INFEASIBLE

024A4R_FROZEN_CAP=
NONFUNDAMENTAL_FEASIBILITY_OBSTRUCTION

024B_DOMAIN_WALL_PULSE=
RED_OR_PAUSED_IN_TESTED_CLASS

024C_STATIC_STRESS_LENS=
RED_VS_006D

024D_SCALAR_STRESS_CONVERSION=
YELLOW_EFFECTIVE_SOURCE_LEVEL_HEADROOM

024D1R_POLAR_STRESS_CONVEYOR=
YELLOW_EFFECTIVE_SOURCE_LEVEL_HEADROOM

024E_024E1_CLOSE_PASS_PORTAL=
CLOSED

025A_HENCKY_LOCAL_MATTER_LAW=
RETAIN

025B0_FULL_LOCAL_ACOUSTIC_CONE=
PASS

025B0_ORDINARY_RADIAL_006B_REALIZATION=
DEMOTED_TO_APPROXIMATELY_005B_SCALE

025B1_GRADING_GEOMETRY_DUALITY=
PASS

025B1_AXISYMMETRIC_STRESS_FREE_REFERENCE_METRIC=
RED

GENERAL_2D_EUCLIDEAN_SHEAR=
UNRESOLVED_DEFERRED

NO_NEW_GLOBAL_PURE_GR_REALIZATION_SUPERSEDES_006D=
YES

PURE_GR_1_OVER_G_SCALING_ESCAPE=
NO

PRACTICAL_ANTIGRAVITY_DEVICE=
NO
```

---

# Verification

## Analytical

The slice produced or preserved analytical results including:

```text
DERIVATIVE_ORDER_ACTIVE_SOURCE_IDENTITY

GENERALIZED_DERRICK_VIRIAL_IDENTITY

SIXTH_ORDER_DEC_DESIGN_BOUND

PURE_BPS_STATIC_S_OVER_RHO_EQUALS_1

ORIENTATION_AVERAGE_KERNEL_THEOREM

PROCA_STRESS_DECOMPOSITION

RIGID_ISOROTATION_ACTIVE_SOURCE_IDENTITY

RIGID_ISOROTATION_GENERALIZED_EIGENVALUE_OPTIMUM

STRICT_STANDOFF_DEC_C_GE_1_OVER_2_BOUND

CLOSED_CYCLE_VIRIAL_COMPENSATION

CANONICAL_SCALAR_S_EQUALS_2_PHIDOT2_MINUS_2V

COUNTERROTATION_T0I_CANCELLATION_BUT_TIJ_ADDITION

MAXWELL_S_EQUALS_2RHO

PURE_TOROIDAL_CONSTANT_KERNEL_THEOREM

PORTAL_MINUS_S_OVER_RHO_EQUALS_2

HENCKY_P_OVER_RHO_EQUALS_TANH_KY

HENCKY_FINITE_STRAIN_DEC

HENCKY_LOG_STRAIN_CONVEXITY

HENCKY_PRINCIPAL_LONGITUDINAL_CHARACTERISTIC

RADIAL_DEFORMATION_COMPATIBILITY_IDENTITY

EXACT_025A_PIECEWISE_RADIAL_INCOMPATIBILITY

COMPATIBLE_RADIAL_STRESS_MAGNITUDE_IDENTITY

GRADING_GEOMETRY_DUALITY

AXISYMMETRIC_REFERENCE_METRIC_EMBEDDING_OBSTRUCTION

GAUSSIAN_CURVATURE_BOUNDARY_INVARIANT

GENERAL_2D_SAME_Z_MEAN_VALUE_LOWER_BOUND
```

---

## Numerical

The known-solution regression baseline remained:

```text
94 PASSED
```

through the major runs.

Important numerical controls included:

* low/primary/high source quadrature;
* exact-map and unrestricted N65 cross-representations;
* independent finite-payload reconstruction;
* midpoint-versus-prism/kernel repairs;
* independent optimization solvers for stand-off feasibility;
* million-case low-discrepancy scans;
* directional bundles;
* full arbitrary-direction acoustic-cone scans;
* finite-difference step variation;
* finite-payload all-outward tests;
* high-resolution compatible-profile reconstruction;
* exact boundary curvature identity;
* deterministic and randomized curvature reconstructions;
* smoke/full-run separation;
* no relaxation of failed numerical tolerances during the 025B1 repair.

The final repaired 025B1 execution completed cleanly.

---

## Dimensional

The pure-GR scaling remains:

```math
E_{\rm physical}
=
C
\frac{
a c^2 h^2
}{
G
}.
```

No branch in this journal changed the $1/G$ dependence.

At:

```text
a=1g
h=1 m
```

the coefficient-one scale is approximately:

```math
1.32\times10^{28}\ {\rm J}.
```

Therefore even a hypothetical pure-GR source with:

```math
C=1
```

remains catastrophically energetic on human scale.

This increasingly dominates the practicality ranking.

---

## Limiting Cases

Important limiting checks included:

### 024A

```text
L6 -> 0:
RECOVERS_STANDARD_SKYRME_CONSTITUTIVE_CEILING

PURE_L6+V_BPS:
S/RHO -> 1
```

### 024A2

```text
M_OMEGA -> INFINITY:
OMEGA_SECTOR_APPROACHES_LOCAL_B0SQ_PLACEMENT
```

### 025A

```text
t=q=s=1:
RECOVERS_IDEAL_006B_STRESS_ARCHITECTURE
```

and increasing allowed strain drove:

```math
C
\rightarrow
C_{006B}
```

from above.

### 025B0

As strain cap increased, the compatible radial material approached:

```math
C
\rightarrow
C_{005B},
```

rather than $C_{006B}$.

This was a powerful independent structural signal that ordinary compatibility restores the supported-tension architecture.

---

## Literature Comparison

The 024A2 omega/Proca route was motivated by established omega-Skyrme/vector-meson formulations, including the project literature anchors:

```text
ADKINS_AND_NAPPI
STABILIZATION_OF_CHIRAL_SOLITONS_VIA_VECTOR_MESONS

GUDNASON_AND_SPEIGHT
OMEGA_SKYRME_WORK

HARLAND_LEASK_SPEIGHT
RECENT_OMEGA_SKYRME_WORK
```

The relativistic elasticity characteristic treatment used the established Hadamard/acoustic-tensor framework.

The 025B1 non-Euclidean/prestrain language is consistent with established elasticity formulations in which incompatible reference metrics generate residual stress, geometric frustration, buckling, or nontrivial relaxed geometry.

No claim in this journal should be promoted to external novelty until a dedicated literature/novelty comparison is completed.

Current state remains:

```text
NOVELTY=
NOT_ESTABLISHED

NEW_PHYSICS_DISCOVERY=
NO
```

---

# Falsification Attempt

The entire slice was organized around progressively stronger attempts to destroy apparently favorable results.

## 024A

Potential failure:

```text
SEXTIC_TERM_CANNOT_REACH_TEACHER_STRESS
```

Result:

```text
FALSE_CONSTITUTIVELY
```

but geometry remained unresolved.

## 024A1

Potential failure:

```text
NATURAL_B0SQ_LOCATION_IS_POOR
```

Result:

```text
YES_FOR_ROBUST_GEOMETRY
```

with near-zero teacher overlap.

## 024A1R

Potential error:

```text
024A1_WORST_DIRECTION_CRITERION_WAS_UNFAIR
```

Result:

```text
YES
```

and criterion repaired.

A limited orientation-locked signal survived.

## 024A2

Potential false positive:

```text
MIDPOINT_PAYLOAD_FORCE
```

Result:

```text
FALSE_POSITIVE
```

after resolved finite-payload reconstruction.

## 024A3

Potential loophole:

```text
WRONG_ISOROTATION_AXIS
```

Result:

```text
CLOSED_BY_GENERALIZED_EIGENVALUE_OPTIMUM
```

and still no efficiency gain.

## Teacher interpretation

Potential error:

```text
RAW_TEACHER_IS_TRUE_STANDOFF_REPULSION
```

Result:

```text
FALSE.
```

The dominant channel was far-side positive active attraction.

## 024A4

Potential error:

```text
STRICT_STANDOFF_INFEASIBILITY_IS_FUNDAMENTAL
```

Result:

```text
FALSE.
```

The frozen density cap caused the infeasibility.

## 024B

Potential loophole:

```text
TRANSIENT_NEGATIVE_STRESS_AVOIDS_COMPENSATION
```

Result:

```text
FALSE_FOR_CLOSED_CYCLE
```

by virial identity.

## 024B2

Potential omission:

```text
HELD_RIM_SUPPORT_DOES_NOT_GRAVITATE
```

Result:

```text
FALSE.
```

Including support reversed the apparent gain.

## 024B3

Potential loophole:

```text
FREE_ANNULAR_WALL_COLLAPSE_PRODUCES_USEFUL_CYCLE
```

Result:

```text
NO_IN_TESTED_NAMBU_GOTO_CLASS.
```

## 024C/C1

Potential loophole:

```text
TEACHER_GEOMETRY_CAN_BE_SIMPLY_REBUILT_ONE_SIDED
```

Result:

```text
NO_BEAT_006D
```

and poor working-region quality.

## 024D

Potential interpretation:

```text
COUNTERROTATION_OR_SPIN_IS_THE_DRIVER
```

Result:

```text
FALSE_IN_TESTED_MODEL.
```

Stress-state conversion was the useful component.

## 024D1R

Potential error:

```text
GEOMETRIC_TOP_EQUALS_KERNEL_MAXIMUM
```

Result:

```text
FALSE.
```

Kernel-adaptive placement repaired the test.

## 024E/E1

Potential loopholes tested:

```text
STRONGER_LOCAL_NEGATIVE_ACTIVE_SOURCE
BETTER_KERNEL_PLACEMENT
ANISOTROPIC_PACKET_SHAPE
LOW_CANCELLATION
LOOSE_CORE_OVERLAP
```

The portal already saturated the local DEC negative-active limit.

Kernel placement and cancellation were good.

The remaining obstruction was participation.

## 025A

Potential failure:

```text
NO_HEALTHY_LOCAL_MATERIAL_LAW_CAN_GENERATE_006B_STRESSES
```

Result:

```text
NOT_ESTABLISHED.
```

A candidate local law was found.

## 025B0

Potential failure:

```text
LOCAL_LAW_HAS_HIDDEN_ARBITRARY_DIRECTION_WAVE_INSTABILITY
```

Result:

```text
NOT_FOUND_IN_TESTED_ACOUSTIC_CONE.
```

Potential failure:

```text
LOCAL_STRESS_STATES_CANNOT_COEXIST_GLOBALLY
```

Result:

```text
CONFIRMED_FOR_ORDINARY_RADIAL_MAP.
```

## 025B1

Potential escape:

```text
NON_EUCLIDEAN_AXISYMMETRIC_REFERENCE_METRIC_IS_STRESS_FREE
```

Result:

```text
EXACTLY_REJECTED.
```

Potential numerical concern:

```text
CURVATURE_INVARIANT_FAILURE
```

Result:

```text
NUMERICAL_DIFFERENTIATION_ARTIFACT
```

and repaired without relaxing gates.

---

# Claims Status

No practical-device or discovery claim is authorized.

No global pure-GR realization superseded 006D.

The main claim updates that should be reflected in repository documentation are:

```text
RAW_TEACHER_17230X_TRUE_STANDOFF_INTERPRETATION=
RETIRED

TRUE_STANDOFF_CLASS_AND_EMBEDDED_TWO_SIDED_CLASS=
SEPARATED

STRICT_STANDOFF_TYPE_I_DEC_BOUND_C_GE_1_OVER_2=
ESTABLISHED_IN_DECLARED_NORMALIZATION

024A_MIXED_L2_L4_L6_V_CONSTITUTIVE_ACCESS=
SUPPORTED_AS_PREFLIGHT

024A1R_ORIENTATION_LOCKED_SEXTIC_SIGNAL=
SUPPORTED

024A2_EXPLICIT_OMEGA_FIXED_FIELD_ROUTE=
RED

024A3_RIGID_ISOROTATION=
RED

024A4R_FROZEN_DENSITY_CAP=
NONFUNDAMENTAL_FEASIBILITY_OBSTRUCTION

024B_TESTED_DOMAIN_WALL_PULSE=
PAUSED_RED

024C_STATIC_STRESS_LENS=
RED_VS_006D

024D_SCALAR_STRESS_STATE_TRANSPORT=
YELLOW_EFFECTIVE_SOURCE_LEVEL

024E_024E1_CLOSE_PASS_PORTAL=
CLOSED

025A_LOCAL_RELATIVISTIC_HYPERELASTIC_WITNESS=
RETAIN

025B0_FULL_LOCAL_ACOUSTIC_CONE=
PASS

025B0_DIRECT_RADIAL_006B_REALIZATION=
DEMOTED

025B1_GRADING_GEOMETRY_DUALITY=
ESTABLISHED_FOR_DECLARED_MODEL

025B1_AXISYMMETRIC_STRESS_FREE_REFERENCE_METRIC=
RED

GENERAL_2D_EUCLIDEAN_SHEAR=
UNRESOLVED_DEFERRED

006D=
REMAINS_STRONGEST_CONSERVATIVE_GLOBAL_TRUE_STANDOFF_RESULT

PRACTICAL_ANTIGRAVITY_DEVICE=
NO

NEW_PHYSICS_DISCOVERY=
NO
```

If `CLAIMS.md` is maintained, it should be reconciled during the documentation closeout rather than treating this journal itself as a formal claims-file edit.

---

# Open Questions

The highest-value unresolved scientific questions are now more sharply defined.

## Pure-GR actual-field branch

For the $B=7$ Skyrmion:

```text
STRICT_N73_STATIONARITY

CONTINUOUS_FORCE_CONVERGENCE

FULL_HESSIAN

FINITE_AMPLITUDE_FISSION

NONLINEAR_EINSTEIN_SKYRME
```

remain unresolved.

## Hyperelastic branch

The local 025A law survived.

Still unresolved:

```text
GENERAL_2D_EUCLIDEAN_SHEAR

FINITE_THICKNESS_MATERIAL_BVP

FULL_GLOBAL_EULER_LAGRANGE_MATERIAL_SOLUTION

NONAXISYMMETRIC_PRESTRAINED_EMBEDDING

MICROSCOPIC_ORIGIN_OF_RHO_STAR_GRADING

FULL_NONLINEAR_STABILITY

NONLINEAR_GR
```

The direct efficient radial realization is paused.

## Dynamic scalar branch

024D2 remains unresolved:

> Can an actual canonical scalar or minimal healthy extension dynamically realize the broad two-zone stress-state conveyor with exact local conservation and a useful complete-cycle finite-payload impulse?

This should not be attempted before the global practicality rerank unless its ranking improves.

## Absolute scaling

The dominant practicality problem remains:

```math
E
\propto
\frac1G.
```

No tested pure-GR source architecture changed this scaling.

## Analogue Antigravity

The project has not yet performed the new global mechanism-selection pass using everything learned from Introspective and 024–025.

This is now the primary practicality frontier.

---

# AI Assistance

AI assistant used:

```text
ChatGPT by OpenAI
```

AI assistance during this research slice included:

* mathematical derivation;
* derivation of source and stress identities;
* derivation of virial and compatibility relations;
* derivation of the Hencky constitutive law consequences;
* derivation of the grading–geometry identity;
* derivation of the reference-metric curvature invariant;
* simulation design;
* Python code generation;
* numerical debugging;
* identification and repair of midpoint/kernel-resolution errors;
* identification and repair of the 025B1 curvature numerical validator;
* low-discrepancy campaign design;
* numerical result interpretation;
* branch falsification strategy;
* claim-boundary enforcement;
* literature-anchor identification;
* documentation and research-state synthesis.

AI-generated calculations were not assumed correct merely because they were generated.

Central results were subjected where feasible to:

```text
ANALYTICAL CROSS-CHECKS
INDEPENDENT NUMERICAL RECONSTRUCTION
RESOLUTION CHECKS
ALTERNATE REPRESENTATIONS
KNOWN-SOLUTION REGRESSION
LIMITING CASES
FALSIFICATION GATES
```

---

# Next Action

The next scientific action should **not** be another pure-GR coefficient scan.

Recommended next slice:

```text
026A
GLOBAL_PRACTICALITY_RERANK
AND
ANALOGUE_ANTIGRAVITY_MECHANISM_SELECTION_GATE
```

Primary question:

> **Given the conservation, participation, compatibility, support, stability, and absolute-scaling barriers now identified, which surviving physical mechanism has the highest information-adjusted chance of producing a practically useful outward force on neutral matter?**

The rerank should compare at minimum:

```text
ANALOGUE_ANTIGRAVITY CANDIDATE CLASSES

023C_023D B7 PURE_GR_FIELD_COMPLETION

025B2 GENERAL_2D_EUCLIDEAN_HYPERELASTIC_SHEAR

024D2 CANONICAL_SCALAR_STRESS_STATE_TRANSPORT
```

but the primary ranking should be driven by practicality.

For each analogue candidate, require analytical upper bounds on:

```text
FINITE_PAYLOAD_ACCELERATION_OR_IMPULSE

SOURCE_ENERGY

POWER

RANGE

SOURCE_MASS

SUPPORT_AND_CONTROL_ENERGY

REACTION_MOMENTUM

RESET

EMPIRICAL_CONSTRAINT_MARGIN

SCALABILITY
```

before authorizing large simulations.

Promotion should require a **parametric or many-orders-of-magnitude absolute scaling advantage**, not another order-unity reduction in a pure-GR dimensionless coefficient.

The appropriate strategic conclusion at this journal close is:

```text
PRACTICALITY_FRONTIER=
ANALOGUE_ANTIGRAVITY_GLOBAL_RERANK

PURE_GR_ACTUAL_FIELD_FALLBACK=
023C_023D_B7

PURE_GR_MATERIAL_FALLBACK=
025B2_GENERAL_2D_SHEAR_DEFERRED

PURE_GR_DYNAMIC_FALLBACK=
024D2_SCALAR_STRESS_TRANSPORT_DEFERRED

006D=
PERMANENT_CONSERVATIVE_TRUE_STANDOFF_BENCHMARK

CURRENT_KNOWLEDGE_HEURISTIC=
APPROXIMATELY_70_TO_71_PERCENT_NOT_A_PROBABILITY

PRACTICAL_ANTIGRAVITY_DEVICE=
NO

NEW_PHYSICS_DISCOVERY=
NO
```

### END JOURNAL — 2026-09-02 — 024A THROUGH 025B1 SUCCESSOR-FIELD AND MATERIAL-REALIZATION CLOSEOUT
