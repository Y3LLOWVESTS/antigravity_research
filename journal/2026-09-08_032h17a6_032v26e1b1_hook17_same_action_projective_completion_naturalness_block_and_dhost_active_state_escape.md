# Research Journal — 2026-09-08

## 032H17A6–032H17A9R3 and 032V26E0–032V26E1B1 — HOOK17 Same-Action Projective Completion, Naturalness Block, DHOST Fallback Resumption, Pure-$j=0$ Failure-Memory Collision, and Quadratic Active-State Escape

## Objective

The primary scientific question investigated in this research slice was:

> **Can the HOOK17 low-capacity mechanism be promoted beyond the previously established source/projector partials into a physically credible same-action protected realization, and, if the strongest tested HOOK17 carrier fails a decisive physical gate, can the preserved $c_T=1$ DHOST/KMM fallback produce a genuinely different healthy active/off-state architecture rather than rediscovering an already-closed low-energy failure class?**

The research entered this slice with two linked but distinct frontiers.

The primary high-value mechanism program was:

```text
032H17
HOOK17 PHYSICALIZATION AND RESCUE
```

built from:

```text
V24
INTRINSIC DIRAC HYPERMOMENTUM SOURCE

+

V26B1
NONLINEAR QUADRATIC HOOK->METRIC NUMERATOR

+

V26B1
ACTIVE / OFFSTATE SEPARATION

+

V26B1R1
EXCEPTIONALLY LOW CANONICAL FIELD-CAPACITY REFERENCE
```

The independent fallback was:

```text
032V26D
PROTECTED cT=1 DHOST / KINETIC-MATTER-MIXING
```

The guiding instruction remained:

> **Do not chase 17 J. Chase a physically consistent same-action theory that preserves why 17 J was possible.**

The scientific program therefore prioritized:

```text
SAME-ACTION PROVENANCE

SOURCE WARD CONSISTENCY

HEALTHY-MODE OVERLAP

ONE UNIVERSAL PHYSICAL METRIC

METRIC WARD CONSISTENCY

CANONICAL NORMALIZATION

FIELD-REDEFINITION INVARIANCE

PRINCIPAL HEALTH

TECHNICAL NATURALNESS

OFFSTATE QUANTUM DESCENDANTS

FINITE-PAYLOAD RESPONSE

ABSOLUTE COMPLETE ENERGY
```

rather than additional field-capacity optimization.

This slice ultimately produced two major outcomes.

First:

> **HOOK17 survived substantially deeper physicalization than an algebraic source/numerator construction: an exact healthy protected $1^+$ pole with nonzero clean V24 source overlap, a local and then covariant projective Dirac completion, and a same-action clean linearized metric-stress / diffeomorphism-Ward reconstruction were all obtained.**

However, the strongest tested Percacci-Sezgin Case-I carrier was then blocked by a technical-naturalness/protection failure.

Second:

> **The preserved V26D fallback was resumed and advanced through a healthy static-spacelike tensor gate, an exact nonsingular Einstein-frame reconstruction, a precise collision with the already-closed V17/V19 pure-$j=0$ failure class, and finally a genuinely different quadratic active-state conformal completion that removes the old tree-level two-scalar material-force vertex while preserving the active response and large principal margins.**

The final state is promising but not a physical antigravity model.

---

## Starting State

### 1. Permanent conservative anchor

The strongest established conservative true-stand-off result remains:

```text
006D
```

with:

```math
C_{006D}
=
23.591586299249.
```

Within its static linearized-GR scope, 006D provides:

```text
FINITE_POSITIVE_ENERGY=YES

LOCAL_CONSERVATION_LINEARIZED_ORDER=PASS

NEC=PASS
WEC=PASS
DEC=PASS

OUTWARD_NEAR_FIELD=YES

POSITIVE_FAR_FIELD_ACTIVE_MASS=YES

TRUE_STANDOFF=YES
```

Nothing in the present slice supersedes 006D as an established true-stand-off architecture.

The catastrophic pure-GR energy scaling remains unchanged.

---

### 2. Practicality policy

The strict complete operating-energy requirement remained:

```math
E_{\rm complete,conservative,operating}
<
10^7\ {\rm J}.
```

Therefore:

```text
EXACTLY_10_MJ=
FAIL
```

The complete ledger must eventually include all unavoidable:

```text
SOURCE

MEDIATOR

COMPENSATOR

SYMMETRY-BREAKING / ACTIVATION

SUPPORT

CONFINEMENT

CONTROL

RESET

REACTION

RADIATION

OFFSTATE

QUANTUM / RG / UV

BACKREACTION

LEAKAGE
```

costs.

No partial field-capacity result is allowed to pass this gate.

---

### 3. Standardized macroscopic output floor

During this research slice the project clarified the performance comparator.

The 006D-style output target is a **minimum floor**, not an exact operating point.

Permanent performance requirement:

```text
MIN_OUTWARD_ACCELERATION_M_S2=
9.80665

MIN_TRUE_STANDOFF_M=
1.0
```

Thus a candidate must eventually demonstrate:

```math
a_{\rm outward}
\ge
1g
```

at:

```math
h_{\rm true\ stand-off}
\ge
1\ {\rm m}
```

for finite neutral matter coupled universally through the physical metric.

Output exceeding either floor is favorable.

Examples:

```text
2 g AT 1 m
=
ABOVE FLOOR

1 g AT 3 m
=
ABOVE FLOOR
```

provided all other consistency and energy gates pass.

---

### 4. HOOK17 capacity claim boundary

The preserved HOOK17 canonical-capacity result remained:

```text
R_P=1:
~1.706764422e-5 J

R_P=1e12:
~17.0676442196 J
```

The latter is internally referred to as the approximately 17-J HOOK17 capacity reference.

It is:

```text
FIELD CAPACITY
```

not:

```text
COMPLETE MODEL ENERGY

SOURCE ENERGY

DEVICE ENERGY

OR
CERTIFIED 1g-AT-1m ENERGY.
```

The earlier capacity calculation used a smaller normalization scale than the newly fixed full $1,\mathrm m$ stand-off requirement.

Therefore:

```text
HOOK17_17J_AT_1M=
NOT_ESTABLISHED
```

and any future surviving HOOK17 realization must be explicitly renormalized to the new:

```text
>=1 g
AT
>=1 m TRUE STANDOFF
```

performance floor before its energy is compared with the practical target.

---

### 5. State entering H17A6

The prior HOOK17 rescue slice had completed through H17A5.

The strongest established ingredients entering the present work were:

```text
V24_INTRINSIC_DIRAC_HYPERMOMENTUM=
PRESERVED

V24_HEALTHY_HOOK_1PLUS_SUPPORT=
NONZERO

V24_HEALTHY_HOOK_2PLUS_SUPPORT=
NONZERO

H17A3_TREE_ACTION_SCAFFOLDS=
PARTIAL

H17A3_FIELD_REDEFINITION_SURVIVAL=
PASS_FOR_TESTED_ACTIVE_METRIC_RESPONSE

H17A4_EXACT_O3_1PLUS_SOURCE=
NONZERO

H17A5_PROTECTED_K3_1PLUS=
EXISTS
```

but the clean direct V24 source failed the K3 Ward condition.

The regression state at the end of H17A5 was:

```text
759 passed
```

The next problem was therefore not whether a healthy protected $1^+$ channel exists.

It was whether the source could be embedded in a **same-action Ward-consistent protected carrier**.

---

# Work Performed

## 1. 032H17A6 — J11 common-Ward and minimal compensator prefilter

H17A6 tested whether the H17A5 K3 failure could be repaired inside the broader J11 protected vector family or by the simplest vector/Stückelberg compensations.

The common source-Ward polynomial reconstructed from the clean V24 current was:

```math
W_0
=
8q_2q_3,
```

```math
W_1
=
0,
```

```math
W_2
=
-16q_0q_3,
```

```math
W_3
=
8q_0q_2.
```

The productive $1^+$ overlap remained nonzero.

The broader J11 structure was reconstructed, with K3 identified as the relevant specialization.

However:

```text
DIRECT_CLEAN_V24->J11=
CLOSED

MINIMAL_VECTOR_AXIAL_IMPROVEMENT=
CLOSED

PURE_STUECKELBERG_GAUGE_IMAGE_RESCUE=
CLOSED
```

The result did **not** close a fully dynamical compensator/Noether completion.

Decision:

```text
RED_SCOPED_H17A6_DIRECT_J11_AND_MINIMAL_VECTOR_STUECKELBERG_REPAIRS_FAIL
DYNAMICAL_SAME_ACTION_COMPENSATOR_REMAINS_OPEN
```

Full regression:

```text
772 passed
```

---

## 2. 032H17A6R1 — exact massless single-compensator class

H17A6R1 promoted the compensation question from heuristic source repair to the exact massless Stückelberg/Noether structure.

The result was that the exact massless single-compensator construction returned to the same source identity.

The tested healthy massless two-form repair likewise did not produce an independent escape.

Therefore:

```text
EXACT_MASSLESS_STUECKELBERG=
CLOSED

HEALTHY_MASSLESS_TWOFORM_REPAIR=
CLOSED

EXACT_MASSLESS_SINGLE_COMPENSATOR_CLASS=
CLOSED
```

while:

```text
FULL_NOETHER_MATTER_PLUS_COMPENSATOR=
OPEN

HIGGSED_OR_MASSIVE_HOOK=
OPEN

OTHER_PROTECTED_FAMILIES=
OPEN.
```

Decision:

```text
RED_SCOPED_A6R1_EXACT_MASSLESS_SINGLE_COMPENSATOR_CLASS_RETURNS_TO_SAME_WARD_IDENTITY
HIGGSED_OR_FULL_NOETHER_COMPLETION_REMAINS_OPEN
```

Full regression:

```text
782 passed
```

---

## 3. 032H17A6R2 — protected massive $1^-$ source-support gate

The next protected family tested a published massive parity-odd spin-1 carrier.

The carrier itself exists and is healthy within its published conditions.

The question was whether the actual clean V24 source has nonzero overlap with that physical mode.

The exact rest-frame source decomposition gave:

```text
TOTAL_SYMMETRIC_1MINUS=
0

HOOK_1MINUS=
0

FULL_V24_1MINUS=
0
```

while preserving:

```text
V24_1PLUS_NORM2=
32

V24_2PLUS_NORM2=
14.2222...
```

Therefore:

```text
TESTED_PROTECTED_MASSIVE_1MINUS_ROUTE=
CLOSED_FOR_DIRECT_CLEAN_V24_SOURCE.
```

Decision:

```text
RED_SCOPED_A6R2_MARZO2022_PROTECTED_MASSIVE_1MINUS_HAS_ZERO_DIRECT_CLEAN_V24_REST_FRAME_SOURCE_SUPPORT
GENERIC_DIRAC_AND_OTHER_PROTECTED_FAMILIES_REMAIN_OPEN
```

Full regression:

```text
794 passed
```

---

## 4. 032H17A7 — Barker-Zell iso-Weyl source matching

H17A7 tested whether the clean V24 source couples directly to the identified projectively/isoweyl-protected vector channels in the Barker-Zell-style realization.

The relevant clean source channels were explicitly constructed.

For the direct route tested:

```text
DIRECT_CLEAN_V24_IDENTIFIED_IW_CHANNEL=
ZERO.
```

Therefore:

```text
TESTED_DIRECT_CLEAN_V24->IW_ROUTE=
CLOSED
```

without closing:

```text
GENERIC_DIRAC_SOURCE_ENGINEERING

FULL_SAME_ACTION_IW

NATIVE_1PLUS

NATIVE_2PLUS.
```

Decision:

```text
RED_SCOPED_A7_DIRECT_CLEAN_V24_IDENTIFIED_Q_AND_T_HAT_CHANNELS_ZERO_FOR_BARKER_ZELL_IW
FULL_SAME_ACTION_IW_AND_NATIVE_1PLUS_2PLUS_REMAIN_OPEN
```

Full regression:

```text
807 passed
```

---

## 5. 032H17A8 — protected $2^+$ atlas and projective $1^+$ promotion

H17A8 tested the remaining protected spin-2-like atlas before returning to a protected $1^+$ route.

The assessed BMS pair-antisymmetric family contained:

```text
206
```

models, including:

```text
22
```

confirmed unitary cases in the scanned published classification.

No acceptable symmetry-supported spin-$2$ survivor emerged from the tested subset.

A separate healthy massive $2^+$ realization existed in another metric-affine construction, but the assessed version did not supply an independent protection mechanism sufficient for project promotion.

Thus the tested protected-$2^+$ atlas produced:

```text
ZERO_ACCEPTABLE_SURVIVORS
```

within the declared scope.

The important positive outcome was instead the torsion-free projective Percacci-Sezgin family.

For the selected healthy Case-I sector, the theory contains a protected massive:

```text
1+
```

mode.

The projective source constraints are:

```math
\tau^\nu{}_{\nu\mu}
=
0
```

and:

```math
\tau_{\mu\nu}{}^\nu
=
0.
```

The clean V24 source satisfied both traces.

Its clean $1^+$ support remained nonzero.

Therefore:

```text
PERCACCI_SEZGIN_PROJECTIVE_1PLUS=
PROMOTED
```

to an exact pole calculation.

Decision:

```text
RED_SCOPED_A8_CURRENT_PROTECTED_2PLUS_ATLAS_HAS_ZERO_ACCEPTABLE_SURVIVORS
PERCACCI_SEZGIN_PROJECTIVE_1PLUS_PROMOTED
```

Full regression:

```text
818 passed
```

---

# 6. 032H17A9 — exact Percacci-Sezgin $1^+$ pole overlap

A9 was one of the strongest positive HOOK17 results obtained to date.

For the representative Case-I parameters, the project reconstructed:

```text
m_+^2=
0.18

RESIDUE_PROXY=
0.36
```

with the healthy Case-I sign conditions satisfied.

The Wheeler/V24 source was mapped into the torsion-free P&S connection representation.

The torsion-free projection was mandatory.

After that projection:

```text
PROJECTIVE_TRACE_CONSTRAINTS=
PASS
```

and the clean rest source retained nonzero $1^+$ support.

The exact saturated pole calculation gave:

```text
EXACT_1PLUS_POLE_NUMERATOR=
1.44.
```

This is crucial.

The result established that the clean intrinsic V24 source does not merely have an abstract SO(3) decomposition into a desirable mode.

It overlaps an actual exact healthy protected pole of the selected theory.

At the same time, the generic diffeomorphism Ward identity showed that a metric stress:

```text
sigma
```

is required.

Thus:

```text
HEALTHY_POLE_OVERLAP=
ESTABLISHED

FULL_SAME_ACTION_MATTER_COMPLETION=
NOT_YET_ESTABLISHED.
```

Decision:

```text
GREEN_PARTIAL_A9_EXACT_PERCACCI_SEZGIN_1PLUS_POLE_OVERLAP_WITH_PROJECTIVE_TRACE_PASS
FULL_MATTER_NOETHER_AND_SAME_ACTION_REMAIN_OPEN
```

Full regression:

```text
830 passed
```

---

# 7. 032H17A9R1 — unmodified Wheeler Dirac matter versus projective identity

A9R1 tested whether the unmodified Wheeler Dirac hypermomentum can serve as the actual projectively admissible P&S matter source.

A complete basis of:

```text
16 HERMITIAN DIRAC PROBES
```

was tested.

Result:

```text
PROJECTIVELY_COMPATIBLE=
3

PROJECTIVELY_INCOMPATIBLE=
13.
```

The maximum trace norm was:

```text
sqrt(4.5)
=
2.121320343559642.
```

Example incompatible traces included:

```text
D1:
[0,2,0,0]

I01:
[-0.5,2,0,0.5].
```

Therefore:

```text
UNMODIFIED_WHEELER_PROJECTIVE_IDENTITY=
FALSE

DIRECT_UNMODIFIED_WHEELER+P&S_SAME_ACTION=
CLOSED.
```

However, the special clean A9 state survived.

Its exact pole numerator remained:

```text
1.44.
```

The same run solved the linearized metric Ward system for the necessary symmetric metric stress.

The system had:

```text
40 unknowns

RANK=
40
```

and therefore a unique local solution.

The nonzero coefficients were:

```text
S032=
4

S302=
4.
```

The polynomial residual was approximately:

```text
6.56e-15.
```

Four independent Ward witnesses passed.

This was an important constructive result.

But the solved $\sigma$ was still a local metric-stress completion, not yet the metric variation of a declared projectively completed Dirac action.

Decision:

```text
RED_SCOPED_A9R1_UNMODIFIED_WHEELER_DIRAC_MATTER_FAILS_PERCACCI_SEZGIN_PROJECTIVE_SOURCE_IDENTITY
CLEAN_A9_POLE_SURVIVES_AND_PROJECTIVE_MATTER_COMPLETION_REMAINS_OPEN
```

Full regression:

```text
843 passed
```

---

# 8. 032H17A9R2 — action-level projective Dirac completion

A9R2 replaced state-by-state source editing with a fixed local projector in the matter action.

For torsion-free distortion:

```math
N_{cab}
=
N_{bac},
```

define the projective gauge image:

```math
G(v)_{cab}
=
\eta_{ab}v_c
+
\eta_{ac}v_b.
```

Define:

```math
{\rm tr}[X]_c
=
\eta^{ab}X_{cab}.
```

In four dimensions:

```math
{\rm tr}[G(v)]
=
5v.
```

The projector is:

```math
P[X]
=
X
-
\frac15
G({\rm tr}[X]).
```

The project verified:

```text
LOCAL=
YES

ALGEBRAIC=
YES

IDEMPOTENT=
YES

SELF_ADJOINT=
YES

PROJECTIVE_GAUGE_IMAGE_ANNIHILATED=
YES

TORSION_FREE_SYMMETRY_PRESERVED=
YES.
```

The declared interaction scaffold was:

```math
S_{\rm int}
=
\int
\sqrt{-g}
\,
H_W^{cab}
P[N]_{cab}.
```

Variation with respect to the connection gives the completed source:

```math
\tau_{\rm completed}
=
P[H_W].
```

Results:

```text
ALL_16_PROJECTED_WHEELER_PROBES_PROJECTIVELY_COMPATIBLE=
TRUE

MAX_PROJECTIVE_TRACE=
~1.11e-16

CLEAN_SOURCE_CHANGED=
NO

CLEAN_A9_POLE_NUMERATOR=
1.44.
```

A separate result showed that the conventional projective Lorentz-Dirac vector class does not supply the useful P&S $1^+$ numerator in the tested basis.

Thus:

```text
STANDARD_PROJECTIVE_LORENTZ_DIRAC_VECTOR_CLASS=
CLOSED_FOR_USEFUL_1PLUS_NUMERATOR.
```

But the projective distortion-coupled matter scaffold remained viable at linearized/action level.

Open after A9R2:

```text
FULL_COVARIANT_WORLD_SPINOR_LIFT

ACTUAL_SAME_ACTION_METRIC_STRESS

FULL_NONLINEAR_NOETHER.
```

Decision:

```text
GREEN_PARTIAL_A9R2_LOCAL_PROJECTIVE_TRACELESS_DISTORTION_MATTER_SCAFFOLD_PRESERVES_A9_1PLUS_POLE
STANDARD_PROJECTIVE_LORENTZ_DIRAC_VECTOR_CLASS_CLOSED
FULL_COVARIANT_WORLD_SPINOR_LIFT_REMAINS_OPEN
```

Full regression:

```text
855 passed
```

---

# 9. 032H17A9R3 — covariant projector and same-action clean metric stress

A9R3 promoted the flat-space algebraic projector to a covariant metric-dependent object.

Define:

```math
{\rm tr}_g[X]_c
=
g^{ab}X_{cab}
```

and:

```math
G_g(v)_{cab}
=
g_{ab}v_c
+
g_{ac}v_b.
```

Then:

```math
P_g[X]
=
X
-
\frac{
G_g({\rm tr}_g X)
}{
d+1
}.
```

The project tested this object on a nonflat Lorentzian metric.

Results:

```text
COVARIANT_TRACE_ZERO=
PASS

IDEMPOTENT=
PASS

PROJECTIVE_GAUGE_IMAGE_ANNIHILATED=
PASS

TORSION_FREE_SYMMETRY=
PASS

SELF_ADJOINT=
PASS

GL4_EQUIVARIANCE=
PASS

FLAT_LIMIT=
MATCHES_A9R2.
```

Representative covariance/equivariance errors were at approximately:

```text
1e-15
```

numerical roundoff.

The clean source survived exactly.

The exact $1^+$ pole numerator remained:

```text
1.4399999999999995.
```

The declared same action was promoted to:

```math
S_{\rm int}
=
\int
\sqrt{-g}
\,
H_W^{cab}
P_g[
A-\Gamma_{\rm LC}[g]
]_{cab}.
```

At zero background distortion, the metric variation through:

```math
-\tau\,\delta\Gamma_{\rm LC}
```

produced:

```text
S032=
4.0

S302=
4.0

ALL_OTHER_COEFFICIENTS=
0.
```

This reproduced the unique A9R1 40-by-40 solution with maximum difference of approximately:

```text
6.16e-15.
```

Four clean same-action linearized diffeomorphism-Ward witnesses had zero residual.

Therefore:

```text
GEOMETRIC_AND_CLEAN_LINEARIZED_NOETHER_PARTIAL_GREEN=
TRUE.
```

This is one of the strongest constructive HOOK17 results.

It demonstrates that the following ingredients can coexist at the tested level:

```text
INTRINSIC DIRAC SOURCE

PROJECTIVE SOURCE COMPLETION

EXACT HEALTHY 1+ POLE OVERLAP

COVARIANT PROJECTOR

SAME-ACTION CLEAN LINEARIZED METRIC STRESS

LINEARIZED DIFFEOMORPHISM WARD CONSISTENCY.
```

---

# 10. A9R3 protection / technical-naturalness failure

A9R3 then applied the project's protection stop rule to the selected P&S Case-I carrier.

The healthy Case-I relations include:

```math
C
+
\frac{16}{25}B
=
0
```

and:

```math
h_{11}
+
\frac{10}{3}h_7
=
0.
```

In the declared coordinates:

```text
B
C
h7
h11
```

this defines a:

```text
CODIMENSION_2
```

surface.

Projective symmetry exists before imposing those relations.

Therefore the projective symmetry does **not** enforce the Case-I health surface.

The unwanted blocks were inspected directly.

For the $2^-$ block, the relevant $q^2$ coefficient vanished under Case I, but a nonzero constant operator remained.

For the representative normalization:

```text
B=
1
```

the constant contribution was:

```text
B/2
=
0.5.
```

For the $1^-$ block, the $q^2$ determinant factor vanished but the constant determinant contribution:

```math
6BC
```

remained nonzero.

Representative value:

```text
-3.84.
```

Thus the unwanted propagating modes disappear through coefficient cancellation rather than an established new gauge-null direction.

No additional symmetry was demonstrated.

No actual beta functions were calculated.

No radiative stability was established.

Therefore:

```text
CASE_I_TECHNICAL_NATURALNESS=
FAIL

CURRENT_P&S_CASE_I_PROMOTION=
BLOCKED.
```

Decision:

```text
YELLOW_BLOCKED_A9R3_COVARIANT_PROJECTOR_AND_SAME_ACTION_CLEAN_LINEARIZED_METRIC_STRESS_PASS
PERCACCI_SEZGIN_CASE_I_TECHNICAL_NATURALNESS_PROTECTION_NOT_ESTABLISHED
```

Full regression:

```text
865 passed
```

---

# 11. HOOK17 status after A9R3

A9R3 did **not** close HOOK17.

Preserve:

```text
HOOK17_CLOSED=
FALSE

UNKNOWN_PROTECTED_CASE_I_COMPLETION_GLOBALLY_CLOSED=
FALSE.
```

The present P&S carrier is blocked.

The broader HOOK17 mechanism remains open.

The result materially changed the interpretation of HOOK17.

Before A9, HOOK17 could still plausibly have been only an attractive algebraic source/numerator construction.

After A9–A9R3, the core had survived:

```text
EXACT HEALTHY POLE

SOURCE PROJECTOR

PROJECTIVE MATTER COMPLETION

COVARIANTIZATION

SAME-ACTION LINEARIZED METRIC STRESS

WARD CONSISTENCY.
```

The failure moved to a later and more specific gate:

```text
TECHNICAL NATURALNESS / PROTECTION.
```

This is scientifically encouraging evidence about the mechanism's depth, but it is not evidence sufficient for a physical model claim.

---

# 12. Stop rule and V26D resumption

The project had explicitly preserved V26D as the independent fallback.

The HOOK17 stop rule allowed the fallback to resume if the current carrier failed at:

```text
SAME-ACTION

WARD

PROJECTOR

PHYSICAL RESPONSE

EMPIRICAL

OR
UNAVOIDABLE ENERGY / NATURALNESS.
```

A9R3 therefore authorized:

```text
V26D_RESUME=
TRUE.
```

The next work did not abandon HOOK17.

It executed the predeclared independent-fallback policy.

---

# 13. 032V26E0 — static-spacelike TT tensor principal symbol

V26D uses the protected quadratic-DHOST branch:

```text
A1=A2=A3=A5=0
```

with:

```math
A_4
=
6
\frac{
F_X^2
}{
F
}.
```

The active background was a local constant spacelike scalar gradient:

```math
\bar\phi
=
vz
```

with:

```math
X=v^2>0.
```

The tensor principal symbol was reconstructed for wavevector angle $\theta$ relative to the preferred gradient.

For the affected tensor polarization, the relative coefficient is:

```math
M_T(\theta)
=
1
-
3\beta_1^2
\sin^4\theta.
```

The other tensor polarization retains coefficient:

```text
1.
```

The same Lorentzian contraction multiplies the frequency and spatial principal terms.

Therefore the tensor characteristic cone remains:

```text
cT^2=
1
```

provided the relative coefficient remains nonzero.

The tensor-health bound is:

```math
|\beta_1|
<
\frac{1}{\sqrt3}
\approx
0.577350269.
```

The representative V26D point has:

```math
\beta_1
=
\frac1{21}
\approx
0.047619047619.
```

The worst-case angle is:

```text
theta=
90 degrees.
```

The relative margin becomes:

```math
1
-
3
\left(
\frac1{21}
\right)^2
=
\frac{146}{147}
\approx
0.993197278912.
```

Thus:

```text
STATIC_SPACELIKE_TENSOR_PRINCIPAL_HEALTH=
PASS

ANISOTROPIC_TENSOR_CONE_CT1=
ESTABLISHED_IN_TESTED_LOCAL_PATCH

TENSOR_MARGIN_COLLAPSE_USED_FOR_GAIN=
FALSE.
```

Full regression:

```text
877 passed
```

---

# 14. V26E0 unsupported-flat background theorem

For flat constant $X$:

```text
R=0
```

and:

```text
phi_mn=0.
```

The $P(X)$ stress is:

```math
T_{\mu\nu}
=
P g_{\mu\nu}
-
2P_X
\phi_\mu\phi_\nu.
```

For a nonzero static gradient in the $z$ direction:

```text
T00=
-P

Txx=
P

Tyy=
P

Tzz=
P-2X P_X.
```

Unsupported Minkowski therefore requires:

```text
P(X0)=0

P_X(X0)=0.
```

At this stationary point:

```math
\delta X_{\rm linear}
=
2v\partial_z\pi.
```

The $P$-sector quadratic scalar Lagrangian contains:

```math
2XP_{XX}
(\partial_z\pi)^2
```

but no standalone:

```math
(\partial_t\pi)^2
```

term.

This does **not** establish a scalar instability because the full degenerate scalar-metric system can supply kinetic structure through constraints and mixing.

The correct result is:

```text
P_ONLY_STATIONARY_BACKGROUND_HAS_TIME_KINETIC=
FALSE

FULL_CONSTRAINED_SCALAR_METRIC_SYMBOL_REQUIRED=
TRUE.
```

The candidate survived the tensor gate but required a full scalar completion.

---

# 15. 032V26E1A — exact Class-Ia Einstein-frame map

Before brute-forcing the full constrained scalar-metric Hessian, the project exploited the special V26D Class-Ia structure.

For a disformal transformation:

```math
\tilde g_{\mu\nu}
=
A(X)g_{\mu\nu}
+
B(X)\phi_\mu\phi_\nu,
```

the inverse Class-Ia equations simplify on:

```text
alpha2=
0

alpha3=
0
```

to:

```math
\frac{A_X}{A}
=
\frac{F_X}{F}.
```

A valid solution is:

```math
A
=
\frac{F}{F_0}.
```

The corresponding $B_X$ numerator vanishes identically:

```text
B_X=
0.
```

Choosing the constant branch:

```text
B=
0
```

gives transformed curvature coefficient:

```math
\tilde F
=
F_0.
```

Thus the quadratic higher-derivative gravitational sector is exactly related to a constant-curvature Einstein-Hilbert sector by an $X$-dependent conformal field redefinition.

Forward reconstruction independently returns:

```text
alpha1=
0

alpha2=
0

alpha3=
0

alpha5=
0
```

and:

```math
\alpha_4
=
6
\frac{
F_X^2
}{
F
}.
```

This exactly reproduces the V26D quadratic coefficients.

For the normalized linear scaffold:

```math
A(x)
=
1+\eta x
```

with:

```math
x
=
X/\Lambda^4,
```

the transformation Jacobian is:

```math
D_{\rm map}
=
A
-
xA_x
=
1
```

identically.

Representative active point:

```text
x=
0.10

eta=
0.50

A=
1.05

D_MAP=
1.0.
```

The kinetic-variable transformation:

```math
\tilde x
=
\frac{x}{A}
```

has:

```math
\frac{d\tilde x}{dx}
=
\frac{1}{A^2}
```

on this branch.

The inverse is:

```math
x
=
\frac{
\tilde x
}{
1-\eta\tilde x
}.
```

The scan maintained:

```text
MIN_SCANNED_MAP_JACOBIAN_MARGIN
~1.
```

Thus:

```text
FIELD_REDEFINITION_NEAR_SINGULAR=
FALSE

FIELD_REDEFINITION_MARGIN_COLLAPSE_USED_FOR_GAIN=
FALSE.
```

Full regression:

```text
889 passed
```

Decision:

```text
GREEN_PARTIAL_V26E1A_V26D_QUADRATIC_DHOST_SECTOR_HAS_EXACT_NONSINGULAR_EINSTEIN_FRAME_MAP
FULL_SCALAR_HEALTH_AND_PHYSICAL_G00_CROSS_RESPONSE_REMAIN_REQUIRED
```

---

# 16. Physical-observable consequence of E1A

The exact map substantially simplifies the gravitational sector.

However it does not make the ordinary matter observable equal to the Einstein-frame metric perturbation.

Ordinary matter is minimal to the physical Jordan/DHOST metric:

```text
g_phys.
```

Under the frame map:

```math
\tilde g_{\mu\nu}
=
A(X)
g^{\rm phys}_{\mu\nu}.
```

Therefore matter is nonminimal in the Einstein frame.

Any real source-to-payload calculation must reconstruct:

```text
g_phys
```

and produce the same physical observable in either frame.

Permanent lesson:

```text
EINSTEIN_FRAME_METRIC_RESPONSE_ALONE
IS NOT
THE PHYSICAL ANTIGRAVITY OBSERVABLE.
```

---

# 17. 032V26E1B0 — collision with historical pure-$j=0$ failure class

E1A exposed an important low-energy equivalence before an expensive cross-propagator calculation was performed.

For:

```math
A(X)
=
1+\kappa X+\cdots,
```

the physical metric is:

```math
g^{\rm phys}_{\mu\nu}
=
A^{-1}
\tilde g_{\mu\nu}
=
\left(
1-\kappa X+\cdots
\right)
\tilde g_{\mu\nu}.
```

For ordinary matter:

```math
\delta S_m
=
\frac12
\int
\sqrt{-g}
\,
T^{\mu\nu}
\delta g_{\mu\nu}.
```

Therefore the leading interaction is:

```math
\mathcal L_{\rm int}
=
-\frac{\kappa}{2}
X\,T
+
O(X^2).
```

This is a universal kinetic-conformal:

```text
PURE TRACE

j=0
```

operator.

No independent traceless $j=2$ operator is generated at the same order by the purely conformal map.

The V26D hidden source also uses the same broad derivative axial-current operator class as the historical V17/V19 realization:

```math
\frac1{f_\Psi}
\nabla_\mu\phi
\,
\bar\Psi\gamma^\mu\gamma_5\Psi.
```

This does not establish an identical source state or source-energy functional.

It does establish low-energy operator-class provenance.

---

# 18. Historical V19R6 failure memory recovered exactly

The old tested V17 hidden-axial pure-$j=0$ implementation had:

```text
EMPIRICAL_METRIC_MIN_EV=
123456.8841240774

STRICT_ENERGY_METRIC_MAX_EV=
122996.18244440094
```

with gap:

```text
460.7016796764656 eV.
```

At the empirical boundary, the old partial source energy was:

```text
10150225.4393259 J
```

or approximately:

```text
10.150225 MJ.
```

Thus no empirical/strict-energy overlap existed for that exact tested realization.

E1B0 also reconstructed the off-state two-scalar force coefficient:

```math
K
=
\frac{
15
}{
8\pi^3
}
\approx
0.06047162706224905
```

for the schematic potential:

```math
V(r)
\propto
-
K
C_1^2
\frac{
m_1m_2
}{
r^7
}.
```

Because this force depends on:

```math
C_1^2,
```

a sign flip does not remove it.

Canonical rescaling also preserves the relevant source/metric combination:

```math
C_1f^2.
```

Therefore wavefunction normalization alone cannot evade the failure.

The correct scoped result is:

```text
EXACT_V17_EQUIVALENT_COMPLETION_CLOSED=
TRUE

ALL_V26D_COMPLETIONS_GLOBALLY_CLOSED=
FALSE

EXACT_V19R6_ENERGY_BOUNDARY_TRANSFERS_TO_ARBITRARY_V26D_SOURCE=
FALSE.
```

Full regression:

```text
899 passed
```

Decision:

```text
RED_SCOPED_V26E1B0_V26D_EINSTEIN_FRAME_LEADING_MATTER_OPERATOR_IS_PURE_J0_PROVENANCE_COLLISION_WITH_V17_V19
IDENTICAL_CANONICAL_COMPLETION_NOT_REOPENED
GENUINELY_NEW_PROTECTED_COMPLETIONS_REMAIN_OPEN
```

This prevented the project from spending an additional run re-deriving an already-falsified canonical low-energy class in a different frame.

---

# 19. 032V26E1B1 — quadratic active-state conformal completion

E1B0 suggested a genuinely different active/off-state structure.

Instead of:

```math
A(x)
=
1+\eta x,
```

test:

```math
A_n(x)
=
1+\eta_nx^n
```

with:

```text
n>=2.
```

The primary candidate was:

```text
n=2.
```

The goal was to preserve the same representative active KMM response while forcing the leading off-state derivative to vanish.

Set:

```text
x_star=
0.1
```

and target:

```math
\beta_\star
=
\frac1{21}.
```

For:

```math
A_n
=
1+\eta x^n,
```

define:

```math
y
=
\eta x_\star^n.
```

Then:

```math
\beta_\star
=
\frac{
ny
}{
1+y
}.
```

Solving gives:

```math
y
=
\frac{
\beta_\star
}{
n-\beta_\star
}.
```

For:

```text
n=2
```

and:

```math
beta_star=1/21,
```

the exact result is:

```math
y
=
\frac1{41}.
```

Therefore:

```math
\eta_2
=
\frac{100}{41}
\approx
2.439024390243902.
```

At the active point:

```math
A_\star
=
\frac{42}{41}
\approx
1.024390243902439.
```

The map Jacobian is:

```math
D_{\rm map}
=
A-xA_x
=
\frac{40}{41}
\approx
0.975609756097561.
```

Thus:

```text
MAP_INVERTIBLE=
TRUE

MAP_NEAR_SINGULAR=
FALSE.
```

The active logarithmic slope is:

```math
\frac{A_x}{A}
=
\frac{10}{21}
\approx
0.47619047619
```

and the active $\beta_1$ reconstructs:

```math
\beta_1
=
\frac1{21}
\approx
0.047619047619.
```

---

# 20. E1B1 active/off-state separation

For the quadratic completion:

```math
A_x(0)
=
0.
```

Therefore:

```text
QUADRATIC_OFFSTATE_BETA_1=
0.
```

At the chosen active point:

```text
QUADRATIC_ACTIVE_BETA_1=
1/21.
```

The inverse physical metric now begins schematically as:

```math
A^{-1}
=
1
-
\eta_2x^2
+
O(x^4)
```

rather than:

```math
1-\eta x+\cdots.
```

Thus the leading universal matter interaction is:

```text
x^2 T
```

and contains four scalar legs.

The previous tree-level two-scalar ordinary-matter vertex is absent.

Therefore:

```text
QUADRATIC_TREE_R5_TWO_SCALAR_VERTEX_ABSENT=
TRUE

ACTIVE_OFFSTATE_SEPARATION=
TRUE.
```

This is a genuine structural distinction from the linear V17/V19-equivalent completion.

It is not merely a reduction of the same coefficient.

---

# 21. E1B1 tensor and map health

The active $\beta_1$ is unchanged.

Therefore the worst tensor principal margin remains:

```math
1
-
3
\left(
\frac1{21}
\right)^2
=
\frac{146}{147}
\approx
0.9931972789115646.
```

The field-redefinition margin is:

```math
\frac{40}{41}
\approx
0.975609756097561.
```

Thus:

```text
PRINCIPAL_MARGIN_COLLAPSE_USED=
FALSE

FIELD_REDEFINITION_MARGIN_COLLAPSE_USED=
FALSE.
```

This distinction is essential because previous large-gain candidates had repeatedly failed when the apparent enhancement came from driving the physical kinetic margin toward zero.

E1B1 does not obtain its active/off-state distinction in that way.

---

# 22. Higher-power active-state family

The same active-$\beta$ matching was tested for:

```text
n=2
n=3
n=4.
```

All three tested powers:

```text
PRESERVE_ACTIVE_BETA=
YES

A_X_OFFSTATE_ZERO=
YES

REMOVE_TREE_R5_TWO_SCALAR_VERTEX=
YES

ACTIVE_MAP_INVERTIBLE=
YES

TENSOR_MARGIN_POSITIVE=
YES.
```

However, the required coefficient $\eta_n$ grows rapidly with increasing power.

No new symmetry is obtained merely by choosing a larger integer $n$.

Therefore:

```text
n=2
```

was retained as the preferred minimal representative.

The project did not promote $n=3$ or $n=4$ merely because their naive loop descendants are smaller.

---

# 23. E1B1 historical-overlap suppression threshold

The old V19R6 conflict was narrow.

If the old pure-$j=0$ coefficient is reduced by a relative factor $s$, while the historical metric scale obeys:

```math
C_1
\propto
M^{-4},
```

then the old empirical lower bound would scale diagnostically as:

```math
M_{\rm emp,new}
=
M_{\rm emp,old}
s^{1/4}.
```

The maximum relative coupling needed merely to reopen the old empirical/energy overlap is:

```math
s_{\rm required}
=
\left(
\frac{
M_{\rm energy,max}
}{
M_{\rm emp,min}
}
\right)^4.
```

The exact project value was:

```text
OLD_OVERLAP_REQUIRED_RELATIVE_OFFSTATE_C1=
0.9851566116468706.
```

Thus suppressing the old effective off-state coupling by only approximately:

```text
1.484%
```

would have been enough to reopen the historical narrow overlap.

This does not certify the new completion.

It quantifies how narrow the old failure was.

---

# 24. Wilsonian NDA descendant diagnostic

The absence of the tree $xT$ term is not sufficient for quantum safety.

A scalar contraction of the higher-power operator can regenerate a lower-order $xT$ descendant.

For the simple diagnostic used in this run:

```math
c_{1,\rm desc}
\sim
\eta_n
\left(
\frac{
1
}{
16\pi^2
}
\right)^{n-1}
```

for:

```text
Lambda_UV/Lambda=
1.
```

This is a Wilsonian NDA estimate only.

It is not:

```text
AN EXACT BETA FUNCTION

UV MATCHING

A COMBINATORICALLY COMPLETE LOOP RESULT

OR
A TECHNICAL-NATURALNESS CERTIFICATE.
```

For:

```text
n=2,
```

the estimate gave:

```text
NDA_GENERATED_xT_COEFFICIENT=
0.015445302384502707.
```

Relative to the active logarithmic slope:

```text
NDA_RELATIVE_OFFSTATE_TO_ACTIVE=
0.03243513500745569.
```

The historical diagnostic empirical scale becomes approximately:

```text
52392.51408887634 eV
```

compared with the historical energy-compatible upper scale:

```text
122996.182 eV.
```

Thus the old overlap would reopen under the declared NDA comparison assumptions.

The multiplicative uncertainty factor before recreating the old historical collision is approximately:

```text
30.373131217749453.
```

For higher powers:

```text
n=3:
NDA_RELATIVE_OFFSTATE_TO_ACTIVE
~0.00135827638

n=4:
NDA_RELATIVE_OFFSTATE_TO_ACTIVE
~6.42513147e-5.
```

The corresponding loop-order counting was:

```text
n=2:
~1 LOOP TO xT

n=3:
~2 LOOPS

n=4:
~3 LOOPS.
```

These are encouraging diagnostics only.

---

# 25. E1B1 technical-naturalness limitation

The declared symmetries currently allow both:

```text
xT
```

and:

```text
x^2T.
```

Specifically:

```text
DIFFEOMORPHISM INVARIANCE
ALLOWS BOTH

SHIFT SYMMETRY
ALLOWS BOTH

phi -> -phi
ALLOWS BOTH.
```

Therefore:

```text
SETTING THE xT COEFFICIENT TO ZERO
DOES NOT CURRENTLY INCREASE
THE DECLARED SYMMETRY.
```

Consequently:

```text
LOW_ENERGY_LOOPS_CAN_GENERATE_xT=
TRUE

UV_THRESHOLDS_CAN_GENERATE_xT=
TRUE

ACTUAL_BETA_FUNCTION_COMPUTED=
FALSE

UV_MATCHING_COMPUTED=
FALSE

TECHNICAL_NATURALNESS_CERTIFIED=
FALSE.
```

The correct status is:

```text
RADIATIVE_HEADROOM_PRESENT
BUT
UV_PROTECTION_NOT_CERTIFIED.
```

---

# 26. V26E1B1 result

The run returned:

```text
QUADRATIC_ACTIVE_STATE_CLASSICAL_ESCAPE=
TRUE

QUADRATIC_CANDIDATE_STATUS=
PROMISING_CONDITIONAL_ACTIVE_STATE_COMPLETION

QUADRATIC_ACTIVE_BETA_1=
0.04761904761904761

QUADRATIC_OFFSTATE_BETA_1=
0.0

QUADRATIC_ACTIVE_MAP_MARGIN=
0.975609756097561

QUADRATIC_TENSOR_MARGIN=
0.9931972789115646

QUADRATIC_TREE_R5_TWO_SCALAR_VERTEX_ABSENT=
TRUE

POWER_FAMILY_CLASSICAL_ESCAPE_PASS=
TRUE

QUADRATIC_NDA_RADIATIVE_HEADROOM_PRESENT=
TRUE

QUADRATIC_NDA_UNCERTAINTY_HEADROOM_FACTOR=
30.373131217749453.
```

But also:

```text
NDA_IS_QUANTUM_CERTIFICATION=
FALSE

TECHNICAL_NATURALNESS_CERTIFIED=
FALSE

UV_MATCHING_COMPUTED=
FALSE

FULL_STATIC_SPACELIKE_SCALAR_HEALTH_ESTABLISHED=
FALSE

PHYSICAL_G00_CROSS_RESPONSE_ESTABLISHED=
FALSE

PHYSICAL_ANTIGRAVITY_MODEL_FOUND=
FALSE.
```

Decision:

```text
YELLOW_PROMISING_V26E1B1_QUADRATIC_ACTIVE_STATE_COMPLETION_REMOVES_TREE_R5_TWO_SCALAR_VERTEX_WHILE_PRESERVING_ACTIVE_BETA_AND_HEALTHY_MAP
LOW_ENERGY_NDA_DESCENDANT_HAS_HISTORICAL_EMPIRICAL_HEADROOM
UV_NATURALNESS_AND_FULL_CROSSPROP_REMAIN_OPEN
```

Full regression:

```text
910 passed
```

---

# Result

This research slice produced a materially sharper theoretical frontier.

## HOOK17 result

HOOK17 advanced from a promising low-capacity source/numerator mechanism to a much deeper physical partial.

The project established, within the tested chain:

```text
INTRINSIC DIRAC SOURCE

EXACT HEALTHY PROTECTED 1+ POLE OVERLAP

PROJECTIVE TRACE COMPATIBILITY FOR CLEAN SOURCE

ACTION-LEVEL PROJECTIVE DIRAC COMPLETION

COVARIANT PROJECTOR

CLEAN NONZERO POLE NUMERATOR

SAME-ACTION CLEAN LINEARIZED METRIC STRESS

LINEARIZED DIFFEOMORPHISM WARD CONSISTENCY.
```

The current P&S Case-I carrier then failed the protection/naturalness gate because its healthy coefficient relations were not shown to follow from the declared symmetry and instead appear as codimension-two cancellations.

Therefore:

```text
CURRENT_P&S_HOOK17_CARRIER=
BLOCKED

HOOK17_GLOBALLY_CLOSED=
NO.
```

The approximately 17.0676-J historical HOOK17 capacity reference remains preserved as a partial field-capacity result.

It is not a complete-energy result and is not yet normalized to the project's final $1g$ at $1,\mathrm m$ performance floor.

---

## V26D fallback result

The resumed V26D program established:

```text
STATIC-SPACELIKE_TENSOR_HEALTH=
PASS

LOCAL_ANISOTROPIC_TENSOR_CT1=
PASS

REPRESENTATIVE_TENSOR_MARGIN=
146/147

EXACT_CLASS_IA_EINSTEIN_FRAME_MAP=
PASS

MAP_JACOBIAN_FOR_LINEAR_SCAFFOLD=
1

NEAR-SINGULAR_FRAME_GAIN=
NO.
```

The original linear conformal completion was then identified as a provenance duplicate of the closed V17/V19 pure-$j=0$ architecture.

That exact canonical completion was closed without wasting further calculation.

A genuinely different quadratic active-state completion:

```math
A(x)
=
1
+
\frac{100}{41}x^2
```

was then constructed.

At:

```text
x_star=
0.1
```

it preserves:

```math
\beta_1
=
1/21,
```

has:

```math
D_{\rm map}
=
40/41,
```

retains:

```math
M_{T,\min}
=
146/147,
```

and satisfies:

```text
A_x(0)=0.
```

Therefore the old tree-level R5 two-scalar matter vertex is absent while the active response survives.

This is a **promising conditional** result.

It is not yet a physical model.

---

# Verification

## Analytical

### H17A6 common Ward polynomial

The exact common-Ward structure was reconstructed and used to show that minimal J11/vector/Stückelberg repairs do not remove the clean V24 obstruction.

### H17A6R2 source projectors

The tested protected massive $1^-$ carrier was rejected by exact zero clean-source overlap rather than parameter weakness.

### H17A8/A9 projective source constraints

The clean torsion-free projected V24 source satisfies both P&S projective traces.

### H17A9 exact pole

The clean source gives exact nonzero healthy $1^+$ pole numerator:

```text
1.44.
```

### H17A9R2 projector

The fixed action-level projector was verified algebraically to be:

```text
LOCAL

IDEMPOTENT

SELF-ADJOINT

PROJECTIVE-GAUGE-ANNIHILATING.
```

### H17A9R3 same-action metric stress

Metric variation of the declared projected distortion interaction reproduces the unique local Ward-completion coefficients:

```text
S032=4
S302=4.
```

### H17A9R3 naturalness stop

The P&S Case-I health relations were shown not to be enforced by the already-present projective symmetry.

### V26E0 tensor theorem

The affected tensor principal coefficient was derived as:

```math
1
-
3\beta_1^2\sin^4\theta.
```

### V26E0 unsupported-flat theorem

Flat constant-gradient unsupported Minkowski requires:

```text
P=0
P_X=0.
```

### V26E1A frame map

The exact Class-Ia map reconstructs constant Einstein-frame curvature coefficient and the full V26D quadratic coefficient relation.

For the original linear scaffold:

```text
D_MAP=1
```

identically.

### V26E1B0 pure-$j=0$ collision

The leading transformed matter operator is analytically:

```math
-\frac{\kappa}{2}XT.
```

### V26E1B1 quadratic escape

The exact matched values:

```math
\eta_2
=
100/41,
```

```math
A_\star
=
42/41,
```

```math
D_\star
=
40/41
```

follow analytically from matching:

```math
\beta_\star
=
1/21
```

at:

```math
x_\star
=
0.1.
```

---

## Numerical

Regression evolution through the slice:

```text
H17A5
759 passed

H17A6
772 passed

H17A6R1
782 passed

H17A6R2
794 passed

H17A7
807 passed

H17A8
818 passed

H17A9
830 passed

H17A9R1
843 passed

H17A9R2
855 passed

H17A9R3
865 passed

V26E0
877 passed

V26E1A
889 passed

V26E1B0
899 passed

V26E1B1
910 passed.
```

The historical protected baseline remains:

```text
94 passed.
```

All latest session runs completed with:

```text
COMPILE_RC=0

TARGETED_REGRESSION_RC=0

SIMULATION_RC=0

FULL_REGRESSION_RC=0.
```

The final expected baseline:

```text
910 passed
```

was reproduced.

---

## Dimensional

The newly standardized performance floor uses:

```text
OUTWARD ACCELERATION:
m/s^2

TRUE STANDOFF:
m
```

with:

```text
1 g=
9.80665 m/s^2.
```

The complete-energy target remains expressed in joules:

```text
1.0e7 J.
```

The E1A/E1B variables:

```text
x=X/Lambda^4
```

and conformal factors:

```text
A(x)
```

are dimensionless in the normalized scaffold.

Therefore the exact rational map and active-$\beta$ relations are dimensionally consistent.

---

## Limiting cases

### HOOK17 off state

The historical V26B1 quadratic metric has vanishing first variation at the exact zero-hook state.

This does not imply quantum silence.

### P&S health relations

Removing the Case-I tuning does not reveal a new gauge-null direction.

Therefore the current carrier's health should not be extrapolated away from the tuned surface.

### V26D linear conformal off state

For:

```math
A=1+\eta x,
```

the leading physical matter interaction is:

```text
xT.
```

This reproduces the historical pure-$j=0$ class.

### V26D quadratic conformal off state

For:

```math
A=1+\eta_2x^2,
```

one has:

```math
A_x(0)=0.
```

Thus the tree-level two-scalar ordinary-matter vertex vanishes.

### Small active response

As:

```text
x -> 0
```

for the quadratic candidate:

```text
beta_1 -> 0.
```

Thus the KMM interaction switches off continuously at the classical off state.

---

## Literature comparison

The HOOK17 carrier search used previously identified protected metric-affine theory families rather than treating algebraic irreducible decompositions as physical theories.

Important literature-backed structures included:

```text
BMS / RELATED PROTECTED METRIC-AFFINE MODE CLASSIFICATIONS

MARZO HEALTHY METRIC-AFFINE MODE ANALYSES

PERCACCI-SEZGIN PROJECTIVE METRIC-AFFINE GRAVITY

WHEELER GL(4) DIRAC HYPERMOMENTUM

DHOST CLASS-Ia / KINETIC-MATTER-MIXING STRUCTURES.
```

The project-derived contribution of this slice is not a claim that these published theories predict antigravity.

Rather, the project used their symmetry and propagator structures as falsification carriers for its independently defined source/metric mechanism.

No novelty claim relative to the complete literature has been established.

Therefore:

```text
NOVELTY=
NOT_ESTABLISHED

NEW_PHYSICS_DISCOVERY=
NO.
```

---

# Falsification Attempt

The slice deliberately attempted to destroy both HOOK17 and V26D at increasingly strong gates.

## 1. Could J11 simply repair the K3 Ward failure?

No.

The broader direct clean source retained the same relevant Ward obstruction.

---

## 2. Could a minimal vector compensator repair it?

No.

The minimal vector axial improvement closed.

---

## 3. Could a pure massless Stückelberg gauge image repair it?

No.

The exact massless single-compensator class returned to the same source identity.

---

## 4. Could the clean V24 source use a protected massive $1^-$ carrier?

Not in the tested direct rest-state projection.

The exact $1^-$ overlap was zero.

---

## 5. Could the tested Barker-Zell iso-Weyl channel carry the clean source?

Not in the tested direct clean source channel.

The relevant projection was zero.

---

## 6. Could a protected $2^+$ carrier solve the problem?

No acceptable protected $2^+$ survivor was found in the tested atlas.

This is a scoped atlas result, not a theorem against every metric-affine spin-$2$ theory.

---

## 7. Could the P&S projective $1^+$ support be an SO(3)-only artifact?

No.

A9 demonstrated an exact nonzero healthy pole numerator.

---

## 8. Could the unmodified Wheeler Dirac matter action satisfy P&S projective source conservation?

No generically.

Thirteen of sixteen Hermitian probes violated the projective traces.

---

## 9. Could a fixed local action-level projection repair the projective source?

Yes at the tested linearized matter-source level.

A9R2 provided such a projector.

---

## 10. Could that projector be covariantized?

Yes at the tested geometric level.

A9R3 provided a covariant $P_g$ with GL(4) equivariance.

---

## 11. Could the required metric stress arise from the same declared interaction?

Yes for the clean linearized background tested.

Metric variation produced the exact unique A9R1 Ward completion.

---

## 12. Does that make the P&S HOOK17 carrier technically natural?

No.

The Case-I health relations remain unexplained codimension-two cancellations under the declared symmetry.

This is the current exact HOOK17 carrier block.

---

## 13. Could V26D's tensor sector hide a near-singular healthy-margin trick?

No at the representative active point.

The worst tensor margin is:

```text
~0.9932.
```

---

## 14. Could the V26D field redefinition itself be near singular?

No for the linear scaffold.

The map Jacobian is exactly:

```text
1.
```

---

## 15. Could the original V26D linear physical metric represent genuinely new low-energy physics?

No.

It maps directly to the already-tested pure-$j=0$ kinetic-conformal operator class.

---

## 16. Could changing the sign of the linear conformal coupling evade the old material force?

No.

The two-scalar force depends on the square of the coupling.

---

## 17. Could canonical rescaling alone evade the old source/metric obstruction?

No.

The relevant:

```math
C_1 f^2
```

combination is invariant.

---

## 18. Can a quadratic active-state conformal factor remove the old tree-level two-scalar vertex while retaining the active response?

Yes.

E1B1 established this at the classical operator level.

---

## 19. Does the quadratic construction automatically solve quantum naturalness?

No.

The dangerous lower-order:

```text
xT
```

operator remains symmetry-allowed.

It can be regenerated by loops or UV matching.

---

## 20. Does NDA prove quantum safety?

No.

The approximately 30-fold historical headroom is an encouraging low-energy diagnostic only.

---

# Claims Status

This journal does not assume that `CLAIMS.md` has already been modified.

Recommended durable classifications are:

```text
CLAIM_ID=
H17A9_PERCACCI_SEZGIN_EXACT_1PLUS_SOURCE_OVERLAP

TYPE=
PROJECT_DERIVED_EXACT_PROJECTOR_AND_POLE_RESULT

STATUS=
SUPPORTED_IN_TESTED_P&S_CASE_I_CARRIER
```

```text
CLAIM_ID=
H17A9R2_PROJECTIVE_DIRAC_MATTER_COMPLETION

TYPE=
PROJECT_DERIVED_ACTION_SCAFFOLD_RESULT

STATUS=
SUPPORTED_AT_LOCAL_LINEARIZED_PROJECTIVE_LEVEL
```

```text
CLAIM_ID=
H17A9R3_COVARIANT_PROJECTOR_AND_LINEARIZED_SAME_ACTION_STRESS

TYPE=
PROJECT_DERIVED_GEOMETRIC_AND_LINEARIZED_NOETHER_PARTIAL

STATUS=
SUPPORTED
```

```text
CLAIM_ID=
H17A9R3_P&S_CASE_I_TECHNICAL_NATURALNESS

TYPE=
PROJECT_DERIVED_PROTECTION_GATE

STATUS=
NOT_ESTABLISHED_CURRENT_CARRIER_BLOCKED
```

```text
CLAIM_ID=
V26E0_STATIC_SPACELIKE_TENSOR_HEALTH

TYPE=
PROJECT_DERIVED_PRINCIPAL_SYMBOL_RESULT

STATUS=
SUPPORTED_ON_TESTED_CONSTANT_GRADIENT_PATCH
```

```text
CLAIM_ID=
V26E1A_EXACT_CLASS_IA_EINSTEIN_FRAME_MAP

TYPE=
PROJECT_DERIVED_EXACT_FIELD_REDEFINITION_RESULT

STATUS=
SUPPORTED
```

```text
CLAIM_ID=
V26E1B0_LINEAR_V26D_PURE_J0_COLLISION

TYPE=
PROJECT_DERIVED_PROVENANCE_AND_FAILURE_MEMORY_RESULT

STATUS=
EXACT_V17_EQUIVALENT_COMPLETION_CLOSED
```

```text
CLAIM_ID=
V26E1B1_QUADRATIC_ACTIVE_STATE_ESCAPE

TYPE=
PROJECT_DERIVED_CLASSICAL_OPERATOR_AND_NDA_PREFLIGHT

STATUS=
PROMISING_CONDITIONAL
```

The following claims remain prohibited:

```text
HOOK17_PHYSICAL_ANTIGRAVITY_MODEL=
ESTABLISHED

HOOK17_COMPLETE_ENERGY=
17_J

HOOK17_1G_AT_1M_ENERGY=
17_J

P&S_CASE_I_TECHNICALLY_NATURAL=
ESTABLISHED

V26D_PHYSICAL_ANTIGRAVITY_MODEL=
ESTABLISHED

V26E1B1_QUANTUM_SAFE=
ESTABLISHED

V26E1B1_FULL_SCALAR_HEALTH=
ESTABLISHED

V26E1B1_PHYSICAL_G00_RESPONSE=
ESTABLISHED

V26E1B1_1G_AT_1M=
ESTABLISHED

CERTIFIED_SUB10MJ_MODEL=
YES

PRACTICAL_ANTIGRAVITY_DEVICE=
YES

NEW_PHYSICS_DISCOVERY=
YES.
```

---

# Open Questions

## 1. Can the E1B1 quadratic active-state completion be technically protected?

The immediate naturalness question is:

> **Can an explicit same-action symmetry, spurion structure, nonrenormalization mechanism, collective-breaking structure, or controlled UV matching suppress the dangerous $xT$ descendant while allowing the active $x^2T$ response?**

The current symmetries do not do so.

A viable continuation must either:

```text
FORBID xT
BY AN ACTUAL SYMMETRY / SELECTION RULE
```

or demonstrate quantitatively that the unavoidable generated coefficient remains below the empirical/energy ceiling.

---

## 2. Does E1B1 possess a healthy scalar degree of freedom on the required background?

V26E0 established only the tensor sector.

The P-only unsupported-flat stationary background has no standalone time kinetic.

The full theory must therefore establish scalar health through:

```text
DHOST CONSTRAINT MIXING

A HEALTHY P/Q COMPLETION

A CURVED ONSHELL BACKGROUND

AN EXPLICIT SUPPORT-BALANCED BACKGROUND

OR
OTHER SAME-ACTION HEALTHY DYNAMICS.
```

Required checks:

```text
NO GHOST

NO GRADIENT INSTABILITY

HYPERBOLICITY

NO STRONG-COUPLING ZERO KINETIC

HEALTHY PRINCIPAL MARGIN.
```

---

## 3. Is the V26D/E1B1 physical $g_{00}$ response nonzero?

This is currently unestablished.

A full calculation must:

```text
ELIMINATE NONDYNAMICAL CONSTRAINTS

CONSTRUCT THE REDUCED PROPAGATOR

INSERT THE ACTUAL HIDDEN SOURCE

RECONSTRUCT THE UNIVERSAL PHYSICAL METRIC

COMPUTE SOURCE->g00

VERIFY THE SAME OBSERVABLE IN BOTH FRAMES.
```

If the physical response is zero or removable under the exact invertible field redefinition, this V26D implementation closes.

---

## 4. Is the sign outward?

A nonzero metric response is not enough.

The finite neutral payload must accelerate outward relative to the source.

The project must not promote a large inward response.

---

## 5. Can the candidate meet the standardized performance floor?

Any survivor must ultimately demonstrate:

```text
a_out >= 9.80665 m/s^2

TRUE_STANDOFF >=1.0 m.
```

Output above either floor is favorable.

---

## 6. What is the actual V26D energy?

Unknown.

Current fields remain:

```text
V26D_FIELD_CAPACITY_J=
None

V26D_COMPLETE_ENERGY_J=
None.
```

The HOOK17 17.0676-J capacity reference does not transfer to V26D.

---

## 7. What is the corrected HOOK17 capacity at 1 g / 1 m?

Unknown.

If HOOK17 obtains a surviving physical same-action carrier, the capacity must be explicitly recomputed at the final performance normalization.

Do not simply rescale the historical 17-J number informally.

---

## 8. Can HOOK17 obtain a protected carrier beyond the current P&S Case-I block?

Still logically open families include:

```text
PROJECTIVE / MAG COMPLETION
WITH ACTUAL SYMMETRY-ENFORCED HEALTH

MASSIVE HOOK / NONMETRICITY

MASSIVE SPIN-3
BEYOND TESTED CLEAN 1- ROUTE

GENUINELY NONLINEAR VECTOR-GRAVITON

GAUGE-INVARIANT COMPOSITE / CURVATURE PORTAL

DIRAC SOURCE-STATE ENGINEERING

FULL HIGGSED / NOETHER-COMPENSATED HOOK.
```

Do not reopen already-closed minimal massless repairs.

---

## 9. Can the HOOK17 nonlinear V26B1 metric numerator be joined to the A9R3 projective same-action source completion?

This remains a central HOOK17 physicalization problem.

The current A9R3 achievement establishes a deep same-action source/metric-stress partial.

It does not yet reconstruct the full V26B1 nonlinear physical metric inside the same protected action.

---

## 10. Can either current frontier remain below 10 MJ complete?

Unknown.

No current candidate has passed the complete-energy ledger.

---

# AI Assistance

AI assistant used: **ChatGPT by OpenAI**

Substantial AI-assisted work during this research slice included:

* reconstruction of the H17A6 common Ward polynomial;
* design and implementation of the J11/minimal-compensator falsification gate;
* exact massless single-compensator/Noether reasoning;
* healthy-mode source-projector decomposition for protected massive channels;
* Barker-Zell iso-Weyl source matching;
* protected $2^+$ atlas reranking;
* identification and reconstruction of the Percacci-Sezgin projective $1^+$ carrier;
* exact P&S pole-overlap calculation;
* complete Hermitian Wheeler-source projective probe audit;
* solution of the linearized metric-stress Ward system;
* construction of the local projective distortion projector;
* covariantization of the projector;
* derivation and numerical checking of the same-action clean linearized metric stress;
* technical-naturalness analysis of the P&S Case-I relations;
* derivation of the V26E0 static-spacelike tensor principal symbol;
* derivation of the unsupported-flat $P=P_X=0$ condition;
* exact Class-Ia Einstein-frame reconstruction;
* field-redefinition invertibility analysis;
* recognition of the linear V26D collision with the historical V17/V19 pure-$j=0$ class;
* reconstruction of the old V19R6 empirical/energy boundary;
* design of the quadratic active-state conformal completion;
* exact active-$\beta$ matching;
* Wilsonian NDA descendant estimates;
* performance-floor clarification;
* regression-test generation;
* simulation code generation;
* interpretation of positive and negative results;
* preparation of carry-over documentation and this journal.

AI-generated calculations, code, physical interpretations, and literature connections are not accepted solely because they were generated by an AI system.

Important results remain subject to:

```text
ANALYTIC_CHECK

NUMERICAL_CHECK

DIMENSIONAL_CHECK

LIMITING_CASE_CHECK

INDEPENDENT_RECONSTRUCTION

LITERATURE_COMPARISON

ASSUMPTION_AUDIT

AND
EXTERNAL SCIENTIFIC REVIEW.
```

Use of ChatGPT does not imply endorsement or sponsorship by OpenAI.

---

# Next Action

The immediate next scientific frontier is:

```text
032V26E1B2
QUADRATIC_ACTIVE_STATE
HEALTHY_SCALAR
+
PHYSICAL_G00_CROSSPROP
+
UV_DESCENDANT
GATE
```

The highest-information execution order is:

```text
1.
PROTECTION / UV PREFILTER

2.
HEALTHY SCALAR / CONSTRAINT SYMBOL

3.
PHYSICAL SOURCE->g00 CROSS RESPONSE

4.
FIELD-REDEFINITION INVARIANCE

5.
OUTWARD SIGN

6.
FINITE PAYLOAD

7.
>=1 g AT >=1 m

8.
SOURCE / CAPACITY ENERGY

9.
QUANTUM / EMPIRICAL

10.
COMPLETE OPERATING ENERGY.
```

No generic energy optimization is authorized before the physical-response gates pass.

No broad AGMINER scan is currently justified.

No sub-100-J capacity optimization should be performed merely to lower an already small partial number.

---

## E1B2 stop rule

Close the current V26D quadratic active-state implementation immediately if any of the following is established:

```text
NO HEALTHY SCALAR COMPLETION

UNAVOIDABLE GHOST

UNAVOIDABLE GRADIENT INSTABILITY

HYPERBOLICITY FAILURE

STRONG-COUPLING ZERO KINETIC

PHYSICAL SOURCE->g00 RESPONSE=ZERO

PHYSICAL RESPONSE REMOVABLE BY INVERTIBLE FIELD REDEFINITION

WRONG SIGN

RESPONSE REQUIRES PRINCIPAL-MARGIN COLLAPSE

UV-GENERATED xT UNAVOIDABLY RECREATES EMPIRICAL FAILURE

FINITE PAYLOAD FAILS

TRUE STANDOFF FAILS

OR

ANY UNAVOIDABLE COMPLETE-ENERGY LOWER BOUND >=10 MJ.
```

If the V26D implementation fails, record the exact reason and return to a genuinely new HOOK17 protected-carrier family.

Do not weaken the gates to preserve either candidate.

---

# Final Scientific State at Journal Close

```text
CURRENT_FULL_REGRESSION=
910_PASSED

HISTORICAL_PROTECTED_BASELINE=
94_PASSED

STRICT_COMPLETE_OPERATING_ENERGY_OBJECTIVE=
E_LT_1.0E7_J

EXACTLY_10MJ=
FAIL

MIN_OUTWARD_ACCELERATION_M_S2=
9.80665

MIN_TRUE_STANDOFF_M=
1.0

PERFORMANCE_ABOVE_FLOOR=
FAVORABLE

006D_STRONGEST_CONSERVATIVE_TRUE_STANDOFF_RESULT=
PRESERVED

006D_C=
23.591586299249

V24_INTRINSIC_DIRAC_HYPERMOMENTUM=
PRESERVED

V26B1_QUADRATIC_HOOK_METRIC_NUMERATOR=
PRESERVED

V26B1_ACTIVE_OFFSTATE_SEPARATION=
PRESERVED

HOOK17_REFERENCE_CAPACITY_RP1E12_J=
~17.0676442196

HOOK17_REFERENCE_CAPACITY_IS_COMPLETE_ENERGY=
NO

HOOK17_REFERENCE_CAPACITY_IS_CERTIFIED_1G_AT_1M=
NO

HOOK17_COMPLETE_ENERGY=
UNKNOWN

H17A6_DIRECT_J11_REPAIR=
CLOSED

H17A6_MINIMAL_VECTOR_REPAIR=
CLOSED

H17A6_PURE_STUECKELBERG_GAUGE_IMAGE_REPAIR=
CLOSED

H17A6R1_EXACT_MASSLESS_SINGLE_COMPENSATOR_CLASS=
CLOSED

H17A6R2_TESTED_MASSIVE_1MINUS_CLEAN_SOURCE=
ZERO_OVERLAP

H17A7_TESTED_DIRECT_CLEAN_ISO_WEYL_CHANNEL=
CLOSED

H17A8_TESTED_PROTECTED_2PLUS_ATLAS_ACCEPTABLE_SURVIVORS=
0

H17A8_PERCACCI_SEZGIN_PROJECTIVE_1PLUS=
PROMOTED

H17A9_EXACT_HEALTHY_1PLUS_POLE_OVERLAP=
YES

H17A9_EXACT_POLE_NUMERATOR=
1.44

H17A9R1_UNMODIFIED_WHEELER_PROJECTIVE_COMPATIBILITY=
FAIL_GENERIC

H17A9R1_CLEAN_SPECIAL_STATE=
SURVIVES

H17A9R1_UNIQUE_LINEARIZED_SIGMA_COMPLETION=
YES

H17A9R2_ACTION_LEVEL_PROJECTIVE_DIRAC_COMPLETION=
YES_LINEARIZED_SCAFFOLD

H17A9R2_ALL_16_PROJECTED_PROBES_PROJECTIVELY_COMPATIBLE=
YES

H17A9R3_COVARIANT_PROJECTOR=
PASS

H17A9R3_GL4_EQUIVARIANCE=
PASS

H17A9R3_SAME_ACTION_CLEAN_LINEARIZED_METRIC_STRESS=
PASS

H17A9R3_LINEARIZED_DIFFEO_WARD=
PASS

H17A9R3_GEOMETRIC_AND_CLEAN_LINEARIZED_NOETHER_PARTIAL=
GREEN

H17A9R3_P&S_CASE_I_HEALTH_SURFACE_CODIMENSION=
2

H17A9R3_ADDITIONAL_PROTECTING_SYMMETRY_ESTABLISHED=
NO

H17A9R3_BETA_FUNCTIONS_COMPUTED=
NO

H17A9R3_TECHNICAL_NATURALNESS=
NOT_ESTABLISHED

CURRENT_P&S_HOOK17_CARRIER=
YELLOW_BLOCKED

HOOK17_GLOBALLY_CLOSED=
NO

V26D_FALLBACK=
RESUMED

V26E0_STATIC_SPACELIKE_TENSOR_CT1=
PASS

V26E0_REPRESENTATIVE_BETA1=
1_OVER_21

V26E0_WORST_TENSOR_MARGIN=
146_OVER_147

V26E0_PRINCIPAL_MARGIN_COLLAPSE_USED=
NO

V26E0_UNSUPPORTED_FLAT_REQUIRES_P_ZERO=
YES

V26E0_UNSUPPORTED_FLAT_REQUIRES_PX_ZERO=
YES

V26E0_FULL_SCALAR_METRIC_SYMBOL_REQUIRED=
YES

V26E1A_EXACT_CLASS_IA_EINSTEIN_FRAME_MAP=
PASS

V26E1A_LINEAR_SCAFFOLD_MAP_JACOBIAN=
1

V26E1A_NEAR_SINGULAR_FIELD_REDEFINITION=
NO

V26E1B0_LINEAR_V26D_LEADING_MATTER_OPERATOR=
PURE_J0

V26E1B0_V17_V19_OPERATOR_CLASS_COLLISION=
YES

V26E1B0_EXACT_V17_EQUIVALENT_COMPLETION=
CLOSED

V26E1B0_ALL_V26D_COMPLETIONS_CLOSED=
NO

V26E1B1_QUADRATIC_ACTIVE_STATE_COMPLETION=
PROMISING_CONDITIONAL

V26E1B1_A_OF_X=
1_PLUS_100_OVER_41_X_SQUARED

V26E1B1_ACTIVE_X_STAR=
0.1

V26E1B1_ACTIVE_BETA1=
1_OVER_21

V26E1B1_OFFSTATE_BETA1=
0

V26E1B1_ACTIVE_MAP_MARGIN=
40_OVER_41

V26E1B1_TENSOR_MARGIN=
146_OVER_147

V26E1B1_TREE_R5_TWO_SCALAR_VERTEX=
ABSENT

V26E1B1_NDA_RELATIVE_OFFSTATE_TO_ACTIVE=
~0.0324351350

V26E1B1_NDA_HISTORICAL_HEADROOM_FACTOR=
~30.3731

V26E1B1_NDA_IS_QUANTUM_CERTIFICATION=
NO

V26E1B1_TECHNICAL_NATURALNESS=
NOT_CERTIFIED

V26E1B1_FULL_SCALAR_HEALTH=
NOT_ESTABLISHED

V26E1B1_PHYSICAL_G00_CROSS_RESPONSE=
NOT_ESTABLISHED

V26E1B1_OUTWARD_SIGN=
NOT_ESTABLISHED

V26E1B1_FINITE_PAYLOAD=
NOT_ESTABLISHED

V26D_FIELD_CAPACITY_J=
UNKNOWN

V26D_COMPLETE_ENERGY_J=
UNKNOWN

HOOK17_CAPACITY_TRANSFERS_TO_V26D=
NO

PHYSICAL_ANTIGRAVITY_MODEL_FOUND=
NO

CERTIFIED_SUB10MJ_MODEL_FOUND=
NO

PRACTICAL_ANTIGRAVITY_DEVICE=
NO

REACTIONLESS_PROPULSION=
NO_CLAIM

NEW_PHYSICS_DISCOVERY=
NO

NOVELTY=
NOT_ESTABLISHED

OVERALL_KNOWLEDGE_ACCOMPLISHMENT_HEURISTIC=
~75_PERCENT_NOT_SUCCESS_PROBABILITY

HOOK17_SPECIFIC_PROGRESS_HEURISTIC=
~68_PERCENT_NOT_SUCCESS_PROBABILITY

IMMEDIATE_NEXT=
032V26E1B2_QUADRATIC_ACTIVE_STATE_HEALTHY_SCALAR_PHYSICAL_G00_CROSSPROP_AND_UV_DESCENDANT_GATE
```

---

# Final Journal Classification

The strongest HOOK17-positive conclusion of this research slice is:

> **The HOOK17 intrinsic-Dirac source survived far beyond an algebraic irreducible-mode preflight. The clean source has exact nonzero overlap with a healthy protected Percacci-Sezgin $1^+$ pole; a fixed action-level projective Dirac completion preserves that pole; the projector admits a covariant GL(4)-equivariant form; and variation of the same declared clean interaction reproduces the unique linearized metric-stress completion required by the diffeomorphism Ward identity. The current P&S carrier is blocked only at the later protection/technical-naturalness gate because the Case-I healthy coefficient relations have not been shown to follow from a symmetry.**

The strongest V26D-positive conclusion is:

> **The protected $c_T=1$ DHOST/KMM fallback has a healthy static-spacelike tensor sector with large principal margin and an exact nonsingular Einstein-frame map. The original linear conformal matter completion was correctly identified as a duplicate of the already-closed V17/V19 pure-$j=0$ failure class. A genuinely different quadratic active-state completion, $A(x)=1+(100/41)x^2$, preserves the representative active $\beta_1=1/21$, retains large tensor and field-redefinition margins, and removes the historical tree-level R5 two-scalar ordinary-matter vertex. Low-energy NDA gives substantial historical empirical headroom, but no symmetry currently protects the missing lower-order $xT$ operator and no full scalar-health or physical-$g_{00}$ response has yet been established.**

The correct interpretation is therefore not:

```text
HOOK17=
SOLVED

17J_ANTIGRAVITY=
ESTABLISHED

OR

V26D=
PHYSICAL_MODEL.
```

The correct interpretation is:

```text
HOOK17=
DEEP_PHYSICAL_PARTIAL
CURRENT_BEST_P&S_CARRIER_BLOCKED_ON_NATURALNESS
BROADER_RESCUE_REMAINS_OPEN

V26D_E1B1=
SPECIFIC_PROMISING_CONDITIONAL_FALLBACK
TREE_LEVEL_HISTORICAL_FAILURE_ESCAPED
FULL_PHYSICAL_RESPONSE_AND_UV_PROTECTION_STILL_REQUIRED.
```

The project exits this slice with a sharper frontier than it entered.

The next decisive question is:

> **Can the V26E1B1 quadratic active-state completion acquire a healthy scalar sector and a nonzero frame-invariant outward physical-$g_{00}$ response while keeping the regenerated off-state operator under quantitative quantum/UV control?**

If yes, proceed immediately to finite payload, the standardized $\ge1g$ at $\ge1,\mathrm m$ true-stand-off gate, and source/complete-energy accounting.

If no, close the exact failing V26D implementation and resume HOOK17 only through a genuinely new protected carrier rather than weakening the physical gates.

### END JOURNAL — 2026-09-08 — 032H17A6–032H17A9R3 SAME-ACTION PROJECTIVE HOOK17 COMPLETION, NATURALNESS BLOCK, V26D FALLBACK RESUMPTION, PURE-J0 FAILURE-MEMORY COLLISION, AND QUADRATIC ACTIVE-STATE ESCAPE
