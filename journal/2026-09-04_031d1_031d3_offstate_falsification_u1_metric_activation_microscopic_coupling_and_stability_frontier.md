# Research Journal — 2026-09-04

## 031D1–031D3 Activated Scalar-Metric Control: Gate-Free Off-State Falsification, Auxiliary-$U(1)$ Activation, 127.782-GJ Microscopic Coupled ON Field, and Coupled-Stability Frontier

## Objective

The primary scientific question of this research slice was:

> **Can the previously established microscopic 031 scalar-metric antigravity source be given a physically dynamical ON/OFF activation mechanism without prescribing a spatial coupling by hand, while preserving positive energy, conserved charges, finite-payload outward response, microscopic self-consistency, and stability?**

The preceding 031A–031C program had already changed the project’s practicality landscape.

It had established:

```text
SCALAR-METRIC CHARGE ENERGY THEOREMS
=
GREEN

EVEN-Z2 SCALARIZATION
=
GREEN AT CLASSICAL FIELD LEVEL

GLOBAL U(1) Q-BALL MICROSCOPIC SOURCE
=
REALIZED

ROBUST MICROSCOPIC SOURCE
=
~121.55 GJ CLASS

EDGE ENERGY LEADER
=
~96.14 GJ CLASS

FINITE-PAYLOAD OUTWARD PHYSICAL-METRIC RESPONSE
=
YES

INTRINSIC X+PHI COUPLED-LINEAR STABILITY
=
GREEN THROUGH l=8 AT DECLARED LEVEL
```

However, the source was still effectively always ON.

A viable 031 architecture requires more than a scalarized field.

It requires a physical control mechanism satisfying:

```text
ON STATE
=
MICROSCOPIC FIELD SOLUTION

OFF STATE
=
MICROSCOPIC FIELD SOLUTION OR CONTROLLED SECTOR

ACTIVATION
=
DYNAMICAL

COUPLINGS
=
LOCAL FIELD FUNCTIONS

SOURCE REACTION
=
INCLUDED

CONTROL ENERGY
=
INCLUDED

CONTROL CHARGE
=
CONSERVED OR EXCHANGED PHYSICALLY

PAYLOAD RESPONSE
=
UNIVERSAL PHYSICAL METRIC

SPATIAL COUPLING PRESCRIPTION BY HAND
=
FORBIDDEN
```

This research slice therefore progressed through three increasingly different activation strategies:

```text
031D1
GATE-FREE SAME-QX OFF-STATE TEST

031D2
CANONICAL AUXILIARY MEDIATOR-MASS GATE

031D3
INDEPENDENT GLOBAL-U(1) METRIC-ACTIVATION Q-BALL
```

The slice produced both decisive negative and major positive results.

The strongest positive result is:

> **A fully reciprocal microscopic ON solution containing the source Q-ball $X$, scalar $\phi$, and an independent conserved activation Q-ball $Y$ has been constructed at fixed $Q_X$ and fixed $Q_Y$, with excellent domain convergence, preserved finite-payload outward physical-metric response, and a conservative positive-energy inventory of approximately $127.7824$ GJ.**

The strongest unresolved issue is:

> **Final coupled-linear stability certification of the activated $X+\phi+Y$ field remains YELLOW because two independent spectral approaches find no reproducible physical exponential instability, but the independent Chebyshev translation/projected-stiffness diagnostic has not yet been formulated in a demonstrably correct weighted variational inner product.**

This journal does **not** establish:

```text
A PRACTICAL ANTIGRAVITY DEVICE

A COMPLETE ACTIVATION RESERVOIR

REVERSIBLE SWITCHING

LOW-POWER SWITCHING

SWITCHING-RADIATION CLOSURE

NONLINEAR FRAGMENTATION STABILITY

FULL EINSTEIN-MATTER BACKREACTION

RADIATIVE NATURALNESS

A UV-COMPLETE THEORY

EMPIRICAL FIFTH-FORCE CLOSURE

EQUIVALENCE-PRINCIPLE CLOSURE

PPN CLOSURE

AN EXPERIMENTALLY OBSERVED NEW FORCE

NEW-PHYSICS DISCOVERY
```

The project’s internal knowledge/accomplishment heuristic remains approximately:

```text
70_TO_71_PERCENT_NOT_A_PROBABILITY
```

until stronger stability, control, nonlinear-gravity, EFT, and empirical gates are closed.

---

# Starting State

## 1. 006D remained the strongest fully conservative pure-GR true-stand-off architecture

The permanent conservative reference remained:

```math
C_{006D}
=
23.591586299249
```

in:

```math
E
=
C
\frac{
a c^2 h^2
}{
G
}.
```

006D remains:

```text
FINITE POSITIVE ENERGY
=
YES

LOCAL CONSERVATION
=
PASS AT LINEARIZED ORDER

NEC
=
PASS

WEC
=
PASS

DEC
=
PASS

LOCAL GRAVITATIONAL FIELD
=
OUTWARD

TRUE STANDOFF
=
YES

FAR-FIELD ACTIVE MASS
=
POSITIVE
```

At:

```text
a = 1 g
h = 1 m
C = 1
```

the pure-GR scale remains approximately:

```math
1.32\times10^{28}\ {\rm J}.
```

No tested pure-GR mechanism has escaped this $1/G$ burden.

Therefore 031 remained qualitatively important because its approximately $10^{11}$-J-class energies are not obtained by reducing the pure-GR coefficient alone.

They arise from a different scalar-charge mechanism.

006D nevertheless remains the most completely conservative true-stand-off architecture in the project.

---

## 2. 031A analytical scalar-metric foundation

031A had already established:

```text
EXACT SCALAR NORMALIZATION
=
PASS

POSITIVE CANONICAL SCALAR SELF-ENERGY
=
PASS

FINITE-PAYLOAD RESPONSE
=
PASS

PAYLOAD-CENTERED CAVITY FLOOR
=
DERIVED

STRICT ONE-SIDED HALF-SPACE FLOOR
=
DERIVED

ABSOLUTE SCALAR-CHARGE FLOOR
=
DERIVED

YUKAWA RANGE PENALTIES
=
EXPLICIT
```

The ordinary-matter physical metric was of conformal form:

```math
\tilde g_{\mu\nu}
=
A_m^2(\phi)g_{\mu\nu}.
```

The useful finite-payload acceleration is therefore a physical-metric response rather than a species-specific externally prescribed force.

---

## 3. Crucial source/matter coupling distinction

A major conceptual distinction entering this slice was:

### Exotic source

The scalarized exotic $X$ source uses:

```math
A_X(u)
=
e^{-u^2/2},
```

with:

```math
u=\frac{\phi}{M_c}.
```

### Ordinary neutral matter

Ordinary neutral matter sees:

```math
A_m(\phi)
=
\exp
\left[
+\frac{\phi^2}{2M_m^2}
\right].
```

For a localized scalar profile decreasing away from the source:

```math
\frac{d}{dr}\phi^2<0.
```

Therefore:

```math
a_r
=
-c^2
\frac{d}{dr}
\ln A_m
>
0.
```

The positive exponent is thus responsible for outward physical-metric acceleration.

The source and ordinary-matter coupling scales are not equal.

The later provenance repair in this session made that distinction quantitatively decisive.

---

## 4. Microscopic global-$U(1)$ Q-ball source

The main microscopic source field entering 031D was:

```math
X
=
\frac{\sigma}{\sqrt2}
e^{-i\omega t}.
```

Define:

```math
y
=
\frac{\sigma}{F},
```

```math
u
=
\frac{\phi}{M_c},
```

```math
x
=
m_Xr,
```

```math
\Omega_X
=
\frac{\omega_X}{m_X},
```

```math
\epsilon
=
\frac{m_\phi}{m_X},
```

and:

```math
\chi
=
\frac{F}{M_c}.
```

The logarithmic source function is:

```math
W_X(y)
=
\frac12\ln(1+y^2).
```

The source/scalar equations are:

```math
y''
+
\frac2x y'
=
e^{-u^2/2}
\frac{y}{1+y^2}
-
\Omega_X^2y,
```

```math
u''
+
\frac2x u'
=
\epsilon^2u
-
\chi^2
e^{-u^2/2}
W_X(y)u.
```

This field has:

```text
POSITIVE HAMILTONIAN
=
YES

GLOBAL U(1) CHARGE
=
CONSERVED

E/(QmX)
<
1

DEC
=
PASS IN TESTED FIELD

SOURCE REACTION
=
INCLUDED

FINITE-PAYLOAD OUTWARD RESPONSE
=
YES

PAYLOAD X OVERLAP
=
NEGLIGIBLE
```

---

## 5. Source energy hierarchy entering 031D

The centered microscopic source had approximately:

```math
E
\approx
273.464\ {\rm GJ}.
```

Morphology optimization later produced approximately:

```text
~103 GJ
```

and then:

```math
96.141319171\ {\rm GJ}.
```

The approximately 96-GJ point lay essentially on the declared source-leak and payload-backreaction operating constraints and was therefore not selected as the robust activation target.

The more conservative interior operating candidate used in 031D had source inventory approximately:

```math
E_{X+\phi}
\approx
121.5536\ {\rm GJ}.
```

Representative parameters were approximately:

```math
\Omega_X
=
0.34,
```

```math
\epsilon
=
0.01,
```

```math
\chi
=
0.4260182686,
```

with source shift approximately:

```math
0.552\ {\rm m}.
```

---

## 6. Intrinsic source coupled-linear stability

Before activation, the source had undergone a coupled-linear stability program.

The $l=1$ sector initially showed an apparent positive real mode.

Because an isolated spherical soliton has an exact translational Goldstone zero mode, that apparent instability was not accepted without a dedicated zero-mode analysis.

The project then:

```text
CONSTRUCTED ANALYTIC TRANSLATION MODE

MEASURED DISCRETE RESIDUAL

MEASURED EIGENVECTOR OVERLAP

REFINED GRID

ENLARGED DOMAIN

PROJECTED TRANSLATION MODE

CHECKED NEXT MODE
```

The resulting D96 intrinsic source certification was GREEN through:

```math
l=0,\ldots,8
```

at the declared coupled-linear level.

This became the stability benchmark for the activation program.

---

# Work Performed

# 1. 031D1 — gate-free same-$Q_X$ OFF-state test

The cheapest possible activation architecture was tested first.

The idea was to use the natural:

```math
u=0
```

solution as the OFF state without adding an auxiliary gate field.

Because the source coupling is even under:

```math
u\rightarrow-u,
```

the unscalarized branch exists structurally.

The question was whether that branch is dynamically stable at the same conserved source charge.

---

## 1.1 OFF scalar operator

At:

```math
u=0,
```

the scalar perturbation operator is:

```math
L_{\rm off}
=
-\nabla_x^2
+
\epsilon^2
-
\chi^2W_X(y).
```

A healthy gate-free OFF state requires its lowest eigenvalue to be nonnegative.

---

## 1.2 Low same-$Q_X$ branch

The fixed source charge was:

```math
I_{Q_X}
=
6594.219892350219.
```

A low-frequency same-charge OFF root was found at approximately:

```math
\Omega_{X,\rm off}
=
0.49396038995.
```

Its source branch itself was favorable:

```math
\frac{dQ_X}{d\omega_X}
\approx
-68415.9.
```

Thus the source sector was on the stable side of the ordinary Q-ball slope criterion.

It also had:

```math
E/(Qm_X)
\approx
0.60212128
<
1.
```

However the scalar Hessian had:

```math
\lambda_0
=
-0.1251375624422016.
```

This is a real tachyonic scalar mode.

The corresponding uncontrolled instability time was approximately:

```math
3.11\times10^{-10}\ {\rm s}.
```

The dimensionless mass shift required merely to reach marginal stability was:

```math
\delta\hat m^2_{\rm crit}
=
0.1251375624422016.
```

Relative to the bare scalar contribution:

```math
\epsilon^2
=
10^{-4},
```

this is approximately:

```math
1251.38
```

times larger.

Therefore:

```text
LOW SAME-QX GATE-FREE OFF STATE
=
SCALAR TACHYON
```

---

## 1.3 High same-$Q_X$ branch

The possibility of a second same-charge unscalarized branch was then explored.

A high-frequency root was found near:

```math
\Omega_X
\approx
0.9999958926.
```

The corresponding scaled parameter was approximately:

```math
\mu
\approx
0.00286615.
```

However:

```math
\frac{dQ}{d\omega}
>
0
```

with magnitude approximately:

```math
8\times10^8.
```

Thus the high branch was source-unstable.

It also had:

```math
E/(Qm_X)
>
1
```

by approximately:

```math
4.1\times10^{-6}.
```

Thus it was slightly unbound.

The second same-charge branch therefore did not rescue the route.

---

## 1.4 D1 conclusion

The gate-free same-$Q_X$ OFF-state route was closed for the tested nodeless single-field $Z_2$ Q-ball architecture.

Final classification:

```text
GATE_FREE_SAME_Q_OFFSTATE_ROUTE_CLOSED=True

AUXILIARY_GATE_REQUIRED=True
```

This is not a theorem against every possible gate-free scalar model.

It is a specific falsification of the tested same-$Q_X$ route.

---

# 2. 031D2 — canonical auxiliary mediator-mass gate

After D1 failed, the next conservative idea was to stabilize the OFF scalar directly using a dynamical auxiliary real scalar $s$.

The tested potential was:

```math
V_s
=
\frac{\lambda_s}{4}
\left(
s^2-v_s^2
\right)^2,
```

with interaction:

```math
V_{\rm int}
=
\frac12
g_s^2s^2\phi^2.
```

The OFF phase would raise the effective scalar mass.

The ON phase would reduce the gate expectation value and restore long-range scalarization.

---

## 2.1 Required mass shift

The critical dimensionless stabilization requirement from D1 was:

```math
0.1251375624422016.
```

A 20% operating margin required:

```math
\delta\hat m^2_{\rm required}
=
0.1501650749306419.
```

The source mass was:

```math
m_X
=
5.9796054667\times10^{-6}\ {\rm eV}.
```

The ON scalar mass was:

```math
m_{\phi,\rm on}
=
5.9796054667\times10^{-8}\ {\rm eV}.
```

The required additional physical scalar mass was approximately:

```math
\Delta m
=
2.31716520714\times10^{-6}\ {\rm eV}.
```

Therefore the OFF scalar mass became approximately:

```math
m_{\phi,\rm off}
=
2.31793661806\times10^{-6}\ {\rm eV}.
```

The corresponding OFF-state scalar range was only approximately:

```math
0.08513\ {\rm m}.
```

At the capacity level this appeared capable of strongly suppressing the long-range scalar field.

---

# 3. 031D2A–D2C — realization difficulties

The D2 gate was then subjected to several increasingly explicit realization attempts.

---

## 3.1 D2A capacity scan

A broad parameter-capacity scan identified apparently viable local stabilization.

However, after translating the dimensionless gate profile back to the physical source scale, the actual gate-wall extent was approximately:

```math
112.7\ {\rm m}.
```

This was already much larger than the desired compact device morphology.

D2A was therefore demoted.

---

## 3.2 D2B corrected scan

A corrected scan found a best nominal candidate near:

```math
g_s
\approx
2.2077\times10^{-9},
```

```math
v_s
\approx
1049.6\ {\rm eV},
```

```math
\lambda_s
\approx
1.95\times10^{-20}.
```

The predicted transition scale was still approximately:

```math
52.77\ {\rm m}.
```

The raw BVP did not converge.

---

## 3.3 D2B-R exact rescaling repair

An exact rescaling identity was derived and passed numerically.

This confirmed that the dimensional rescaling itself was not the source of the previous failure.

However the corrected BVP still failed with excessive node growth.

Therefore the architecture was not promoted.

---

## 3.4 D2C full fixed-$Q_X$ $X+\phi+s$ solve

The gate field was then incorporated directly into the full fixed-charge microscopic source problem.

The full BVP again failed to produce a clean promotion-grade solution.

---

## 3.5 D2C-R logarithmic gate

A logarithmic gate homotopy was tested to avoid part of the stiffness of the original parameterization.

The continuation became singular.

No physical existence result was obtained.

---

## 3.6 D2C-V variational attempt

A lower-dimensional fixed-charge variational formulation was then attempted.

The baseline reconstruction had approximately:

```math
1.18\times10^{-5}
```

relative mismatch.

However:

```text
THREE SEEDS
=
OPTIMIZER FAILURE

GRADIENTS
=
LARGE

ENDPOINTS
=
ON-LIKE

PROMOTION-GRADE OFF SOLUTION
=
NO
```

This result must not be promoted.

---

## 3.7 D2C-FV finite-volume trust-region attempt

A sparse finite-volume trust-region realization was attempted as the final D2 numerical rescue.

The run terminated with:

```text
RUN_RC=1
```

after reaching the maximum function evaluations essentially at the beginning of the homotopy.

---

## 3.8 D2 conclusion

The canonical mediator-mass gate was strongly demoted under the project stop rule.

Correct claim:

```text
031D2
=
STRONGLY DEMOTED IN TESTED IMPLEMENTATIONS
```

Incorrect claim:

```text
ALL CANONICAL MASS-GATE THEORIES
=
MATHEMATICALLY IMPOSSIBLE
```

No such universal no-go theorem was obtained.

The important practical lesson was:

> **A local OFF-state scalar-mass shift that looks cheap algebraically can become geometrically huge and numerically difficult once the gate field is required to be a genuine self-consistent field.**

---

# 4. 031D3 — independent global-$U(1)$ metric-activation Q-ball

D2’s difficulties motivated a conceptually different control mechanism.

Rather than stabilize the scalar OFF state by changing its mass, introduce a second positive-energy conserved soliton $Y$ whose local amplitude multiplies both:

1. exotic-source scalarization; and
2. ordinary-matter physical-metric coupling.

Let:

```math
a
=
\frac{|Y|}{V}.
```

Define:

```math
\boxed{
f(a)
=
1-e^{-a^2/2}
}
```

Then use:

```math
\boxed{
A_X(u,a)
=
\exp
\left[
-\frac12
f(a)u^2
\right]
}
```

for the exotic source, and:

```math
\boxed{
A_m(\phi,a)
=
\exp
\left[
+
\frac{
f(a)\phi^2
}{
2M_m^2
}
\right]
}
```

for ordinary neutral matter.

This is the central D3 activation architecture.

---

# 5. Structural D3 ON/OFF properties

## 5.1 Exact OFF state

At:

```math
Y=0,
```

we have:

```math
a=0
```

and:

```math
f(0)=0.
```

Therefore:

```math
A_X=1,
```

and:

```math
A_m=1.
```

Thus:

```text
SOURCE SCALARIZATION
=
OFF

ORDINARY-MATTER SCALAR METRIC RESPONSE
=
OFF
```

The configuration:

```math
\phi=0
```

is then an exact OFF solution with the positive bare scalar mass.

---

## 5.2 Near-OFF expansion

For small activation amplitude:

```math
f(a)
=
\frac{a^2}{2}
+
O(a^4).
```

Thus there is no bilinear:

```text
PHI-Y
```

mixing in the quadratic Hessian about the exact OFF vacuum.

This is a favorable structural property.

---

## 5.3 ON state

For:

```math
a\gg1,
```

we have:

```math
f(a)\rightarrow1.
```

The original scalarized source and matter metric are then recovered.

---

## 5.4 Conserved-charge consequence

The OFF state has:

```math
Q_Y=0.
```

The ON state has:

```math
Q_Y\neq0.
```

Therefore:

> **D3 activation is not a same-charge switching process.**

A physical switch must either:

```text
INJECT / REMOVE QY
```

or:

```text
EXCHANGE QY WITH A RESERVOIR.
```

This is not optional.

A spatially prescribed $Y$ profile is forbidden.

---

# 6. Activation Q-ball field

The activation field was chosen as another global-$U(1)$ logarithmic Q-ball.

Use:

```math
Y
=
\frac{\eta}{\sqrt2}
e^{-i\omega_Yt}.
```

Define:

```math
a
=
\frac{\eta}{V}.
```

The potential function is:

```math
W_A(a)
=
\frac12\ln(1+a^2).
```

Dimensionless activation energy:

```math
I_E
=
4\pi
\int
\rho^2
\left[
\frac12a'^2
+
\frac12\Omega_Y^2a^2
+
W_A(a)
\right]
d\rho.
```

Charge:

```math
I_Q
=
4\pi\Omega_Y
\int
\rho^2a^2
\,d\rho.
```

Physical activation energy:

```math
E_Y
=
I_E
\frac{V^2}{m_A}.
```

The field has:

```text
POSITIVE CANONICAL HAMILTONIAN
=
YES

GLOBAL U(1) CHARGE
=
YES
```

and therefore supplies an actual microscopic activation degree of freedom rather than an externally specified control function.

---

# 7. 031D3A — activation capacity

D3A first tested whether such a conserved activation soliton could geometrically cover both:

```text
SOURCE REGION

PAYLOAD REGION
```

while remaining negligible farther away.

The capacity scan tested:

```text
228 CASES
```

with:

```text
227 PASSING
```

the declared capacity conditions.

A best permissive point had approximately:

```math
\Omega_Y
=
0.35,
```

```math
\mu
=
0.0603406865.
```

The raw activation energy appeared extraordinarily small.

However that point sat at a permissive strong-coupling boundary approximately:

```math
m_A^2/V^2
\sim
4\pi.
```

The corresponding source-side operator scale was only of order:

```text
TENS OF eV.
```

Therefore the spectacular tiny activation-energy quote was **not promoted**.

D3A established capacity only.

---

# 8. Near-OFF source operator and EFT issue

Expanding the source coupling near:

```math
Y=0
```

gives schematically:

```math
\ln A_X
\sim
-\frac14
\frac{
|Y|^2\phi^2
}{
V^2M_c^2
}.
```

The associated schematic source-side operator scale is:

```math
\Lambda_X
\sim
\sqrt{
VM_c
}.
```

This showed that arbitrarily tiny $V$ cannot be treated as a free reduction in activation energy.

A stronger EFT preflight was mandatory.

---

# 9. 031D3A-R — strengthened EFT / payload / reaction preflight

A declared source-side operator floor of:

```math
10\ {\rm GeV}
```

was imposed.

Important:

```text
10 GEV
=
PREFLIGHT FLOOR

10 GEV
!=
FINAL UV COMPLETION
```

The scan also considered weaker and stronger operator scales.

Approximate activation energy ladder:

```text
1 MeV
=
~1.4e-15 GJ

1 GeV
=
~8.1e-4 GJ

10 GeV
=
~6.23 GJ

100 GeV
=
~4.60e4 GJ
```

The 100-GeV stress test therefore demonstrated the severe cost of pushing this operator scale much higher.

The 10-GeV candidate became the working D3 activation point.

Representative values:

```math
\Omega_Y
=
0.45,
```

```math
\mu
=
0.05346177557001615,
```

```math
V
=
1.458108242865562\ {\rm GeV},
```

```math
m_A
=
3.196803254561750\times10^{-7}\ {\rm eV}.
```

Activation energy:

```math
E_Y
=
6.228597773926\ {\rm GJ}.
```

Physical activation charge:

```math
Q_Y
=
2.201803600111565\times10^{35}.
```

Source reaction ratio:

```math
7.62\times10^{-15}.
```

The pre-coupling source-plus-activation inventory was approximately:

```math
127.782184791444\ {\rm GJ}.
```

---

# 10. 031D3A-R2 — physical-metric normalization provenance repair

This was an important scientific software/provenance correction.

The earlier D3A-R payload reaction estimate used the exotic source scale:

```math
M_c
```

where ordinary matter should have used:

```math
M_m.
```

The source and matter couplings were therefore explicitly separated.

Operating ordinary-matter coupling:

```math
\alpha_m
=
29.19409424975629.
```

Near-payload scalar:

```math
u
\approx
0.210732112305.
```

Physical scalar amplitude:

```math
\phi
\approx
14.4524326871\ {\rm GeV}.
```

The ordinary-matter scale reconstructed as:

```math
M_m
=
1.097924452346906\times10^9\ {\rm GeV}.
```

The ratio is:

```math
\frac{M_m}{M_c}
=
1.600892694010682\times10^7.
```

The old incorrect payload-reaction estimate had been:

```math
0.08812516897724.
```

The corrected value became:

```math
3.438551375014167\times10^{-16}.
```

The expected suppression was:

```math
\left(
\frac{M_c}{M_m}
\right)^2
=
3.901894787744707\times10^{-15}.
```

The observed suppression matched this to machine precision.

This was a strong independent provenance confirmation.

The matter-side metric operator scale became approximately:

```math
40011.1571\ {\rm GeV}
```

or approximately:

```text
40 TeV.
```

The source-side declared preflight scale remained:

```text
10 GeV.
```

D3A-R2 classification:

```text
GREEN_D3AR2_CORRECT_SOURCE_AND_PHYSICAL_METRIC_NORMALIZATION_PASS
```

---

# 11. 031D3B — full fixed-$Q_X,Q_Y$ microscopic ON field

D3B was the decisive microscopic activation-existence gate.

The new ON solution simultaneously included:

```text
X SOURCE FIELD

PHI SCALAR

Y ACTIVATION FIELD
```

with:

```text
FIXED QX
=
YES

FIXED QY
=
YES

SOURCE-ACTIVATION RECIPROCITY
=
INCLUDED
```

The activated equations used:

```math
f(a)
=
1-e^{-a^2/2},
```

```math
A_X
=
e^{-f(a)u^2/2}.
```

The source equation was:

```math
y''
+
\frac2x y'
=
A_X
\frac{y}{1+y^2}
-
\Omega_X^2y.
```

The scalar equation was:

```math
u''
+
\frac2x u'
=
\epsilon^2u
-
\chi^2
f(a)
A_X
W_X(y)
u.
```

The activation equation was:

```math
a''
+
\frac2x a'
=
\mu^2
\left[
\frac{a}{1+a^2}
-
\Omega_Y^2a
\right]
-
\frac{
u^2A_XW_X(y)f'(a)
}{
2\rho_Y
}.
```

The last term is the reciprocal source reaction of the scalarized source on the activation field.

---

# 12. Fixed charges and operating parameters

The conserved charge targets were:

```math
I_{Q_X}
=
6594.219892350219,
```

and:

```math
I_{Q_Y}
=
10583.53778605269.
```

Physical activation charge:

```math
Q_Y
=
2.201803600111565\times10^{35}.
```

Operating frequencies:

```math
\Omega_X
\approx
0.3399999998877,
```

```math
\Omega_Y
\approx
0.4500000012825.
```

Activation/source mass ratio:

```math
\mu
=
0.05346177557001615.
```

Canonical activation normalization:

```math
\rho_Y
=
2.490592146370240\times10^{-3}.
```

---

# 13. D3B coupling homotopy

A coupling homotopy:

```text
t =
0.00
0.10
0.25
0.50
0.75
1.00
```

was used to continue from the independently reconstructed source and activation solitons into the complete reciprocal theory.

Across the entire homotopy:

```text
OMEGA_X
=
ESSENTIALLY CONSTANT

OMEGA_Y
=
ESSENTIALLY CONSTANT

U0
=
ESSENTIALLY CONSTANT

A0
=
ESSENTIALLY CONSTANT
```

Central activation amplitude:

```math
a_0
\approx
9.498546.
```

Thus:

```math
f(a_0)
\approx
1
```

to machine precision.

This explained why the physical coupling homotopy barely moved the solution.

The activation field is deeply saturated wherever the source is productive.

Since:

```math
f'(a)
=
ae^{-a^2/2},
```

the reciprocal $Y$ correction is exponentially small in the productive source core.

The coupled field is therefore extraordinarily close to a direct product of the source soliton and activation soliton while still solving the reciprocal microscopic theory.

---

# 14. D3B domain convergence

The full $t=1$ ON field was solved on:

```text
XMAX = 500
XMAX = 700
XMAX = 900
```

The conservative total energies were approximately:

```math
127.782358314\ {\rm GJ},
```

```math
127.782437144\ {\rm GJ},
```

and:

```math
127.782438518\ {\rm GJ}.
```

Fractional domain energy spread:

```math
6.2766203\times10^{-7}.
```

The source and activation frequencies showed zero printed relative spread.

Likewise:

```text
U0 DOMAIN SPREAD
=
0 AT PRINTED PRECISION

A0 DOMAIN SPREAD
=
0 AT PRINTED PRECISION
```

The fixed charges closed at printed precision.

---

# 15. D3B independent residual reconstruction

The dense equation residual reconstruction gave:

```math
8.58706835\times10^{-6}.
```

Maximum BVP RMS residual:

```math
5.62150512\times10^{-6}.
```

Thus the coupled field was not promoted merely from solver termination.

Its equations were independently re-evaluated on a dense reconstruction.

---

# 16. D3B finite-payload physical response

The activation fraction in the productive source region was:

```math
f_{\min}
=
1
```

at printed precision.

Across the finite payload:

```math
f_{\min}
=
0.9999999999997287.
```

The acceleration ratio relative to the original scalarized source lay approximately in:

```math
1.000000001418515
\le
\frac{
a_{\rm D3}
}{
a_{\rm source}
}
\le
1.000000001425199.
```

The force remained outward.

Thus:

```text
ACTIVATION PRESERVES FINITE-PAYLOAD EFFECT
=
PASS
```

---

# 17. D3B conservative energy ledger

Activation inventory:

```math
E_Y
=
6.228597769673\ {\rm GJ}.
```

Conservative source-plus-scalar inventory:

```math
E_{X+\phi}
=
121.553840748599\ {\rm GJ}.
```

Full conservative ON inventory:

```math
\boxed{
E_{\rm ON}
=
127.782438518272\ {\rm GJ}
}
```

Pre-coupling reference:

```math
127.782184791444\ {\rm GJ}.
```

Source inventory fractional shift after full reciprocal coupling:

```math
2.087396656\times10^{-6}.
```

Activation energy as a fraction of source inventory:

```math
5.12415793\%.
```

Final D3B classification:

```text
GREEN_D3B_FULL_FIXED_QX_QY_COUPLED_MICROSCOPIC_ON_FIELD_EXISTS
```

This is a major project milestone.

It establishes microscopic ON-field existence for the activation architecture.

It does not establish stability or switching.

---

# 18. 031D3C — full five-field coupled-linear spectrum

The next question was whether adding $Y$ introduced a new instability.

Canonical perturbation sectors were:

```text
X AMPLITUDE

X PHASE

PHI / CHI

Y AMPLITUDE

Y PHASE
```

The coupled dynamics were represented schematically as:

```math
q_{tt}
+
Gq_t
+
Kq
=
0,
```

with rotating-frame gyroscopic blocks associated with both Q-ball phases.

The quadratic eigenvalue problem:

```math
\left(
s^2I+sG+K
\right)q
=
0
```

was converted to first order.

Two exact $l=0$ global-$U(1)$ phase modes are expected.

One exact $l=1$ common translational Goldstone mode is expected.

The relative displacement of the source and activation solitons is **not** an exact symmetry and must not be removed.

---

# 19. D3C activation branch slope

The activation Q-ball was tested around:

```math
\Omega_Y
=
0.45.
```

The measured charge slope was:

```math
\frac{
dQ_Y
}{
d\Omega_Y
}
=
-1.1861315061\times10^5.
```

Therefore:

```text
Y_BRANCH_SLOPE_STABLE=True
```

at the declared radial Q-ball criterion.

---

# 20. D3C cross-Hessian diagnostics

The productive source remained completely saturated:

```math
1-f
=
0
```

at machine precision.

Maximum productive:

```math
|f'|
=
2.0377004626\times10^{-18}.
```

Background equation defect associated with the direct-product approximation:

```math
4.4066463045\times10^{-17}.
```

New canonical source/$Y$ cross-Hessian magnitude:

```math
1.6221855066\times10^{-18}.
```

Thus:

> **The D3 activation field changes the theory structurally while introducing essentially no local linear mixing inside the productive saturated core.**

This strongly reduced the prior probability that a new large coupled instability would arise there, although it did not replace the actual stability calculation.

---

# 21. D3C finite-difference spectrum

The first complete five-field sparse calculation tested:

```math
l=0,\ldots,8.
```

For:

```math
l=0
```

and:

```math
l=2,\ldots,8,
```

the measured real parts were essentially numerical zero.

The worst inherited non-$l=1$ value was approximately:

```math
1.4184605840\times10^{-11}.
```

Thus no physical instability was found outside the translation sector.

The apparent $l=1$ real mode was:

```math
h=0.50:
4.16301741077\times10^{-5}
```

```math
h=0.375:
3.12347491176\times10^{-5}
```

```math
h=0.25:
2.08462969337\times10^{-5}.
```

The common translation overlap remained approximately:

```text
0.976
```

and the suspicious relative-translation diagnostic also had high translation-sector overlap.

A domain enlargement:

```text
RMAX 600 -> 800
```

at fixed:

```math
h=0.5
```

left the apparent growth unchanged to approximately:

```math
1.4\times10^{-18}.
```

This strongly suggested grid splitting rather than a physical finite-domain instability.

---

# 22. D3C numerical incompleteness

Several global rightmost sparse ARPACK requests using:

```text
which="LR"
```

failed to complete.

Therefore the D3C script correctly refused to certify coupled-linear stability despite the small measured growth.

Classification:

```text
RED_OR_YELLOW_D3C_COUPLED_LINEAR_STABILITY_NOT_CERTIFIED
```

Important claim correction:

```text
TRUE_D3_INSTABILITY
=
NOT ESTABLISHED
```

The failure was numerical incompleteness, not a demonstrated growing physical mode.

---

# 23. D3C switching / resource ledger

D3C also quantified the unavoidable change in activation-charge sector.

OFF:

```math
Q_Y=0.
```

ON:

```math
Q_Y
=
2.201803600111565\times10^{35}.
```

At the same source charge, the OFF source had:

```math
\Omega_{X,\rm off}
=
0.4939603907371556.
```

Its energy was:

```math
E_{\rm OFF}
=
90.815960813651\ {\rm GJ}.
```

Full ON inventory:

```math
E_{\rm ON}
=
127.782438518272\ {\rm GJ}.
```

Therefore:

```math
\boxed{
E_{\rm ON}
-
E_{\rm OFF}
=
36.966477704620\ {\rm GJ}
}
```

This is an inventory difference.

It was explicitly **not** claimed to equal irreversible switching work.

Likewise, the activation-field energy:

```math
6.228597769673\ {\rm GJ}
```

was **not** assumed recoverable.

---

# 24. Illustrative switching-power scales

If the full ON-OFF inventory difference had to be supplied on timescale $\Delta t$, the simple inventory-over-time scales would be:

```text
1 s:
~3.70e10 W

0.1 s:
~3.70e11 W

0.01 s:
~3.70e12 W

1 ms:
~3.70e13 W

1 us:
~3.70e16 W
```

These are not proven minimum switching powers.

They are dimensional inventory/time benchmarks.

A physical reservoir and transition path are still required.

---

# 25. D3C-R first repair — deliberately aborted

The first attempted D3C repair substantially raised the ARPACK iteration ceiling:

```text
30000
->
120000
```

and enlarged the Krylov workload.

A process check showed:

```text
~227% CPU
```

so the calculation was not frozen.

However inspection showed that the repair was doing far more work than necessary and could run for an unnecessarily long period.

A second provenance issue was also found:

the copied script still pointed its summary/CSV output to the original D3C filenames.

Thus completion could overwrite original scientific artifacts.

The run was intentionally stopped.

Correct classification:

```text
ABORTED FOR COMPUTATIONAL / PROVENANCE REASONS

PHYSICS RESULT
=
NONE
```

This is preserved because it changed subsequent numerical strategy.

---

# 26. D3C-R surgical finite-difference closeout

A narrower repair was constructed.

It:

```text
REUSED COMPLETED D3C RESULTS

RERAN ONLY INCOMPLETE RIGHTMOST SECTORS

ADDED ONE FINER l=1 POINT

PRESERVED ORIGINAL D3C ARTIFACTS

USED UNBUFFERED OUTPUT

STOPPED BROAD RECOMPUTATION
```

The sparse rightmost solver again failed to return eigenpairs in all five requested repair cases.

This established a practical numerical conclusion:

> **Global `which="LR"` ARPACK should not be brute-forced further for this non-normal companion problem.**

However the near-zero $l=1$ analysis became substantially stronger.

---

# 27. D3C-R four-grid $l=1$ convergence

The new fine point was:

```math
h
=
0.1875,
```

with apparent growth:

```math
1.56590106464\times10^{-5}.
```

The full sequence became:

```math
\begin{array}{c|c}
h & |{\rm Re}\,s|\\
\hline
0.5000 &
4.16301741077\times10^{-5}\\
0.3750 &
3.12347491176\times10^{-5}\\
0.2500 &
2.08462969337\times10^{-5}\\
0.1875 &
1.56590106464\times10^{-5}
\end{array}
```

Power-law fit:

```math
|{\rm Re}\,s|
\propto
h^{0.9969441541}.
```

Linear $h\rightarrow0$ intercept:

```math
7.1603366271\times10^{-8}.
```

Quadratic intercept:

```math
1.0587229918\times10^{-7}.
```

The common Goldstone itself scaled with power:

```math
0.9989492183.
```

Therefore the suspicious finite-difference mode and the known common translation artifact both approached zero essentially linearly with grid spacing.

The script classified:

```text
L1_CONTINUUM_ZERO_MODE_CLOSEOUT_PASS=True
```

while still withholding the global stability claim because rightmost ARPACK remained incomplete.

---

# 28. D3C-R translation residual

The analytic common-translation residual sequence was less clean:

```text
h=0.50:
1.43423e-3

h=0.375:
8.47038e-4

h=0.25:
5.87945e-4

h=0.1875:
7.42493e-4
```

The final point increased rather than decreased.

Overall residual power was approximately:

```math
0.705.
```

Therefore:

> **The eigenvalue continuum evidence is stronger than the raw analytic-vector residual evidence.**

The residual test must not be overstated.

---

# 29. D3C-R2 — independent Chebyshev dense spectrum

Because sparse ARPACK repeatedly failed, a genuinely independent spectral method was implemented.

Method:

```text
RADIAL DISCRETIZATION
=
CHEBYSHEV-LOBATTO COLLOCATION

EIGENSOLVER
=
COMPLETE DENSE SCIPY.LINALG.EIG

SPARSE ARPACK
=
NO

RECOMPUTED
=
l=0,1,2

INHERITED
=
l=3,...,8
```

The purpose was to determine whether an entirely different discretization/eigensolver found a genuine growing mode.

---

# 30. D3C-R2 initial implementation failure

The first Chebyshev run failed before computing any physics.

`numpy.block` was supplied nested tuples rather than nested lists.

Error:

```text
TypeError:
arrays is a tuple.
Only lists can be used to arrange blocks.
```

This occurred on the first:

```text
l=0
N=112
```

matrix construction.

The 94-test baseline had passed.

The background fields had reconstructed correctly.

Thus:

```text
FIRST D3C-R2 PHYSICS RESULT
=
NONE

FAILURE TYPE
=
IMPLEMENTATION ONLY
```

---

# 31. D3C-R2 structural repair

The code was repaired by changing exactly three `np.block` structural containers:

```text
K

G

COMPANION
```

from tuples to lists.

No equations, parameters, thresholds, or physics logic were changed.

The script recompiled.

The 94-test baseline again passed.

The repaired dense Chebyshev calculation then completed.

---

# 32. Independent Chebyshev $l=0$ result

Across:

```text
N=112
N=144
N=176
```

the worst physical real growth was:

```math
6.01400444250\times10^{-15}.
```

Growth spread:

```math
4.95970763712\times10^{-15}.
```

Therefore:

```text
CHEB_L0_PASS=True
```

---

# 33. Independent Chebyshev $l=2$ result

The worst physical real growth was:

```math
1.75269758087\times10^{-15}.
```

Growth spread:

```math
9.66585850370\times10^{-16}.
```

Therefore:

```text
CHEB_L2_PASS=True
```

---

# 34. Independent Chebyshev $l=1$ direct-spectrum result

Across the primary:

```text
N=112
N=144
N=176
N=208
```

sequence, the largest physical real growth was:

```math
1.34447348282\times10^{-6}.
```

The declared direct Chebyshev growth gate was:

```math
10^{-5}.
```

Therefore:

```text
CHEB_L1_PASS=True
```

Growth spread:

```math
4.22349860685\times10^{-7}.
```

A separate:

```text
RMAX=800
N=224
```

case gave:

```math
1.20108349696\times10^{-6}.
```

Difference from the fine $R=600$ calculation:

```math
1.04798959404\times10^{-7}.
```

Therefore:

```text
CHEB_DOMAIN_PASS=True
```

The maximum real growth among modes with low common-translation overlap was:

```math
1.34447348282\times10^{-6}.
```

Therefore:

```text
CHEB_LOW_OVERLAP_MODES_PASS=True
```

No independent dense Chebyshev spectrum produced a direct physical growth exceeding the declared threshold.

---

# 35. Inherited $l=3$ through $l=8$

The original D3C rightmost solves had completed in all:

```math
l=3,\ldots,8
```

sectors.

The worst physical real growth there was:

```math
1.41846058401\times10^{-11}.
```

Therefore:

```text
INHERITED_L3_TO_L8_PASS=True
```

---

# 36. Chebyshev translation overlaps

The common-translation overlap sequence was:

```text
N=112:
0.864677388

N=144:
0.990671309

N=176:
0.991790844

N=208:
0.991952678

N=224, R=800:
0.992096007
```

The declared minimum was:

```text
0.95
```

and therefore the gate formally failed because:

```text
N=112
```

was too coarse.

However every finer grid gave approximately:

```text
0.991 to 0.992.
```

Thus the scientific interpretation is:

> **The translation mode appears cleanly at adequate Chebyshev resolution, but the predeclared all-grid overlap gate formally fails because the coarsest grid does not resolve it sufficiently.**

This is not evidence by itself for physical instability.

---

# 37. Chebyshev projected-stiffness diagnostic

The R2 run attempted to reproduce the earlier D96 idea of projecting out the exact common translation direction and examining the next static stiffness.

It produced:

```text
N=112:
KPROJ0 = -0.370914

N=144:
KPROJ0 = -1.014738

N=176:
KPROJ0 = -2.265645

N=208:
KPROJ0 = -4.421052

N=224, R=800:
KPROJ0 = -3.345229
```

Therefore the script’s declared projected-stiffness gate failed.

This prevented formal GREEN.

---

# 38. Critical post-run audit of the projected-stiffness test

The negative projected-stiffness values are **not yet accepted as evidence of a physical negative mode**.

The reason is numerical/variational.

The Chebyshev collocation second-derivative operator is not generally self-adjoint in the ordinary Euclidean vector inner product.

The script constructed the projector schematically as:

```math
P
=
I-tt^T
```

and then treated:

```math
PKP
```

as though it were a symmetric physical quadratic form.

It then passed that object to:

```text
scipy.linalg.eigh
```

which assumes a Hermitian/symmetric problem.

That assumption was not independently demonstrated.

Therefore the quantity called:

```text
KPROJ0
```

may not equal the true minimum of the physical second variation orthogonal to translation.

The correct next calculation must reconstruct the actual variational inner product and use an appropriate weighted projector or generalized eigenproblem.

This is the principal unresolved numerical/mathematical issue at the end of the session.

---

# Result

The central result of this research slice is:

> **The 031 scalar-metric source now possesses an explicit microscopic activation architecture. An independent positive-Hamiltonian global-$U(1)$ Q-ball $Y$ can dynamically control both exotic-source scalarization and the ordinary-matter physical metric through the local function $f(a)=1-e^{-a^2/2}$. A fully reciprocal fixed-$Q_X,Q_Y$ microscopic ON solution of the coupled $X+\phi+Y$ field equations exists, converges strongly with domain, preserves the finite-payload outward response, and has a conservative ON inventory of approximately $127.782438518$ GJ.**

This materially advances 031 beyond the previous journal.

The source is no longer merely:

```text
MICROSCOPIC X+PHI
+
PRESCRIBED ACTIVATION
```

It is now:

```text
MICROSCOPIC X
+
MICROSCOPIC PHI
+
MICROSCOPIC Y
+
FIXED QX
+
FIXED QY
+
RECIPROCAL SOURCE REACTION
```

The current energy ledger is:

```math
E_{X+\phi}
=
121.553840748599\ {\rm GJ},
```

```math
E_Y
=
6.228597769673\ {\rm GJ},
```

```math
\boxed{
E_{\rm ON}
=
127.782438518272\ {\rm GJ}
}.
```

The corresponding same-$Q_X$ OFF source has:

```math
E_{\rm OFF}
=
90.815960813651\ {\rm GJ}.
```

Thus the conservative inventory difference is:

```math
36.966477704620\ {\rm GJ}.
```

However ON and OFF live in different $Q_Y$ sectors.

Therefore the model still lacks a complete physical switching implementation.

The stability status is also deliberately conservative.

Direct spectral evidence is strongly favorable:

```text
FINITE-DIFFERENCE NON-l1 MODES
=
PASS

FINITE-DIFFERENCE l1 H->0
=
PASS

CHEBYSHEV l0
=
PASS

CHEBYSHEV l1 DIRECT GROWTH
=
PASS

CHEBYSHEV l2
=
PASS

INHERITED l3...l8
=
PASS

CHEBYSHEV LOW-OVERLAP MODES
=
PASS

CHEBYSHEV DOMAIN CHECK
=
PASS
```

But:

```text
CHEBYSHEV ALL-GRID TRANSLATION OVERLAP
=
FORMAL FAIL FROM COARSE N112

CHEBYSHEV PROJECTED STIFFNESS
=
FORMAL FAIL

PROJECTED-STIFFNESS VARIATIONAL VALIDITY
=
UNRESOLVED
```

Therefore the final status is:

```text
TRUE_D3_INSTABILITY
=
NOT ESTABLISHED

D3_COUPLED_LINEAR_STABILITY
=
YELLOW / NOT YET CERTIFIED
```

---

# Verification

## Analytical

The D3 architecture satisfies several important structural checks analytically.

### Exact OFF decoupling

At:

```math
Y=0,
```

```math
f(0)=0,
```

therefore:

```math
A_X=A_m=1.
```

Both scalarization and ordinary-matter physical-metric activation turn off without prescribing position dependence externally.

### Near-OFF expansion

```math
f(a)
=
\frac12a^2
+
O(a^4),
```

so there is no bilinear $\phi-Y$ mixing about the exact OFF vacuum.

### Saturated ON limit

```math
a\gg1
```

implies:

```math
f(a)\rightarrow1,
```

which reproduces the previously tested scalarized source and ordinary-matter metric response.

### Charge conservation

The $Y$ activation field is a global-$U(1)$ Q-ball.

Its ON configuration therefore carries a real conserved Noether charge.

This makes explicit why ON/OFF switching cannot simply change the field amplitude arbitrarily.

### Reciprocal reaction

The full D3B $Y$ equation contains the derivative of the same source coupling used in the $X/\phi$ equations.

The activation field therefore reacts to source scalarization rather than being a one-way external control.

---

## Numerical

Every major promoted run in this slice began from the known-solution baseline:

```text
94 passed
```

D3B independently reconstructed:

```text
FIXED QX
=
PASS

FIXED QY
=
PASS

DENSE EQUATION RESIDUAL
=
8.59e-6

BVP RMS RESIDUAL
=
5.62e-6

DOMAIN ENERGY SPREAD
=
6.28e-7

FINITE-PAYLOAD OUTWARD SIGN
=
PASS
```

D3C-R showed:

```math
|s_{l=1}|
\propto
h^{0.99694}
```

with approximately zero continuum intercept.

D3C-R2 independently replaced both the radial discretization and eigensolver.

Its complete dense Chebyshev spectra found:

```text
l=0 DIRECT GROWTH
=
~6e-15

l=1 DIRECT GROWTH
=
~1.34e-6

l=2 DIRECT GROWTH
=
~1.75e-15
```

with the $l=1$ value below the declared $10^{-5}$ direct-growth threshold.

Thus the absence of a large physical growth is not dependent on one numerical method.

---

## Dimensional

The D3A-R scan explicitly translated the activation-field scales into physical energy units.

At the 10-GeV source-operator preflight:

```math
E_Y
\approx
6.23\ {\rm GJ}.
```

The source/matter normalization repair reconstructed:

```math
M_m
\approx
1.10\times10^9\ {\rm GeV},
```

and correctly reproduced the expected:

```math
(M_c/M_m)^2
```

suppression of payload reaction.

The ON/OFF energy ledger was expressed in absolute joules rather than only dimensionless coefficients.

Switching power examples were explicitly labeled as:

```text
ENERGY INVENTORY / TIME
```

rather than as established irreversible work.

---

## Limiting cases

The architecture passes the relevant formal limits:

```text
Y -> 0:
A_X -> 1
A_m -> 1

a -> infinity:
f -> 1

PHI -> 0:
OFF SCALAR FIELD EXACT

PRODUCTIVE ON CORE:
f -> 1
f' -> 0
```

The last limit explains the extremely small D3 cross-Hessian.

The matter reaction repair also recovered the exact expected coupling-scale suppression.

---

## Literature comparison

No new literature survey sufficient to establish novelty was completed in this research slice.

The source and activation solitons remain based on the known global-$U(1)$ Q-ball class.

The underlying use of scalar-tensor/conformal physical metrics and spontaneous/even scalarization is likewise not treated as a new discovery merely because this project combines them in a particular antigravity-motivated architecture.

Therefore:

```text
NOVELTY
=
NOT ESTABLISHED

NEW PHYSICS DISCOVERY
=
NO
```

Any future novelty statement requires an explicit literature search and comparison after the model survives the remaining physical gates.

---

# Falsification Attempt

This research slice repeatedly attempted to kill the activation architecture rather than optimize around failures.

## Gate-free OFF-state falsification

The simplest possible OFF architecture was tested first.

It failed because:

```text
LOW SAME-QX BRANCH
=
SCALAR TACHYON

HIGH SAME-QX BRANCH
=
SOURCE-UNSTABLE + UNBOUND
```

The project did not reinterpret these failures as optimization opportunities.

The tested route was closed.

---

## Canonical mass-gate falsification

The D2 gate was subjected to:

```text
CAPACITY SCAN

PHYSICAL RESCALING

BVP

RESCALED BVP

FULL FIXED-QX BVP

LOG GATE HOMOTOPY

MULTISEED VARIATIONAL OPTIMIZATION

FINITE-VOLUME TRUST REGION
```

and repeatedly failed realization or became geometrically excessive.

The route was strongly demoted rather than escalated indefinitely.

---

## EFT falsification of tiny D3A energy

The original ultra-low D3A activation energy was not accepted.

The source-side operator scale was raised.

This increased activation energy dramatically.

At the 100-GeV stress test:

```math
E_Y
\sim
4.6\times10^4\ {\rm GJ}.
```

Thus the model is not protected against arbitrary UV-scale demands.

The 10-GeV point remains only a preflight.

---

## Physical-metric provenance falsification

The payload reaction was independently rederived.

The earlier approximately 8.8% estimate was found to use the wrong coupling scale.

After correction:

```math
3.44\times10^{-16}
```

was obtained.

This demonstrated both that the model was more favorable than first thought and that coupling provenance must remain explicit.

---

## Microscopic reciprocal field falsification

D3B did not simply add source and activation energies.

It solved the coupled equations at fixed charges and included reciprocal source reaction.

A large backreaction would have invalidated the pre-coupling inventory.

Instead the source inventory moved only approximately:

```math
2.09\times10^{-6}
```

fractionally.

---

## Stability falsification

D3C explicitly included all five linear dynamical sectors.

The relative source/activation translation was not projected out as a symmetry.

The initial $l=1$ candidate was tracked through:

```text
GRID REFINEMENT

DOMAIN ENLARGEMENT

ANALYTIC TRANSLATION OVERLAP

FOUR-GRID CONTINUUM FIT
```

It decreased essentially linearly to zero.

Sparse rightmost ARPACK failure was not interpreted as stability evidence.

Instead an independent Chebyshev/dense method was built.

That independent method also found no direct growing mode above the declared threshold.

---

## Remaining falsification risk

The most important unresolved issue is the R2 projected-stiffness diagnostic.

The negative values could indicate a real negative second variation.

They could also be artifacts of using the wrong inner product for a non-self-adjoint collocation representation.

This distinction has not yet been resolved.

Therefore the project does **not** promote D3 stability.

---

# Claims Status

`CLAIMS.md` was not updated in this session.

The broader public/documentation state was intentionally not promoted.

Per project-owner instruction, the only session-boundary documentation being advanced is:

```text
CARRY-OVER NOTES / NOTES

THIS JOURNAL

NEXT-SESSION INTRO

CURRENT CODEBUNDLE

NEW SIMULATION FILES

NEW DATA ARTIFACTS

NEW LOGS
```

The following are intentionally **not** being updated yet:

```text
RESEARCH_BUILDPLAN.md

README.md

INTROSPECTIVE_BUILDPLAN.md

OLDER JOURNALS

OTHER BROAD PROJECT DOCUMENTATION
```

Reason:

> **The project owner wants the new 031 model completely verified before propagating its current status throughout the broader repository documentation.**

Therefore newer notes, this journal, codebundle, scripts, data, and logs supersede lagging buildplan/README content where conflicts occur.

The current defensible claim is:

> **A fully reciprocal positive-energy microscopic activated $X+\phi+Y$ ON field has been constructed at fixed source and activation charges with approximately $127.7824$ GJ conservative inventory, excellent domain convergence, and preserved finite-payload outward physical-metric response. No true coupled D3 instability has been established, but final coupled-linear certification remains withheld pending correction of the Chebyshev variational/projected-stiffness diagnostic.**

The following claims remain prohibited:

```text
FULLY STABLE ACTIVATED ANTIGRAVITY FIELD
=
NO

COMPLETE ON/OFF DEVICE
=
NO

PRACTICAL ANTIGRAVITY
=
NO

NEW PHYSICS DISCOVERY
=
NO
```

---

# Open Questions

## 1. Is the negative Chebyshev projected stiffness physical?

This is the immediate frontier.

The R2 test used a Euclidean projector and symmetric eigensolver on a Chebyshev collocation representation whose self-adjointness in that metric was not demonstrated.

The correct physical second variation must be reconstructed.

---

## 2. What is the correct radial variational inner product?

The next analysis must derive the exact quadratic energy norm for the reduced:

```text
X AMPLITUDE

X PHASE

PHI

Y AMPLITUDE

Y PHASE
```

perturbations.

The associated discrete Chebyshev quadrature weighting must then be included.

---

## 3. Does the true translation-orthogonal $l=1$ stiffness remain positive?

After constructing the correct weighted projector, only the exact common translation should be removed.

The relative displacement between:

```text
X+PHI
```

and:

```text
Y
```

must remain physical.

A convergent negative relative-translation mode would be a real D3 instability.

---

## 4. Can a physical $Q_Y$ reservoir be realized?

If stability survives, the next activation problem is not field existence.

It is charge transport.

Need:

```text
TOTAL QY CONSERVATION

RESERVOIR STRESS-ENERGY

TRANSFER FIELD

FORMATION ENERGY

RESET ENERGY

RADIATION

MOMENTUM REACTION

SWITCHING TIME

SWITCHING POWER
```

---

## 5. Can the 10-GeV source-side EFT floor survive a genuine UV/naturalness analysis?

The current 10-GeV threshold is only a declared preflight.

Radiative corrections and protective symmetry remain open.

---

## 6. Does the antigravity response survive full Einstein backreaction?

The current physical response is calculated in the scalar-metric framework without full nonlinear Einstein closure of the complete:

```text
X

PHI

Y

RESERVOIR

PAYLOAD

SUPPORT / CONTROL
```

stress-energy.

Ordinary attractive gravity from the approximately 128-GJ inventory must be included explicitly.

---

## 7. Nonlinear stability

Coupled-linear stability is not enough.

Still required:

```text
FINITE-AMPLITUDE PERTURBATIONS

FISSION

FRAGMENTATION

RELATIVE SOLITON DISPLACEMENT

CHARGE REDISTRIBUTION

RADIATIVE LOSS

LONG-TIME EVOLUTION
```

---

## 8. Empirical constraints

Still open:

```text
FIFTH-FORCE LIMITS

EQUIVALENCE PRINCIPLE

PPN

FAR-FIELD SCALAR LEAKAGE

LABORATORY CONSTRAINTS

ASTROPHYSICAL CONSTRAINTS

COSMOLOGICAL CONSISTENCY WHERE APPLICABLE
```

---

## 9. Practicality

Even if every theoretical gate survives:

```math
E_{\rm ON}
\approx
1.28\times10^{11}\ {\rm J}
```

remains an enormous stored-energy inventory.

Practicality therefore remains a separate accomplishment from theoretical possibility.

---

# AI Assistance

AI assistant used: **ChatGPT by OpenAI**

Substantial AI-assisted work in this research slice included:

```text
031D1 OFF-STATE OPERATOR ANALYSIS

FIXED-CHARGE BRANCH INTERPRETATION

D1 HIGH-BRANCH FALSIFICATION DESIGN

D2 AUXILIARY MASS-GATE MODEL DESIGN

D2 PARAMETER / MASS-SCALE ACCOUNTING

BVP AND VARIATIONAL RUN DESIGN

D2 STOP-RULE INTERPRETATION

D3 AUXILIARY GLOBAL-U1 ACTIVATION CONSTRUCTION

DERIVATION OF f(a)=1-exp(-a^2/2) ACTIVATION LOGIC

SOURCE / ORDINARY-MATTER COUPLING SEPARATION

EFT PREFLIGHT DESIGN

PAYLOAD-REACTION PROVENANCE AUDIT

FIXED-QX,QY COUPLED D3B EQUATIONS

HOMOTOPY DESIGN

DOMAIN-CONVERGENCE DESIGN

CONSERVATIVE ENERGY LEDGER

COUPLED FIVE-FIELD STABILITY MODEL

TRANSLATION-GOLDSTONE ANALYSIS

ARPACK FAILURE DIAGNOSIS

SURGICAL NUMERICAL REPAIR DESIGN

INDEPENDENT CHEBYSHEV SPECTRAL METHOD DESIGN

NUMPY np.block IMPLEMENTATION BUG DIAGNOSIS

POST-RUN PROJECTED-STIFFNESS VALIDITY AUDIT

SESSION CLAIM AUDIT

CARRY-OVER DOCUMENTATION

RESEARCH-JOURNAL PREPARATION
```

AI-generated mathematics, code, numerical strategies, and interpretations were not assumed correct because they were AI-generated.

The session repeatedly subjected AI-assisted outputs to:

```text
94-TEST REGRESSION BASELINE

INDEPENDENT FIELD RECONSTRUCTION

DOMAIN CONVERGENCE

GRID CONVERGENCE

ALTERNATE DISCRETIZATION

ALTERNATE EIGENSOLVER

DIMENSIONAL ACCOUNTING

LIMITING-CASE CHECKS

PROVENANCE AUDITS

FAIL-CLOSED CLAIM GATES
```

Use of ChatGPT does not imply endorsement or sponsorship by OpenAI.

---

# Next Action

The highest-value first task of the next research session is:

```text
031D3C-R3
WEIGHTED VARIATIONAL l=1 TRANSLATION / SECOND-VARIATION CLOSEOUT
```

Do **not** begin with activation-reservoir optimization.

First determine whether the current D3 microscopic ON field is actually coupled-linearly stable.

The next calculation should:

1. derive the continuous quadratic second variation of the $X+\phi+Y$ system;

2. identify the correct radial canonical inner product;

3. discretize that quadratic form using Chebyshev/Clenshaw-Curtis or another explicitly weighted variational method;

4. verify the discrete Hessian is self-adjoint in the declared weighted metric;

5. construct the analytic common $l=1$ translation mode;

6. measure its weighted residual and overlap under increasing resolution/domain;

7. project only that exact translation using the correct weighted projector;

8. leave relative $X+\phi$ versus $Y$ translation untouched;

9. compute the next weighted Rayleigh/eigenvalue minimum;

10. independently compare the result with the already favorable finite-difference continuum spectrum.

Decision rule:

```text
IF NEXT l1 WEIGHTED EIGENVALUE
IS ROBUSTLY POSITIVE:

    PROMOTE D3 COUPLED-LINEAR STABILITY TO GREEN

IF NEXT l1 WEIGHTED EIGENVALUE
IS ROBUSTLY NEGATIVE:

    D3 ACTIVATION HAS A REAL INSTABILITY
    DEMOTE OR REPAIR

IF IT CONVERGES TO ZERO:

    IDENTIFY THE ADDITIONAL ZERO/SYMMETRY/DISCRETIZATION STRUCTURE
    BEFORE CLAIMING STABILITY
```

Only after this gate becomes GREEN should the project advance to:

```text
031D3D
EXPLICIT QY RESERVOIR / TRANSFER / RESET / SWITCHING RADIATION
```

followed by:

```text
031E
FULL PHYSICAL METRIC / EINSTEIN BACKREACTION
```

and finally:

```text
031F
NONLINEAR STABILITY
+
EFT / RADIATIVE NATURALNESS
+
EMPIRICAL CLOSURE
```

The buildplan, README, and other broad repository documentation should remain intentionally unchanged until this new model has passed the complete verification sequence.

---

# Final Journal Classification

The durable scientific state at the end of 2026-09-04 is:

```text
006D_STRONGEST_FULLY_CONSERVATIVE_TRUE_STANDOFF_ARCHITECTURE
=
YES

031_SCALAR_METRIC_MICROSCOPIC_SOURCE
=
REALIZED

031_ROBUST_SOURCE_INVENTORY
=
~121.55 GJ

031D1_GATE_FREE_SAME_Q_OFFSTATE
=
CLOSED_IN_TESTED FORM

031D2_CANONICAL_MEDIATOR_MASS_GATE
=
STRONGLY DEMOTED

031D3_GLOBAL_U1_ACTIVATION_FIELD
=
REALIZED

031D3A_R2_SOURCE_MATTER_NORMALIZATION
=
GREEN

031D3B_FULL_FIXED_QX_QY_MICROSCOPIC_ON_FIELD
=
GREEN

031D3B_FULL_CONSERVATIVE_ON_INVENTORY
=
127.782438518272 GJ

031D3B_FINITE_PAYLOAD_OUTWARD_RESPONSE
=
PASS

031D3B_DOMAIN_CONVERGENCE
=
PASS

031D3C_ACTIVATION_QBALL_BRANCH_SLOPE
=
PASS

031D3_NEW_CROSS_HESSIAN
=
~1.62e-18

031D3C_FINITE_DIFFERENCE_l1_CONTINUUM
=
PASS

031D3CR2_CHEBYSHEV_l0_DIRECT_SPECTRUM
=
PASS

031D3CR2_CHEBYSHEV_l1_DIRECT_SPECTRUM
=
PASS

031D3CR2_CHEBYSHEV_l2_DIRECT_SPECTRUM
=
PASS

031D3C_INHERITED_l3_TO_l8
=
PASS

TRUE_D3_INSTABILITY
=
NOT ESTABLISHED

D3_COUPLED_LINEAR_STABILITY_CERTIFICATION
=
YELLOW / UNRESOLVED

REASON
=
WEIGHTED VARIATIONAL TRANSLATION / PROJECTED-STIFFNESS GATE NOT CLOSED

EXPLICIT_QY_RESERVOIR
=
NOT YET

SWITCHING / RESET / RADIATION
=
NOT YET

FULL EINSTEIN BACKREACTION
=
NOT YET

NONLINEAR STABILITY
=
NOT YET

RADIATIVE NATURALNESS
=
NOT YET

EMPIRICAL CLOSURE
=
NOT YET

PRACTICAL ANTIGRAVITY DEVICE
=
NO

NEW PHYSICS DISCOVERY
=
NO

NOVELTY
=
NOT ESTABLISHED

ACTIVE_FRONTIER
=
031D3C-R3 WEIGHTED VARIATIONAL l1 STABILITY CLOSEOUT
```

The principal durable conclusion of this slice is:

> **The project has progressed from a microscopic scalar-metric antigravity source to a fully reciprocal microscopic activated ON field containing two independently conserved global-$U(1)$ solitons and the scalar mediator, with approximately $127.8$ GJ conservative energy, negligible new source/activation mixing in the saturated operating core, and preserved finite-payload outward response. Repeated direct spectral tests have not established a physical instability. Final linear-stability promotion is nevertheless deliberately withheld until the independent Chebyshev projected-stiffness diagnostic is rebuilt in the correct weighted variational inner product.**

### END JOURNAL — 2026-09-04 — 031D ACTIVATION, MICROSCOPIC COUPLING, AND STABILITY FRONTIER
