# Research Journal — 2026-09-14

## 032V26E1B2 and 032H17A10A–032H17A11A — DHOST Naturalness Closeout, Spin-Engineered Dirac Source Escape, Protected Massive \(1^-\) Pole, Quadratic Universal Metric, Finite \(1g/1m\) Payload Witness, Microscopic Naturalness Block, and Exact-Current Protection Triage

## Objective

The primary scientific question investigated in this research slice was:

> **Can the surviving HOOK17 mechanism be promoted from the A9R3 same-action partial into a protected source-to-healthy-mode-to-universal-metric chain that produces true finite-payload outward response at the project’s \(1g/1m\) benchmark without catastrophic field energy, and if the first microscopic realization fails, can the exact reason for failure be isolated tightly enough to identify the shortest genuinely new protected successor?**

A secondary question concerned the independent V26D/DHOST fallback:

> **Can the V26E1B1 quadratic active-state completion survive technical-naturalness, scalar-health, frame-invariant physical-\(g_{00}\), and outward-sign gates strongly enough to remain a live alternative to HOOK17?**

The permanent project standards remained:

```text
MIN_OUTWARD_ACCELERATION_M_S2=
9.80665

MIN_TRUE_EXTERNAL_STANDOFF_M=
1.0

FINITE_NEUTRAL_PAYLOAD_REQUIRED=
YES

ONE_UNIVERSAL_PHYSICAL_METRIC_REQUIRED=
YES

STRICT_COMPLETE_OPERATING_ENERGY=
LESS_THAN_10_MJ

EXACTLY_10_MJ=
FAIL

PARTIAL_OR_CAPACITY_ENERGY_MAY_PROMOTE_MODEL=
NO
```

The guiding instruction remained:

> **Do not chase 17 J. Chase a physically consistent same-action theory that preserves why 17 J was possible.**

The approximately \(17.0676\,\mathrm J\) historical HOOK17 quantity therefore remained only a canonical field-capacity reference.

It was never treated as total operating energy or as a certified \(1g/1m\) device energy.

---

## Starting State

The preceding durable journal ended with:

```text
032H17A9R3
+
032V26E1B1
```

and a full regression of:

```text
910 passed
```

### Preserved conservative GR anchor

006D remained the strongest conservative true-stand-off gravitational anchor.

Its best tested finite coefficient remained:

```math
C_{006D}
=
23.591586299249
```

with:

```text
FINITE_RADIUS=
YES

FINITE_THICKNESS=
YES

POSITIVE_ENERGY=
YES

LOCAL_CONSERVATION_LINEARIZED_ORDER=
ESTABLISHED

NEC=
PASS

WEC=
PASS

DEC=
PASS

OUTWARD_NEAR_GRAVITY=
YES

POSITIVE_FAR_FIELD_ACTIVE_MASS=
YES
```

At \(1g\) and \(1\,\mathrm m\), however, 006D requires approximately:

```text
M_EQUIVALENT≈3.47e12 kg

E_EQUIVALENT≈3.12e29 J
```

Thus 006D remained the conservative physics anchor but not an energetically practical route.

No result in the present research slice invalidated 006D.

---

### HOOK17 state entering this slice

HOOK17 combined:

```text
V24
INTRINSIC DIRAC HYPERMOMENTUM SOURCE

+

V26B1
NONLINEAR QUADRATIC HOOK -> METRIC NUMERATOR

+

V26B1
ACTIVE / OFF-STATE SEPARATION

+

V26B1R1
LOW CANONICAL FIELD-CAPACITY STRUCTURE
```

The historical reference remained:

```text
HOOK17_REFERENCE_CANONICAL_FIELD_CAPACITY_RP1E12_J=
17.0676442196
```

with mandatory qualification:

```text
THIS_IS_COMPLETE_ENERGY=
NO

THIS_IS_1G_1M_CERTIFIED=
NO

THIS_IS_A_DEVICE=
NO
```

The A6–A9R3 rescue chain had already established:

```text
A6 / A6R1
TESTED J11 / MINIMAL VECTOR /
MASSLESS STUECKELBERG /
SINGLE-COMPENSATOR SHORTCUTS CLOSED

A6R2
TESTED CLEAN HISTORICAL V24 MASSIVE 1− SUPPORT ZERO

A7
TESTED DIRECT ISO-WEYL ROUTE CLOSED

A8
NO ACCEPTABLE TESTED PROTECTED 2+ SURVIVOR

A9
EXACT HEALTHY PROJECTIVE 1+ POLE OVERLAP

A9R2
ACTION-LEVEL PROJECTIVE DIRAC COMPLETION

A9R3
COVARIANT PROJECTOR
+
SAME-ACTION CLEAN LINEARIZED METRIC STRESS
+
LINEARIZED DIFFEOMORPHISM WARD CONSISTENCY
```

The Percacci-Sezgin \(1^+\) route was blocked on technical naturalness/protection rather than source overlap.

HOOK17 itself remained open.

---

### V26D / E1B1 state entering this slice

The independent protected \(c_T=1\) DHOST/KMM fallback had established:

```text
V26E0
STATIC-SPACELIKE TENSOR HEALTH
MARGIN=146/147

V26E1A
EXACT NONSINGULAR CLASS-Ia EINSTEIN-FRAME MAP

V26E1B0
LINEAR COMPLETION REDUCES TO CLOSED PURE-j0 V17/V19 CLASS

V26E1B1
QUADRATIC ACTIVE-STATE COMPLETION
A(x)=1+(100/41)x^2

ACTIVE_BETA1=
1/21

OFFSTATE_BETA1=
0

MAP_MARGIN=
40/41

TENSOR_MARGIN=
146/147

TREE_R5_TWO_SCALAR_MATTER_VERTEX=
ABSENT
```

E1B1 was promising but not yet a model.

The immediate executable gate was E1B2.

---

## Work Performed

# 1. 032V26E1B2 — DHOST quadratic active-state naturalness and physical-response gate

E1B2 tested whether the E1B1 quadratic completion could survive the gates that had been deliberately postponed:

```text
SCALAR HEALTH

FRAME-INVARIANT PHYSICAL g00

OUTWARD SIGN

GENERATED LOWER-ORDER xT OPERATOR

UV / NATURALNESS

FINITE-PAYLOAD SIGN
```

### 1.1 Exact frame map

For the conformal transformation

```math
\tilde g_{\mu\nu}
=
A(X)g_{\mu\nu}
```

with

```math
A
=
F/F_0
```

the transformed scalar kinetic invariant obeys

```math
\tilde X
=
\frac{X}{A}
```

and the Jacobian is controlled by

```math
D
=
A-XA_X.
```

The exact differential identity is

```math
\frac{d\tilde X}{dX}
=
\frac{D}{A^2}.
```

The previously established nonsingular map remained intact.

---

### 1.2 Scalar-health witness

A canonical Einstein-frame scalar witness

```math
\tilde P
=
-\frac Z2\tilde X
```

maps to a Jordan-frame expression of the form

```math
P
=
-\frac Z2 A(X)X.
```

The tested \(Q(X)\Box\phi\) structure gives no bulk quadratic kinetic term on the flat constant-gradient witness background.

Thus no immediate local scalar ghost was forced by this particular witness.

This was only a local scalar-health preflight.

It was not a complete nonlinear constraint analysis.

---

### 1.3 Physical metric response

The physical acceleration from the conformal metric can be written schematically as

```math
\mathbf a
=
\frac{c^2}{2}
\nabla\ln A.
```

The frame-invariant scalar-to-metric response coefficient contains

```math
\frac{AA_X}{D}.
```

At the quadratic off state \(x=0\), the first derivative vanishes.

Therefore the desired active/off-state structure survives at tree level.

---

### 1.4 Outward-sign mirror

The originally positive branch produced the wrong sign for the desired physical response.

The negative mirror was therefore tested.

The representative mirror used:

```text
BETA_STAR=
-1/21

ETA=
-100/43
```

with representative active point:

```text
x_star=
0.1
```

giving:

```text
A_star=
42/43

D_star=
44/43

TENSOR_MARGIN=
146/147
```

The physical \(g_{00}\) response had the outward sign.

A finite-payload sign scout using the previously declared morphology gave approximately:

```text
SOURCE_RADIUS_A_M=
0.2574008856

SOURCE_SCALE_C_M=
0.0965912100

PAYLOAD_RADIUS_M=
0.1

PAYLOAD_CENTER_Z_M=
1.1

Q≈
0.0302010966

Q_SQUARED≈
9.12106e-4

PAYLOAD_SURFACE_ACCELERATION_MIN_M_S2=
9.80665

PAYLOAD_SURFACE_ACCELERATION_MAX_M_S2≈
80.3628

PAYLOAD_MEAN_ACCELERATION_M_S2≈
31.5523

PAYLOAD_CENTER_ACCELERATION_M_S2≈
26.9463
```

This was a sign/scale scout only.

It did not certify the branch.

---

### 1.5 Fatal naturalness/UV obstruction

The decisive result was that both

```text
xT
```

and

```text
x^2 T
```

are symmetry allowed in the tested completion.

The historical empirical/source scales implied that maintaining the missing lower-order \(xT\) operator at the required level would demand approximately:

```text
GENERATED_xT_SUPPRESSION_REQUIRED≈
9.005e-12

LAMBDA_UV_OVER_M<
0.00413085

LAMBDA_UV<
0.88344 eV

SOURCE_HARD_SCALE_NF14≈
81.9965 eV
```

Thus the source hard scale lay far above the allowed cutoff.

There was no UV-overlap window in the current unprotected single-scalar realization.

### E1B2 result

```text
CURRENT_UNPROTECTED_E1B1_SINGLE_SCALAR_REALIZATION=
FAIL_TECHNICAL_NATURALNESS_UV

LOCAL_SCALAR_HEALTH_WITNESS=
PRESERVED

FRAME_INVARIANT_PHYSICAL_G00_RESPONSE=
PRESERVED

OUTWARD_MIRROR_SIGN=
PRESERVED

DHOST_FAMILY_GLOBALLY_CLOSED=
NO

CURRENT_IMPLEMENTATION_PROMOTED_TO_MODEL=
NO
```

The project therefore returned to HOOK17 from the A9R3 failure memory, as previously required.

---

# 2. 032H17A10A — spin-engineered Dirac source-state atlas

A6R2 had closed the **historical clean rest pair** as a source for the protected massive \(1^-\) route.

It had not proved that all Dirac source states had zero support.

A10A therefore changed the **source state**, not the carrier coefficients.

The rest basis was:

```text
U1=e0

U2=e1

V1=e2

V2=e3
```

and the four particle/antiparticle pairings were tested.

### 2.1 Full Weyl/dilation channel remained closed

For every tested pair:

```text
FULL_WEYL_DILATION_TRACE=
0
```

Therefore the old direct Weyl route did not reopen.

---

### 2.2 New totally symmetric trace-vector escape

Two spin-engineered pairs produced nonzero trace-vector support:

```text
U1_V1

U2_V2
```

with trace magnitude:

```math
|t|
=
\frac83
```

and direct pure-trace carrier overlap magnitude:

```text
8
```

The two engineered states carried opposite trace signs.

For the historical clean pair \(U1,V2\):

```text
TRACE_VECTOR=
0
```

The fourth rest pair \(U2,V1\) had zero full Wheeler source.

The exact direct contraction identity for a totally symmetric source and pure-trace carrier was independently verified.

### 2.3 Representation content

For the two engineered nonzero states, the spatial totally symmetric tensor contained nonzero negative-parity spin-one representation support.

Representative values included:

```text
SPATIAL_TRACE_VECTOR_MAGNITUDE=
8/3

SPATIAL_SPIN1_TENSOR_NORM≈
2.06559111798

SPATIAL_SPIN3_STF_NORM≈
4.13118223595
```

Therefore the clean-state A6R2 result could not be generalized to all spin-engineered rest states.

### A10A decision

```text
DECISION=
GREEN_A10A_REST_BASIS_TRACE_VECTOR_ESCAPE_SOURCE_LEVEL_ONLY

SPIN_ENGINEERED_REST_PAIR_TRACE_ESCAPE=
True

ENGINEERED_NONZERO_PAIR_IDS=
U1_V1,U2_V2

ENGINEERED_TRACE_MAGNITUDE=
2.6666666666666665

ENGINEERED_MAX_DIRECT_TRACE_CARRIER_OVERLAP=
8.0

ALL_FULL_WEYL_DILATION_TRACES_ZERO=
True

EXACT_PROTECTED_CARRIER_WARD_EVALUATED=
False

ENERGY_OPTIMIZATION_AUTHORIZED=
False
```

Regression:

```text
918 passed
```

This was genuine source-level progress, not yet a protected-carrier success.

---

# 3. 032H17A10B — BMS Ward closeout and Marzo \(1^-\) representation reopen

A10B tested two distinct vector-carrier possibilities.

## 3.1 Exact BMS totally symmetric source Ward identity

For the protected totally symmetric rank-three gauge carrier, the linearized transformation implies the direct-source Ward identity:

```math
3\partial_aJ^{abc}
-
\partial^{(b}t^{c)}
=
0
```

with

```math
t^a
=
\eta_{bc}J^{abc}.
```

In Fourier space:

```math
W^{bc}
=
3q_aJ^{abc}
-
q^{(b}t^{c)}
=
0.
```

This is stronger than merely requiring:

```math
q_at^a=0.
```

---

## 3.2 Fixed engineered states fail the exact BMS Ward gate

The \(U1,V1\) and \(U2,V2\) engineered trace states failed the exact BMS Ward identity at representative generic momenta.

---

## 3.3 Full rest-density source-space theorem

The complete block-diagonal zero-momentum Dirac rest-density space was represented as eight real dimensions:

```text
PARTICLE 2x2 HERMITIAN BLOCK:
P0
P1
PR
PI

ANTIPARTICLE 2x2 HERMITIAN BLOCK:
A2
A3
AR
AI
```

The exact symbolic BMS Ward matrix had:

```text
GENERIC_RANK=
6

GENERIC_NULLITY=
2
```

Crucially, the BMS trace map annihilated the entire generic Ward nullspace.

Thus:

```text
GENERIC_TRACE_ESCAPE_EXISTS=
False
```

Representative null and static momentum strata were also tested.

The trace remained zero on the Ward-compatible rest-density subspaces.

For a compact source, analyticity of the Fourier transform prevents lower-dimensional exceptional momentum strata from rescuing a nonzero generic trace charge.

Therefore the **direct identification** of the rest-density Wheeler totally symmetric response as a BMS current was scoped closed.

This was not a theorem against all constitutive maps or all BMS matter completions.

---

## 3.4 Marzo protected massive \(1^-\) representation reopens

The separate Marzo protected metric-affine family contains a published healthy massive \(1^-\) pole.

The A10A engineered states contain nonzero negative-parity totally symmetric spin-one representation content.

Therefore the historical A6R2 zero-support result for the clean source did not apply to the engineered source state.

However:

```text
REPRESENTATION_SUPPORT
!=
EXACT_HEALTHY_POLE_OVERLAP
```

The exact projector still had to be reconstructed.

### A10B decision

```text
DECISION=
RED_SCOPED_A10B_DIRECT_BMS_REST_DENSITY_TRACE_ROUTE_CLOSED__
GREEN_MARZO2022_ENGINEERED_1MINUS_REPRESENTATION_REOPENED

BMS_GENERIC_WARD_RANK=
6

BMS_GENERIC_WARD_NULLITY=
2

BMS_GENERIC_TRACE_ESCAPE_EXISTS=
False

BMS_GLOBAL_FAMILY_CLOSED=
False

MARZO2022_ENGINEERED_1MINUS_REPRESENTATION_REOPENED=
True

MARZO2022_EXACT_POLE_OVERLAP_ESTABLISHED=
False
```

Regression:

```text
927 passed
```

---

# 4. 032H17A10C — same-action Stückelberg/Noether source completion

A10C addressed the central provenance issue left by A10B:

> **The Wheeler geometric response tensor could not simply be declared to be the variational current of the protected Marzo action.**

The actual source transformation law and compensator had to be reconstructed.

The Marzo Abelian symmetry acts schematically as:

```math
\delta A^\rho{}_{\mu\nu}
\propto
g_{\mu\nu}\partial^\rho\Omega.
```

The massive extension introduces a Stückelberg scalar transforming so that an invariant connection/distortion combination can be formed.

A10C reconstructed the direct pure-connection source Ward test and found:

```text
ENGINEERED_DIRECT_PURE_CONNECTION_WARD=
FAIL_GENERIC
```

for both engineered source states.

This was not treated as a fatal source failure because the massive protected action contains the compensating scalar sector.

Using the invariant combination schematically:

```math
B
=
N
-
g\frac{\nabla\phi}{f}
```

a same-action Stückelberg/Noether source completion was built.

The completed source identity passed.

Thus:

```text
BARE_CONNECTION_CURRENT_EXACTLY_CONSERVED=
NO

STUECKELBERG_COMPLETION_REQUIRED=
YES

STUECKELBERG_COMPLETED_WARD=
PASS
```

A10C also retained the engineered negative-parity source support and prepared the exact action-level \(1^-\) projector calculation.

The key scientific lesson was:

> **The source can couple consistently to the protected massive carrier, but only through the full Noether/Stückelberg completion. The bare connection current alone is not the protected physical source.**

---

# 5. 032H17A10D — independently reconstructed healthy massive \(1^-\) pole

A10D tested the central healthy-mode question directly from the action rather than by representation labels.

The linearized \(1^-\) operator was independently reconstructed and saturated with the engineered source.

At the anchor point, the published pole was reproduced at:

```math
m^2
=
1
```

in the dimensionless benchmark.

The pole was unique in the tested sector.

The derivative of the pole denominator had the healthy positive sign.

The engineered source had nonzero positive saturated residue.

### Initial response-normalized result

Using the Wheeler response normalization then present in the source pipeline:

```text
SOURCE_SATURATED_POLE_RESIDUE=
16

CANONICAL_POLE_COUPLING_MAGNITUDE=
4
```

for both engineered sign-related source states.

A second healthy parameter point with nonzero \(d_2\) retained nonzero positive overlap.

Representative residue:

```text
~68.15
```

This established that the overlap was not an isolated numerical accident at a single anchor point.

### A10D result

For the first time in the HOOK17 rescue chain, one candidate simultaneously had:

```text
EXPLICIT_INTRINSIC_DIRAC_SOURCE=
YES

PROTECTED_MASSIVE_CARRIER=
YES

INDEPENDENT_ACTION_RECONSTRUCTION=
YES

HEALTHY_PHYSICAL_POLE=
YES

NONZERO_SOURCE_SATURATION=
YES

POSITIVE_RESIDUE=
YES

ROBUST_SECOND_HEALTHY_POINT=
YES
```

This was a major promotion.

The later A10F1 normalization audit corrected the **absolute magnitude** of the source residue but did not remove the nonzero overlap.

---

# 6. 032H17A10E — protected quadratic universal-metric response

A10E asked whether the same protected healthy carrier could generate a universal metric without recreating the A9R3/E1B2 naturalness problem.

## 6.1 Exact vector factorization of the healthy pole

At the exact A10D anchor, the healthy pole tensor factorized as:

```math
\mathcal B_{abc}
=
-\sqrt{\frac32}\eta_{ac}V_b.
```

Thus the propagating rank-three pole contains an exact vector representative.

This materially simplified the universal-metric problem.

---

## 6.2 Quadratic metric

A universal physical metric was constructed schematically as:

```math
g^{\rm phys}_{\mu\nu}
=
e^{2\sigma}g_{\mu\nu}
```

with:

```math
\sigma
=
\lambda V_\rho V^\rho.
```

At the off state:

```math
V_\mu=0
```

the first variation of the metric with respect to \(V\) vanishes.

At nonzero \(V\), the quadratic response is nonzero.

This preserved the desired active/off-state structure.

---

## 6.3 Linear physical-metric protection

A zero-derivative symmetric rank-two tensor linear in a single rank-three distortion is forbidden by Lorentz-index parity using only invariant tensors.

After reduction to the transverse physical vector pole, the generic local source-free linear symmetric metric response has the schematic form:

```math
A(q^2)q_{(\mu}V_{\nu)}
+
B(q^2)\eta_{\mu\nu}(q\cdot V)
+
C(q^2)q_\mu q_\nu(q\cdot V).
```

For the transverse physical mode:

```math
q\cdot V=0.
```

The surviving:

```math
q_{(\mu}V_{\nu)}
```

piece is a pure linearized field redefinition/diffeomorphism.

Its linearized Riemann tensor vanishes.

Thus the tested local analytic source-free sector had no nonredundant physical metric response at first order.

This was an important difference from E1B1, where a lower-order physical \(xT\) operator was symmetry allowed.

---

## 6.4 Static outward witness

Use a source-free transverse profile:

```math
V_x(z)
=
V_0e^{-mz}.
```

Then:

```math
\sigma
\propto
e^{-2mz}.
```

The physical acceleration is:

```math
a_z
=
-c^2\partial_z\sigma
=
2c^2m\sigma.
```

For positive \(\lambda\):

```text
OUTWARD_SIGN=
YES
```

At:

```text
RANGE=
1 m
```

the carrier mass is:

```text
m_V≈
1.973269804e-7 eV
```

and the metric amplitude required at the payload for \(1g\) is only approximately:

```text
SIGMA_AT_1M_FOR_1G≈
5.46e-17
```

The corresponding linearized tidal curvature is nonzero.

Therefore the quadratic response is not a pure coordinate artifact.

### A10E decision

```text
GREEN_A10E_PROTECTED_QUADRATIC_UNIVERSAL_METRIC_STATIC_OUTWARD_RESPONSE_PREFLIGHT

ONE_UNIVERSAL_PAYLOAD_METRIC=
True

SOURCEFREE_NONREDUNDANT_LINEAR_METRIC_RESPONSE=
False

QUADRATIC_ACTIVE_RESPONSE=
True

OUTWARD_STATIC_G00_RESPONSE=
True

INVARIANT_TIDAL_RESPONSE=
True

FINITE_PAYLOAD_ESTABLISHED=
False

COMPLETE_ENERGY_ESTABLISHED=
False
```

Regression:

```text
960 passed
```

---

# 7. 032H17A10F0 — absolute normalization and payload-loading preflight

A10F0 restored physical scales before spending computation on a finite BVP.

The selected protected subfamily used:

```text
a4=a5=a6=d2=0.
```

The exact massive pole simplifies to:

```math
m_{1^-}^2
=
\frac{d_1^2}{c_7}.
```

The pole mass is therefore independent of the Einstein-Hilbert normalization \(a_0\).

This removed a potentially fatal fear that obtaining a \(1\,\mathrm m\) range would force an enormous \(c_7\) after restoring the Planck normalization.

With:

```text
a0=
-Mbar_Pl^2

c7=
1
```

one can take:

```text
d1≈
1.973269804e-7 eV
```

and retain the one-metre carrier.

The published healthy branch-II inequalities remained satisfied.

Therefore:

```text
PLANCK_NORMALIZATION_KILLS_ONE_METRE_CARRIER=
False

HUGE_C7_REQUIRED=
False
```

---

## 7.1 Payload matter loading

For:

```math
g_{\rm phys}
=
e^{2\lambda V^2}g
```

ordinary nonrelativistic matter contributes a quadratic loading of the carrier.

The homogeneous scout used:

```math
m_{\rm inside}^2
=
m^2
+
2\lambda\rho.
```

At the empirical portal ceiling, a \(1\,\mathrm{kg}\), \(0.1\,\mathrm m\)-radius payload had approximately:

```text
m_inside * R≈
386
```

indicating severe self-screening/loading.

Thus the empirical coupling ceiling was not the practical operating point.

---

## 7.2 Load-controlled corridor

Requiring:

```text
m_inside * R=
0.2
```

gave approximately:

```text
lambda≈
5.676e-32 eV^-2
```

well below the empirical upper bound.

The required canonical field amplitude rose into the tens-of-MeV range.

However, the optimistic existing scalar-Yukawa spherical capacity comparator remained approximately:

```text
PARTIAL_CAPACITY≈
4.89e5 J
```

or:

```text
0.489 MJ.
```

Thus payload loading did not automatically push even the optimistic canonical capacity above \(10\,\mathrm{MJ}\).

This result was explicitly classified as:

```text
CORRIDOR_ONLY

NOT_EXACT_VECTOR_SOURCE

NOT_FINITE_PAYLOAD_CERTIFICATION

NOT_COMPLETE_ENERGY
```

Regression:

```text
971 passed
```

---

# 8. 032H17A10F1 — Wheeler source-normalization repair and finite transverse-vector payload BVP

A10F1 was the strongest classical/reduced-EFT result of the slice.

It combined two tasks:

```text
ABSOLUTE SOURCE NORMALIZATION REPAIR

+

FINITE TRANSVERSE VECTOR SOURCE / FINITE PAYLOAD BVP
```

---

## 8.1 Wheeler response is not the bare connection current

The earlier A10D source saturation used Wheeler's trace-altered nonmetricity response.

That object is proportional to the matter variation but is not numerically identical to the canonically normalized symmetric connection current.

After canonical fermion normalization and the relation between nonmetricity and the symmetric connection variation, the physical connection-current magnitude corresponding to the repository's unit Wheeler response is half the response magnitude, up to an overall convention sign.

Therefore the A10D absolute normalization was corrected:

```text
A10D_RESPONSE_NORMALIZED_RESIDUE=
16

A10D_RESPONSE_NORMALIZED_COUPLING=
4

CORRECTED_CONNECTION_CURRENT_RESIDUE=
4

CORRECTED_CANONICAL_PAIR_POLE_COUPLING=
2
```

The important fact remained:

```text
NONZERO_HEALTHY_POLE_OVERLAP=
PRESERVED
```

The normalization repair reduced the source strength by a factor of two in amplitude.

It did not kill the mechanism.

---

## 8.2 Exact reduced transverse-vector BVP

The physical \(1^-\) pole was represented by an azimuthal vector component \(V_\phi\).

For an axisymmetric transverse source, the static equation was:

```math
\left[
-\partial_\rho^2
-\frac1\rho\partial_\rho
-\partial_z^2
+\frac1{\rho^2}
+m_{\rm eff}^2
\right]
V_\phi
=
J_\phi.
```

Using:

```math
u
=
\sqrt{\rho}V_\phi
```

produces the symmetric numerical operator:

```math
\left[
-\partial_\rho^2
-\partial_z^2
+\frac{3}{4\rho^2}
+m_{\rm eff}^2
\right]u
=
\sqrt\rho J_\phi.
```

The compact finite source used:

```math
J_\phi
=
J_0
\frac{\rho}{R_s}
\left(
1-\frac{r^2}{R_s^2}
\right)^2
```

inside:

```text
R_s=
2 m
```

and zero outside.

Because the source is purely azimuthal and axisymmetric:

```text
DIV_J=
0
```

identically.

The resulting field is also transverse by construction.

---

## 8.3 Finite neutral toroidal payload

The primary payload was:

```text
MASS=
1 kg

SHAPE=
TORUS

MAJOR_RADIUS=
1.0 m

MINOR_RADIUS=
0.2 m
```

Its location was chosen so that the minimum geometric gap between the finite source sphere and the payload surface was exactly:

```text
1.0 m
```

The reduced BVP was scaled to give:

```text
PAYLOAD_CENTER_OF_MASS_OUTWARD_ACCELERATION=
9.80665 m/s^2
```

The field/loading energy was approximately:

```text
2.15e5 J
```

or:

```text
0.215 MJ.
```

An independent source-work reconstruction agreed with the canonical field/loading energy.

Grid and domain convergence passed.

---

## 8.4 Microscopic source-number diagnostic

Using the corrected canonical pair coupling:

```text
g_pair=
2
```

the required source population was approximately:

```text
SOURCE_PAIR_COUNT≈
1.78e16
```

The optimistic rest-energy comparators were approximately:

```text
ELECTRON_POSITRON_REST_FLOOR≈
2.9e3 J

PROTON_ANTIPROTON_REST_COMPARATOR≈
5.35e6 J

FIELD_PLUS_ELECTRON_REST≈
2.18e5 J

FIELD_PLUS_PROTON_REST≈
5.56e6 J
```

These were not complete source energies.

They excluded:

```text
CONFINEMENT

ANNIHILATION / REACTION

ACTIVATION

CONTROL

SUPPORT

RADIATION

FULL UV COMPLETION

NONLINEAR BACKREACTION
```

---

## 8.5 Important geometry failure

The same azimuthal mode vanishes regularly on the symmetry axis.

A compact spherical payload centered on that axis is therefore geometrically unfavorable.

The spherical comparator required approximately:

```text
~6.2e9 J
```

of field energy.

Thus:

```text
ON_AXIS_SPHERICAL_PAYLOAD_GEOMETRY=
REJECTED_BY_ENERGY

TOROIDAL_PAYLOAD_WITNESS=
PRESERVED

PAYLOAD_SHAPE_INDEPENDENCE=
NOT_ESTABLISHED
```

This was a useful falsification rather than a nuisance result.

---

## 8.6 A10F1 limitations

The BVP demonstrated a finite reduced-EFT source and finite neutral payload.

However, the initial scaling used the **center-of-mass** \(1g\) condition.

The minimum local acceleration over the sampled toroidal payload was only approximately:

```text
3.7406 m/s^2.
```

Thus it did not yet satisfy the project's stricter interpretation that the finite payload should not contain underperforming regions when the benchmark is claimed.

This was repaired analytically in A10F2.

Regression:

```text
983 passed
```

---

# 9. 032H17A10F2 — strict finite-payload floor and microscopic source-naturalness closeout

A10F2 produced the most important split of the entire slice.

The reduced-EFT mechanism became stronger.

The current ordinary-Dirac microscopic realization failed.

---

## 9.1 Exact strict-payload rescaling

For the fixed linear BVP:

```text
V
scales linearly with source amplitude

sigma=lambda V^2
scales quadratically

acceleration
scales quadratically

field energy
scales quadratically

source number
scales linearly
```

Therefore the A10F1 solution could be rescaled exactly so that the least-accelerated payload point reaches \(1g\).

Required amplitude factor:

```text
1.6191528439
```

Required energy factor:

```text
2.6216559318
```

The rescaled finite payload then had:

```text
LOCAL_MIN_ACCELERATION_M_S2=
9.80665

COM_ACCELERATION_M_S2≈
25.71

TRUE_GEOMETRIC_GAP_M=
1.0
```

The strict canonical field/loading energy became approximately:

```text
562965 J
```

or:

```text
0.563 MJ.
```

The strict source population became approximately:

```text
2.88e16 engineered pairs.
```

The optimistic partial energy floors became approximately:

```text
FIELD_PLUS_ELECTRON_POSITRON_REST=
567681 J

FIELD_PLUS_PROTON_ANTIPROTON_REST=
9221930 J
```

Thus even after strengthening the finite-payload performance requirement, the reduced-EFT field sector did **not** force the energy above \(10\,\mathrm{MJ}\).

This was a significant positive result.

But these remained partial energies.

---

## 9.2 Microscopic coupling lower bound

The corrected engineered-pair healthy-pole coupling is:

```text
g_pair=
2.
```

For a two-constituent pair, the triangle inequality implies at least one constituent amplitude of magnitude:

```text
>=1.
```

Thus the current ordinary-Dirac realization contains an order-unity microscopic coupling.

The Wheeler normalization parameter does not provide an arbitrarily small physical coupling after canonical fermion normalization.

Therefore:

```text
FREE_SMALL_SOURCE_COUPLING_KNOB=
NO
```

in the current realization.

---

## 9.3 Stückelberg scale

The one-metre carrier requires:

```text
|f|
=
m_V
≈
1.973269804e-7 eV.
```

A10C already established that the bare connection current is not protected by an exact standalone conservation law.

The Stückelberg completion is essential.

The ratio of the electron mass threshold to the Stückelberg scale is approximately:

```text
m_e / |f|≈
2.5896e12.
```

The longitudinal diagnostic:

```math
gE/|f|
```

is therefore also of order:

```text
2.6e12
```

for the conservative constituent coupling \(g\ge1\).

This was not declared an exact unitarity theorem.

It was a power-counting warning that an additional exact-current or Higgs/Noether protection mechanism would be mandatory.

---

## 9.4 Technical-naturalness NDA

Because the invariant Stückelberg field permits a quadratic mass operator and no matter-loop nonrenormalization theorem had been established, the conservative threshold estimate used:

```math
\delta m_V^2
\sim
c_{\rm loop}
\frac{
g^2m_f^2
}{
16\pi^2
}.
```

For an electron threshold, \(g\ge1\), and the one-metre target mass, the nominal ratio was approximately:

```text
DELTA_M2_OVER_TARGET≈
4.24666e22
```

for:

```text
c_loop=
1.
```

Even:

```text
c_loop=
1e-12
```

left the correction more than ten orders of magnitude above the target mass squared.

The loop coefficient required merely to make the threshold correction comparable to the desired mass was approximately:

```text
2.35479e-23.
```

No symmetry or same-action theorem generated such a suppression.

---

## 9.5 Hypothetical weak-coupling escape also collides with energy

A hypothetical additional source coefficient \(\epsilon\) was considered diagnostically.

It is **not** a free parameter of the current canonical Wheeler realization.

Smaller \(\epsilon\) would improve naturalness but increase the number of microscopic source constituents needed to generate the same macroscopic field.

The optimistic electron-source energy floor required approximately:

```text
EPSILON_ENERGY_MIN≈
4.997e-4
```

whereas the nominal \(c_{\rm loop}=1\) naturalness requirement gave approximately:

```text
EPSILON_NATURALNESS_MAX≈
4.85e-12.
```

There was no overlap.

Even allowing loop coefficients through the tested suppressed range did not produce simultaneous energy and naturalness headroom.

A simple weak-coupling repair would require an additional loop suppression of approximately:

```text
~9.4e-17
```

before an energy/naturalness overlap could begin.

---

## 9.6 A10F2 decision

```text
DECISION=
RED_A10F2_CURRENT_WHEELER_MARZO_ORDINARY_DIRAC_REALIZATION_BLOCKED_ON_ULTRALIGHT_SOURCE_NATURALNESS__
STRICT_REDUCED_EFT_1G_1M_PAYLOAD_SURVIVES
```

The correct interpretation was:

```text
STRICT_REDUCED_EFT_FINITE_PAYLOAD_MECHANISM=
GREEN

CURRENT_WHEELER_MARZO_ORDINARY_DIRAC_MICROSCOPIC_REALIZATION=
RED_TECHNICAL_NATURALNESS

CURRENT_REALIZATION_FULL_CERTIFICATION=
STOP

EXPENSIVE_NONLINEAR_BVP=
NOT_AUTHORIZED

COMPLETE_ENERGY_OPTIMIZATION=
NOT_AUTHORIZED

HOOK17=
NOT_GLOBALLY_CLOSED
```

Regression:

```text
995 passed
```

This was not a collapse of HOOK17.

It isolated the remaining failure to the microscopic source/protection sector.

---

# 10. 032H17A11A — exact-current and Higgs protection rescue atlas

After A10F2, three obvious repairs were tested analytically before allowing another large simulation.

The point was to avoid spending time on cosmetic fixes.

The optimistic source-energy floor implied that even an incomplete sub-\(10\,\mathrm{MJ}\) electron-source realization required an effective microscopic source coupling of approximately:

```text
4.99714e-4.
```

Three protection lanes were then tested.

---

## 10.1 Lane 1 — perturbative heavy-radial Abelian Higgs completion

For the standard relations:

```math
m_V
=
gv
```

and:

```math
m_h
=
\sqrt{2\lambda_H}v,
```

requiring a radial Higgs mode at least as heavy as the electron threshold and using the permissive perturbativity ceiling:

```math
\lambda_H
\le
4\pi
```

gives approximately:

```text
g_max≈
1.936e-12.
```

The optimistic energy requirement is:

```text
g_min≈
4.997e-4.
```

The gap is therefore approximately:

```text
2.58e8.
```

Thus:

```text
SIMPLE_PERTURBATIVE_HEAVY_RADIAL_HIGGS_COMPLETION=
CLOSED
```

This did **not** close:

```text
LIGHT_RADIAL_COMPLETIONS

STRONGLY_COUPLED HIGGS COMPLETIONS

COMPOSITE HIGGS COMPLETIONS

GENERAL NOETHER COMPLETIONS
```

---

## 10.2 Lane 2 — identically conserved Pauli/derivative current

The dimension-five portal:

```math
\frac1M
F^V_{\mu\nu}
\bar\psi\sigma^{\mu\nu}\psi
```

generates an identically conserved derivative current.

This removes the direct longitudinal-current problem.

Importantly, the finite A10F1 source morphology can be written exactly as a curl.

The compact azimuthal profile:

```math
J_\phi
\propto
\frac{\rho}{R}
\left(
1-\frac{r^2}{R^2}
\right)^2
```

is generated by the compact magnetization profile:

```math
M_z
\propto
\frac R6
\left(
1-\frac{r^2}{R^2}
\right)^3.
```

Thus morphology itself was not the failure.

For the local dimension-five operator to remain valid through the electron threshold:

```text
M>=m_e.
```

Using the characteristic source momentum gives an optimistic effective coupling ceiling of approximately:

```text
g_eff_max≈
3.862e-13.
```

Compared with:

```text
g_energy_min≈
4.997e-4,
```

the mismatch is approximately:

```text
1.29e9.
```

Thus:

```text
SIMPLE_DIMENSION5_PAULI_CURRENT_VALID_THROUGH_ELECTRON_THRESHOLD=
CLOSED
```

This did not close an explicit new UV completion below the electron scale.

---

## 10.3 Lane 3 — exact ordinary-matter renormalizable vector current

For ordinary electrons, protons, and neutrons with direct charges:

```text
q_e
q_p
q_n
```

an electrically neutral atom with proton number \(Z\) and neutron number \(N\) has vector charge:

```math
Q_{\rm atom}
=
Z(q_p+q_e)
+
Nq_n.
```

Requiring exact direct silence for arbitrary neutral compositions gives:

```math
q_n
=
0
```

and:

```math
q_p
=
-q_e.
```

Thus the ordinary \(e,p,n\) solution is one-dimensional and electromagnetic-like up to normalization.

The corresponding ultralight electron-vector coupling was then tested against the current electron \(g-2\) bounds used in the run.

The optimistic energy requirement translated to approximately:

```text
EPSILON_ENERGY_MIN≈
0.00165019.
```

Independent treatment of the two incompatible electron inputs gave approximate one-sided 95% limits:

```text
RUBIDIUM:
EPSILON_95≈
2.30e-5

CESIUM:
EPSILON_95≈
8.69e-6
```

giving energy-to-bound gaps of approximately:

```text
RB_GAP≈
71.8

CS_GAP≈
190.
```

Therefore:

```text
MINIMAL_EM_LIKE_EXACT_ORDINARY_CURRENT_RESCUE=
CLOSED
```

This did not close:

```text
SOURCE_ONLY HIDDEN CURRENTS

NONSTANDARD NOETHER CURRENTS

OTHER EXACT CONSERVED COMPOSITE CURRENTS
```

---

## 10.4 A11A decision

All three lowest-complexity repairs failed.

The proper decision was:

```text
RED_SCOPED_A11A_THREE_MINIMAL_SOURCE_PROTECTION_REPAIRS_CLOSED__
PROMOTE_GENUINELY_NEW_MASSLESS_K2_TORSION_VECTOR_SOURCE_GATE
```

The following remained explicitly open:

```text
ALL_HIGGSED_NOETHER_COMPLETIONS_CLOSED=
False

ALL_EXACT_CONSERVED_CURRENT_COMPLETIONS_CLOSED=
False

ALL_DERIVATIVE_COMPOSITE_CURRENTS_CLOSED=
False

HOOK17_CLOSED=
False
```

The old K3 clean-source route was not reopened.

The next genuinely new carrier was selected as:

```text
032H17A11B
K2 MASSLESS TORSION-VECTOR
ENGINEERED DIRAC
EXACT SOURCE WARD
+
HEALTHY PROJECTOR GATE
```

Regression:

```text
1006 passed
```

This was the live regression state at journal close.

---

# Result

The research slice produced a much sharper scientific state than existed at the end of A9R3/E1B1.

## Strong positive results

### 1. Spin engineering reopened source support

The old clean-rest-state zero-support result does not extend to all intrinsic Dirac source states.

Engineered rest states possess nonzero protected-vector representation content.

```text
DIRAC_SOURCE_STATE_ENGINEERING=
PRODUCTIVE
```

---

### 2. A protected healthy massive \(1^-\) pole was reached from the intrinsic Dirac source

The project independently reconstructed the protected Marzo \(1^-\) pole and obtained nonzero positive source saturation.

After the later source-normalization repair:

```text
CORRECTED_SOURCE_SATURATED_RESIDUE=
4

CORRECTED_CANONICAL_PAIR_POLE_COUPLING=
2
```

Thus:

```text
SOURCE_EXISTENCE=
YES

HEALTHY_MODE_EXISTENCE=
YES

NONZERO_SOURCE_TO_HEALTHY_MODE_OVERLAP=
YES

CANONICAL_NORMALIZATION_REPAIRED=
YES
```

This is stronger than source representation support alone.

---

### 3. A protected quadratic universal-metric mechanism survived

The protected \(1^-\) pole factorizes into a vector representative.

The source-free physical metric has no demonstrated nonredundant first-order vector response in the tested local analytic transverse sector.

A quadratic universal metric produces:

```text
ACTIVE_RESPONSE=
YES

OFFSTATE_FIRST_VARIATION=
ZERO

OUTWARD_STATIC_G00_SIGN=
YES

FIELD_REDEFINITION_INVARIANT_TIDAL_RESPONSE=
YES
```

This is one of the strongest structural HOOK17 results obtained to date.

---

### 4. Planck normalization does not automatically kill the one-metre carrier

The selected protected subfamily obeys:

```math
m_{1^-}^2
=
\frac{d_1^2}{c_7}.
```

Therefore:

```text
ONE_METRE_RANGE_REQUIRES_HUGE_C7=
NO

PLANCK_NORMALIZATION_FATAL=
NO
```

This removed an important possible absolute-scale obstruction.

---

### 5. Finite-payload reduced-EFT \(1g/1m\) performance was achieved

A compact divergence-free finite source and finite neutral toroidal payload were solved in a converged reduced transverse-vector BVP.

After the strict A10F2 rescaling:

```text
PAYLOAD_MASS=
1 kg

MINIMUM_PAYLOAD_ACCELERATION=
9.80665 m/s^2

COM_ACCELERATION≈
25.71 m/s^2

MINIMUM_GEOMETRIC_SOURCE_TO_PAYLOAD_GAP=
1.0 m

FIELD_LOADING_ENERGY≈
5.63e5 J
```

Thus:

```text
STRICT_REDUCED_EFT_1G_1M_FINITE_PAYLOAD_WITNESS=
ESTABLISHED
```

within the declared reduced model.

This is an important achievement.

It is not yet project-level true-stand-off certification.

---

### 6. Partial energy remained below 10 MJ

For the strict \(1g/1m\) finite-payload witness:

```text
FIELD_LOADING_ENERGY≈
562965 J

FIELD_PLUS_ELECTRON_SOURCE_REST≈
567681 J

FIELD_PLUS_PROTON_MASS_COMPARATOR≈
9221930 J
```

Therefore the classical reduced-EFT mechanism does not itself force catastrophic operating energy.

However:

```text
COMPLETE_OPERATING_ENERGY=
UNKNOWN
```

and the project prohibition remains:

> **Partial energy may reject a model but may never certify one.**

---

## Strong negative results

### 1. V26E1B1 current implementation failed technical naturalness

The E1B1 quadratic DHOST completion retained healthy local and sign properties but had no protection against the lower-order generated \(xT\) interaction.

The required cutoff fell below the physical source scale.

The current implementation was therefore closed.

---

### 2. Direct BMS rest-density trace route closed

The exact BMS source Ward identity annihilates the trace escape on the complete generic rest-density Ward nullspace.

The direct BMS identification is therefore closed in that scope.

---

### 3. Current ordinary-Dirac Wheeler→Marzo microscopic realization failed naturalness

Despite the strong classical performance, the one-metre protected massive carrier requires an ultralight scale:

```text
m_V≈
1.97e-7 eV
```

while the microscopic source coupling is order unity.

The conservative electron-threshold naturalness diagnostic misses the desired mass by approximately:

```text
4.25e22
```

at nominal loop coefficient one.

The required unexplained loop suppression is approximately:

```text
2.35e-23.
```

No established same-action protection produces that suppression.

Therefore this specific realization cannot currently be promoted.

---

### 4. Three simple protection repairs were scoped closed

A11A rejected:

```text
STANDARD PERTURBATIVE HEAVY-RADIAL HIGGS

SIMPLE DIMENSION-5 PAULI / DERIVATIVE CURRENT

MINIMAL EM-LIKE EXACT ORDINARY-MATTER CURRENT
```

by very large margins.

These closeouts substantially reduce the risk of repeatedly revisiting obvious repairs.

---

# Current Energy State

The historical:

```text
~17.07 J
```

remains a field-capacity reference only.

It is not the current finite-payload operating requirement.

The strongest latest finite-payload reduced-EFT witness requires approximately:

```text
STRICT_FIELD_LOADING_ENERGY=
0.563 MJ
```

The most favorable currently calculated partial source comparator is approximately:

```text
STRICT_FIELD_PLUS_ELECTRON_SOURCE_REST=
0.568 MJ
```

The heavier proton/antiproton rest-mass comparator is approximately:

```text
9.22 MJ
```

before omitted operating costs.

Therefore:

```text
BEST_CURRENT_PARTIAL_ENERGY_SCALE=
~0.57 MJ
```

for the electron-source comparator.

But:

```text
COMPLETE_CONSERVATIVE_OPERATING_ENERGY=
UNKNOWN

CERTIFIED_SUB10MJ_MODEL=
NO
```

Support, confinement, activation, control, reset, reaction, radiation, nonlinear backreaction, full quantum/RG/UV completion, leakage, and off-state costs remain uncounted.

---

# Relationship to 006D

The present HOOK17 sequence has **not** superseded 006D as the strongest conservative true-stand-off result.

006D still has the stronger established source-side properties:

```text
FINITE CONSERVED STRESS-ENERGY

POSITIVE ENERGY

NEC/WEC/DEC

OUTWARD NEAR GRAVITY

POSITIVE FAR-FIELD ACTIVE MASS
```

The HOOK17 reduced-EFT branch has achieved dramatically better energy and finite-payload response:

```text
1 KG FINITE NEUTRAL PAYLOAD

1 M GEOMETRIC GAP

>=1 G THROUGHOUT SAMPLED PAYLOAD AFTER STRICT RESCALE

~0.563 MJ FIELD ENERGY
```

but lacks a technically natural microscopic source realization.

Therefore:

```text
006D_STRONGEST_CONSERVATIVE_TRUE_STANDOFF_ANCHOR=
YES

HOOK17_HAS_BEATEN_006D_ENERGY_SCALING_IN_REDUCED_EFT=
YES

HOOK17_HAS_SUPERSEDED_006D_AS_CONSERVATIVE_CERTIFIED_MODEL=
NO

PROJECT_TRUE_STANDOFF_CERTIFIED_FOR_HOOK17=
NO
```

The distinction must be preserved.

---

# Verification

## Analytical

The following major analytical results were established or used in this slice:

### E1B2 frame map

```math
\tilde X
=
\frac XA
```

with:

```math
\frac{d\tilde X}{dX}
=
\frac{A-XA_X}{A^2}.
```

### BMS source Ward identity

```math
3\partial_aJ^{abc}
-
\partial^{(b}t^{c)}
=
0.
```

### Marzo selected-family pole identity

```math
m_{1^-}^2
=
\frac{d_1^2}{c_7}.
```

### Protected-pole vector factorization

```math
\mathcal B_{abc}
=
-\sqrt{\frac32}\eta_{ac}V_b.
```

### Quadratic universal metric

```math
g^{\rm phys}_{\mu\nu}
=
e^{2\lambda V^2}g_{\mu\nu}.
```

### Static source-free outward response

```math
V_x
=
V_0e^{-mz}
```

implies:

```math
a_z
=
2c^2m\sigma.
```

### Axisymmetric transverse finite-source equation

```math
\left[
-\partial_\rho^2
-\frac1\rho\partial_\rho
-\partial_z^2
+\frac1{\rho^2}
+m_{\rm eff}^2
\right]
V_\phi
=
J_\phi.
```

### A11A neutral-current theorem

Exact direct silence on arbitrary neutral \(e,p,n\) compositions requires:

```math
q_n=0
```

and:

```math
q_p=-q_e.
```

These results were used as theorem-first gates before expensive simulation.

---

## Numerical

Regression progression during the slice was:

```text
START=
910 passed

A10A=
918 passed

A10B=
927 passed

A10E=
960 passed

A10F0=
971 passed

A10F1=
983 passed

A10F2=
995 passed

A11A=
1006 passed
```

The finite BVP included:

```text
GRID CONVERGENCE=
PASS

DOMAIN CONVERGENCE=
PASS

SOURCE-WORK / FIELD-ENERGY RECONSTRUCTION=
PASS
```

The source normalization was independently repaired before source-number interpretation.

The spherical axial payload geometry was retained as a negative control rather than discarded.

---

## Dimensional

The one-metre carrier mass follows:

```math
m_V
=
\frac{\hbar c}{1\,{\rm m}}
\approx
1.9733\times10^{-7}\ {\rm eV}.
```

The quadratic metric coupling has dimensions:

```text
[lambda]=eV^-2
```

for canonically normalized \(V\) in four dimensions.

The matter-loading term:

```math
2\lambda\rho
```

therefore has dimensions of mass squared.

The finite BVP source-number conversion included the required \(\hbar c\) factor between metre-coordinate and natural-unit source densities.

---

## Limiting cases

### Off state

As:

```math
V->0,
```

the quadratic universal metric returns to the baseline physical metric and its first variation vanishes.

### Linear metric response

For a transverse source-free physical vector:

```math
q\cdot V=0.
```

The surviving tested linear symmetric metric structure is pure gauge and carries zero linearized Riemann curvature.

### Payload loading

As:

```math
lambda->0,
```

matter loading disappears but the canonical field amplitude required for fixed metric response grows.

This exposes the field-energy/source-strength tradeoff.

### Source amplitude

For the fixed linear BVP:

```text
FIELD AMPLITUDE ~ SOURCE AMPLITUDE

ACCELERATION ~ SOURCE AMPLITUDE^2

FIELD ENERGY ~ SOURCE AMPLITUDE^2

SOURCE NUMBER ~ SOURCE AMPLITUDE
```

which justified the exact A10F2 strict-payload rescaling.

---

## Literature Comparison

The research slice relied on several external field-theory structures.

### Wheeler Dirac metric-affine source

Wheeler's covariant Dirac/metric-affine analysis supplied the intrinsic torsion and nonmetricity response structures used for V24 and the A10 source-state work.

A10F1 clarified that the geometric response object and the canonically normalized bare connection current must not be numerically conflated.

### Marzo protected massive \(1^-\) MAG family

The Marzo metric-affine action supplied the protected Abelian/Stückelberg carrier and published healthy massive \(1^-\) sector.

The project independently reconstructed the relevant pole rather than accepting representation labels as proof of overlap.

### Barker-Marzo-Santoni protected tensor family

The BMS rank-two gauge symmetry supplied the exact source Ward identity used to close the direct rest-density trace-vector route.

### Electron \(g-2\)

A11A used contemporary electron \(g-2\) inputs as separate, mutually incompatible measurements rather than combining them.

No novelty claim is made for these external theories.

The project-derived contribution is the specific sequence of source engineering, same-action saturation, universal-metric construction, finite-payload BVP, naturalness falsification, and repair triage.

---

# Falsification Attempt

The central purpose of the slice was to try to destroy the attractive HOOK17 mechanism cheaply and honestly.

## 1. Could the A10A source escape simply violate the protected BMS Ward identity?

Yes.

The direct BMS rest-density trace route was closed in A10B.

This prevented a source-overlap artifact from being promoted.

---

## 2. Could the engineered source have representation support but zero overlap with the actual healthy pole?

This possibility was tested directly.

A10D independently reconstructed the action pole and found nonzero positive saturation.

Thus this failure mode did not kill the Marzo engineered-source route.

---

## 3. Could the source overlap disappear after correcting Wheeler's absolute normalization?

No.

The residue changed:

```text
16 -> 4
```

and canonical coupling changed:

```text
4 -> 2
```

but the overlap remained strictly nonzero.

---

## 4. Could restoring Planck normalization make the one-metre carrier impossible?

No for the selected protected subfamily.

The exact identity:

```math
m^2=d_1^2/c_7
```

decouples the carrier range from the Einstein-Hilbert coefficient \(a_0\).

---

## 5. Could payload loading force the canonical field energy above 10 MJ?

Not automatically.

A controlled-load corridor survived with field energies well below the project's complete-energy ceiling.

This remained a partial result.

---

## 6. Could finite-payload geometry destroy the effect?

Yes for some geometries.

The on-axis spherical payload was catastrophically inefficient because the regular azimuthal transverse mode vanishes on the axis.

The toroidal payload avoided this node.

Therefore geometry dependence is real and must not be hidden.

---

## 7. Could the finite-payload result be a grid/domain artifact?

The primary toroidal result passed declared grid and domain convergence checks and an independent source-work energy reconstruction.

---

## 8. Could the apparent \(1g\) performance depend only on the payload COM while large parts underperform?

Yes in A10F1.

The minimum sampled acceleration was only approximately:

```text
3.74 m/s^2.
```

A10F2 repaired this by exact scaling until the minimum sampled local acceleration reached:

```text
9.80665 m/s^2.
```

The reduced-EFT energy remained below 1 MJ.

---

## 9. Could microscopic source naturalness kill the model even though the classical BVP is excellent?

Yes.

This became the decisive failure.

The current ordinary-Dirac Wheeler→Marzo implementation has:

```text
ONE-METRE ULTRALIGHT MASS

+

ORDER-UNITY MICROSCOPIC SOURCE COUPLING

+

NO ESTABLISHED NONRENORMALIZATION THEOREM
```

and fails the source-threshold naturalness preflight catastrophically.

---

## 10. Could an obvious Higgs completion repair the mass hierarchy?

Not in the tested simple perturbative heavy-radial class.

The energy-required coupling and Higgs-protected coupling differ by more than eight orders of magnitude.

---

## 11. Could an identically conserved Pauli current solve the problem cheaply?

The required source morphology can be produced exactly.

However, a local dimension-five operator valid through the electron threshold gives an effective coupling approximately nine orders of magnitude below the optimistic energy requirement.

Thus the simple portal is closed.

---

## 12. Could an exact conserved ordinary-matter vector current solve both naturalness and neutral-matter silence?

Exact direct neutral-matter silence forces the ordinary \(e,p,n\) charge into an EM-like direction.

Current electron \(g-2\) constraints then place the allowed coupling far below the optimistic source-energy requirement.

Thus the minimal ordinary-current rescue is closed.

---

# Claims Status

`CLAIMS.md` was not verified as updated during this research slice.

This journal therefore records the recommended claim status but does not assert that the claims file has already been synchronized.

Recommended durable classifications:

```text
CLAIM_ID=
032H17A10A_SPIN_ENGINEERED_DIRAC_TRACE_ESCAPE

TYPE=
PROJECT_DERIVED_SOURCE_STATE_RESULT

STATUS=
SUPPORTED_AT_SOURCE_REPRESENTATION_LEVEL
```

```text
CLAIM_ID=
032H17A10B_DIRECT_BMS_REST_DENSITY_TRACE_ROUTE

TYPE=
PROJECT_DERIVED_EXACT_WARD_CLOSEOUT

STATUS=
CLOSED_IN_DECLARED_DIRECT_REST_DENSITY_SCOPE
```

```text
CLAIM_ID=
032H17A10D_ENGINEERED_DIRAC_PROTECTED_MASSIVE_1MINUS_POLE_OVERLAP

TYPE=
PROJECT_DERIVED_ACTION_LEVEL_HEALTHY_POLE_RESULT

STATUS=
SUPPORTED_WITH_CORRECTED_ABSOLUTE_SOURCE_NORMALIZATION_FROM_A10F1
```

```text
CLAIM_ID=
032H17A10E_PROTECTED_QUADRATIC_UNIVERSAL_METRIC

TYPE=
PROJECT_DERIVED_SAME_ACTION_REDUCED_EFT_METRIC_PREFLIGHT

STATUS=
SUPPORTED_IN_DECLARED_LOCAL_SOURCEFREE_TRANSVERSE_SCOPE
```

```text
CLAIM_ID=
032H17A10F1_FINITE_TRANSVERSE_SOURCE_AND_FINITE_NEUTRAL_PAYLOAD

TYPE=
PROJECT_DERIVED_REDUCED_EFT_BVP_RESULT

STATUS=
SUPPORTED_WITH_GRID_DOMAIN_AND_ENERGY_RECONSTRUCTION
```

```text
CLAIM_ID=
032H17A10F2_STRICT_FINITE_PAYLOAD_1G_1M_REDUCED_EFT_WITNESS

TYPE=
PROJECT_DERIVED_REDUCED_EFT_FINITE_PAYLOAD_RESULT

STATUS=
SUPPORTED
```

```text
CLAIM_ID=
032H17A10F2_CURRENT_WHEELER_MARZO_ORDINARY_DIRAC_REALIZATION

TYPE=
PROJECT_DERIVED_TECHNICAL_NATURALNESS_CLOSEOUT

STATUS=
BLOCKED
```

```text
CLAIM_ID=
032H17A11A_THREE_MINIMAL_SOURCE_PROTECTION_REPAIRS

TYPE=
PROJECT_DERIVED_ANALYTIC_SCOPED_CLOSEOUT

STATUS=
CLOSED_FOR_TESTED_HEAVY_HIGGS_PAULI_AND_EM_LIKE_CLASSES
```

The following remain prohibited:

```text
HOOK17_PHYSICAL_ANTIGRAVITY_MODEL=
ESTABLISHED

PROJECT_TRUE_STANDOFF=
CERTIFIED_FOR_HOOK17

COMPLETE_OPERATING_ENERGY_LT_10MJ=
CERTIFIED

PRACTICAL_ANTIGRAVITY_DEVICE=
YES

REACTIONLESS_PROPULSION=
ESTABLISHED

NEW_PHYSICS_DISCOVERY=
YES
```

---

# Open Questions

## 1. Can a massless protected carrier preserve the good HOOK17 source/metric structure?

This is now the highest-value immediate question.

The current massive Marzo carrier failed because the meter-range ultralight mass had to coexist with ordinary-Dirac threshold physics.

A genuinely massless protected carrier would remove that particular hierarchy at the carrier level.

The next selected family is:

```text
BMS K2
MASSLESS PROTECTED TORSION VECTOR
```

The first test must be source/Ward/projector based.

Do not begin with payload or energy work.

---

## 2. Does the engineered Wheeler Dirac torsion source have nonzero exact K2 healthy-mode overlap?

Wheeler provides explicit torsion response structure.

The next run should reconstruct:

```text
FULL ENGINEERED TORSION SOURCE

K2 GAUGE WARD IDENTITY

HEALTHY K2 PROJECTOR

SOURCE SATURATION

CANONICAL NORMALIZATION
```

The stop rule is:

> **If the exact K2 source Ward identity or healthy projector overlap is zero, close the direct K2 route immediately.**

---

## 3. Does K2 admit a protected quadratic universal metric?

Even nonzero source overlap is insufficient.

A surviving K2 carrier must support a same-action universal metric whose first physically nonredundant response preserves the HOOK17 quadratic active/off-state mechanism.

The A10E construction is mechanism knowledge, not automatically transferable.

---

## 4. Can a genuinely exact-current or fully Noether-protected source exist?

A11A closed only the easiest repairs.

Still logically open are:

```text
FULL HIGGSED / NOETHER-COMPENSATED HOOK

SOURCE-ONLY HIDDEN EXACT CURRENT

NONSTANDARD COMPOSITE CONSERVED CURRENT

MASSIVE HOOK / NONMETRICITY WITH NEW PROTECTION

MASSIVE SPIN-3 BEYOND TESTED 1−

GAUGE-INVARIANT CURVATURE / COMPOSITE PORTAL

DIRAC SOURCE-STATE ENGINEERING IN NEW REPRESENTATIONS
```

These should be tested only when they contain genuinely new symmetry structure.

---

## 5. Can the finite-payload result survive a full nonlinear same-action theory?

The A10F1/F2 BVP is a reduced healthy-pole EFT.

It is not the full nonlinear connection/scalar/metric/Dirac system.

A future successful microscopic carrier would still require:

```text
FULL NONLINEAR CONSTRAINT HEALTH

SOURCE BACKREACTION

PAYLOAD BACKREACTION

SUPPORT / REACTION

RADIATION

ACTIVATION / OFFSTATE

STABILITY

QUANTUM / RG / UV

EMPIRICAL CONSISTENCY

COMPLETE ENERGY
```

---

## 6. What is the actual complete energy of the best reduced-EFT witness?

Unknown.

The strongest current calculated partial is approximately:

```text
~0.568 MJ
```

for field plus electron/positron rest energy.

That number must not be called complete operating energy.

---

## 7. Has HOOK17 surpassed 006D?

Not in conservative certification.

HOOK17 has achieved dramatically better reduced-EFT finite-payload energy scaling, but its current microscopic realization fails naturalness.

006D therefore remains the stronger conservative true-stand-off anchor.

---

# Current Progress Heuristics

At journal close, the internal research-management heuristics were updated approximately to:

```text
OVERALL_PROJECT_KNOWLEDGE_ACCOMPLISHMENT_HEURISTIC=
~84%

HOOK17_SPECIFIC_HEURISTIC=
~88%
```

These are:

```text
NOT PROBABILITIES OF SUCCESS

NOT BAYESIAN CONFIDENCE

NOT DEVICE COMPLETION PERCENTAGES
```

They measure accumulated resolved questions, durable failure memory, and progress through the project's theoretical certification ladder.

The increase reflects major progress in:

```text
SOURCE STATE ENGINEERING

SAME-ACTION SOURCE COMPLETION

HEALTHY POLE OVERLAP

CANONICAL NORMALIZATION

UNIVERSAL METRIC RESPONSE

FINITE-PAYLOAD BVP

STRICT 1G/1M REDUCED-EFT PERFORMANCE

ABSOLUTE FIELD ENERGY

MICROSCOPIC NATURALNESS FALSIFICATION

PROTECTION-REPAIR TRIAGE
```

The remaining unresolved fraction is disproportionately difficult.

It contains the transition from a successful reduced-EFT mechanism to a technically natural microscopic theory and then to complete-energy/nonlinear certification.

---

# AI Assistance

AI assistant used: **ChatGPT by OpenAI**

Substantial AI-assisted work in this research slice included:

* reconstruction of the E1B2 Einstein/Jordan frame map and physical-\(g_{00}\) response;
* identification of the E1B2 generated-\(xT\) naturalness obstruction;
* reconstruction of the Wheeler rest-state source atlas;
* design of spin-engineered Dirac source states;
* derivation of the exact BMS source Ward identity;
* exact symbolic rest-density Ward/nullspace analysis;
* identification of the engineered negative-parity \(1^-\) source support;
* reconstruction of the Marzo protected massive \(1^-\) action sector;
* construction of the Stückelberg/Noether source completion;
* independent healthy-pole saturation analysis;
* derivation of the protected-pole vector factorization;
* design of the quadratic universal physical metric;
* field-redefinition/pure-gauge audit of the linear metric response;
* static source-free physical-\(g_{00}\) sign derivation;
* Planck-normalized one-metre carrier scaling analysis;
* payload-loading analysis;
* construction of the finite axisymmetric transverse-vector BVP;
* grid/domain and source-work consistency checks;
* Wheeler response-to-bare-current normalization repair;
* strict finite-payload rescaling;
* source-number and partial rest-energy reconstruction;
* microscopic Stückelberg/naturalness analysis;
* perturbative Higgs, Pauli-current, and EM-like exact-current closeout calculations;
* preparation of carry-forward research state and this journal entry.

AI-generated code, mathematics, literature interpretation, and numerical strategies are not assumed correct merely because they were generated by an AI system.

Important results remain subject to:

```text
INDEPENDENT HUMAN REVIEW

ANALYTIC RECONSTRUCTION

NUMERICAL REPRODUCTION

DIMENSIONAL CHECK

LIMITING-CASE CHECK

LITERATURE COMPARISON

SAME-ACTION PROVENANCE AUDIT

FIELD-REDEFINITION AUDIT

QUANTUM / RG / UV REVIEW
```

Use of ChatGPT does not imply endorsement or sponsorship by OpenAI.

---

# Next Action

Do not run more finite-payload optimization on the blocked Wheeler–Marzo ordinary-Dirac realization.

Do not tune the \(0.563\,\mathrm{MJ}\) field energy downward.

Do not reopen:

```text
BMS DIRECT REST-DENSITY TRACE ROUTE

A6R2 CLEAN MASSIVE 1− ZERO-SOURCE RESULT

K3 HISTORICAL CLEAN ROUTE

SIMPLE HEAVY-RADIAL HIGGS REPAIR

SIMPLE DIMENSION-5 PAULI REPAIR

MINIMAL EM-LIKE ORDINARY CURRENT REPAIR
```

The immediate highest-information task is:

```text
032H17A11B
K2 MASSLESS TORSION-VECTOR
ENGINEERED DIRAC
EXACT SOURCE WARD
AND HEALTHY PROJECTOR GATE
```

The required order is:

```text
1.
RECONSTRUCT THE K2 PROTECTED MASSLESS TORSION-VECTOR ACTION / GAUGE SYMMETRY

2.
RECONSTRUCT THE FULL WHEELER ENGINEERED TORSION SOURCE
FOR THE A10 SOURCE STATES

3.
DERIVE THE EXACT K2 SOURCE WARD / NOETHER IDENTITY

4.
TEST THE ENGINEERED DIRAC SOURCE AGAINST THAT IDENTITY

5.
RECONSTRUCT THE HEALTHY K2 PROJECTOR / RESIDUE

6.
SATURATE THE EXACT SOURCE AGAINST THE HEALTHY POLE

7.
CANONICALLY NORMALIZE THE SURVIVING COUPLING

8.
STOP IMMEDIATELY IF WARD OR HEALTHY-POLE OVERLAP IS ZERO

9.
ONLY IF GREEN:
TEST WHETHER A PROTECTED QUADRATIC UNIVERSAL METRIC EXISTS

10.
DO NOT RUN ANOTHER PAYLOAD BVP UNTIL THE NEW CARRIER PASSES
SOURCE + WARD + PROJECTOR + METRIC PROTECTION
```

The scientific motivation is simple:

> **The successful A10 reduced-EFT mechanism should be preserved, but the next carrier should remove the ultralight massive-vector naturalness obstruction rather than trying to tune around it.**

A massless symmetry-protected torsion vector is therefore currently the highest-value new family.

---

# Final Scientific State at Journal Close

```text
CURRENT_FULL_REGRESSION=
1006_PASSED

HISTORICAL_PROTECTED_BASELINE=
94_PASSED

006D_STRONGEST_CONSERVATIVE_TRUE_STANDOFF_ANCHOR=
PRESERVED

006D_C=
23.591586299249

V26E1B2_CURRENT_UNPROTECTED_SINGLE_SCALAR_REALIZATION=
CLOSED_ON_TECHNICAL_NATURALNESS_UV

HOOK17_A10A_SPIN_ENGINEERED_SOURCE_ESCAPE=
GREEN_SOURCE_LEVEL

HOOK17_A10B_DIRECT_BMS_REST_DENSITY_TRACE_ROUTE=
CLOSED

HOOK17_A10B_MARZO_ENGINEERED_1MINUS_REPRESENTATION=
REOPENED

HOOK17_A10C_STUECKELBERG_NOETHER_SOURCE_COMPLETION=
GREEN

HOOK17_A10D_PROTECTED_HEALTHY_1MINUS_POLE_OVERLAP=
GREEN

HOOK17_A10F1_CORRECTED_CANONICAL_PAIR_POLE_COUPLING=
2

HOOK17_A10E_QUADRATIC_UNIVERSAL_METRIC_PREFLIGHT=
GREEN

HOOK17_A10E_STATIC_OUTWARD_PHYSICAL_G00=
GREEN

HOOK17_A10F0_PLANCK_NORMALIZED_ONE_METRE_CARRIER=
GREEN

HOOK17_A10F1_FINITE_COMPACT_TRANSVERSE_SOURCE=
GREEN_REDUCED_EFT

HOOK17_A10F1_FINITE_NEUTRAL_TOROIDAL_PAYLOAD=
GREEN_REDUCED_EFT

HOOK17_A10F2_STRICT_WHOLE_PAYLOAD_MINIMUM_ACCELERATION=
9.80665_M_PER_S2

HOOK17_A10F2_TRUE_GEOMETRIC_GAP=
1.0_M

HOOK17_A10F2_STRICT_FIELD_LOADING_ENERGY=
~5.63E5_J

HOOK17_A10F2_FIELD_PLUS_ELECTRON_SOURCE_REST_PARTIAL=
~5.68E5_J

HOOK17_A10F2_FIELD_PLUS_PROTON_MASS_COMPARATOR_PARTIAL=
~9.22E6_J

HOOK17_A10F2_CURRENT_WHEELER_MARZO_ORDINARY_DIRAC_REALIZATION=
BLOCKED_ON_ULTRALIGHT_SOURCE_NATURALNESS

A11A_SIMPLE_PERTURBATIVE_HEAVY_RADIAL_HIGGS=
CLOSED

A11A_SIMPLE_DIMENSION5_PAULI_CURRENT=
CLOSED

A11A_MINIMAL_EM_LIKE_EXACT_ORDINARY_CURRENT=
CLOSED

ALL_HIGGSED_NOETHER_COMPLETIONS=
NOT_CLOSED

ALL_EXACT_CONSERVED_CURRENT_COMPLETIONS=
NOT_CLOSED

HOOK17_REDUCED_EFT_1G_1M_FINITE_PAYLOAD_MECHANISM=
SUPPORTED

HOOK17_PROJECT_TRUE_STANDOFF_CERTIFIED=
NO

HOOK17_COMPLETE_OPERATING_ENERGY=
UNKNOWN

CERTIFIED_SUB10MJ_COMPLETE_MODEL=
NO

PHYSICAL_ANTIGRAVITY_MODEL_FOUND=
NO

PRACTICAL_ANTIGRAVITY_DEVICE=
NO

REACTIONLESS_PROPULSION=
NO_CLAIM

NEW_PHYSICS_DISCOVERY=
NO

OVERALL_INTERNAL_PROGRESS_HEURISTIC=
~84_PERCENT_NOT_A_PROBABILITY

HOOK17_SPECIFIC_PROGRESS_HEURISTIC=
~88_PERCENT_NOT_A_PROBABILITY

NEXT=
032H17A11B_K2_MASSLESS_TORSION_VECTOR_ENGINEERED_DIRAC_EXACT_SOURCE_WARD_PROJECTOR_GATE
```

---

# Final Journal Classification

The strongest positive result of this research slice is:

> **HOOK17 was promoted far beyond its historical low-capacity algebraic partial. A spin-engineered intrinsic Dirac source was carried through a same-action Stückelberg/Noether completion into a protected healthy massive \(1^-\) pole with nonzero canonically normalized overlap, then into a protected quadratic universal physical metric with an outward invariant static response, and finally into a converged finite reduced-EFT source/payload calculation in which a finite \(1\,\mathrm{kg}\) neutral toroidal payload experiences at least \(1g\) everywhere across a true \(1\,\mathrm m\) geometric external gap with approximately \(0.563\,\mathrm{MJ}\) of canonical field/loading energy.**

The strongest negative result is:

> **The current ordinary-Dirac Wheeler→Marzo microscopic realization cannot presently support that successful reduced-EFT mechanism naturally. Its one-metre protected massive carrier lives at approximately \(1.97\times10^{-7}\,\mathrm{eV}\) while the canonically normalized microscopic source coupling is order unity, and no demonstrated symmetry protects the carrier mass from ordinary-Dirac threshold corrections. The required unexplained loop suppression is of order \(10^{-23}\). The three simplest source-protection repairs tested in A11A also fail by large margins.**

The correct project conclusion is therefore not:

```text
HOOK17=
FAILED
```

and not:

```text
ANTIGRAVITY_MODEL=
FOUND
```

It is:

```text
HOOK17_CORE_REDUCED_EFT_MECHANISM=
STRONGLY_PRESERVED

FINITE_1G_1M_PAYLOAD_WITNESS=
ACHIEVED_IN_REDUCED_EFT

CURRENT_WHEELER_MARZO_ORDINARY_DIRAC_MICROSCOPIC_REALIZATION=
CLOSED

MICROSCOPIC_PROTECTION_PROBLEM=
NOW_DOMINANT

COMPLETE_ENERGY=
UNKNOWN

006D=
STILL_STRONGEST_CONSERVATIVE_TRUE_STANDOFF_ANCHOR

NEXT_PROTECTED_FAMILY=
MASSLESS_K2_TORSION_VECTOR
```

The project therefore ends this slice with a substantially more precise frontier than it began with.

The central question is no longer whether the HOOK17 mechanism can generate useful outward finite-payload response.

Within the reduced healthy-pole EFT, it can.

The central question is now:

> **Can a genuinely symmetry-protected microscopic carrier/source theory preserve the successful HOOK17 source overlap, quadratic universal-metric mechanism, and finite-payload energy corridor without exposing a meter-range ultralight mass to ordinary-matter threshold naturalness?**

That is the problem carried into the next research session.
