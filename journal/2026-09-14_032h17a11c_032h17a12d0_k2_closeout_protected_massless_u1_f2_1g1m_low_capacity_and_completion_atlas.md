# Research Journal — 2026-09-14

## 032H17A11C–032H17A12D0 — Exact K2 Ordinary-Dirac Closeout, Engineered Iso-Weyl Source Reopening, Protected Concurrent-\(U(1)\) Massless Carrier, Gauge-Invariant \(F^2\) Finite-Payload \(1g/1m\) Response at \(2.656859\,\mathrm J\), Ordinary EM-Like Source Falsification, and Low-Capacity Completion Atlas

## Objective

The primary scientific question investigated in this research slice was:

> **Can HOOK17 escape the ordinary-Dirac microscopic naturalness and Ward obstructions identified in A10–A11 by finding a genuinely protected massless carrier with an exact conserved source, and can that carrier retain the low-capacity nonlinear physical-metric mechanism strongly enough to produce true finite-payload \(1g/1m\) outward response?**

A second question became decisive after the carrier problem was partially solved:

> **If an exact massless gauge symmetry invalidates the earlier vector-potential metric portal, does a gauge-invariant field-strength physical metric preserve the HOOK17 low-capacity advantage, and if so, which microscopic source families remain capable of physicalizing it?**

The permanent project standards remained:

```text
MIN_OUTWARD_ACCELERATION_M_S2=
9.80665

MIN_TRUE_EXTERNAL_STANDOFF_M=
1.0

FINITE_NEUTRAL_PAYLOAD_REQUIRED=
YES

ONE_UNIVERSAL_PHYSICAL_METRIC_REQUIRED=
YES

STRICT_COMPLETE_OPERATING_ENERGY=
LESS_THAN_10_MJ

EXACTLY_10_MJ=
FAIL

PARTIAL_OR_CAPACITY_ENERGY_MAY_CERTIFY_DEVICE=
NO

NEGATIVE_MASS_OR_ARBITRARY_EXOTIC_MATTER_ROUTE=
NOT_AUTHORIZED
```

The central instruction remained:

> **Do not chase a small field-energy number. Chase a physically consistent same-action theory that preserves why low field capacity was possible.**

By the end of the present slice this became even more specific:

> **Do not optimize the new \(2.656859\,\mathrm J\) field term downward. Preserve the protected A12 carrier and \(F^2\) metric mechanism while physicalizing the microscopic source.**

---

## Starting State

The preceding durable journal ended with the A10/A11 source-protection program.

The full regression had reached:

```text
1006 passed
```

The strongest conservative established true-stand-off result remained 006D.

Its finite-source coefficient remained:

```math
C_{006D}
=
23.591586299249
```

with:

```text
FINITE_RADIUS=
YES

FINITE_THICKNESS=
YES

POSITIVE_ENERGY=
YES

LOCAL_CONSERVATION_LINEARIZED_ORDER=
ESTABLISHED

NEC=
PASS

WEC=
PASS

DEC=
PASS

OUTWARD_NEAR_GRAVITY=
YES

POSITIVE_FAR_FIELD_ACTIVE_MASS=
YES
```

At the permanent \(1g/1m\) benchmark, direct GR still required approximately:

```text
E_006D_1G_1M≈3.1e29 J
```

Thus 006D remained the conservative model anchor but not a practical energy route.

No result in the present slice invalidated 006D.

---

# HOOK17 State Entering This Slice

The primary HOOK17 mechanism still rested on:

```text
V24
INTRINSIC DIRAC HYPERMOMENTUM

+

V26B1
NONLINEAR QUADRATIC SOURCE -> METRIC NUMERATOR

+

V26B1
ACTIVE / OFF-STATE SEPARATION

+

LOW CANONICAL FIELD-CAPACITY STRUCTURE
```

The old approximately \(17.0676\,\mathrm J\) HOOK17 number remained only a historical field-capacity reference.

It was not:

```text
COMPLETE ENERGY=
NO

FINAL 1G/1M NORMALIZED ENERGY=
NO

DEVICE ENERGY=
NO
```

The more advanced A10F2 result had subsequently established a properly normalized finite-payload reduced-EFT witness:

```text
PAYLOAD_MASS=
1 kg

TRUE_EXTERNAL_STANDOFF=
1.0 m

MIN_PAYLOAD_ACCELERATION=
9.80665 m/s^2

FIELD_LOADING_ENERGY≈
562,964.83 J

FIELD_PLUS_MODELED_ELECTRON_POSITRON_REST_PARTIAL≈
567,680.65 J
```

but its ordinary-Dirac microscopic realization required an unexplained ultralight-vector loop suppression of order:

```text
~2.35e-23
```

and therefore failed technical naturalness.

A11A had then closed several simple source-protection repairs while leaving genuinely different derivative, composite, and full Noether completions open.

The key problem entering the present slice was therefore:

```text
LOW-CAPACITY MACROSCOPIC RESPONSE EXISTS

BUT

MICROSCOPIC PROTECTED SOURCE / CARRIER COMPLETION
REMAINS UNRESOLVED
```

---

# 1. 032H17A11C — Exact Finite-Momentum Ordinary-Dirac Wheeler \(\rightarrow\) K2 Closeout

## Objective

A11B had already shown that the direct K2 Wheeler rest-density route fails at zero spatial momentum, but it left a potentially important loophole:

> Could nonzero momentum transfer or spin-textured free on-shell Dirac states satisfy the full K2 source constraints while retaining nonzero Wheeler torsion source?

A11C answered this exactly.

---

## 1.1 Breit-frame parameterization

The complete stationary equal-mass free on-shell particle and antiparticle transition class was expressed in the Breit frame using:

```math
z
=
\tanh\left(\frac{\eta}{2}\right)
```

with physical finite nonzero spacelike transfer domain:

```math
0<z<1
```

The exact energy and momentum-transfer ratios are:

```math
\frac{E}{m}
=
\frac{1+z^2}{1-z^2}
```

and:

```math
\frac{Q}{m}
=
\frac{4z}{1-z^2}.
```

The stationary free transition basis contained:

```text
4 positive-energy spin transitions

+

4 negative-energy spin transitions

=

8 complex transition directions
```

Positive/negative-frequency cross terms were excluded because they are time dependent and therefore not stationary source components.

---

## 1.2 Exact Wheeler source reconstruction

The full Wheeler torsion source operator was reconstructed for this complete class.

The reconstruction error was:

```text
MAX_RECONSTRUCTION_ERROR=
0.0
```

Thus the subsequent source-space theorem did not depend on an approximate bilinear reduction.

---

## 1.3 Necessary Ward condition versus full K2 constraints

A critical subtlety emerged.

The weaker necessary K2 Ward test did admit nonzero nonrest survivors.

The necessary-Ward system had:

```text
rank =
4

nullity =
4

source-image rank =
4
```

Therefore it would have been incorrect to conclude from the weaker Ward condition that all nonrest ordinary-Dirac K2 sources vanish.

The full K2 source conditions were then reconstructed.

They contain:

```text
24 pair-antisymmetric source components

20 pure-trace constraints

+1 current-conservation constraint

=

21 independent K2 source constraints
```

The full physical source problem was therefore stronger than the necessary-Ward prefilter.

---

## 1.4 Exact determinant witness

For the complete 8-dimensional free on-shell Dirac transition class, an exact \(8\times8\) witness determinant was obtained:

```math
D(z)
=
\frac{
z^2
(z-1)^4
(z+1)^4
(z^2+1)^2
}{
2592
}.
```

Inside the physical domain:

```math
0<z<1
```

there are no roots:

```text
PHYSICAL_DOMAIN_ROOTS=
[]
```

Therefore the full K2 source-constraint matrix has:

```text
rank =
8

nullity =
0
```

for every finite nonzero physical momentum transfer in the declared class.

Representative exact rank checks at:

```text
z=
1/10

1/4

1/2

3/4

9/10
```

all reproduced the full-rank result.

These sample points were validation only.

The continuum theorem is carried by the exact determinant.

---

## 1.5 Boundary points

At:

```text
z=0
```

the matrix has:

```text
rank =
6

nullity =
2
```

but this is:

```text
ZERO_MOMENTUM_TRANSFER_NO_STANDOFF_GRADIENT
```

and therefore is not a usable finite-gradient antigravity source.

At:

```text
z=1
```

the matrix has:

```text
rank =
4

nullity =
4
```

but this is the:

```text
INFINITE_RAPIDITY
INFINITE_Q_OVER_M
BOUNDARY
```

and is not a finite physical source loophole.

---

## 1.6 A11C decision

The exact decision was:

```text
RED_SCOPED_A11C_DIRECT_K2_ORDINARY_DIRAC_ONSHELL_WHEELER_ROUTE_CLOSED
```

Specifically:

```text
DIRECT_K2_ORDINARY_DIRAC_ONSHELL_WHEELER_ROUTE_CLOSED=
True

HEALTHY_POLE_EVALUATION_AUTHORIZED=
False

METRIC_GATE_AUTHORIZED=
False

PAYLOAD_GATE_AUTHORIZED=
False

ENERGY_OPTIMIZATION_AUTHORIZED=
False

HOOK17_CLOSED=
False
```

A11C closed:

> the complete tested free equal-mass on-shell ordinary-Dirac Wheeler \(\rightarrow\) K2 route at every finite nonzero physical momentum transfer.

It did not close:

* different microscopic K2 sources;
* derivative/composite sources;
* full Noether completions;
* interacting/bound-state Dirac states;
* other protected torsion/vector families;
* HOOK17.

Regression after A11C:

```text
1027 passed
```

---

# 2. 032H17A12A — Engineered Dirac Reopening of the Barker–Zell Iso-Weyl Source Channel

## Objective

A7 had previously examined the Barker–Zell iso-Weyl vector family using the historical clean HOOK17 source.

That source had given zero coupling to the relevant source channels.

However, A10A and A11B had subsequently produced engineered Dirac states with nonzero torsion response.

A12A therefore asked:

> Does the later spin-engineered Dirac source reopen the earlier A7 iso-Weyl vector channel?

---

## 2.1 Engineered source atlas

Four rest-pair states were evaluated:

```text
U1_V1
U1_V2
U2_V1
U2_V2
```

The historical clean pair remained:

```text
U1_V2
```

and continued to give zero full torsion norm in the relevant axial channel:

```text
J_T_HAT=
0
```

The engineered pairs instead gave:

```text
U1_V1:

J_T_HAT=
[0,+1,0,0]

NORM=
1
```

and:

```text
U2_V2:

J_T_HAT=
[0,-1,0,0]

NORM=
1
```

These were exact equal-and-opposite nonzero axial torsion sources.

The direct Weyl-trace source remained zero for all four tested rest pairs:

```text
J_Q=
0
```

---

## 2.2 Iso-Weyl source mixing

For the reduced iso-Weyl vector block, the effective propagating source has the structure:

```math
J_Q^{\rm eff}
=
J_Q
-
\frac{b_3}{2b_2}
J_{\hat T}.
```

Since:

```text
J_Q=
0
```

but:

```text
J_T_HAT != 0
```

for the engineered `U1_V1` and `U2_V2` states, any finite nonzero allowed mixing coefficient generates a nonzero propagating \(Q_\mu\) source.

Therefore:

```text
ENGINEERED_DIRAC_REOPENS_A7_T_HAT_CHANNEL=
True

FINITE_NONZERO_MIXING_REOPENS_EFFECTIVE_Q_SOURCE=
True
```

This was a genuine source-overlap reopening of A7.

The earlier zero-source result had depended on the historical source state rather than on a universal no-source theorem for the iso-Weyl family.

---

# 3. A12A Maxwell Ward Obstruction

The source reopening did not automatically produce a valid exact massless Maxwell source.

For an unmodified massive Dirac axial current:

```math
\partial_\mu j_5^\mu
=
2im
\bar\psi\gamma_5\psi
+
\text{quantum anomaly terms}.
```

Therefore:

```text
UNMODIFIED_MASSIVE_DIRAC_AXIAL_CURRENT_EXACTLY_CONSERVED=
False
```

A state-specific static vanishing divergence cannot replace an operator-level gauge identity.

Thus:

```text
DIRECT_ENGINEERED_DIRAC_IW_MAXWELL_BRANCH_CLOSED=
True
```

The generic iso-Weyl Proca branch avoids the Maxwell source Ward requirement but does not protect an ultralight meter-range mass.

A12A found:

```text
ELECTRON_MASS_OVER_METER_VECTOR_MASS=
2.5896050756169175e12
```

and:

```text
METER_SCALE_PROCA_MASS_TECHNICALLY_PROTECTED_BY_IW_ALONE=
False
```

The A10F2 loop coefficient was deliberately not transferred to the different A12A action.

A12A decision:

```text
YELLOW_A12A_ENGINEERED_DIRAC_REOPENS_BARKER_ZELL_IW_AXIAL_SOURCE
DIRECT_UNMODIFIED_MAXWELL_BRANCH_CLOSED_ON_AXIAL_CURRENT_WARD
METER_RANGE_PROCA_NOT_PROTECTED_BY_IW_ALONE
PROMOTE_ONLY_NEW_SAME_ACTION_SOURCE_PROTECTION
```

Regression:

```text
1037 passed
```

---

# 4. 032H17A12B — Concurrent-IW Enhanced-\(U(1)\) Same-Action Protected Massless Carrier

## Objective

A12A showed that repairing the axial current directly was unlikely to be the shortest route.

The Barker–Zell concurrent-symmetry vector sector contains a broader current structure, including an ordinary Dirac vector Noether current.

A12B therefore asked:

> Can the concurrent vector system admit an exact nonsingular massless gauge direction whose source rotates entirely onto a conserved massive-Dirac vector current?

This targeted both of the major A12A obstructions simultaneously:

```text
AXIAL MAXWELL WARD FAILURE

+

UNPROTECTED ULTRALIGHT PROCA MASS
```

---

## 4.1 Concurrent vector block

The relevant connection vectors were:

```text
Q_mu

T_hat_mu

Zc_mu
```

with algebraic mass/mixing block:

```math
\frac{\mathcal L_{\rm mass}}{M_P^2}
=
b_1 Q^2
+
b_2 \hat T^2
+
b_3 Q\cdot\hat T
+
b_4 Z_c^2
+
b_5 Z_c\cdot Q
+
b_6 Z_c\cdot\hat T.
```

For the nondynamical pair:

```math
y
=
\begin{pmatrix}
\hat T\\
Z_c
\end{pmatrix},
```

the Hessian is:

```math
H
=
\begin{pmatrix}
2b_2 & b_6\\
b_6 & 2b_4
\end{pmatrix}.
```

Its determinant is:

```math
\det H
=
4b_2b_4-b_6^2.
```

The physical branch requires:

```text
4*b2*b4-b6^2 != 0
```

so that source enhancement is not generated by a singular auxiliary sector.

---

## 4.2 Exact axial-decoupling condition

Eliminating the nondynamical vectors gives the exact \(\hat T\) admixture proportional to:

```math
\frac{
2b_3b_4-b_5b_6
}{
4b_2b_4-b_6^2
}.
```

Therefore the exact condition:

```math
2b_3b_4-b_5b_6
=
0
```

removes the axial-current contamination of the propagating massless direction.

---

## 4.3 Exact massless condition

After imposing axial decoupling, the effective propagating vector mass vanishes when:

```math
4b_1b_4-b_5^2
=
0.
```

Define:

```math
c
=
\frac{b_5}{2b_4}.
```

Then the exact null vector of the full vector mass matrix is:

```math
\delta
\begin{pmatrix}
Q\\
\hat T\\
Z_c
\end{pmatrix}
\propto
\begin{pmatrix}
1\\
0\\
-c
\end{pmatrix}.
```

This is an exact zero mode rather than a parametrically small eigenvalue.

---

## 4.4 Effective current

On this exact corridor the source reduces to:

```math
J_{\rm eff}
=
J_N
\frac{
2b_4-b_5
}{
2b_4
}
```

or equivalently:

```math
J_{\rm eff}
=
(1-c)J_N.
```

The projective-aligned boundary:

```text
c=1
```

causes the source to cancel.

Generic:

```text
c != 1
```

allows a nonzero source.

---

## 4.5 Exact conserved massive-Dirac current

The concurrent matter coupling contains:

```math
J_N
=
\frac{\alpha_{\rm nm}}{4}
\bar\psi\gamma^\mu\psi
-
2\beta_{\rm nm}J_M.
```

Taking:

```text
beta_nm=
0
```

leaves:

```math
J_N
=
\frac{\alpha_{\rm nm}}{4}
\bar\psi\gamma^\mu\psi.
```

This is the ordinary massive-Dirac vector Noether current and is classically conserved:

```math
\partial_\mu
\left(
\bar\psi\gamma^\mu\psi
\right)
=
0.
```

Thus the carrier no longer depends on the nonconserved axial current that killed A12A.

---

## 4.6 Enhanced gauge transformation

The massless direction was reconstructed from projective and iso-Weyl trace transformations.

The resulting exact gradient transformations are:

```math
\delta Q_\mu
=
\partial_\mu\lambda
```

```math
\delta Z_{c\mu}
=
-c\,\partial_\mu\lambda
```

```math
\delta Z_{{\rm ep}\mu}
=
(1-c)\partial_\mu\lambda
```

```math
\delta\hat T_\mu
=
0.
```

A compensating fermion phase transformation cancels the variation of the \(Z_{\rm ep}\cdot J_N\) coupling.

The fermion mass term is phase invariant.

Therefore the declared subfamily admits a classical same-action local \(U(1)\)-type gauge completion.

---

## 4.7 Holst-square consequence

The post-Riemannian Holst pseudoscalar contains a term proportional to:

```math
\hat T\cdot Z_{\rm ep}.
```

For generic:

```text
c != 1
```

the enhanced gauge transformation shifts this structure.

Therefore the Holst-squared interaction is not merely required to be numerically tiny.

It is forbidden by the exact enhanced gauge symmetry of the selected subfamily.

This is a stronger form of protection.

---

## 4.8 Exact rational witness

A particularly clean rational witness was:

```text
b1=
1/4

b2=
1

b3=
0

b4=
1

b5=
1

b6=
0

c=
1/2
```

The nondynamical Hessian is:

```math
H
=
\begin{pmatrix}
2&0\\
0&2
\end{pmatrix}
```

with:

```text
det(H)=
4

eigenvalues=
2,2
```

The full vector mass Hessian has eigenvalues:

```text
0,2,5/2
```

The null vector is exactly:

```text
(1,0,-1/2)
```

with zero residual.

The surviving vector-current fraction is:

```text
1/2
```

Therefore:

```text
GAIN_FROM_NEAR_SINGULAR_MIXING=
False

MASSLESSNESS_IS_EXACT_SYMMETRY_NULL=
True

ULTRALIGHT_PROCA_MASS_REQUIRED=
False
```

---

## 4.9 A12B decision

The exact decision was:

```text
GREEN_SCOPED_A12B_CONCURRENT_IW_ENHANCED_U1_
CLASSICAL_SAME_ACTION_MASSLESS_VECTOR_AND_
EXACT_DIRAC_VECTOR_CURRENT_CORRIDOR
```

This was the strongest carrier/protection result yet obtained in the HOOK17 rescue.

It established, within the declared classical subfamily:

```text
EXACT MASSLESS VECTOR=
YES

CLASSICAL GAUGE PROTECTION=
YES

EXACT MASSIVE-DIRAC VECTOR NOETHER CURRENT=
YES

SAME-ACTION MATTER VARIATION CANCELLATION=
YES

NONDYNAMICAL SECTOR NONSINGULAR=
YES

NEAR-SINGULAR GAIN=
NO

ULTRALIGHT PROCA MASS REQUIRED=
NO
```

It did not establish:

* full Standard Model anomaly completion;
* canonical physical source strength;
* direct payload-force silence;
* empirical consistency;
* a gauge-invariant universal metric;
* finite-payload response;
* complete energy.

Regression:

```text
1048 passed
```

---

# 5. Consequence of Exact Gauge Symmetry — Old \(Q_\mu Q_\nu\) Metric No Longer Transferable

A12B solved the carrier problem by making the vector exactly gauge protected.

That simultaneously invalidated direct reuse of the earlier A10 vector-potential metric.

A physical metric containing:

```math
Q_\mu Q_\nu
```

is not invariant under:

```math
Q_\mu
\rightarrow
Q_\mu
+
\partial_\mu\lambda.
```

Therefore the A10 metric scaffold could not simply be attached to A12B.

This forced a genuine change in the HOOK17 physical-metric realization.

The new metric had to be built from gauge-invariant quantities.

---

# 6. 032H17A12C — Gauge-Invariant \(F_X^2\) Physical Metric

## Objective

A12C asked:

> Can the exact A12B massless gauge field produce the required universal physical-metric response through a gauge-invariant field-strength portal, and can that response retain the low-capacity HOOK17 advantage for a finite neutral payload?

The minimal magnetostatic invariant considered was:

```math
F_X^{\mu\nu}F^X_{\mu\nu}.
```

The physical metric was taken in conformal form:

```math
g^{\rm phys}_{\mu\nu}
=
e^{2\sigma}
g_{\mu\nu}
```

with:

```math
\sigma
=
\frac{
F_X^{\mu\nu}F^X_{\mu\nu}
}{
2M_X^4
}.
```

For a purely magnetic static background:

```math
F_X^2
=
2B_X^2
```

so:

```math
\sigma
=
\frac{
B_X^2
}{
M_X^4
}.
```

Properties:

```text
GAUGE_INVARIANT=
YES

OFF-STATE FIRST VARIATION ZERO=
YES

ACTIVE-STATE RESPONSE=
NONZERO

OUTWARD SIGN POSSIBLE=
YES

INVARIANT TIDAL RESPONSE POSSIBLE=
YES
```

---

# 7. A12C Finite \(1\,\mathrm{kg}\) / \(1\,\mathrm m\) BVP

## 7.1 Geometry

The A10F1 compact source morphology was reused:

```math
J_\phi
=
J_0
\frac{\rho}{R_s}
\left(
1-\frac{r^2}{R_s^2}
\right)^2
```

inside:

```text
R_s=
2.0 m
```

The current is divergence-free by axisymmetry.

Payload:

```text
MASS=
1 kg

KIND=
TORUS

MAJOR_RADIUS=
1.0 m

MINOR_RADIUS=
0.2 m

TRUE_EXTERNAL_SURFACE_GAP=
1.0 m
```

---

## 7.2 Canonical massless field equation

The azimuthal potential satisfies the canonical massless axisymmetric equation:

```math
\left[
-\partial_\rho^2
-
\frac{1}{\rho}\partial_\rho
-
\partial_z^2
+
\frac{1}{\rho^2}
\right]
A_\phi
=
J_\phi.
```

Using:

```math
u
=
\sqrt{\rho}\,A_\phi
```

gives the sparse operator:

```math
-\partial_\rho^2u
-
\partial_z^2u
+
\frac{3}{4\rho^2}u.
```

The source amplitude was rescaled until the least-accelerated resolved payload point reached exactly:

```text
9.80665 m/s^2
```

---

# 8. A12C \(1g/1m\) Result

At reference metric scale:

```text
REFERENCE_PORTAL_SCALE_EV=
1000.0
```

the production solve gave:

```text
PAYLOAD_LOCAL_ACCELERATION_MIN_M_S2=
9.80665

PAYLOAD_LOCAL_ACCELERATION_MAX_M_S2=
22.889174637504706

PAYLOAD_COM_ACCELERATION_M_S2=
15.042170908175194

TRUE_EXTERNAL_STANDOFF_M=
1.0
```

Therefore:

```text
STRICT_WHOLE_PAYLOAD_1G_PASS=
True
```

The field produced a nonzero invariant tidal response.

This is a genuine reduced-EFT finite-payload stand-off witness.

---

# 9. A12C Field Capacity

The canonical field energy was:

```text
REFERENCE_FIELD_ENERGY_J=
2.6568591420597114
```

The corresponding source-work integral was:

```text
REFERENCE_SOURCE_WORK_J=
2.6577816871671844
```

with relative mismatch:

```text
REFERENCE_SOURCE_WORK_RELATIVE_ERROR=
0.000347231470750019
```

or approximately:

```text
0.0347 %
```

The integrated canonical source was:

```text
INTEGRATED_CANONICAL_SOURCE_EV_M=
20464005.700253956
```

The result therefore satisfies a strong internal field/source-work consistency check.

---

# 10. Numerical Convergence

A12C was deliberately treated as a high-margin preflight BVP rather than the final promotion-quality loaded solve.

Production grid:

```text
GRID_SPACING=
0.05 m
```

Coarse grid:

```text
GRID_SPACING=
0.075 m
```

Grid-energy relative difference:

```text
0.04949715194585902
```

or approximately:

```text
4.95 %
```

Grid-source relative difference:

```text
0.025265661326387297
```

or approximately:

```text
2.53 %
```

Larger-domain energy relative difference:

```text
0.005234400965629043
```

or approximately:

```text
0.523 %
```

The field/source-work mismatch remained much smaller:

```text
~3.47e-4
```

Therefore:

```text
BVP_PREFLIGHT_CONVERGENCE_PASS=
True

PROMOTION_QUALITY_PRECISION_BVP=
False
```

Loaded matter backreaction was not included.

The result is sufficient to freeze the mechanism reference but not sufficient to certify a final physical model.

---

# 11. A12C Improvement Relative to A10F2

A10F2 field/loading reference:

```text
562964.8297553215 J
```

A12C field reference:

```text
2.6568591420597114 J
```

The improvement factor is:

```text
211891.10888238018
```

Thus the A12C field term is over:

```text
2.1e5
```

times smaller than A10F2's already-sub-10-MJ field/loading term.

This is the lowest current project field-capacity result tied to:

* the final \(1g\) floor;
* a true \(1\,\mathrm m\) external stand-off;
* a finite \(1\,\mathrm{kg}\) neutral payload;
* one declared physical metric;
* a protected exact massless carrier;
* a finite numerical BVP.

The field term is now far below the project's approximately \(100\,\mathrm J\) threshold below which further efficiency optimization is not useful.

Therefore:

```text
FIELD_EFFICIENCY_OPTIMIZATION_AUTHORIZED=
False
```

The scientific bottleneck has moved decisively away from field capacity.

---

# 12. A12C Ordinary Neutral-Matter Source Theorem

A11A had already established that a renormalizable vector charge acting on stable ordinary:

```text
electron
proton
neutron
```

number currents is exactly silent on every neutral atomic composition only if:

```math
q_n
=
0
```

and:

```math
q_p
=
-q_e.
```

Therefore the stable ordinary payload-silent source direction is one-dimensional and EM-like up to overall normalization.

A12C reused this theorem.

---

# 13. Important Exactly-Massless \(U(1)\) Correction

The older A11A analysis had compared a separate ultralight vector against independent-vector electron \(g-2\) bounds.

That interpretation cannot be transferred blindly to the A12B exact-massless proportional-current corridor.

For two exactly massless canonical \(U(1)\) fields coupled to the same current:

```math
e_0 A_\mu J^\mu
+
g_X X_\mu J^\mu,
```

an orthogonal transformation produces:

```text
ONE COUPLED PHOTON-LIKE DIRECTION

+

ONE STERILE DIRECTION
```

with coupled field:

```math
A_{\rm phys}
=
\frac{
e_0 A
+
g_X X
}{
\sqrt{e_0^2+g_X^2}
}.
```

Therefore:

```text
A11A_MASSIVE_VECTOR_GMINUS2_BOUND_REUSED=
False
```

for the exact A12B proportional-current massless branch.

However, the universal metric portal makes the field orientation physically observable.

For the EM-like source realization, the A12C geometric field becomes an effective physical-electromagnetic \(F^2\) metric portal.

That produces a different and much more direct empirical test.

---

# 14. A12C Ambient Magnetic-Field Empirical Falsifier

The field-only strict-10-MJ ceiling gives the most favorable possible effective metric scale because adding source/support energy can only reduce the allowed scale.

That maximally favorable scale was:

```text
FIELD_ONLY_10MJ_MAX_M_EM_EV=
44046.14127845582
```

The corresponding payload magnetic field required for \(1g\) is only:

```text
B_MIN=
0.08051312858475622 T

B_MAX=
0.11588214224843833 T
```

The minimum required field-squared gradient is:

```text
GRAD_B2_1G=
0.010761469691625843 T^2/m
```

A documented MRI-related nonclinical environment used in the sanity test had:

```text
B=
1.7 T

GRAD_B=
4.7 T/m
```

giving:

```math
\nabla(B^2)
=
2B\nabla B
=
15.98\ {\rm T^2/m}.
```

The ratio is:

```text
FDA_GRAD_B2_OVER_CANDIDATE_1G_GRADIENT=
1484.927287620855
```

Therefore the minimal universal \(F^2\) metric aligned with ordinary electromagnetism would predict approximately:

```text
1484.927287620855 g
```

of universal acceleration in an already-realized magnetic-gradient environment.

The reported deflection in that environment was less than:

```text
2 degrees
```

rather than the near-horizontal universal deflection implied by the candidate portal.

This is not treated as a dedicated precision gravity experiment.

It is a high-margin empirical sanity falsifier.

---

# 15. A12C Decision

A12C therefore produced a split result.

The low-capacity field mechanism passed:

```text
GAUGE_INVARIANT_MASSLESS_F2_REDUCED_EFT_1G_1M_WITNESS=
True
```

The ordinary EM-like microscopic realization failed:

```text
ORDINARY_EM_LIKE_MINIMAL_F2_A12B_REALIZATION_CLOSED=
True
```

But:

```text
A12B_EXACT_MASSLESS_CARRIER_CLOSED=
False

NONORDINARY_SOURCE_A12B_COMPLETION_CLOSED=
False

ALL_FIELD_STRENGTH_METRIC_PORTALS_CLOSED=
False

HOOK17_CLOSED=
False
```

The exact decision was:

```text
RED_SCOPED_A12C_A12B_MASSLESS_CARRIER_AND_
GAUGE_INVARIANT_F2_METRIC_HAVE_A_FINITE_1G_1M_
REDUCED_EFT_WITNESS__BUT_STABLE_ORDINARY_
PAYLOAD_SILENT_SOURCE_IS_EM_LIKE_AND_THE_RESULTING_
MINIMAL_F2_PHOTON_ALIGNED_PORTAL_FAILS_EXISTING_
MRI_FIELD_GRADIENT_SANITY_BY_ORDERS_OF_MAGNITUDE__
PIVOT_SOURCE_OR_FAMILY
```

Regression:

```text
1058 passed
```

---

# 16. Scientific Interpretation of A12C

A12C must not be summarized as:

```text
THE 2.66-J MODEL FAILED
```

The correct decomposition is:

```text
A12B PROTECTED MASSLESS CARRIER=
GREEN SCOPED

A12C GAUGE-INVARIANT F2 METRIC=
GREEN SCOPED

A12C FINITE 1KG / 1M / >=1G FIELD RESPONSE=
GREEN SCOPED

A12C ORDINARY EM-LIKE SOURCE=
RED

PHYSICAL COMPLETE MODEL=
NOT YET
```

This distinction became the central strategic result of the session.

The source realization failed.

The protected low-capacity carrier/metric mechanism survived.

---

# 17. Strategic Pivot After A12C

Because:

```text
FIELD ENERGY=
2.6568591420597114 J
```

the project no longer has any rational reason to optimize field efficiency before source physicalization.

The remaining `<10 MJ` budget is orders of magnitude larger than the field-capacity term.

The high-value question became:

> **Can another exact conserved, source-selective microscopic current produce the required canonical field without violating UV, empirical, support, reaction, or complete-energy constraints?**

The candidate space therefore shifted from:

```text
CARRIER / METRIC SEARCH
```

toward:

```text
SOURCE / COMPLETION PHYSICALIZATION
```

while preserving the A12B/A12C backbone.

---

# 18. 032H17A12D0 — Low-Capacity \(F^2\) Mechanism-Preserving Completion Atlas

## Objective

A12D0 was deliberately chosen as the final physics run of the session.

Rather than test only one microscopic source, it asked two broader questions:

1. **How much of the low-order gauge-invariant physical-metric space has already been exhausted by A12C?**

2. **Which microscopic source families can preserve the A12B/A12C low-capacity mechanism and should be tested first?**

A12D0 did not rerun or optimize the already-sub-\(100\,\mathrm J\) BVP.

It froze the successful A12C kernel and constructed theorem-first field-sector and source-completion atlases.

---

# 19. A12D0 Frozen Low-Capacity Reference

The A12C reference was frozen as:

```text
REFERENCE_ID=
A12C_MASSLESS_F2_1G_1M
```

with:

```text
REFERENCE_KERNEL_FROZEN=
True

REFERENCE_PORTAL_SCALE_EV=
1000.0

REFERENCE_FIELD_ENERGY_J=
2.6568591420597114

REFERENCE_SOURCE_WORK_J=
2.6577816871671844

REFERENCE_SOURCE_WORK_RELATIVE_ERROR=
0.000347231470750019

REFERENCE_PAYLOAD_MASS_KG=
1.0

REFERENCE_STANDOFF_M=
1.0

REFERENCE_PAYLOAD_MIN_ACCEL_M_S2=
9.80665

REFERENCE_PAYLOAD_MAX_ACCEL_M_S2=
22.889174637504706

REFERENCE_PAYLOAD_COM_ACCEL_M_S2=
15.042170908175194

FIELD_ENERGY_IMPROVEMENT_VS_A10F2=
211891.10888238018
```

The stored internal claim was deliberately narrow:

```text
LOWEST_CURRENT_PROJECT_NORMALIZED_FINITE_PAYLOAD_
FIELD_CAPACITY_REFERENCE_AT_DECLARED_PORTAL_NORMALIZATION
```

A12D0 also recorded:

```text
FIELD_TERM_BELOW_100J=
True

FIELD_EFFICIENCY_OPTIMIZATION_AUTHORIZED=
False

COMPLETE_ENERGY_ESTABLISHED=
False

CERTIFIED_SUB10MJ_MODEL=
False

PHYSICAL_ANTIGRAVITY_MODEL=
False
```

---

# 20. A12D0 Exact Quadratic Field-Strength Metric Basis

## 20.1 Declared theorem scope

The tensor-basis result is exhaustive only within:

```text
FOUR DIMENSIONS

ONE ABELIAN FIELD STRENGTH F

LOCAL

ALGEBRAIC

SYMMETRIC RANK-TWO TENSOR

QUADRATIC IN F

NO DERIVATIVES

NO CURVATURE

NO ADDITIONAL FIELDS
```

Within this scope, the parity-even rank-two basis is:

```math
g_{\mu\nu}
F_{\alpha\beta}F^{\alpha\beta}
```

and:

```math
F_{\mu\alpha}
F_\nu{}^\alpha.
```

---

## 20.2 Parity-odd identity

The apparent independent parity-odd tensor satisfies:

```math
F_{\mu\alpha}
\widetilde F_\nu{}^\alpha
=
\frac14
g_{\mu\nu}
F_{\alpha\beta}
\widetilde F^{\alpha\beta}.
```

The symbolic identity passed exactly:

```text
ODD_RANK2_IDENTITY_PASS=
True
```

Thus there is only one independent parity-odd quadratic rank-two structure.

---

## 20.3 Dual-dual identity

The second apparent tensor redundancy is:

```math
\widetilde F_{\mu\alpha}
\widetilde F_\nu{}^\alpha
=
F_{\mu\alpha}
F_\nu{}^\alpha
-
\frac12
g_{\mu\nu}
F_{\alpha\beta}F^{\alpha\beta}.
```

This identity also passed exactly:

```text
DUALDUAL_IDENTITY_PASS=
True
```

Therefore:

```text
PARITY_EVEN_INDEPENDENT_DIMENSION=
2

PARITY_ODD_INDEPENDENT_DIMENSION=
1
```

and:

```text
QUADRATIC_METRIC_BASIS_EXHAUSTIVE_WITHIN_DECLARED_SCOPE=
True
```

---

# 21. Pure Magnetostatic Consequence

For a pure magnetostatic configuration:

```math
F_{0i}
=
0.
```

The disformal tensor then gives:

```math
\left(
F_{\mu\alpha}F_\nu{}^\alpha
\right)_{00}
=
0.
```

Also:

```math
F_{\alpha\beta}
\widetilde F^{\alpha\beta}
=
0
```

for pure \(B\).

Therefore the disformal quadratic term cannot create the leading static \(g_{00}\) response required for the rest-payload acceleration in this sector.

The parity-odd term also vanishes.

Thus within the declared local algebraic quadratic one-\(F\) basis:

```text
PURE_MAGNETOSTATIC_USEFUL_STATIC_G00_BASIS_DIMENSION=
1
```

and the unique useful structure is:

```math
g_{\mu\nu}
F_{\alpha\beta}F^{\alpha\beta}.
```

This is exactly the A12C conformal \(F^2\) portal.

Therefore A12C did not merely select one convenient metric from a large arbitrary quadratic magnetostatic family.

Within the explicitly declared basis, it already exhausted the useful static \(g_{00}\) direction up to normalization.

---

# 22. Scope of the Metric-Basis Result

The theorem does not close:

```text
DERIVATIVE METRIC DESCENDANTS

CURVATURE PORTALS

F^4 / F^6 / HIGHER POWERS

MULTIPLE FIELD STRENGTHS

COMPENSATOR-DEPENDENT PORTALS

NONLOCAL STRUCTURES

ELECTROSTATIC BACKGROUNDS

MIXED ELECTRIC / MAGNETIC BACKGROUNDS
```

Therefore:

```text
METRIC_BASIS_GLOBALLY_EXHAUSTED=
False
```

The result is powerful because it exhausts the most immediately relevant low-order pure-magnetostatic algebraic space without overclaiming the rest.

---

# 23. A12D0 Field-Sector Portal Atlas

The declared quadratic field-sector atlas contains:

```text
FIELD_SECTOR_PORTAL_COUNT=
9
```

with:

```text
STRUCTURALLY_OPEN_PORTAL_COUNT=
6
```

---

## 23.1 Pure magnetostatic sector

### Conformal \(F^2\)

```text
MAGNETOSTATIC_CONFORMAL_F2
```

Status:

```text
ESTABLISHED_A12C_REFERENCE
```

This is the frozen \(2.656859\,\mathrm J\) kernel.

### Disformal \(FF\)

```text
MAGNETOSTATIC_DISFORMAL_FF
```

Status:

```text
CLOSED_ZERO_G00_FOR_PURE_B_STATIC_REST_PAYLOAD
```

### Parity-odd

```text
MAGNETOSTATIC_PARITY_ODD
```

Status:

```text
CLOSED_F_DUALF_ZERO_FOR_PURE_B
```

Thus the pure magnetostatic local quadratic metric space is effectively reduced to the already successful A12C conformal direction.

---

## 23.2 Pure electrostatic sector

Open:

```text
ELECTROSTATIC_CONFORMAL_F2
```

and:

```text
ELECTROSTATIC_DISFORMAL_FF
```

The pure-electric parity-odd invariant vanishes.

These branches require new BVPs only if a surviving microscopic source naturally favors the electric sector.

---

## 23.3 Mixed electric/magnetic sector

Open:

```text
MIXED_EB_CONFORMAL_F2

MIXED_EB_DISFORMAL_FF

MIXED_EB_PARITY_ODD
```

The parity-odd branch becomes nontrivial when:

```math
\mathbf E\cdot\mathbf B
\neq
0.
```

These branches were preserved for later use but were not promoted above source physicalization of the already successful magnetostatic kernel.

---

# 24. A12D0 Source-Completion Atlas

A12D0 represented:

```text
SOURCE_FAMILY_COUNT=
12
```

Of these:

```text
OPEN_OR_RESEARCHABLE_SOURCE_FAMILY_COUNT=
8
```

Crossing source families with applicable field-sector portals gave:

```text
COMPLETION_COMBINATION_COUNT=
78
```

of which:

```text
OPEN_COMPLETION_COMBINATION_COUNT=
35
```

remain structurally open.

The atlas score is a deterministic research-management ranking only.

It is not a probability.

---

# 25. Permanent Closed Source Routes Preserved in A12D0

## 25.1 Ordinary EM-like vector current

Status:

```text
CLOSED_A12C_EMPIRICAL_PHOTON_ALIGNMENT
```

The minimal ordinary EM-like \(F^2\) realization must not be reopened by parameter tuning.

---

## 25.2 Direct ordinary-Dirac Wheeler \(\rightarrow\) K2

Status:

```text
CLOSED_A11C_ALL_FINITE_NONZERO_ONSHELL_TRANSFER_IN_DECLARED_CLASS
```

The exact determinant theorem must not be replaced by another finite-momentum scan.

---

## 25.3 Unmodified iso-Weyl axial Dirac current

Status:

```text
CLOSED_A12A_MAXWELL_WARD
```

The nonconserved massive-Dirac axial source must not be reintroduced into the exact massless Maxwell branch without a genuinely new Noether/source completion.

---

# 26. Highest-Ranked Open Source Completion — Electron Pauli / Magnetization

The top-ranked source family after all existing closures was:

```text
ELECTRON_PAULI_MAGNETIZATION
```

paired with:

```text
MAGNETOSTATIC_CONFORMAL_F2
```

and:

```text
EXACT_A12C_KERNEL_REUSE=
True
```

Its ranking score was:

```text
150
```

with mandatory qualification:

```text
NOT_A_PROBABILITY
```

---

## 26.1 Why Pauli/magnetization was promoted

A11A had already shown that a compact magnetization profile can reproduce the exact desired azimuthal source morphology.

The derivative current is identically conserved.

Therefore the Pauli source has several unusually strong features:

```text
EXACT CURRENT CONSERVATION

EXACT A12C SOURCE-SHAPE COMPATIBILITY

SOURCE SELECTIVITY THROUGH SPIN / DERIVATIVE STRUCTURE

NO NEED FOR ORDINARY RENORMALIZABLE EM-LIKE CHARGE

EXACT REUSE OF THE CONVERGED A12C MAGNETOSTATIC KERNEL
```

The old A11A Pauli energy rejection had been derived against the much more expensive A10F2 source requirement.

Because A12C reduced the field-capacity requirement by over \(2.1\times10^5\), that earlier source-energy closeout cannot automatically be transferred.

Therefore:

```text
OLD_A11A_ENERGY_CLOSEOUT_TRANSFERS_AUTOMATICALLY=
False
```

The Pauli lane must be reevaluated against the new A12C canonical source requirement.

---

# 27. Other High-Ranked Open Source Families

Second-ranked:

```text
COMPOSITE_ATOMIC_NUCLEAR_MAGNETIZATION
+
MAGNETOSTATIC_CONFORMAL_F2
```

with score:

```text
110
```

This route may benefit from identically conserved effective magnetization current structure but requires microscopic current-per-joule reconstruction.

Third-ranked:

```text
FULL_NOETHER_SOURCE_SELECTIVE_VECTOR
+
MAGNETOSTATIC_CONFORMAL_F2
```

with score:

```text
105
```

This is potentially the strongest theory-level alternative because an explicit same-action source-selective Noether current could preserve exact gauge conservation while avoiding ordinary payload charge.

The same Noether source family also remains open in:

```text
ELECTROSTATIC_CONFORMAL_F2

ELECTROSTATIC_DISFORMAL_FF

MIXED_EB_CONFORMAL_F2

MIXED_EB_DISFORMAL_FF

MIXED_EB_PARITY_ODD
```

each with lower atlas priority because they require a new field solve rather than direct A12C kernel reuse.

Other open classes include:

```text
KNOWN_PARTICLE_FLAVOR_SELECTIVE_VECTOR

INTERACTING_BOUND_STATE_DIRAC

PSEUDOTENSOR_DERIVATIVE_CURRENT

CURVATURE_OR_COMPOSITE_SOURCE_CURRENT

NEUTRINO_VECTOR_CURRENT
```

Unobserved hidden/exotic matter was explicitly retained as:

```text
NOT_AUTHORIZED_BY_CURRENT_PROJECT_POLICY
```

and was not allowed to win the source-selection problem merely by being convenient.

---

# 28. A12D0 Decision

The exact final-session decision was:

```text
GREEN_SCOPED_A12D0_LOW_CAPACITY_F2_MECHANISM_FROZEN
LOCAL_QUADRATIC_ONE_F_METRIC_BASIS_EXHAUSTED_IN_DECLARED_SCOPE
PURE_MAGNETOSTATIC_STATIC_G00_PORTAL_UNIQUE
SOURCE_COMPLETION_ATLAS_PROMOTES_PAULI_MAGNETIZATION_FIRST
```

Key flags:

```text
LOW_CAPACITY_MECHANISM_REFERENCE_PRESERVED=
True

A12B_EXACT_MASSLESS_CARRIER_PRESERVED=
True

A12C_F2_METRIC_MECHANISM_PRESERVED=
True

A12C_ORDINARY_EM_LIKE_ROUTE_REMAINS_CLOSED=
True

METRIC_BASIS_GLOBALLY_EXHAUSTED=
False

SOURCE_SPACE_GLOBALLY_EXHAUSTED=
False

NEW_PHYSICS_DISCOVERY_CLAIM=
False

PROJECT_LEVEL_MECHANISM_RESULT=
True

COMPLETE_ENERGY_ESTABLISHED=
False

PHYSICAL_ANTIGRAVITY_MODEL_FOUND=
False

CERTIFIED_SUB10MJ_MODEL_FOUND=
False

PRACTICAL_DEVICE_FOUND=
False

FIELD_EFFICIENCY_OPTIMIZATION_AUTHORIZED=
False

NEW_BVP_AUTHORIZED_IMMEDIATELY=
False
```

---

# 29. Regression Progression

The completed chain advanced the full regression as follows:

```text
PRE-A11C=
1006 passed

A11C=
1027 passed

A12A=
1037 passed

A12B=
1048 passed

A12C=
1058 passed

A12D0=
1080 passed
```

The final A12D0 run reported:

```text
COMPILE_RC=
0

TARGETED=
12 passed

TARGET_RC=
0

SIM_RC=
0

FULL=
1080 passed

FULL_RC=
0

FINAL_RUN_RC=
0
```

This is the new regression baseline.

Any subsequent session failing to reproduce approximately this state must first investigate repository or artifact divergence before drawing new physics conclusions.

---

# 30. Implementation and Result Artifacts

## A11C

```text
src/antigravity_research/agminer/
    hook17_k2_onshell_dirac_bilinear.py

simulations/
    032h17a11c_hook17_k2_onshell_dirac_bilinear_gate.py

tests/agminer/
    test_032h17a11c_hook17_k2_onshell_dirac_bilinear.py

results/data/
    032h17a11c_hook17_k2_breit_rank_checks.csv
    032h17a11c_hook17_k2_onshell_dirac_bilinear_summary.json
```

## A12A

```text
src/antigravity_research/agminer/
    hook17_iw_engineered_axial_source.py

simulations/
    032h17a12a_hook17_iw_engineered_axial_source_gate.py

tests/agminer/
    test_032h17a12a_hook17_iw_engineered_axial_source.py

results/data/
    032h17a12a_hook17_iw_engineered_axial_source_summary.json
    032h17a12a_hook17_iw_engineered_source_atlas.csv
    032h17a12a_hook17_protected_source_rerank.csv
```

## A12B

```text
src/antigravity_research/agminer/
    hook17_concurrent_iw_exact_source.py

simulations/
    032h17a12b_hook17_concurrent_iw_exact_source_gate.py

tests/agminer/
    test_032h17a12b_hook17_concurrent_iw_exact_source.py

results/data/
    032h17a12b_hook17_concurrent_iw_corridor_witness.csv
    032h17a12b_hook17_concurrent_iw_exact_source_summary.json
```

## A12C

```text
src/antigravity_research/agminer/
    hook17_concurrent_u1_fieldstrength_metric.py

simulations/
    032h17a12c_hook17_concurrent_u1_fieldstrength_metric_gate.py

tests/agminer/
    test_032h17a12c_hook17_concurrent_u1_fieldstrength_metric.py

results/data/
    032h17a12c_hook17_concurrent_u1_fieldstrength_metric_summary.json
    032h17a12c_hook17_f2_portal_scale_scan.csv
```

## A12D0

```text
src/antigravity_research/agminer/
    hook17_low_capacity_f2_completion_atlas.py

simulations/
    032h17a12d0_hook17_low_capacity_f2_completion_atlas.py

tests/agminer/
    test_032h17a12d0_hook17_low_capacity_f2_completion_atlas.py

results/data/
    032h17a12d0_hook17_low_capacity_f2_completion_atlas_summary.json
    032h17a12d0_hook17_low_capacity_reference.csv
    032h17a12d0_hook17_quadratic_metric_sector_atlas.csv
    032h17a12d0_hook17_source_completion_atlas.csv
    032h17a12d0_hook17_completion_combination_atlas.csv
```

Exploratory Pauli/A12D files also existed in the worktree at session close.

Those should be inspected before promoting any standalone A12D result.

The formal completed closeout authority for this journal is A12D0.

---

# 31. Updated Energy Hierarchy

The project now contains three particularly important energy regimes.

## 31.1 006D

At \(1g/1m\):

```text
~3e29 J
```

Strong conservative physics.

Completely impractical energy.

---

## 31.2 A10F2

Finite-payload reduced-EFT result:

```text
FIELD_LOADING≈
0.563 MJ

FIELD_PLUS_MODELED_ELECTRON_POSITRON_REST_PARTIAL≈
0.568 MJ
```

Excellent compared with GR.

Microscopic realization blocked on naturalness.

---

## 31.3 A12C / A12D0

Finite-payload field-capacity result:

```text
FIELD=
2.6568591420597114 J

SOURCE_WORK=
2.6577816871671844 J
```

This is vastly below the strict \(10\,\mathrm{MJ}\) policy.

However:

```text
MICROSCOPIC SOURCE COMPLETE ENERGY=
UNKNOWN

SUPPORT=
UNKNOWN

CONTROL=
UNKNOWN

UV COST=
UNKNOWN

BACKREACTION=
NOT FULLY INCLUDED

COMPLETE OPERATING ENERGY=
UNKNOWN
```

The scientific problem is now whether source/support/UV costs can remain within the enormous nominal energy headroom.

---

# 32. Has A12 Replaced 006D?

No.

The project state is:

```text
006D=
STRONGEST CONSERVATIVE ESTABLISHED TRUE-STANDOFF MODEL

A12B+A12C+A12D0=
STRONGEST LOW-CAPACITY SUCCESSOR MECHANISM
```

A12 has substantially stronger practicality potential but weaker current microscopic completeness.

For A12 to succeed 006D as the project's primary antigravity model it must still obtain:

```text
MICROSCOPIC SOURCE EXISTENCE

SAME-ACTION SOURCE/CARRIER/METRIC PROVENANCE

EXACT WARD / GAUGE IDENTITIES

CANONICAL NORMALIZATION

SOURCE -> HEALTHY MODE OVERLAP

UNIVERSAL METRIC RESPONSE

FIELD-REDEFINITION INVARIANCE

FINITE SOURCE SUPPORT

REACTION / CONSERVATION

STABILITY

NONLINEAR BACKREACTION

TECHNICAL NATURALNESS

QUANTUM / RG / UV CONTROL

EMPIRICAL CONSISTENCY

ACTIVATION / OFF-STATE CONSISTENCY

HIGHER-QUALITY LOADED FINITE-PAYLOAD CONVERGENCE

COMPLETE CONSERVATIVE OPERATING ENERGY <10 MJ
```

Until those are established:

```text
006D REMAINS THE CONSERVATIVE ANCHOR.
```

---

# 33. New Primary Research Program

The primary active program is now:

```text
PHYSICALIZE THE A12B/A12C LOW-CAPACITY BACKBONE
```

Freeze:

```text
A12B
EXACT MASSLESS PROTECTED CARRIER

+

A12C
GAUGE-INVARIANT MAGNETOSTATIC F2 PHYSICAL METRIC

+

A12C
FINITE 1KG / 1M / WHOLE-PAYLOAD >=1G BVP

+

A12D0
2.656859-J FIELD-CAPACITY REFERENCE
```

Do not casually modify the carrier or field geometry.

The dominant search variable is now the microscopic source completion.

---

# 34. Immediate Next Branch

The highest-ranked next branch is:

```text
032H17A12D1
PAULI_MAGNETIZATION_SOURCE_ENERGY_
UV_RADIATIVE_MIXING_AND_EMPIRICAL_GATE
```

The purpose is not to rerun the field BVP.

The purpose is to determine whether an exact conserved electron Pauli/magnetization current can produce the frozen A12C canonical source while remaining physically admissible.

A12D1 must test:

```text
CANONICAL SOURCE NORMALIZATION

SOURCE REST / POLARIZATION ENERGY

UV COMPLETION

WILSON-COEFFICIENT NATURALNESS

RADIATIVE PHOTON KINETIC MIXING

STELLAR / ASTROPHYSICAL CONSTRAINTS

POLARIZED-SPIN LABORATORY CONSTRAINTS

SOURCE SUPPORT / DEPOLARIZATION

ORDINARY MAGNETIC FIELD COST

REACTION / TORQUE

CONTROL / RESET

STRICT PARTIAL <10 MJ PREFLIGHT
```

Only if these survive should a loaded same-action finite-payload BVP be authorized.

---

# 35. If Pauli/Magnetization Fails

Do not resume broad random search.

Proceed through the A12D0 ranked completion atlas.

Priority sequence:

```text
1.
COMPOSITE ATOMIC / NUCLEAR MAGNETIZATION

2.
FULL SOURCE-SELECTIVE NOETHER CURRENT

3.
INTERACTING / BOUND-STATE DIRAC CURRENT

4.
KNOWN-PARTICLE FLAVOR-SELECTIVE VECTOR CURRENT

5.
PSEUDOTENSOR DERIVATIVE CURRENT

6.
CURVATURE / COMPOSITE SOURCE CURRENT

7.
NEUTRINO CURRENT IF SOURCE-POWER PREFLIGHT IS NOT FATAL
```

Do not reopen:

```text
ORDINARY EM-LIKE MINIMAL F2

DIRECT FREE ORDINARY-DIRAC K2

UNMODIFIED MASSIVE-DIRAC AXIAL IW MAXWELL
```

without a genuinely new theoretical ingredient.

---

# 36. Permanent Stop Rules After A12D0

## Field-capacity stop rule

The field term is already:

```text
2.656859 J
```

Therefore:

```text
DO_NOT_OPTIMIZE_FIELD_CAPACITY
```

solely to make it smaller.

---

## Source-energy stop rule

If an optimistic source/support lower bound alone reaches:

```text
>=10 MJ
```

close that realization.

Do not compensate by trying to reduce the field from \(2.66\,\mathrm J\) to \(1\,\mathrm J\).

---

## Ward stop rule

If a candidate microscopic source fails the exact gauge/Noether identity:

```text
CLOSE BEFORE BVP
```

---

## Near-singular gain rule

Never accept source or metric gain obtained primarily by collapse of a kinetic or auxiliary-sector margin.

A12B explicitly satisfies:

```text
GAIN_FROM_NEAR_SINGULAR_MIXING=
False
```

and that property must be preserved.

---

## Radiative-mixing rule

If the source inevitably regenerates photon or ordinary-matter mixing above existing empirical limits:

```text
CLOSE
```

even if the tree-level mixing is set to zero.

---

## Ambient-field rule

If a universal physical metric predicts large accelerations in magnetic, electric, or other already-realized field environments:

```text
CLOSE
```

A12C's MRI comparison is the model for this type of cheap falsification.

---

## Exotic-matter rule

Do not solve source selectivity by adding arbitrary unobserved hidden matter under current project policy.

---

# 37. Central Scientific Result of This Slice

The session produced a qualitatively different HOOK17 state.

Before the present chain, the project had:

```text
LOW-CAPACITY MECHANISM

BUT

NO SATISFACTORY PROTECTED MICROSCOPIC CARRIER
```

After A11C–A12D0, the project has:

```text
EXACT MASSLESS PROTECTED CARRIER

+

EXACT CLASSICAL CONSERVED DIRAC VECTOR NOETHER SOURCE CORRIDOR

+

NONSINGULAR AUXILIARY SECTOR

+

NO ULTRALIGHT PROCA MASS TUNING

+

GAUGE-INVARIANT UNIVERSAL PHYSICAL METRIC

+

FINITE COMPACT SOURCE

+

FINITE 1 KG NEUTRAL PAYLOAD

+

TRUE 1.0 M EXTERNAL STANDOFF

+

>=1G THROUGHOUT SAMPLED PAYLOAD

+

NONZERO INVARIANT TIDAL RESPONSE

+

2.656859-J CANONICAL FIELD-CAPACITY REFERENCE
```

while the first stable ordinary-matter microscopic source realization was separately and decisively falsified.

The low-capacity mechanism survived that falsification.

---

# 38. Claim Limits

The strongest scientifically justified current statement is:

> **A12B/A12C/A12D0 establishes a protected exact-massless-vector, gauge-invariant \(F^2\)-metric reduced-EFT mechanism with a finite \(1\,\mathrm{kg}\) neutral payload, true \(1\,\mathrm m\) stand-off, and at least \(1g\) outward acceleration throughout the sampled payload at a canonical field-energy reference of approximately \(2.656859\,\mathrm J\) for the declared \(1\,\mathrm{keV}\) metric normalization. The minimal stable ordinary EM-like microscopic source realization is empirically closed, but the protected carrier and low-capacity metric mechanism remain open to source-selective derivative, composite, Noether, bound-state, flavor, and related completions.**

The following claims remain unsupported:

```text
PHYSICAL ANTIGRAVITY MODEL FOUND

COMPLETE ENERGY <10 MJ CERTIFIED

006D REPLACED

PRACTICAL DEVICE FOUND

NEW FUNDAMENTAL PHYSICS DISCOVERED

2.66-J COMPLETE ANTIGRAVITY DEVICE
```

All remain false as of this journal entry.

---

# 39. Final Decision

```text
032H17A11C=
RED_SCOPED_DIRECT_K2_ORDINARY_DIRAC_ONSHELL_WHEELER_ROUTE_CLOSED

032H17A12A=
YELLOW_ENGINEERED_DIRAC_REOPENS_IW_SOURCE_CHANNEL_
BUT_UNMODIFIED_AXIAL_MAXWELL_WARD_AND_PROCA_PROTECTION_FAIL

032H17A12B=
GREEN_SCOPED_EXACT_MASSLESS_CONCURRENT_U1_
SAME_ACTION_CONSERVED_VECTOR_CURRENT_CORRIDOR

032H17A12C=
RED_SCOPED_ORDINARY_EM_LIKE_SOURCE_
BUT_GREEN_SCOPED_PROTECTED_F2_FINITE_PAYLOAD_MECHANISM

032H17A12D0=
GREEN_SCOPED_LOW_CAPACITY_F2_MECHANISM_FROZEN_
PURE_MAGNETOSTATIC_QUADRATIC_G00_DIRECTION_EXHAUSTED_
SOURCE_COMPLETION_ATLAS_ESTABLISHED
```

The current frontier is therefore:

```text
CARRIER PROTECTION=
STRONGEST YET

FIELD CAPACITY=
EXCELLENT

FINITE PAYLOAD RESPONSE=
ESTABLISHED AT REDUCED-EFT PREFLIGHT LEVEL

MICROSCOPIC SOURCE=
PRIMARY OPEN BOTTLENECK

COMPLETE ENERGY=
UNKNOWN

006D SUCCESSION=
NOT YET
```

---

# 40. Next

```text
NEXT=
032H17A12D1_PAULI_MAGNETIZATION_SOURCE_ENERGY_
UV_RADIATIVE_MIXING_AND_EMPIRICAL_GATE
```

The next research session should begin by reconstructing the final A12D0 state and testing the Pauli/magnetization completion analytically against the frozen A12C canonical source requirement.

Do not begin with:

```text
NEW GEOMETRY SEARCH

LOWER FIELD ENERGY OPTIMIZATION

BROAD AGMINER SCANNING

GENERIC CARRIER SEARCH

ENGINEERING
```

Begin with:

> **Can an identically conserved, source-selective Pauli/magnetization current physically source the protected A12B massless carrier strongly enough to retain the A12C \(2.656859\,\mathrm J\) finite-payload response while surviving UV completion, radiative mixing, spin/stellar constraints, support costs, and the strict complete-energy objective?**

If yes, continue physicalization.

If no, move immediately to the next source family in the A12D0 ranked atlas.

---

## Session-Close State

```text
DATE=
2026-09-14

PRIMARY_PROGRAM=
032H17_HOOK17

LATEST_COMPLETED_BRANCH=
032H17A12D0

FULL_REGRESSION=
1080_PASSED

006D_CONSERVATIVE_ANCHOR=
PRESERVED

C_006D=
23.591586299249

A12B_PROTECTED_MASSLESS_CARRIER=
PRESERVED

A12B_NEAR_SINGULAR_GAIN=
NO

A12C_GAUGE_INVARIANT_F2_METRIC=
PRESERVED

A12C_PAYLOAD_MASS_KG=
1.0

A12C_STANDOFF_M=
1.0

A12C_MIN_ACCELERATION_M_S2=
9.80665

A12C_MAX_ACCELERATION_M_S2=
22.889174637504706

A12C_COM_ACCELERATION_M_S2=
15.042170908175194

A12C_REFERENCE_PORTAL_SCALE_EV=
1000.0

A12C_FIELD_ENERGY_J=
2.6568591420597114

A12C_SOURCE_WORK_J=
2.6577816871671844

A12C_FIELD_ENERGY_IMPROVEMENT_VS_A10F2=
211891.10888238018

A12C_ORDINARY_EM_LIKE_SOURCE=
CLOSED

A12B_CARRIER_CLOSED=
NO

A12D0_QUADRATIC_METRIC_BASIS_EXHAUSTIVE_WITHIN_DECLARED_SCOPE=
YES

PURE_MAGNETOSTATIC_QUADRATIC_STATIC_G00_DIMENSION=
1

OPEN_SOURCE_PORTAL_COMPLETIONS=
35

TOP_SOURCE_COMPLETION=
ELECTRON_PAULI_MAGNETIZATION

FIELD_EFFICIENCY_OPTIMIZATION_AUTHORIZED=
NO

COMPLETE_ENERGY_ESTABLISHED=
NO

PHYSICAL_ANTIGRAVITY_MODEL_FOUND=
NO

CERTIFIED_SUB10MJ_MODEL_FOUND=
NO

PRACTICAL_DEVICE_FOUND=
NO

HOOK17_CLOSED=
NO

NEXT=
032H17A12D1
```

### Durable conclusion

The most important result of this research slice is not simply that a field-energy number became small.

It is that a sequence of exact source-space falsifications and symmetry constructions converted HOOK17 from an unprotected low-capacity idea into a substantially more coherent mechanism:

> **an exact massless symmetry-protected carrier, a conserved same-action classical source corridor, and a gauge-invariant finite-payload physical-metric response whose canonical field capacity is only \(2.656859\,\mathrm J\) at the declared reference normalization.**

The ordinary EM-like microscopic realization has already been falsified.

The field mechanism has not.

The research problem has therefore narrowed sharply:

> **Find a physically admissible source that preserves the A12 low-capacity backbone.**

That is the next frontier.

### END JOURNAL ENTRY
